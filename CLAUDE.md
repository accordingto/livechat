## 專案背景

我是一個**英文線上聊天室的主持人**，負責帶領參與者進行互動活動。

### 溝通限制
- ✅ 語音溝通（所有互動皆透過語音進行）
- ✅ 主持人可分享桌面（螢幕共享）
- ❌ 無視訊鏡頭（不能開視訊）

### 核心需求
開發適合線上語音聊天室使用的**破冰遊戲（Icebreaker Games）**，遊戲內容必須：
1. 不依賴視訊，純語音即可進行
2. 主持人可透過**分享桌面**展示遊戲畫面給參與者看
3. 以**英文**為主要語言
4. 適合陌生人或初次見面的參與者快速熟識

---

## 專案結構（現役檔案）

**Let's Talk v0.1 新增說明**：原本八款遊戲之外新增一個談話模式，入口 `lets-talk.html`，沿用首頁房間設定與 `play.html` 玩家連結。下表原有「八款／七款」敘述保留為原版遊戲背景；共用頁現在另外服務 Let's Talk。詳細規則與同步設計見 `talk-mode.md`，修改本模式前請先讀取。新檔案為 `talk-engine.js`（純規則）、`talk-sync.js`（主持頁狀態與玩家同步）、`talk-ui.js`（雙語與玩家卡片）、`talk-host.js`（共同畫面與單機示範）、`talk-topics.js`（12 題原創簡單英文話題）、`talk.css`（限定本模式樣式）、`tests/talk-*.test.cjs`（無依賴測試）。`room.js` 僅新增主持端 `ROOM.playerRef(i)`；不可把其他人的 token 傳入玩家 payload。`?demo=1` 是不寫入 Firebase 的單機操作示範。暫不擴充原本 Google Sheet 題庫格式；本模式使用頁內收合說明，沒有另加強制教學步驟。

**Let's Talk v0.2**：題庫已由 12 題擴成 8 類、48 組議題與 192 個延伸問題（共 240 個問題），可依分類與中英文關鍵字搜尋、預覽討論路徑，或用「以這題修改」帶入手動出題。主問題可直接輸入中英文，選填名稱與每行一題的延伸問句；草稿用獨立的 `lets-talk-topic-draft.v1` 保存。進行中可選取任一延伸方向或臨場自訂問句，使用主持 `extend` 命令同步，保留輪次與口頭追問；玩家投影沿用 `talk.topic.followUp` 以相容已開啟的 v0.1 卡片。詳見 `talk-mode.md`；不變更既有 Google Sheet 格式。

**Let's Talk v0.3（試玩回饋）**：主持交棒／結束提問按鈕移到目前發言者下方，臨時有人離開可由主持接手；仍保留待追問提醒與過期操作防護。頁面上方先隨機預選完整問題，可重新抽題、手動改選或自訂；頁尾直接列出完整題庫與可展開的延伸，並提供分類／中英搜尋與「選這題」。隨機抽題依目前篩選，有其他題目時避開目前活動與預覽；抽題只改預覽，按「開啟話題」才發布。玩家輪到自己時有金色外框、大字「輪到你了！」與「我說完了」／「I'm done」按鈕。21 項規則／同步測試及新三人 Firebase 房間的主持代操作、重連、換題與切回 Word Wolf 已驗證，詳見 `talk-mode.md`。

Let's Talk 後續介面精簡：主持畫面移除 Round 數字與 Host controls 提示，保留直接操作按鈕；共同畫面與玩家卡的說明改為可以隨性自然地聊、卡片只是輔助。題庫／預覽的延伸問句不顯示方向標籤，延伸選單也顯示完整問題。輪次規則與題目資料不變。

**Let's Talk v0.4（引題試用）**：48 組話題增加簡短英文 `starter`，在問題下方提供情境或思考入口。主持「顯示引題（試用）」開關同步共同畫面與玩家卡片；活動內保存 `showStarters`，主持命令 `starters` 接受布林 `show`，不改倒數、追問或分享順序。新活動預設開啟，舊活動缺欄位時先關閉；本機偏好另存在 `lets-talk-starters.v1`。自訂引題最多 400 字，隨原草稿保存，沒填可用第一題延伸作為引題。空白的倒數／追問／反應區塊不再佔位，桌面狀態與主要按鈕並排，手機緊接排列。詳見 `talk-mode.md`。

**Let's Talk v0.5（題目說明）**：依試玩回饋將 48 段引題改寫為解釋原問題的兩段說明：釐清題意／關鍵詞，再提示如何開始回答；不用另一個情境或追問取代題意。開關改稱「顯示題目說明（試用）」，自訂說明上限 600 字，保留原 `starter`／`showStarters` 與草稿、偏好欄位。空白說明不再回退到 follow-up。舊房間若題目 ID 與主問句完全符合內建題庫，主持頁會用 `explain` 命令更新說明而不重設活動；自訂題不覆蓋。延伸問題及分享規則保持原樣。

> ⚠️ **只讀取以下檔案。`_archive/` 資料夾內的所有檔案為舊版廢棄遊戲，除非使用者明確要求，否則永遠不要讀取或修改。**

| 檔案 | 說明 |
|------|------|
| `index.html` | 主選單 / 遊戲入口頁，**兩步驟流程**：以前一進站就是選遊戲，主持人要先點進某一款才會看到「選人數／發連結」，八款各自重複一次；現在房間設定被拉到選遊戲**之前**、全站只做一次。**Step 1「Set up your room」**直接在首頁掛一份 `room.js` 的 `ROOM.init({ mount: 'room-mount', accent: '#e94560', counts: [2..9], defaultCount: 5 })`（跟各遊戲頁一模一樣的元件：人數鈕／姓名欄／Copy All Links／QR／Send Card Check），**但這裡永遠不呼叫 `ROOM.publish()`**——首頁不屬於任何一款遊戲，不發任何牌，玩家連結打開後就是 `play.html` 原本就有的「⏳ Waiting for the host to start a game...」預設畫面，等主持人稍後在某一款遊戲頁按下真正的發牌鈕才會變成該遊戲的卡片；因為房號／人數／姓名都是靠 `room-session-{CODE}`／`room-last-session` 這包 `localStorage` 在八款遊戲間同步（`dare-conquest.html` 雖然沒用 `room.js`，也讀寫同一包），所以這裡設定好、按下**Step 2** 前的「▶️ Continue to Games」按鈕後，等一下點進任何一款遊戲，該遊戲頁自己的 `ROOM.init()` 會自動讀到同一個房號與人數，不用重新設定一次（唯一的前提是 Firebase 要能連上——`ROOM.save()` 內部靠「已經配過 token」才會真的寫進 localStorage，沒有 Firebase 時人數/姓名只留在當前分頁的記憶體裡，不會跨頁同步，這點跟各遊戲頁原本的行為一致，不是本頁新引入的限制）。**Step 2「Choose a game」**就是原本的遊戲選單格線（`#game-list`），預設用 `.hidden` 藏起來，按下 Continue 才連同一條「🔗 Room ABC123 · N players ／ ✏️ Edit room setup」摘要列一起淡入；摘要列右側的「✏️ Edit room setup」可以隨時跳回 Step 1（`showStep(1)`／`showStep(2)` 兩個函式互斥切換 `#step-setup`／`#step-games`／`#game-list` 的 `.hidden`，並捲回頁面最上方），沒有任何資料會因為來回切換而遺失，因為狀態本來就活在 `ROOM` 自己的模組變數與 localStorage 裡，這兩個步驟只是同一份 DOM 的顯示/隱藏。**單獨打開首頁（網址列直接輸入或書籤進來，沒有帶 `?step=2`）都會先落在 Step 1**（不會因為之前設定過就自動跳到 Step 2），但欄位會用 `room.js` 既有的還原機制帶出上次的人數與姓名，所以多半只是確認一下就能按 Continue，不必每次重打。**但從遊戲頁按「← Back to Hub」回來的這條路徑例外**：八款遊戲頁的返回連結一律是 `index.html?step=2`（不是單純的 `index.html`），首頁的 inline script 在 `renderSetupSummary()` 之後會檢查 `new URLSearchParams(location.search).get('step') === '2'`，是的話直接呼叫 `showStep(2)`，讓主持人回到首頁時停在「選擇遊戲」畫面而不是要重新跑一次房間設定——房間本來就已經設定過（不然也進不了任何一款遊戲），沒有理由讓主持人每次換遊戲都要重新看一次 Step 1；要調整人數或連結，摘要列右側的「✏️ Edit room setup」隨時可以手動跳回 Step 1。人數／發連結這一步現在**只在首頁做一次**：八款遊戲各自頁面裡原本重複的房間設定卡片（人數鈕／姓名欄／Copy All Links／QR／Send Card Check）已經拆除，改用 `room.js` 的 `ROOM.init({ hideSetup: true, ... })`（`dare-conquest.html` 因為沒用 `room.js`，是直接刪掉自己那份對應實作），各遊戲頁進場時只剩下「這一頁自己才有」的功能——按鈕（例如 Word Wolf 的 Deal Words／Reveal All／Pick Someone、Kangaroo Court 的 Judge 開關）與發牌後的唯讀名單／答案顯示；要臨時調整人數或補發連結，要回首頁 Step 1（按摘要列的「✏️ Edit room setup」）處理，不再能在遊戲頁裡直接改，細節見 `room.js` 與各遊戲頁自己的說明）。**Step 1 本身現在是一個五步驟的精靈（wizard）**，一次只顯示一個子步驟，填完按「下一步」才會看到下一個，不會像過去那樣把人數鈕、姓名欄、Copy All Links、Send Card Check、Continue to Games 全部攤開在同一畫面：子步驟 1「設定房間人數」標題下方多一段說明文字（`data-i18n="index.substep1Desc"`，只有子步驟 1 才有，`initSetupWizard()` 組 `panel.innerHTML` 時特別判斷 `n === 1` 才插入這段 `<p class="substep-desc">`）：先講「先設定玩家人數」，接著解釋房號的用途（讓所有分頁/裝置同步同一批連結）並明講「通常不需要自己設定」，只有要換回之前用過的房間才需要用「載入」——這是因為房號欄位對第一次使用的主持人來說意義不明、容易誤以為是必填欄位，實際上 `ROOM.init()` 已經自動產生好一組房號，多數情況全程不用碰它。**PLAYERS 人數鈕現在排在 ROOM 列上面**（`room.js` 的 `buildDOM()` 把兩個 `.room-row` 對調順序，`room-code-row`／`room-code-divider` 這兩個既有 id 沒有變，Firebase 未啟用時一樣靠 `id` 隱藏，只是物理位置換到人數鈕下方）——人數是每次進來都要看的必填設定，房號多數時候只是背景自動運作的識別碼，理應排在後面、且不搶視覺重點；這個順序只影響 `!cfg.hideSetup` 的完整版 UI（目前只有 `index.html` 用得到），七款 `hideSetup: true` 的遊戲頁不會畫出這兩列，不受影響。子步驟 2「設定玩家名稱」**只有**姓名輸入欄，沒有 Link／QR；子步驟 3「傳送玩家連結」（標題刻意不叫「複製所有連結」，因為 Copy All 只是三種發連結方式之一，取名成單一方法的名字會誤導）不會把 QR／Link／Copy All 三種發連結的方式同時攤開——這是實際發生過的卡關熱點，主持人常常搞不清楚三種方法要選哪一種、畫面資訊太多。改成**先選一種方法，才出現對應的介面**：畫面最上面是一句「選一種方式，把連結分享給玩家」（room.js 原本的「🔗 Player Links (optional)」標題與說明文字，跟這句話重複，已經拿掉——但那兩個節點還是要留在文件裡的某個地方，藏進一個 `display:none` 的容器，因為 `room.js` 的 `render()` 每次都會用 `getElementById` 抓這兩個節點寫入內容，整個刪除節點會讓 `render()` 噴錯），接著三顆等寬的方式選擇鈕（📱 手機掃描 QR Code／🔗 私訊連結給每個人／📋 複製全部貼到聊天室），選了其中一個才會往下展開對應的**一段說明文字**（解釋主持人具體要做什麼，例如 QR 方式會提醒「一次只顯示一位玩家的 QR Code」）＋對應的介面：QR 方式是每位玩家一欄的**唯讀**姓名＋一顆「QR」切換鈕，點下去才展開該玩家的 QR code，**跟舊版 `toggleQR()` 一樣一次只能開一個**——不能同時展開多位玩家的 QR code，因為主持人是要把螢幕分享出去讓大家各自掃描，好幾組 QR code 同時出現在畫面上時，手機鏡頭很容易掃到隔壁那組，這是實際試過同時展開全部又改回來的教訓；私訊連結方式是唯讀姓名＋一顆「連結」按鈕（不顯示 QR）；複製全部方式則完全不顯示逐一玩家的欄位，只有一顆 Copy All Links 按鈕。三種方式的姓名都跟子步驟 2 共用同一份資料、同樣是**唯讀**（要改名字得點回子步驟 2）。技術上 `room.js` 的 `ROOM.setLinksView()` 多兩種模式：`'link-only'`（唯讀姓名＋只有 Link 按鈕）、`'qr-only'`（唯讀姓名＋一顆 QR 切換鈕，內部一樣呼叫共用的 `toggleQR()`，跟 `'full'` 模式的 QR 按鈕行為完全一致）；`index.html` 用 `chosenMethod` 這個變數記住主持人選了哪個方法，切去子步驟 4 再切回子步驟 3 時會用 `selectMethod(chosenMethod)` 把 `#room-links` 重新搬回對應的插槽並還原畫面，離開整個 Step 1（按「✏️ Edit room setup」重新進來）才會把 `chosenMethod` 清空、重新要求選一次；子步驟 4「傳送核對卡片」＝ Send Card Check 按鈕＋每位玩家一欄的**唯讀**姓名（同樣不能改名字、也不提供 Link／QR）。**核對字結果不會一按 Send Card Check 就顯示**——這是刻意分成兩個動作：按下 Send Card Check 只把字發到玩家卡片上、主持人畫面上還看不到答案，`check-note` 顯示「請每位玩家先唸出卡片上看到的內容，核對完後按『顯示所有答案』查看正確答案」＋一顆獨立的「👁️ 顯示所有答案」按鈕；主持人先聽每位玩家唸出自己卡片上的字，聽完才按下這顆按鈕，這時候每位玩家欄位下方才會冒出核對字結果，`check-note` 也換成「✅ 答案已顯示」。如果先讓答案跟著 Send Card Check 一起顯示，主持人等於是自己先看到答案再核對，這場核對就失去意義了。技術上 `room.js` 新增 `checkRevealed` 這個獨立於 `checkWords` 的旗標：`sendCardCheck()` 只設定 `checkWords` 並把 `checkRevealed` 重設成 `false`；新函式 `revealCardCheck()` 才把 `checkRevealed` 設成 `true`；`render()` 畫每位玩家的 `.check-word-badge` 時同時檢查 `checkWords[i]` 與 `checkRevealed`，兩者都成立才畫出來；`clearCardCheck()`（`check-note` 裡的「✖️ 清除」連結）把兩個旗標一起歸零；子步驟 5「載入題庫並開始」＝ 題庫來源列（見 `sheet-data.js`：讀取／開啟試算表／恢復預設三顆按鈕與逐分頁結果）＋原本的 Continue to Games 按鈕。題庫讀取刻意放在**房間設定的最後一步**而不是 Step 2：主持人每次帶活動前本來就會走一次這個精靈，順手按一下讀取最自然；放在 Step 2 的遊戲清單上方反而是一塊每次都要跳過的東西。三種畫面共用同一個 `#room-links` 節點與同一份玩家陣列，靠 `room.js` 新增的 `ROOM.setLinksView('names' | 'links' | 'check' | 'full')` 切換 `render()` 要畫出姓名欄／Link-QR／核對字這三塊的哪幾塊（`'full'` 是舊版三塊全部一起畫，預設值，只有 `index.html` 會呼叫這個 API 切換成其他三種）：姓名輸入框在非 `'names'`／`'full'` 模式下改成跟 `renderCompact()` 一樣的唯讀 `<div class="name-input is-static">`。`index.html` 的 `initSetupWizard()` 在 `ROOM.init()` 跑完後，把 `.room-card`／`#room-hdr`／`#room-note`／`#copy-all-btn`／`#check-btn`／`#check-note` 這些既有 DOM 節點分別「搬進」（`appendChild` 對已存在的節點是移動不是複製）五個 `.substep` 容器裡；`#room-links` 則不固定屬於哪一步，`showSubStep(n)` 每次都呼叫 `placeRoomLinks(n)`，依 `n` 是 2／3／4 把它搬進對應子步驟自己的一個空 `<div id="substep-N-links-slot">` 插槽、同時呼叫對應的 `ROOM.setLinksView(...)`，其餘步驟則保持原樣不動（反正父層是 `.hidden` 的子步驟，不會顯示）。節點本身、事件監聽器、`room.js` 內部靠 `id` 抓取節點的邏輯都不受影響，只是父層換了。子步驟上方有一排可點擊的數字圓點（`#substep-dots`）顯示目前進度，**只有已經走訪過的步驟才能點回去**（`subStepReached` 記錄目前最遠走到第幾步），不能跳著點還沒填過的步驟，符合「依序出現」的設計；每次從遊戲頁按「✏️ Edit room setup」或直接重新整理回到 Step 1，都會重置回子步驟 1（不記憶上次停在哪一步），因為房間本身多半已經設定過，主持人此時通常是要整個重新走一次，而不是接著上次的某個中間步驟。切換語言（`I18N.setLang`）不會把 wizard 重置回第一步，只會重新翻譯當前子步驟的標題與按鈕文字（`I18N.applyStatic()` 涵蓋新建立的節點）。 |
| `say-it-without-saying-it.html` | 🙊 Say It Without Saying It（Taboo 玩法，**主持人畫面現在分成「設定階段」與「遊戲階段」兩部分**，同樣套用 `index.html` Step 1 那套「一次只顯示一個子步驟、依序填完才能往下」的精靈模式，只是這裡走的是這款遊戲自己的三個參數而不是房間設定：`#phase-setup`（4 個 `.substep`：1／4 難度、2／4 禁字與生效個數、3／4 回合秒數、4／4 純粹一顆「🎮 開始遊戲」按鈕，`showTabooSubStep(n)` 切換 `.hidden`、`renderTabooDots()` 畫上方的圓點進度列，只有已經走訪過的步驟才能點回去，寫法與 `subStepReached` 邏輯跟 `index.html` 的 `showSubStep()`／`renderSubStepDots()` 完全同構）與 `#phase-game`（原本就有的計分板／控制列／發牌鈕／玩家名單／題庫，一字不改）；**這三個設定元件（`#rules-group-difficulty`／`#rules-group-forbidden`／`#secs-field`）只存在於精靈裡，遊戲階段完全看不到、也改不到**——遊戲進行中不可以調整設定，是刻意的限制，不是疏漏。`startGame()`／`editSettings()` 因此只單純切換 `#phase-setup`／`#phase-game` 兩個容器的 `.hidden`，不搬動任何 DOM（跟 `index.html` 搬動 `room-card`／`room-links` 的做法不同，這裡的三個元件從頭到尾都待在原地）。遊戲階段的控制列（`#control-bar`）因此只剩下左邊的回合計時（`⏱️ Round timer` 標籤＋Start／Reset 兩顆按鈕，這兩顆是操作中的計時器本身，不是設定，所以留下）與正中央的倒數圓圈，右側原本放禁字／難度的 `.cb-sect.rules` 整塊已經拿掉；`grid-template-columns: 1fr auto 1fr` 這組三欄設定沒有跟著拿掉——即使只剩兩個子元素落在前兩欄，CSS Grid 仍然照樣保留第三欄 `1fr` 的空間（顯式宣告的欄位數量不受實際子元素個數影響），倒數圓圈因此還是精準落在整條控制列的正中央，不會因為少了一欄內容就偏向左邊。遊戲階段頂端的 `.phase-summary` 摘要列（🎚️ 難度 · 🚫 禁字 ON/OFF（生效個數）· ⏱️ 秒數，`renderPhaseSummary()` 在每次 `renderAll()`／`renderTimer()`／語言切換時重算，讀的是既有的 `level`／`forbiddenOn`／`bannedCount`／`timer.total` 這幾個變數，不是另外存一份）因此是遊戲進行中**唯一**看得到目前設定的地方；要調整，只能按右側「⚙️ 編輯設定」按鈕（`editSettings()`）跳回精靈、`tabooSubStepReached` 重設為 1（回到子步驟 1，邏輯與 `index.html` 的「✏️ Edit room setup」一致），**但刻意不呼叫 `resetRound()`／不動 `state`／`round`／`scores`／`queue`**——回合進行到一半也可以跳回去調整難度或秒數，調完按「開始遊戲」跳回來，遊戲會停在原本的畫面繼續進行，因為這三個設定本來就只影響下一次 `dealRound()`，不影響正在進行的這一輪（例如 `roundForbidden` 是發牌當下的快照，中途改 `bannedCount` 不會動到手上這輪的裁判與描述者已經看到的禁字清單）。首次載入永遠停在設定階段（`phase = 'setup'` 初始值），必須按過一次「開始遊戲」才會看到計分板與發牌鈕。**整局的資訊與判定全在玩家卡片上，主持人畫面只有計分板**（因為主持人在分享桌面，目標字與禁字都不能出現在上面）。選 **2–6 人**（每輪要一位描述、一位裁判、至少一位猜，但 **2 人局沒有裁判**——見下方 Forbidden words 開關），按「🎬 Deal a Word」抽卡並發出兩個角色。**角色是隨機抽的，但保證公平**：clue giver 用一個洗過牌的袋子（`giverBag`）輪流發，每位玩家都當過一次才會重新洗牌，且重新洗牌時不會讓剛當完的人接著再當；referee 則從「目前當過裁判次數最少」的人裡面隨機挑（`refCount`），所以順序每輪都不一樣、但次數完全平均（實測 4 人局 24 輪：每人各當 6 次 clue giver、6 次 referee，0 次連莊）：**clue giver 的卡片**＝倒數圈＋（有人搶答時才出現的）「🙋 xxx buzzed in」與「✅ Correct! +1 each／❌ Not it — keep going」兩顆判定鈕（實心填滿綠／紅底色，看起來就是可以按的按鈕，不是純文字配深底）＋目標字＋禁字（搶答訊息與那兩顆判定鈕刻意排在**倒數圈與目標字之間**、也就是卡片最上方，因為那是有人搶答的瞬間 clue giver 唯一需要立刻反應的東西，不該讓他往下捲；沒人搶答時這一區完全不佔位置，改在卡片最下方顯示一行「Keep describing…」提示）；**👀 referee 的卡片**＝倒數圈＋禁字＋緊接在清單下方的「🚨 Taboo!」按鈕（實心紅底，跟判定鈕、搶答鈕走同一套「填滿底色」風格，不再是透明外框；禁字清單與那顆按鈕刻意相鄰，說明文字「Listen for a forbidden word…」移到按鈕**下方**，不讓它把清單與按鈕拆開），該輪不搶答，但舉發成立可以得分；**其餘 guesser 的卡片**＝**看不到禁字**，只有倒數圈與一顆大的「🙋 Buzz in!」搶答鈕。（原本禁字是發給所有 guesser 的，但禁字就是「解釋這個字最先會想到的詞」，等於直接把答案送出去——改成只有描述者與裁判持有這份清單，猜的人純靠聽。）流程：**主持人按「🎬 Deal a Word」只是抽角色、抽卡，回合還沒開始**——題目是「鎖著」的（`state = 'ready'`），目標字與禁字**連 clue giver 與裁判都還看不到**（`payloadFor()` 的 `sealed` 判斷把 `target`／`emoji`／`forbidden` 一律壓成 `null`），倒數也設好秒數但**不跑**；輪到的那位 clue giver 要在**自己的卡片**上按「👀 看題目」（`play.html` 的 `tabooOpen()`，寫回 `{ id, choice: 'ready' }`），主持人端收到才 `startRound()`：這時才把題目與禁字推出去、才開始倒數（`state = 'playing'`）。這是為了換人的那幾秒——上一位 clue giver 答完題、手機要傳給下一位，或下一位還在切回自己的分頁，題目卻已經攤在螢幕上、秒數也在跑，等於白白吃掉一輪的時間，而且中途任何人瞄到那支手機就直接看到答案。主持人畫面在等待期間狀態列寫「🎤 換 {name} 描述 — 請他在自己的卡片上按『看題目』」，發牌鈕變成「👀 幫 {name} 開啟題目」、按下去等同替他開啟（手機壞掉、連不上時的備援；`toggleTimer()` 在這個狀態下按 ▶️ Start 也是同樣效果，因為「還沒開題目就開始計時」沒有意義）。接著 clue giver 用英文描述 → guesser 想到就按搶答（用講的把答案喊出來）→ 他的名字會出現在 clue giver 的卡片上 → clue giver 按 ✅ 則**他與該 guesser 各 +1**、這輪結束，**而且不用主持人再按任何按鈕：5 秒後自動換人、自動抽下一題**（`awardPoint()` → `endRound(res, true)` → `armNextRound()`，這 5 秒是留給主持人把答案唸出來的；期間按任何一顆按鈕都會取消倒數改成立刻執行）。**這 5 秒的倒數與「下一回合輪到誰」會同步顯示在每一位玩家的卡片上**：`armNextRound()` 刻意**在倒數開始前就把下一輪的 clue giver 與 referee 抽好**存進 `nextRoles`，而不是等 `dealRound()` 當下才抽——因為卡片要在這 5 秒內就報出名字，下一位描述者才有時間把手機拿起來、切回分頁；等回合開始才知道就來不及了（這也是上面那個「題目鎖著等他按」的同一個問題的另一半）。`endRound()` 因此把 `armNextRound()` 排在 `publishRound()` **之前**，結果與「下一位是誰」用同一次寫入送到所有卡片。`dealRound()` 之後會**消費**這份 `nextRoles`（用掉就清空）而不是重抽，所以卡片上報過的名字一定就是實際發到的人（`keepGiver`／Skip 不吃這份預抽；若中途把禁字關掉，`refIdx` 一律回到 −1，卡片也不會報裁判）。倒數本身跟回合計時走同一套「只推一次、卡片自己跑秒」的做法：payload 是 `next { seq, in, giver, ref, youGive, youRef }`，`seq` 變了卡片才重新對時，**整段 5 秒倒數 0 次 Firebase 寫入**（實測 4 人局）。主持人畫面的狀態列同一行也會接上「🎤 下一位描述者：X · 👀 裁判：Y」，因為那是分享出去給所有人看的畫面。`cancelNextRound()` 只停倒數、**不清掉 `nextRoles`**（主持人提早按「下一回合」時，發到的還是剛剛報過的那兩個人）；要真的丟掉這份預抽只有 `dealRound()` 用掉它、或 `resetRound()`／換人數時整個清空。只有「答對」會自動續下一輪：時間到、Taboo! 成立、主持人手動結束這三種結尾都停在結果畫面等主持人自己按「▶️ 下一回合」，因為那幾種情況通常還要講評或處理。按 ❌ 則換排隊中的下一位。同時有多人搶答會排隊，第 2 位以後的卡片顯示「You're #N in the queue」。**被判錯的人不能馬上再搶答**（`guesserNums()` — clue giver／referee 以外的所有人）：判錯後那個人會被鎖住（存進 `rejected` 陣列），直到**這一輪剩下的每一位 guesser 都各自搶答並被判錯過一次**，鎖定才會整批解除、所有人重新開放搶答——例如 5 人局（C／D／E 三位 guesser）：C 搶答判錯 → 只剩 D、E 能搶；D 搶答判錯 → 只剩 E 能搶；E 搶答判錯 → C／D／E 三人同時解鎖，重新開放。被鎖住的人卡片上原本的「🙋 Buzz in!」按鈕會整個換成一則提示「❌ Not it — hang tight」，沒有可以按的按鈕；主持人送出的判錯（`handleAnswer` 的 `wrong` 分支）在 `rejected.push(...)` 之後會檢查 `rejected.length >= guesserNums().length`，滿了就整批清空 `rejected`，下一次搶答自然又全員開放。答對、換一位新的 clue giver、或裁判喊 Taboo! 都會直接結束整輪（`dealRound()`／`resetRound()` 會清空 `rejected`），所以這個鎖定規則只在同一輪、同一個人還沒答對之前生效。**Taboo! 是「舉發」而不是直接判定**：裁判按下後不會馬上結束，主持人畫面上方的狀態列會亮紅色顯示「🚨 Cara called Taboo! on Amy」（誰按的、被舉發的是誰，計分卡也會紅框標示），**畫面下方則跳出裁決面板（`#call-row`）**：紅框紅底、外圈慢慢明暗脈動，上面一行問句「Amy 真的說了禁字嗎？由你裁決。」，下面兩顆**實心填滿**的大按鈕（紅／綠，18px 內距、字級 1.05rem、900 字重），窄螢幕各自佔滿一整排。這個面板刻意長在**原本 Deal／結束回合那一排的位置**，而且面板出現時整條 `#deal-row` 會被隱藏（`renderAll()` 的 `ruling` 判斷同時控制兩者，一次只會有一組按鈕在畫面上）——舊版是把裁決鈕放在計分板**上方**、Deal 鈕留在下方，主持人的手本來就伸向畫面最下面那顆按鈕，兩個決定分在兩個地方，結果常常誤按成「🛑 結束這一回合」；同理，狀態列只講「發生了什麼」（分享畫面上所有人都看得到），問句只出現在下方的面板裡（主持人自己要回答的那一句），不再兩邊各講一次。「🚨 成立 — 裁判 +2，描述者 −1」＝成立，**抓到的裁判 +2 分**（這是裁判唯一的得分方式），**讓對方抓到的 clue giver −1 分**（可以扣成負數，沒有下限），猜題者 0 分，換下一位描述；分數卡在裁決當下會各自跑一次動畫：裁判是綠色的 `pointPop`（跟猜對時同一種「加分」動畫），clue giver 則是紅色的 `pointLoss`（scale 縮小＋紅色 box-shadow 閃一下，`popIdx`／`lostIdx` 各自追蹤要跑哪種動畫，避免扣分也套用跟加分一樣的綠色特效造成誤解）；「👌 不成立 — 繼續這一輪」＝不成立，裁決面板消失、Deal 那一排回來、回合原封不動繼續（舉發者可以再按一次）。裁決期間所有卡片都會顯示這條舉發訊息，裁判自己的卡片變成「🚨 You called Taboo! — waiting for the host」，其他人的搶答鈕仍然可用。主持人畫面只有：一行狀態列（誰在描述／誰搶答了／誰喊了 Taboo!／這輪結果與答案）、**一條控制列**，左右分成兩個一眼可辨的區塊（各有自己的彩色側邊條與小標題、中間一道分隔線；窄螢幕會上下堆疊）：**倒數圓圈固定在畫面正中央**（控制列用 `1fr auto 1fr` 三欄，不論左右控制項多寬，數字都剛好落在螢幕水平中線，實測 390–1280px 各寬度誤差皆為 0px），左邊「⏱️ Round timer」＝一組「− ／ 秒數 ／ +」的秒數欄位（**預設 90 秒**；−／+ 每按一次 ±30 秒並自動對齊到 30／60／90／120… 的整數倍，範圍 30–900；中間欄位仍可直接打任意秒數 5–900，打字途中不會被夾住、離開欄位才會校正。**刻意不用 `<datalist>`**：Safari 會依欄位現有內容過濾建議，iPad 上欄位是 60 時就只看得到 60 一個選項）＋Start／Reset；右邊是禁字開關（**無外框**的純文字切換鈕，不像其他按鈕有邊框，靠顏色跟滑動開關本身區分 on／off）＋**難度選擇**（🟢 Easy 100 字／🟡 Medium 100 字／🔴 Hard 100 字，選了之後只抽該難度的字，換難度會清空「已出過」清單、但不會動到正在進行的那一輪；選擇存進同一包 `localStorage`）＋**🔢 Banned words 數量選擇**（2／3／4／5／6 五顆按鈕，控制每張卡的 6 個禁字裡實際生效幾個，數字愈小愈簡單；跟難度一樣，改變設定只影響下一次 Deal，不會動到正在進行的那一輪——`dealRound()` 會把 `card.forbidden.slice(0, bannedCount)` 存進獨立的 `roundForbidden`，卡片看到的清單一律讀 `roundForbidden` 而不是即時重算，所以就算回合進行中主持人手滑改了數量，這一輪裁判與描述者手上的清單也不會被動過；選擇同樣存進 `dontSayItOptions.v1`）（右半區塊本身不再有「🚫 Round rules」這行標題，窄螢幕時圓圈移到最上方、下面依序是計時控制與禁字設定；難度按鈕與 Banned words 按鈕**都會自動換行**（`flex-wrap: wrap`），手機直向窄螢幕下不會被切斷在螢幕邊緣外——這兩排按鈕原本沒設 `flex-wrap`，在窄螢幕上會撐開整個控制列、把畫面往右推出可視範圍）。這些設定分兩個 `.rules-group`（每組上方一行小字標籤、組跟組之間用一條淡紅色虛線分隔，避免在手機分享畫面上擠成一整團）：**Forbidden words 開關與 Banned words 數量選擇合併在同一組**（`🚫 Forbidden words` 標籤下先是 ON／OFF 開關，下面用自己的虛線再分隔出一行更小的 `🔢 how many` 子標籤與 2–6 按鈕——因為「開幾個字」本來就是「開不開禁字」的子設定，兩者永遠一起看、一起決定），關掉 Forbidden words 或人數鎖到 2 人時，這排數量按鈕會整組變半透明且無法點擊（`.banned-count-row.is-off`，`renderForbiddenToggle()` 統一控制），因為停用禁字時「幾個生效」已經沒有意義；`🎚️ Difficulty`（難度）是完全獨立的第二組，因為它跟開不開禁字無關，一直都能選。開關本身文字也簡化成單純的「ON／OFF」，因為標籤已經講了是什麼；窄螢幕時每組置中對齊，寬螢幕時整組靠右對齊。按「▶️ Start」開始倒數、可「⏸️ Pause」暫停續跑、「🔄 Reset」歸零重來。**有人搶答或喊 Taboo! 時倒數會自動凍結**（搶答的人不該在等 clue giver 判定時被扣秒數，喊 Taboo! 更是直接中斷了這一回合），主持人畫面與每位玩家的卡片同步停住，狀態列會補上「⏸️ Clock on hold」說明為什麼停了；等 clue giver 按 ❌（且排隊中沒有下一位）或主持人按「👌 No — keep playing」才從停住的秒數接著跑，按 ✅ 或裁決成立則直接結束該輪。程式用 `timer.autoHeld` 記住「這次是系統自己暫停的」，所以只會解除自己造成的暫停：主持人手動按過 ⏸️ Pause 的計時器不會被搶答結束時意外喚醒，而主持人手動按 ▶️／🔄 一律優先、會清掉這個旗標。凍結期間一樣不寫 Firebase（實測 4 人局凍結 5 秒＝0 次寫入，一次搶答含暫停共 5 次寫入）；大圓形數字會隨剩餘比例變色綠→橘→紅並在最後閃爍。**倒數在 clue giver 於自己卡片上按下「👀 看題目」的那一刻才開始**（見上面的 `ready` 狀態；主持人不用再按一次 Start，那只是白白吃掉秒數），主持人畫面與所有卡片同步起跑；**倒數同步顯示在每一位玩家的卡片上**（同款圓圈，clue giver 在題目上方、guesser 在禁字上方），做法跟 Dare Conquest 一樣：主持人只在狀態改變時推一次 `timer { seq, state, remaining, total }`，卡片收到後自己每秒遞減，**倒數過程中完全不寫 Firebase**（實測 4 人局跑 8 秒＝0 次寫入）。歸零時該輪自動結束、0 分並公布答案；每抽一張新卡計時器會重設成設定秒數、但要等 clue giver 開啟題目才起跑）、Taboo! 裁決鈕（只在有人舉發時出現）、每人一張大計分卡（clue giver 靛色highlight 並標「🎤 Clue giver」、裁判琥珀色標「👀 Referee」、搶答者綠色浮起；全站一律用 **clue giver** 這個稱呼，不再出現 describing 字樣）、「Deal a Word／Next Round」與「Skip this word」按鈕（一輪結束後 Deal 按鈕會變成綠色並持續脈動、文字改為「▶️ Next Round — new clue giver」，避免主持人分享畫面時看不出來換人了）、連結區，以及一份**預設收合**的題庫（按「👁️ Browse all 300 words」才展開，避免分享畫面時暴雷；展開後依難度分成三組顯示）。控制列右半的「🚫 Forbidden words ON／OFF」開關：關掉時**所有人的卡片都不會出現禁字、也不再發裁判**（clue giver 只剩目標字與一行「No forbidden words this round」，原本的裁判變回一般 guesser 可以搶答），搶答與計分照常運作；開關可在回合進行中即時切換、會存進 `localStorage`（key: `dontSayItOptions.v1`）重整後保留。**2 人局強制關閉禁字**：1 位 clue giver＋1 位 guesser 已經沒有第三人能當裁判，所以人數選到 2 時開關會整顆鎖住變灰、文字改成「Forbidden words OFF (needs 3+ players)」，`toggleForbidden()` 這時點了也不會有反應；若原本 3 人以上且已開啟禁字，中途把人數調到 2 會自動關閉（`onCountChange`），調回 3 人以上開關才會解鎖（但不會自動重新打開，要主持人自己再按一次）；`ROOM.init()` 讀到其他遊戲存的房間人數剛好是 2 時也一樣會在載入當下強制關閉，不會停在「2 人卻要抽裁判」的壞狀態。沒有 Firebase 時這款無法進行（目標字沒有地方私下給 clue giver 看），會顯示警告並鎖住 Deal 按鈕。技術上：開新回合用 `ROOM.publish()`（`.set()`，順便清掉上一輪的作答），回合進行中改用 `ROOM.update()` 避免蓋掉玩家正在寫入的搶答；主持人收到搶答後會 `ROOM.clearAnswer()` 把該欄位清空，所以同一個人可以重複搶答。`ROOM.init()` 帶 `hideSetup: true`（人數／連結已在 `index.html` 設定好），`room-mount` 只留發牌後的唯讀名單：clue giver 的欄位框線用**綠色**（`is-giver-link`，跟頁面本身的靛色主題色不同，才能在一片靛色 UI 裡一眼認出來），referee 維持琥珀色（`is-ref-link`，跟計分板上的裁判配色一致）；「👁️ Show what's on player cards (spoilers — don't open while sharing)」這顆按鈕現在是透過 `extraRowHTML` 掛在該名單上方（不再用 `insertAdjacentHTML` 手動插在 `room-note` 後面——`hideSetup` 模式下本來就沒有 `room-note` 這個元素了），**預設關閉**，按下後每位玩家的連結卡片下方會即時多印一份**縮小版的卡片重現**（`cardPreviewText()`），不是純文字轉述而是照著卡片本身的視覺元素畫：目標字用跟卡片上 `.taboo-target` 同款的深底框線小方塊（emoji＋粗體字）、禁字是一顆顆獨立的紅色藥丸（`.cp-pill`，跟卡片上 `.tb-word` 同一套配色），凡是卡片上**真的是按鈕**的東西這裡也用按鈕外觀呈現（`.cp-btn`）：guesser 沒搶答時的「🙋 Buzz in!」是綠色實心按鈕、referee 的「🚨 Taboo!」是紅色實心按鈕、有人搶答時 clue giver 那兩顆「✅ Correct／❌ Not it」判定鈕也各自用綠／紅按鈕呈現，其餘像排隊中、已搶答、被鎖住、回合結果等純資訊性狀態則維持一般文字（`.cp-note`），讓主持人不用拿起手機就能一眼看出每個人卡片上「長什麼樣子」，不只是「寫了什麼」；再按一次變回「🙈 Hide player cards」收合。主題色靛 `#6366f1`） |
| `pick-a-side.html` | 🔥 Pick a Side!（這頁的 `<style>` 開頭補了一行 `:root { --accent: #ff7043; }`——其餘七頁本來就有，只有這頁從頭到尾把 `#ff7043` 寫死，一直沒人發現，直到 `shared.css` 出現真的要靠 `var(--accent)` 取色的共用元件（說明頁）才炸出來：那些元件在這一頁會拿到空值，變成沒有底色的深字。主持人按 Spin 抽一句爭議意見，每位玩家在自己的卡片上按 👍 Agree／🤷 It Depends／👎 Disagree，主持人畫面的三根長條即時累加；主持人自己也可以直接點畫面上的按鈕幫沒有連結的人計票，最終票數 =「卡片票 + 手動點的票」。每次 Spin 會換一個 `voteId`，上一輪的票自動失效；`ROOM.init()` 帶 `hideSetup: true`，人數／連結已在 `index.html` 設定好；頁面最下方有**全部 10 句犀利意見**供主持人瀏覽（`hottake` 是純字串陣列、沒有分類，所以跟其他遊戲的題庫瀏覽比起來少了分類篩選列，直接編號 #1–#10 列成一個 grid），**預設收合**（按「👁️ 瀏覽全部 10 句犀利意見」才展開，避免分享畫面時暴雷），主題色橘 `#ff7043`） |
| `sophies-choice.html` | 🤔 Sophie's Choice（抽一則兩難劇本，每位玩家在自己的卡片上按 A／B／C（C 是「自己的答案」），主持人畫面在劇本卡下方顯示三欄即時統計：票數 + 投給該選項的玩家名字，最高票那欄框線亮起；`ROOM.init()` 帶 `hideSetup: true`，人數／連結已在 `index.html` 設定好；頁面最下方有**全部劇本**供主持人瀏覽（含分類篩選列），**預設收合**（按「👁️ 瀏覽全部 N 個情境」才展開，避免分享畫面時暴雷），主題色琥珀 `#f59e0b`） |
| `persuade-together.html` | 🤝 Persuade Together!（抽劇本時會**隨機發牌決定誰當裁判**：一人拿到 👨‍⚖️ Judge、其餘拿到 👥 Persuader Team，各自在卡片上看到自己的身分與劇情；主持人畫面的「The Judge」下方會標出「🎴 Played by 名字」，並在下方顯示一條裁判狀態列（等待中／✅ 被說服／❌ 沒被說服）。裁判的卡片上有「✅ You convinced me!／❌ Not convinced」兩顆按鈕，按下後主持人畫面即時變色；`ROOM.init()` 帶 `hideSetup: true`，人數／連結已在 `index.html` 設定好；頁面最下方有**全部劇本**供主持人瀏覽（含分類篩選列），**預設收合**（按「👁️ 瀏覽全部 N 個場景」才展開，避免分享畫面時暴雷），主題色青 `#06b6d4`） |
| `youre-in-the-scene.html` | 🎭 You're In The Scene（抽場景後**自動依房間人數把角色隨機發給每位玩家**，每個人的卡片上只有自己那一個角色（字母、角色名、演法提示）＋場景描述，不用再互相喬誰演誰；主持人畫面的角色卡下方會標出「🎴 名字」對照表；`ROOM.init()` 帶 `hideSetup: true`，房間人數／連結已在 `index.html` 設定過，這頁的 `room-mount` 只剩發牌後的唯讀名單顯示每人分到的角色；頁面最下方有**全部場景**供主持人瀏覽（含分類篩選列），**預設收合**（按「👁️ 瀏覽全部 N 個場景」才展開，避免分享畫面時暴雷），主題色粉 `#ec4899`） |
| `word-wolf.html` | 🐺 Word Wolf（主持人頁：選 3–6 人、發牌，多數人拿到同一個字、一人拿到臥底字；`ROOM.init()` 帶 `hideSetup: true`，人數／連結已在 `index.html` 設定好，這頁的 `room-mount` 只留 `actionsHTML`（Deal Words／Reveal All／Pick Someone 三顆按鈕）與發牌後標出誰拿到哪個字、誰是臥底的唯讀名單；**🎲 Pick Someone 一開房間就會列出所有玩家的姓名欄**（`ROOM.setDecorator()` 之後多一行 `ROOM.render()`，讓還沒發牌也看得到完整名單），不用等到抽過一次才冒出來——`ROOM.setDecorator()` 回傳的 `answer` 在未揭曉（`revealed === false`）時一律是一個零寬字元（`&#8203;`），不再只有「目前被點到」或「這輪已經點過」的人才有值，因為 `room.js` 的精簡名單只畫得出 `answer` 為真的列（見 `room.js` 該段說明），舊寫法會讓還沒被點過的玩家整欄從畫面上消失，跟「一次看到所有人名字」的用途相反；**被點到的人（`is-picked`）與這輪已經點過的人（`is-picked-done`）現在底色也會跟著變**（`rgba(217,70,239,.18)` 洋紅／`rgba(20,184,166,.14)` 青綠，各自跟自己的框線同色系），不再只有框線與外發光改變顏色——純框線在同一片深色底的分享畫面上不夠明顯，容易讓人分不出誰被點到。原本設定卡下方另一段從未被任何程式碼填過內容的死區塊「🔗 Player Links」標題＋空的 `#player-links` 容器已一併移除。頁面最下方有**全部 80 組詞組**供主持人瀏覽，依類別分組、每組標上編號與「A vs B」，**預設收合**（按「👁️ 瀏覽全部 80 組詞組」才展開，避免分享畫面時暴雷）） |
| `dare-conquest.html` | 🏰 Dare Conquest（主持人單頁，Firebase 只用於選配的玩家投票、沒有 Firebase 時整個遊戲照常運作：選 2–6 人、8×4 寬螢幕比例的 board 外圍 20 格路徑，START 在左上角、順時針前進，輪到自己時按地圖正中央的「🎲 Roll」丟一顆骰子、移動 1–6 格（只有一顆骰子，沒有兩顆骰子的選項）；骰子按鈕旁還有兩顆技能鈕「🎯 High Roll (N)」與「🔻 Low Roll (N)」，各自整局限用 2 次，要在按「Roll」之前先按下啟動（按鈕文字不變，改成框線變紅、文字變黃色表示已啟動），啟動後當回合丟出的每一顆骰子都保證是 4/5/6（High Roll）或 1/2/3（Low Roll），直接換到下一位玩家後才會解除；已啟動的技能再按一次可以取消（框線與文字變回灰色）並退回這次名額，若啟動其中一個時另一個技能還沒用掉、按下另一顆技能鈕會直接切換（原本啟動的那個技能名額退回、改扣新按的），兩者互斥、同時只會有一個生效，次數用完按鈕不會消失，會顯示「(0)」並鎖定無法再按，次數會跟著玩家資料一起存進 localStorage，重新整理不會重置，停在一般格上，若該地已經是自己的土地：不會有任何事發生，直接換下一位玩家；停在無主或對手土地上才會先跳出綠色/紅色的「Truth or Dare」選擇畫面，由當前玩家自己自由喊出「😇 TRUTH」或「😈 DARE」，主持人依玩家喊的內容點擊對應按鈕（不是系統或對手指定）：選 TRUTH 會從 50 則真心話題庫隨機抽一題讓玩家誠實回答（按鈕文字為「✅ Honest!／❌ Pass!」、面板呈金色主題、題目除了個人隱私類（尷尬回憶、小謊言、不敢說的怪癖等）也有不少評論／比較其他玩家的題目，例如「你覺得這房間裡誰最可能先被淘汰？」「{player} 給你的第一印象是什麼？」，帶點爭議性與互動性）、選 DARE 則從一般 50 題互動挑戰題庫隨機抽一題（跟其他玩家告白、開嗆聲互酸、投票淘汰、幫其他玩家頒一個損人不利己的獎、唱指定歌曲、說笑話、講繞口令等，過半題目會直接點名 {player} 互動），不論 TRUTH 或 DARE 全部題目一律 45 秒，題目文字本身不再寫秒數（改由計時器顯示），畫面會顯示一顆「⏱️ Start 45s Timer」按鈕，主持人先把題目唸完、按下按鈕才開始倒數，倒數圈會隨秒數變色：綠→橘→紅並閃爍，開始倒數後會多出現「⏸️ Pause／▶️ Resume」與「🔄 Restart」兩顆按鈕，可以暫停/繼續、或直接重新從頭倒數；倒數秒數會同步到**每一位玩家**的卡片上（當事人在題目下方、其他人在投票鈕上方，都是一顆同款的倒數圈，一樣綠→橘→紅並閃爍），主持人端只在狀態改變時（開始／暫停／繼續／重新開始／歸零）推送一次 `players/{token}/conquestTimer`，卡片收到後自己每秒遞減，避免每秒寫一次 Firebase，過程中 Did It!/Failed!（或 Honest!/Pass!）仍可隨時按，計時不會自動判定，完成才算過關；**設定畫面現在只剩一顆「🎲 Start Game」按鈕**（`#setup-card` 裡的 `.control-row`，外加規則說明 `.rules-note`）：房號／人數／姓名／連結這一步已經搬到 `index.html` 的 Step 1 統一設定過一次，`dare-conquest.html` 原本因為沒用 `room.js` 而自己仿寫的那一整份設定 UI 與邏輯——ROOM 列（房號輸入框／Load／New Room）、PLAYERS 人數鈕、「🔗 Player Links」每人一欄的姓名輸入框＋Link／QR、Copy All Links、Send Card Check，以及背後的 `cardURL()`／`cardShareText()`／`copyCardLink()`／`copyAllLinks()`／`sendCardCheck()`／`clearCardCheck()`／`qrSVG()`／`toggleCardQR()`／`renderPlayerLinks()`／`loadRoomCode()`／`newRoom()`／`setPlayerCount()` 等函式與對應 CSS——已整段移除；`initSession()`（沿用 `room-last-session` 讀出房號／人數／姓名／token，跟其他遊戲共用同一包 localStorage）與 `attachVoteListeners()`（用 `ensureTokens()` 依人數配 token 供玩家投票用）不受影響、Start Game 時一樣各自跑一次，所以房號、玩家人數、姓名依然是跟其他遊戲共用的同一組設定，只是不能再從這頁調整人數或補發連結——要改，回 `index.html` 的 Step 1（「✏️ Edit room setup」）。輪到誰的回合時，**那位玩家的卡片上會由上而下垂直排列「🔻 Low Roll (N)」「🎯 High Roll (N)」兩顆技能鈕與一顆「🎲 Roll the Dice」按鈕，按下去就會觸發主持人畫面上對應的動作**（技能的啟動／取消／互斥切換與名額扣退全部由主持人端的 toggleSkill 處理，卡片只是發出請求，啟動中的技能在卡片上一樣是紅框＋黃字，次數用完顯示「(0)」並鎖定；擲骰同理）（骰子與結果仍由主持人畫面決定；主持人畫面正在擲骰／移動／有挑戰進行中時三顆按鈕都會自動變灰，不是自己回合則完全不顯示，重複送出的請求會被忽略）；接著跳出 Truth or Dare 選擇畫面時，**同一位玩家的卡片上也會出現 😇 TRUTH／😈 DARE 兩顆按鈕**，可以直接從自己手機喊題型（不必用講的讓主持人代點；其他人的卡片顯示回合列、「picking Truth or Dare…」與 ⏳，主持人畫面上的兩顆按鈕仍然可以按，先到先算），主持人抽到題目時會把「誰的挑戰／題型／題目文字」推送到每位玩家的卡片（`players/{token}` 的 `conquest` 欄位，`{player}` 會換成純文字名字），**其他玩家**的卡片上出現跟主持人畫面一樣的兩顆按鈕（✅ Did It!／❌ Failed!，Truth 題則是 ✅ Honest!／❌ Pass!），投票寫進自己的 `players/{token}/conquestVote`（含該題的 id）；**輪到的那位玩家自己不投票**，他的卡片改成顯示這題的題目文字（他要照著做，是唯一需要在手機上看到題目的人），主持人畫面把票數直接做成兩顆判定按鈕內的小圓形徽章（「✅ Did It! (2)」「❌ Failed! (1)」），該側 0 票時徽章不出現、按鈕跟沒有投票功能時長得一樣，但**投票只是參考、最終仍由主持人按判定按鈕**；每抽一題會產生新的 vote id，舊票自動失效不用清除，換題或判定後玩家卡片會回到待機狀態（名字、回合列、⏳、戰績）；未設定 Firebase（或 CDN 連不到）時投票區塊自動隱藏，遊戲完全不受影響；抽到題目後、判定之前，挑戰卡右上角（跟題型標籤同一排）還有一顆「🔄 New Question (N)」，玩家看到題目覺得不想做可以直接換一題（換到的一定是同類型：抽 TRUTH 就換 TRUTH、抽 DARE 就換 DARE），按下去不會馬上換，會先在按鈕正下方跳出「Change this question?」確認小視窗（✅ Yes／✖️ Cancel，並顯示剩幾次），確認期間題目與判定按鈕會變暗且不可點，避免誤觸浪費次數；每位玩家整局限用 3 次，換題會重置計時器、被換掉的題目這輪不會再出現，次數用完按鈕不會消失、顯示「(0)」並鎖定，次數跟 High/Low Roll 一樣會存進 localStorage；若落地的是無主土地，過關即直接佔領，若落地的是對手土地（搶地盤），過關後不會馬上換手，而是彈出金色「🎲 Dice-Off!」面板讓進攻方與地主各自的骰子（可一次看到雙方骰子與點數）比大小：進攻方先按「🎲 Roll」骰（進攻方的骰子只會出現 4、5、6，算是完成挑戰後的小優勢，地主骰子維持 1～6 正常機率）、骰出結果後地主的骰子按鈕才會解鎖，地主按下後骰；**這兩顆骰子也可以由當事人自己在卡片上按**（進攻方與地主各自的卡片會出現一顆「🎲 Roll」，主持人畫面上該側按鈕還沒解鎖時卡片上也是灰的，其他人的卡片只顯示「A vs B」與 ⏳；主持人端會驗證是不是本人、以及該側是否真的可以骰，主持人畫面上的兩顆按鈕一樣可以代按）、進攻方點數大於地主才算搶地成功（平手算地主守住），骰出結果的瞬間贏的一方骰子會立刻發光脈動、頭上跳出皇冠 👑、點數變成綠色高亮，輸的一方則變暗變灰階，清楚標示贏家，成功搶地瞬間該格會有搶奪特效：格子震動晃動、指揮官色系放射狀色彩衝擊波、白色粒子爆散、並彈出「🏴 CAPTURED!」浮動標籤，挑戰沒過關（Failed!/Pass!）則不會進入 Dice-Off、土地直接留在地主手上，地圖格子不顯示題型、可佔領的一般格不放任何圖示（純空白，佔領後才會整格變色），20 格中除了 START 還有 2 個 BONUS、1 個 LUCKY、1 個 GIFT，停在 START/BONUS/LUCKY/GIFT 上都要先過一次 Truth or Dare 挑戰（同樣由玩家自選 TRUTH/DARE、主持人點對應按鈕，面板會依格子類型變色並顯示「🏁/⭐/🎲/🎁 START/BONUS/LUCKY/GIFT Challenge」），過關才會真的觸發獎勵、失敗則什麼都不會發生直接換人：BONUS 必定從目前地最多的對手（領先者）手上奪取 2 塊地（多人並列領先時隨機挑一位下手），觸發搶奪特效；只有在沒有任何對手擁有土地時（例如遊戲剛開始）才會改成隨機取得 2 塊無主土地、LUCKY 與指定玩家交換全部領地（2 人局自動指定唯一對手，3 人以上由主持人點選對象）、GIFT 效果與 BONUS **完全相同**（同樣從領先者手上奪取 2 塊地，只有圖示、顏色與文案不同；兩者共用同一個 `seizeFromLeader()`，數量由 `SEIZE_COUNT` 常數控制），START 格現在也帶有 LUCKY 屬性：玩家停在 START 時不再只是「安全格、什麼都不會發生」，過關後會直接觸發跟 LUCKY 格一樣的「交換全部領地」效果，觸發特殊事件時該格會有發光閃爍特效、挑戰卡框線與圖示也會依事件類型變色，特殊事件訊息不會自動消失，主持人看完按下「👍 OK」才會換下一位玩家，玩家目前所在格子（無論一般格或特殊格）都會顯示玩家圖示，輪到誰的那位玩家會有明顯標示：所在格子外圍有對應顏色的發光脈動外框、該玩家的圖示會放大並持續彈跳、圖示上方有小箭頭指標與擴散光環，換人時立即跟著移動，方便主持人一眼看出目前輪到誰，一般土地佔領後整個格子會填滿玩家對應顏色（不再只是外框變色或右上角小圖示），格子正中央還會顯示該玩家的名字（白字、超過一行會自動換行最多兩行並截斷），骰子改為可 3D 翻滾的真實骰子（CSS 3D 立方體、6 面點數、擲出時有拋起彈跳動畫）並放在地圖正中央，回合資訊獨立顯示在 board 上方的控制列（「Player X's turn · Round N」，Round 是每個玩家都輪過一次才會 +1 的完整回合數，不是每人每次擲骰都算一輪），挑戰卡改為浮動疊加在地圖正中央的卡片（不佔版面、不會把地圖往下擠），只有真的有挑戰/特殊事件/LUCKY 選擇對象時才會出現，地圖上方所有區塊（標題、計分板、控制列）皆採緊湊排版以節省版面，遊戲不會因為土地全數佔領而自動結束、答對挑戰仍可從對手手中搶地讓局勢持續變化，遊戲畫面沒有「Restart Board」「End Game」「Change Players」等控制按鈕，相鄰（棋盤外圈路徑上前後相接）且同一位玩家擁有的土地之間，交界處會出現一小段磚紋橋樑/城牆結構、顏色跟該玩家的地一致，視覺上把兩塊地連成一體（純視覺標示，不影響分數），頁面最下方分別有 Dare 題庫與 Truth 題庫供主持人瀏覽，兩份題庫**各自獨立、預設收合**（各自一顆「📋 瀏覽全部 N 個大冒險／😇 瀏覽全部 N 個真心話」按鈕，互不影響——只展開 Dare 不會連帶展開 Truth，避免分享畫面時暴雷），遊戲進度會在每個回合結束（換人、Dice-Off 結算、特殊格確認）時自動存進 `localStorage`（key: `dareConquestSave.v1`），只存「回合與回合之間」的乾淨狀態、不存挑戰/Dice-Off 進行到一半的中間畫面，重新整理或關閉分頁後回到設定畫面會看到「🕹️ You have a game in progress」提示，可選擇 Resume Game 還原棋盤與計分，或 Start New Instead 捨棄重來） |
| `kangaroo-court.html` | ⚖️ Kangaroo Court（主持人單頁；人數 **4–9 人**（陪審團人數 = 總人數 − 4，若關掉 Judge 則是總人數 − 3）與玩家連結已在 `index.html` 統一設定過，這頁的 `ROOM.init()` 帶 `hideSetup: true`，`room-mount` 只留 Judge 開關（`extraRowHTML`）與發牌後標出每人角色的唯讀名單，每局隨機發出 🙇 Defendant／⚔️ Prosecutor／🛡️ Defense Attorney／🧑‍⚖️ Judge 四個不同角色（Judge 是否上場由主持人自己控制，見下方「Judge 開關」），其餘全是 👥 Jury；發牌方式仿 `say-it-without-saying-it.html`：Defendant 用一個洗過牌的袋子輪流發、每人都當過一次才重新洗牌且不連莊，Prosecutor／Defense／Judge 則各自從「目前擔任次數最少」的人裡隨機挑（Judge 只在開關為 ON 時才會抽），四個角色互斥，長期次數完全平均。按「⚖️ Draw a Charge」從 30 條短、生活化、用字簡單的「罪名」（`game-data.js` 的 `kangaroo`，每條是一個能一口氣唸完的小情境，通常兩個小節：一個大家都做過的小習慣，接一個好笑的轉折，讀起來像個迷你故事而不是乾巴巴的罪名標籤，刻意不用法律術語或生僻字，例如「wearing headphones with no music playing, just to avoid talking to people」「using someone else's shampoo in the shower and never mentioning it」）隨機抽一條，同時開放陪審團在自己卡片上**秘密**投票 Guilty／Not Guilty（寫進 `players/{token}/kcPrevote = {id, choice}`，這一票在結果公布前完全不會顯示在主持人畫面或任何人的卡片上）。流程共九步（這局有 Judge 的話是十步），主持人依序按左側流程圖下方那顆狀態鈕（同時是 CTA，見下）推進：開放陪審團投票 → Prosecution 發言（只有一段論述，不分開庭／結辯）→ **Prosecution's Witness** → Defense 發言 → **Defense's Witness** → Cross-Examination（Prosecutor／Defense 輪流問 Defendant 問題、Defendant 必須回答）→ 陪審團**最終**投票（寫進另一個獨立欄位 `kcFinalvote`，跟 `kcPrevote` 分開存，因為兩票都要留到 Reveal 才一起讀出來比較，若共用同一個欄位會被最終投票蓋掉）→「⚖️ Reveal Verdict!」→（這局有 Judge 才會出現）**🧑‍⚖️ Judge's Ruling**（純儀式性的最後一步，沒有任何按鈕或功能——主持人按狀態鈕推進，把發言權交給 Judge，讓他當場口頭宣讀已經公布的判決；沒有 Judge 的局直接從 Reveal Verdict 跳去開下一案，不會出現這步，`FLOW_STEPS` 側欄清單也不會顯示這一格，靠 `judgeIdx >= 0` 動態過濾）。Reveal 揭曉的判決結果與完整陪審團明細，會一路留到 Judge's Ruling 這步結束才清空（`payloadFor()`／`renderJuryTally()` 都在 `phase === 'reveal' || phase === 'judgment'` 時顯示同一份結果），讓法官宣讀判決時，主持人畫面與所有玩家卡片上都還看得到剛公布的判決。每一方都是先發言、再傳喚證人補強論點，兩方之間依然交替出場、絕不連續兩步輪到同一方。

**🧑‍⚖️ Judge 開關**：ROOM 設定卡片（`room.js` 的 `extraRowHTML`）多一列「🧑‍⚖️ Judge」的無外框 ON/OFF 切換鈕（`toggle-btn`，跟 `say-it-without-saying-it.html` 的 Forbidden words 開關同一套視覺，配色換成 Judge 角色色紫 `#8b5cf6`），預設 ON；狀態存進同一包 room session（`ROOM.setExtra('judgeEnabled', …)`），換遊戲、重新整理都會保留。切換只影響**下一次** Draw a Charge：`dealRoles()` 只在開新案件時讀一次這個開關，正在進行中的案件不會被動到。關掉時該局不發 Judge，陪審團因此多一位（回到總人數 − 3），角色卡片、流程圖側欄、玩家連結欄的 Judge 相關 UI 都會自動不顯示；重新打開開關不會補發，要等下一次 Draw a Charge。**4 人局強制關閉 Judge**：Defendant／Prosecutor／Defense 三個角色已經佔滿 3 位，4 人局若再發一位 Judge 陪審團就會歸零、投票沒有意義，所以人數選到 4 時開關會整顆鎖住變灰、文字改成「OFF (needs 5+ players)」，`toggleJudge()` 這時點了也不會有反應；若原本 5 人以上且已開啟 Judge，中途把人數調到 4 會自動關閉（`onCountChange`），調回 5 人以上開關才會解鎖（但不會自動重新打開，要主持人自己再按一次）；`dealRoles()` 另外也擋了一層（`judgeEnabled && n >= 5` 才會抽 Judge），就算開關狀態不小心跟人數對不上也不會真的發出一個沒有陪審團的 Judge。**這款遊戲本身需要至少 4 人**（被告／檢察官／辯護律師三個固定角色，加上沒有法官時至少要留 1 人當陪審團），但房間人數是在 `index.html` Step 1 統一設定、七款遊戲共用的同一個數字，`ROOM.init()` 傳的 `counts: [4, 5, 6, 7, 8, 9]` 只是「這頁自己看得懂的範圍」，不代表可以把共用人數直接搬過來用——若房主在 Step 1 設定的是 3 人以下（Kangaroo Court 用不到的人數），這頁**不會**把 `ROOM.count` 偷偷墊高成 4 並存回共用的 `room-session-{CODE}`（那樣一來，房主晚點切回 `index.html` Step 1 會發現人數莫名其妙從 3 變成 4——這是實際發生過的 bug，根因是 `room.js` 的 `ROOM.init()` 以前不分青紅皂白地對每個遊戲頁都呼叫 `clampCount()` 把讀到的人數硬夾進該頁自己的 `counts` 範圍、夾完又經由 `ensureTokens()`／`save()` 寫回同一包 localStorage，等於任何一款 `counts` 範圍比首頁窄的遊戲，光是被訪問過一次就會悄悄覆寫全站共用的人數；`room.js` 現在改成只有非 `hideSetup`（目前只有 `index.html` 自己）才會夾範圍，七款 `hideSetup: true` 的遊戲一律照實讀共用人數，絕不回寫夾過的值）。取而代之，`fb-warning` 下方多一條同樣風格的 `#count-warning` 提示，`ROOM.count < 4` 時顯示「⚠️ Kangaroo Court 至少需要 4 位玩家…請回首頁把房間人數調高」，狀態鈕（`advance()`／`goBack()`／`dealNewCase()`）在人數不足時全部直接 return（`countOK()` 統一判斷），`renderStatus()` 也會把狀態鈕文字換成「🔗 房間人數不足（需要 4 人以上）」並加上 `not-clickable`，跟 Firebase 未設定時的鎖法一致；要玩，只能回 `index.html` Step 1 把人數調到 4 以上，這頁自己絕不會替房主做這個決定。全程沒有倒數計時，每個階段都由主持人自己掌握節奏、按狀態鈕才推進到下一步；狀態鈕在進入 Reveal 階段時會脈動提醒主持人該按下一步。狀態鈕下方另有一顆較低調的「⬅️ Previous Step」，可以退回上一階段修正誤按：由於每一方的證人只在各自的 Witness 階段抽一次、直到對方進入自己的 Witness 階段前都不會被覆寫，所以退回任何一個 Witness 階段都能拿回原本那位證人，不需要重抽。Pre-Vote（第一步）與尚未抽罪名時沒有東西可以退，按鈕會鎖住。

**⚔️🛡️ 傳喚證人（Witness）**：Prosecution's Witness／Defense's Witness 這兩步，各自從**目前的陪審團**裡隨機挑一位扮演證人（跟 Prosecutor／Defense 一樣的「目前次數最少」公平演算法，`witnessCount` 記錄每人被抽中次數；同一案子優先讓兩位不同的陪審員分別演雙方證人，`witnessThisCase` 記錄本案已用過的人，人數不夠（例如只有 1 位陪審員）時允許同一人連續扮演兩次），同時從新題庫 `game-data.js` 的 `kangarooWitness` 隨機抽一張證人卡（`{ emoji, name, evidence }`，`evidence` 刻意寫得簡短，一行放得下，例如「🧑‍🏫 The Old Teacher — remembers this exact behavior from years ago」）。主持人畫面此時：①罪名卡本身排版跟兩張證人卡看齊、統一走**小標題列（icon＋文字）在上、內容在下**的格式（emoji 縮小跟「⚖️ THE DEFENDANT IS CHARGED WITH…」小標籤同一行，罪名句子在下面一行；不像早期版本那樣把大 emoji 跟句子擠在一起硬撐出很高的卡片），高度因此跟下面的證人卡幾乎一樣高，三張卡疊在一起不會一大一小；②角色卡（這局有 Judge 的話依序是 Judge／Defendant／Prosecutor／Defense，Judge 固定排最前面；沒有 Judge 就只有後三格）旁多一格金黃色「🕵️ Witness — 某某」，正在傳喚的那一方（Prosecutor 或 Defense）跟 Witness 兩格同時發光（只在該方的 Witness 階段當下顯示，過了就消失；Judge 則是輪到 Judge's Ruling 那一步時自己發光）；③罪名卡下方是一份**證人記錄**（`#witness-log`），依實際發生的先後順序（永遠是 Prosecution 先、Defense 後）往下疊放——**傳喚過的證人會一路留在畫面上到這局結束**，不會在換下一步之後就消失：兩張證人卡**用不同顏色區分**、且跟角色卡的配色對應——Prosecution 的證人是橘色（跟 Prosecutor 角色卡同一色），Defense 的證人是藍色（跟 Defense 角色卡同一色），一眼就能看出這張是哪一方傳喚的；正在傳喚中的那一張標「⚔️/🛡️ Prosecution/Defense calls their witness…」並整張發光；已經問完的那一張標題改成過去式「called their witness」、發光效果收掉、透明度降到 .7，但內容（證人 emoji 跟角色名同一行顯示、下面接證物一句話＋「🎭 Played by 某某」，emoji 跟名字擠在同一行是為了省版面，兩張證人卡疊在一起時才不會太佔高度）原封不動留著，讓主持人分享畫面時，晚一步才專心聽的人也能回頭看到前面證人說過什麼；**Prosecution 的證人卡在 Defense／Defense's Witness 這兩步會暫時整張隱藏**（`renderWitness()` 的 `hideProsecution` 判斷），把畫面留給正在進行的 Defense 論述與證人，直到 Cross-Examination 開始才重新出現（Defense 的證人卡不受影響，一直都在），用「⬅️ Previous Step」從 Cross-Examination 退回 Defense's Witness 也會正確重新把 Prosecution 證人卡藏起來；重新 Draw a Charge 才會清空這份記錄。玩家卡片（`play.html`）依 `isWitness` 欄位切換：被抽中的陪審員看到金黃主題卡＋證人角色與證物提示（「Stay in character — improvise your answers!」）；正在傳喚的那一方看到「❓ Question your witness!」；其餘所有人（被告、另一方、其他陪審員）只看到一行「👀 某某 is testifying…」。**這款遊戲沒有計分**：Reveal 時只比較每位陪審員的預投票與最終投票，標示誰翻了票（往哪個方向），純粹當作話題／戰績參考，不換算成任何分數；**被告**的命運只看最終票數：Guilty **大於等於** Not Guilty（含平手、含沒人投票）就是有罪，只有 Not Guilty 真的較多才是無罪——這是一個 Kangaroo Court，平手不會給被告罪疑唯輕的好處。主持人畫面有一份「⚖️ Verdict」卡：投票期間顯示「N / M 位陪審員已投票」，Reveal 後改成完整結果——逐一列出每位陪審員的預投票→最終投票（翻票的會標一枚「flipped to Guilty」或「flipped to Not Guilty」的小標籤，純資訊、不帶分數）。玩家連結區用 `room.js`，Defendant／Prosecutor／Defense／Judge 的連結欄位分別用紅／橘／藍／紫框線標示身分（`is-role-defendant`／`is-role-prosecutor`／`is-role-defense`／`is-role-judge`）；Jury 的連結欄位不特別標色。玩家卡片（`play.html` 的 `game: 'kangaroo'`）依 `role` 與 `phase` 切換：非 Jury 在投票階段看到「🤫 陪審團正在投票」等待訊息；Jury 在 `prevote`／`finalvote` 階段看到 🚨 Guilty／✅ Not Guilty 兩顆大按鈕；輪到自己發言的 Prosecutor／Defense 看到「🎤 The floor is yours」，其他人看到「⚔️/🛡️ 某某正在發言…」；Cross-Examination 階段 Defendant 看到「🎯 Answer every question」、Prosecutor／Defense 看到「❓ Ask the defendant a question」、Jury 看到「👀 Watching…」；**Judge's Ruling** 階段（只在這局有 Judge 時才會出現）Judge 自己的卡片顯示「🧑‍⚖️ Deliver your ruling!」（純文字、沒有按鈕——這一步不需要任何互動，只是提醒該他上場口頭宣讀陪審團的判決），其他人看到「👀 某某 is delivering the verdict…」；Reveal 階段每張卡片都會顯示這局的判決結果，以及（若是陪審員）自己是否翻票。沒有 Firebase 時整局無法進行（陪審團的票必須保密，不能讓主持人畫面直接看到），會顯示警告並鎖住「Draw a Charge」按鈕。頁面最下方有**全部 30 條罪名**供主持人瀏覽，**預設收合**（按「👁️ 瀏覽全部 30 條罪名」才展開，避免分享畫面時暴雷）；主題色紅 `#e94560`（`index.html` 裡唯一沒被其他遊戲用過的預留色）） |
| `play.html` | 🔗 **全部八款遊戲**共用的玩家頁面：玩家開啟自己的私人連結後看到當下這局的內容，依 Firebase 資料中的 `game` 欄位自動切換樣式；同一個 Room Code + 同一批玩家連結可以在八款遊戲間直接切換使用，不用重新產生連結。所有卡片最上方都用**同一顆名字晶片**（同字級、同形狀）。**卡片上的大字永遠不會被從中間切斷**：Word Wolf／核對卡的單字（`.card-word`）、Say It Without Saying It 的目標字（`.tt-word`）、You're In The Scene／Persuade Together 的角色名（`.rb-name`）、Kangaroo Court 的證人名（`.wb-name`）這四種「一個字就是整張卡」的文字，字級原本是寫死的、或由 `wordFontSize()` 依字串長度分級猜的——但那個猜法不知道這支手機有多寬，實測 320–390px 下 Neighborhood／Videographer／Procrastination 都會被拆成「Neighborho／od」這種前後看起來都不是字的兩段。現在 `showCard()` 改成先 `renderCard()` 畫完、再跑一次 `fitCardWords()`：用 Range 量出每個英文單字實際佔了幾行，只要有單字跨行就把字級調小 1px 再量一次，直到整個單字落在同一行或碰到各自的下限（18／18／16／14px）。刻意用「量」而不是再加一層字數分級，因為題庫可以從 Google Sheet 換成任何人寫的字（見 `sheet-data.js`），字數分級永遠追不上。中文不參與這個調整（`wordSplitAcrossLines()` 會跳過含 CJK 的字串）——中文本來就沒有空格、整句就是一個「字」，到行尾換行是正常排版，跟著縮小只會讓每張卡的說明文字莫名其妙變小。實測 300 個 taboo 目標字、160 個 Word Wolf 詞、200 個核對字、全部場景角色與證人名在 320／360／390px 三種寬度下都沒有任何單字被切斷，縮到最小的是 320px 的場景角色名 17px，其餘都在 21px 以上。**八款遊戲共用同一組玩家配色**，前六色順序就是 Dare Conquest 棋子的順序：1 紅 `#ef4444`／2 藍 `#4f9eff`／3 綠 `#22c55e`／4 黃 `#f59e0b`／5 紫 `#a78bfa`／6 青 `#06b6d4`，所以同一個人不管主持人切到哪一款遊戲，卡片顏色都不會變，也永遠對得上他在棋盤上的棋子。Dare Conquest 棋盤本身只有 6 顆棋子（人數上限 6），第 7、8 色（萊姆綠 `#84cc16`、粉紅 `#ec4899`）只給允許超過 6 人的遊戲用，目前只有 Kangaroo Court（4–9 人）會用到。晶片是**整塊填滿該玩家的顏色**、配深色文字（`#0b0b18`）：八個顏色配深字的對比度都在 5:1 以上（紅色最低，實測 5.19:1），彩色文字配深底則做不到八色都合格。四款「用手機作答」的遊戲各有一組樣式：`game: 'hottake'` 顯示題目＋👍/🤷/👎 三顆按鈕、`game: 'sophies'` **不顯示情境**（情境留在主持人分享的畫面上，卡片再印一次只會讓大家低頭看手機），只有 A/B/C 三顆選項按鈕（兩者都寫進 `players/{token}/vote = { id, choice }`，可隨時改投）、`game: 'scene'` 顯示自己被分到的角色與提示、`game: 'persuade'` 顯示自己是 Judge 還是 Team（只有 Judge 有 ✅／❌ 判決鈕）、`game: 'taboo'` 依 `state` 與 `isGiver` 切換：`state: 'ready'`（題目已抽、還沒開始）時 clue giver 看到一顆「👀 看題目」按鈕（按下去才會拿到目標字並讓全場開始倒數，見 `say-it-without-saying-it.html` 那一列），其他人看到「等 {name} 開啟題目…」＋⏳；`'playing'` 時 clue giver 看到目標字＋禁字、guesser 只看到禁字＋🙋／🚨 兩顆鈕、回合結束後所有人看到結果與答案；答對而自動續下一輪時（見 `say-it-without-saying-it.html` 那一列），答案下方還會多一塊 `.next-up`：左邊一顆倒數圈（卡片自己每秒遞減，`nextTimer` 靠 `next.seq` 對時，跟回合計時同一套做法），右邊寫出下一回合的描述者與裁判是誰；輪到自己的那張卡整塊變綠並持續脈動、文字換成「🎤 換你描述！」／「👀 你當裁判」，讓下一位在這 5 秒內就把手機拿起來。底部一行是自己的積分（禁字清單上方的小標籤全站統一寫「🚫 Forbidden words」，不再用「Never say these」等其他說法，跟主持人畫面右側的 Forbidden words 開關、`play.html` 其餘提示文字用詞一致）。`game: 'conquest'` 時卡片刻意做得很輕：自己的名字、**一條永遠都在的回合列**（大字顯示現在輪到誰：色球圖示＋名字，整條的框線、底色、光暈都用該玩家的棋子顏色；輪到自己時顯示「YOUR TURN」並持續明暗脈動），當下能按的按鈕、題型標籤，以及底部一行自己的戰績（🚩 N tiles · Round N）；Truth or Dare 選擇階段回合列下方會多一行小字（自己是「Truth or Dare?」、別人是「picking Truth or Dare…」）。題目文字**只有正在接受挑戰的那位玩家**看得到（他要照著做；其他人看主持人畫面就好，卡片再印一次只會讓大家低頭看手機）。按鈕依狀態切換：輪到自己且可擲骰時是「🔻 Low Roll (N)」「🎯 High Roll (N)」兩顆技能鈕加上「🎲 Roll the Dice」（棋盤忙碌中則全部變灰）、Truth or Dare 選擇階段是 😇 TRUTH／😈 DARE 兩顆、有題目在進行時，別人是 ✅／❌ 兩顆投票鈕（選過的那顆會亮起、可以改投）、當事人則是題目文字（不能投自己），其餘情況只顯示一個 ⏳。`game: 'kangaroo'` 依 `role`（defendant／prosecutor／defense／judge／jury）、`phase` 與 `isWitness` 切換：Jury 在 `prevote`／`finalvote` 階段看到 Guilty／Not Guilty 兩顆按鈕（分別寫進 `kcPrevote`／`kcFinalvote`，兩個獨立欄位不互相覆蓋），其他角色看到「陪審團投票中」等待訊息；`prosecution-witness`／`defense-witness` 階段被抽中的陪審員（`isWitness: true`）卡片變金黃主題，顯示證人角色與證物提示，正在傳喚的那一方看到「❓ Question your witness!」，其餘人看到「👀 某某 is testifying…」；輪到發言的 Prosecutor／Defense 看到「🎤 The floor is yours」；Cross-Examination 階段 Defendant 看到「🎯 Answer every question」、Prosecutor／Defense 看到「❓ Ask a question」；Reveal 階段顯示判決結果（平手一律算 Guilty），陪審員另外會看到自己是否翻票；**`judgment` 階段**（緊接在 Reveal 之後、只在這局有 Judge 時才會出現）判決結果留在卡片上不會消失，Judge 自己另外看到「🧑‍⚖️ Deliver your ruling!」（純文字、沒有按鈕，這一步不需要互動，只是提醒該他上場口頭宣讀剛公布的判決），其他人看到「👀 某某 is delivering the verdict…」——這款遊戲不計分，全部只是資訊性顯示。`game: 'cardcheck'` 不屬於任何一款遊戲，是 `room.js`「🔍 Send Card Check」核對連結用的：卡片只有名字晶片＋大大的字＋emoji＋「Tell the host what you see!」，沒有任何按鈕或互動，純粹唸出來給主持人核對 |
| `firebase-config.js` | 八款遊戲共用的 Firebase Realtime Database 設定（host 與 play.html 共用，需自行申請免費專案並填入；全部共用同一個 `rooms/{roomCode}/players/{token}` 路徑。沒有 Firebase 時八款遊戲分成三種狀況：①**主要按鈕會被真的鎖住**的有 **Kangaroo Court**（陪審團的票必須保密）與 **Say It Without Saying It**（目標字沒有地方私下給 clue giver 看）這兩款，`deal.disabled = !ROOM.enabled`，按鈕文字改成「🔗 Player links needed」——**沒有任何「👁️ Reveal」之類的主持人畫面備援**，主持人畫面上絕不會出現目標字；②**Word Wolf** 只會顯示 `.fb-warning` 警告條、按鈕**不會鎖**，照按還是會發牌，但牌送不到任何人手上，所以實際上一樣不能玩；③其餘五款只是關掉卡片互動、遊戲本身照常可玩） |
| `room.js` | 🔗 **共用的玩家卡片層**（`ROOM`），**七款遊戲共用**（Pick a Side / Sophie's Choice / Persuade Together / You're In The Scene / Say It Without Saying It / Word Wolf / Kangaroo Court；只有 `dare-conquest.html` 因為設定畫面長得不一樣，保留自己的實作）。**這七款遊戲現在全部帶 `hideSetup: true`**——房號／人數／姓名／連結這一步已經搬到 `index.html` 的 Step 1 統一做過一次，遊戲頁自己不再需要重複那份設定 UI，`ROOM.init()` 收到 `hideSetup: true` 時會整個跳過下面這段「設定畫面 UI」的完整渲染邏輯（`buildDOM()`／`render()` 開頭各自多一條 `if (cfg.hideSetup) { … return; }` 分支），改走新函式 `renderCompact()`：不畫 ROOM 列／PLAYERS 人數鈕／room-hdr／room-note／Copy All Links／Send Card Check／姓名輸入框／Link／QR，只保留 `cfg.actionsHTML`（例如 Word Wolf 的 Deal Words／Reveal All／Pick Someone）與 `cfg.extraRowHTML`（例如 Kangaroo Court 的 Judge 開關、Say It Without Saying It 的 card-preview 開關）這兩個「真的是遊戲功能」的掛點，下面另外重新畫一份**唯讀**的名單：只列出「有 `setDecorator` 回傳 `.answer`」的玩家（沒發牌或還沒作答的人不出現），每一列是靜態的名字文字（不能編輯）＋該玩家的 `answer` 內容，`cls` 沿用同一套 CSS class（`.link-col.is-role-defendant` 這類），所以七款遊戲原本就有的角色/狀態配色完全不用改就能在精簡版底下正常顯示。要臨時調整人數或補發連結，回 `index.html` 的 Step 1（`✏️ Edit room setup`）處理，不能再從遊戲頁裡改。以下這段「設定畫面 UI」完整版的行為說明，現在只適用於 `hideSetup` 不為 true 的情況（目前只有 `index.html` 自己的 Step 1 是這樣用）。負責：房號與 token 的產生與保存、`room-session-{CODE}`／`room-last-session` 的讀寫（**跟其他五款遊戲完全共用**，人數與姓名雙向同步；`clampCount()` 只在 `!cfg.hideSetup` 時套用——也就是目前只有 `index.html` 自己會把讀到的人數夾進 `cfg.counts` 範圍。七款 `hideSetup: true` 的遊戲讀到共用人數時一律照實使用，就算比自己宣告的 `counts` 範圍還小或還大也不會夾，因為夾完的值會經由 `ensureTokens()`／`save()` 寫回同一包共用 session，等於把首頁設定的人數悄悄改掉；哪一款遊戲需要更多人才能玩（例如 Kangaroo Court 的 4 人下限），要由那款遊戲自己判斷並鎖住按鈕、提示回首頁調整，不能靠夾人數硬湊出一個不存在的玩家）、設定畫面 UI（ROOM 列 ／ PLAYERS 人數鈕 ／ 一位玩家一欄的姓名＋Link／QR，排版與 `word-wolf.html`、`dare-conquest.html` 完全一致，主題色由 `accent` 參數決定；每位玩家一欄的卡片用 CSS Grid 排版（`.room-links { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); }`，不是舊版的 `flex-wrap`）：220px 這個下限在這個網站實際會出現的版面寬度下自然算出「每排最多 3 欄」，人數不是 3 的倍數時，最後一排沒填滿的卡片會維持跟其他卡片一樣的寬度、不會被拉伸撐滿整排（例如 5 人局時第 4、5 位不會有一張佔滿整排的巨大卡片）；2–3 人這種本來就不到 3 欄的情況，`auto-fit` 會讓卡片照樣平分整排寬度，不會出現空白欄位。Link 按鈕複製成功後文字會短暫變成 Copied、按鈕本身也會綠色填色並跑一次放大彈回的 `copyPop` 動畫再變回 Link，`btn._copyRevert` 確保連續快點時只有最後一次點擊的還原計時器生效，`.btn-copy` 帶 `min-width: 0` 避免窄螢幕下文字收縮不了、把 QR 按鈕擠出卡片外）、`publish()` 推播到每位玩家的 token 節點並寫 roster、每個 token 各自掛一個 listener 回收玩家的回覆。姓名輸入框每打一個字就會觸發 `onNameChange` → `render()` 重繪整份連結列表（讓 decorator 能即時反映），但重繪不會動到正在輸入中的那個欄位本身——`render()` 會先把目前有焦點的 `.name-input`（如果有）從 DOM 摘下來，重繪其餘欄位後原封不動插回同一個節點並呼叫一次 `focus()`，值、游標位置、選取範圍全部沿用同一個 DOM element，不重新 `createElement`；若每次都重新建立並事後用 `setSelectionRange` 還原，在連續多次重繪下會有時機差、丟字（實測：打字打到一半、空白鍵後面的字元完全消失）。同一個 handler 也不再對每個字元呼叫 `.trim()`（那樣會在打出空白的當下就把它吃掉，導致打不出兩個字的英文名字），改成只在讀取端（`ROOM.name()`、`cardURL()`、`shareText()`）trim。`room-note` 下方固定有一顆「📋 Copy All Links」按鈕（`.room-btn.primary`，跟 Load 按鈕同一套外框樣式；只在 Firebase 啟用時顯示），一次把目前所有玩家的連結複製到剪貼簿，每位玩家一段「姓名＋換行＋連結」、段落之間空一行分隔，方便主持人整包貼到聊天室；複製成功後文字變成綠色的「Copied」並跑 `copyPop` 動畫（跟每欄自己的 Link 按鈕共用同一顆動畫、同一種「連續快點只有最後一次計時器生效」的 `_copyRevert` 寫法），1.5 秒後變回原本文字。這顆按鈕在 DOM 裡緊接在 `room-note` 之後、`room-links` 之前，若遊戲自己想在同一行加一顆按鈕，兩顆按鈕會自然並排在同一行（`say-it-without-saying-it.html` 的「👁️ Show what's on player cards」目前是透過下面的 `extraRowHTML` 掛上去，不再用 `insertAdjacentHTML` 手動插入）。緊接著 Copy All Links 右邊是「🔍 Send Card Check」（`.room-btn.check-btn`，自己一套天藍色 `#38bdf8` 外框＋淡藍底色，跟 New Room／QR 那種中性灰的次要按鈕區分開——原本這顆是刻意跟 New Room 走同一套中性灰、不跟 Copy All Links 的強調色搶視覺，但實測下來太不明顯、容易被忽略，所以改成自己專屬的藍色調：既不是會被誤認成「跟 Copy All Links 同一個動作」的粉紅強調色，也不是容易被忽略的灰色），發完連結、正式開局前的一次性檢查：從 `game-data.js` 的 `forbidden`（200 個日常單字，每個都帶 emoji，人數上限 8 遠遠夠用）洗牌抽出跟目前人數一樣多、互不重複的字，`ROOM.publish()` 蓋成 `{ game: 'cardcheck', word, emoji }` 送給每個人，玩家卡片（`play.html` 的 `game: 'cardcheck'`）只顯示名字晶片＋大大的字＋「Tell the host what you see!」，主持人就能一個一個問「你卡片上是什麼」，答案要跟自己畫面上看到的對得起來，對不起來就代表兩人的連結拿反了。送出後不會馬上變成答案卷——`checkWords` 有值只代表「已經發出去」，`room-links` 底下要等主持人聽完每位玩家唸出卡片內容、自己按下另一顆「👁️ Reveal Answers」按鈕（`revealCardCheck()`，設定獨立的 `checkRevealed = true`）之後，每一欄名字連結卡片才會冒出一排「emoji 字」小字（`.check-word-badge`，虛線分隔，跟 say-it-without-saying-it.html 的 card-preview 一樣是主持人限定的私人資訊；只印該字自己的 emoji，不再多加一個放大鏡圖示）；沒按 Reveal 之前，旁邊只有一行提示「🔍 Check words sent — have each player say what they see out loud first, then tap "Reveal Answers" to check them against the real thing.」＋「👁️ Reveal Answers」與「✖️ Clear」兩顆按鈕，按下 Reveal 之後提示改成「✅ Answers revealed — re-deal the game whenever you're done checking.」且 Reveal 按鈕消失、只留「✖️ Clear」（純文字按鈕，點了把 `checkWords` 與 `checkRevealed` 一起設回 `null`／`false` 重繪，不會動 Firebase 上的資料）。這個「發送」與「顯示答案」刻意分兩步，是因為核對的意義在於主持人**還沒看到答案之前**先聽玩家自己唸出來，若送出當下就顯示答案，主持人等於是自己先偷看了答案，核對就失去意義。這是一次性的覆蓋：`ROOM.publish()` 用的是 `.set()`，會直接蓋掉玩家卡片上原本那款遊戲真正發的資料，所以要在還沒開局、或兩輪之間用；核對完之後主持人只要照原本流程重新發牌（`Deal a Word`／`Draw a Charge`…）就會把 `game` 欄位蓋回真正的遊戲，卡片自然變回正常內容。換人數、換房號（`setCount`／`loadRoomCode`／`newRoom`）都會把 `checkWords` 清空，避免舊的答案卷對到新的人。API：`ROOM.init({ mount, accent, counts, defaultCount, linksNote, actionsHTML, extraRowHTML, hideSetup, onPlayerData, onCountChange, onNameChange })`（`hideSetup: true` 即上述精簡模式）、`ROOM.publish(payload 或 i => payload)`（`.set()`）、`ROOM.update(...)`（`.update()`，不會蓋掉玩家的作答）、`ROOM.clearAnswer(i)`、`ROOM.setDecorator(i => ({ marked, cls, answer }))`（`cls` 讓各遊戲把自己的狀態色畫在欄位上，例如 Word Wolf 的 `is-wolf`、Kangaroo Court 的 `is-role-defendant`／`is-role-prosecutor`）、`ROOM.setExtra(key, value)`／`ROOM.getExtra(key)`（把該遊戲自己的設定存進同一包 room session，例如 Kangaroo Court 的 `judgeEnabled`；經由這組 API 存取才不會被別的遊戲覆蓋掉）、``ROOM.setLinksView('names' | 'link-only' | 'qr-only' | 'check' | 'full')`（切換 `render()` 要畫姓名欄／Link／QR／核對字這幾塊的哪幾塊，非 `'names'`／`'full'` 時姓名欄改成唯讀；`'qr-only'` 直接展開 QR code、不透過原本一次只能開一個的 `toggleQR()`；只有 `index.html` 的 Step 1 精靈會用到，預設 `'full'` 等同舊版行為，其餘七款 `hideSetup: true` 的遊戲完全不受影響）、ROOM.enabled / count / names / code / answers / name(i)`。沒有 Firebase（或 CDN 連不到）時自動隱藏 ROOM 列與連結按鈕、標題改成「👥 Players」，遊戲照常運作 |
| `i18n.js` | 🌐 **全站雙語切換層**（`I18N`），**九個頁面共用**（`index.html`、`play.html`、`room.js` 服務的七款遊戲，加上獨立實作的 `dare-conquest.html`）。**預設繁體中文，畫面右上角一顆固定定位的「中文／EN」切換鈕**（樣式與 DOM 由 `i18n.js` 自己注入，各頁不用另外寫）可切到英文；選擇存進 `localStorage`（key: `site-lang`），跨頁、跨遊戲共用同一個語言設定，且套用範圍是**整個瀏覽器**而不是單一房間——這跟 `room-session-{CODE}` 那組房間資料是完全獨立的兩包 `localStorage`。**翻譯範圍的界線**：主持人畫面上「跟遊戲主內容無關」的所有介面文字——按鈕（含推進遊戲流程的動作鈕，例如 Kangaroo Court 的「⚖️ Draw a Charge」、Word Wolf 的「🎬 Deal Words」、Dare Conquest 的「🎲 Roll the Dice」都會翻譯）、狀態列、標籤、說明文字、警告訊息、題庫瀏覽區的分類標題——預設全部顯示繁體中文，可切回英文；**兩種東西永遠不翻譯、兩種語言下都固定顯示英文**：①每頁最上方的遊戲名稱標題（`<h1>`，例如「⚖️ Kangaroo Court」「🐺 Word Wolf」），視為品牌名稱；②`game-data.js` 裡的所有題目內容本身（罪名、真心話/大冒險題目、兩難劇本、Taboo 目標字與禁字、Word Wolf 詞組、證人證詞等）——這些就是 CLAUDE.md 開頭说的「英文單字／劇情／問題」，是遊戲的英文教學素材，故意不跟著切換。API：`I18N.registerDict(namespace, { key: { zh, en } })` 讓每個頁面（或 `room.js`）登記自己的一份翻譯表，`I18N.t(namespace, key)` 依目前語言取字串（找不到則退回 `en`、再退回 key 本身）、`I18N.onChange(fn)` 讓頁面在切換語言的當下重新呼叫自己的 render 函式（否則已經畫出來的動態內容不會自動換字）、`data-i18n="namespace.key"` 屬性讓純靜態 HTML 文字自動被套用與同步（`I18N.applyStatic()` 在每次切換語言時全文件重新掃一次，不管這個節點是何時被誰插進 DOM 的，所以就算是 `room.js` 的 `buildDOM()` 或某款遊戲自己動態组出來的 `actionsHTML`／`extraRowHTML` 字串，只要帶了這個屬性一樣會被抓到並同步更新）。每個頁面的做法都是同一套：`<script src="i18n.js">` 放在 `<head>`、盡量早（不依賴任何其他腳本，其他腳本可以放心晚載入），頁面自己的 inline script 裡用一個簡短的小 helper（`room.js` 是 `rt(key)`、`play.html` 是 `pt(key, vars)`，帶 `{placeholder}` 字串替換）包一層 `I18N.t()`，取代原本寫死的英文字串。沒有這個腳本時（理論上不會發生，因為每頁都會載入）`I18N` 是 undefined，各頁呼叫端有做 `typeof I18N !== 'undefined'` 的防呆，不會直接噴錯 |
| `guide.js` | 📖 **共用的玩法說明層**（`GUIDE`），**八款遊戲頁全部使用**。每一頁進入時的第一個畫面都是這款遊戲的說明，讀完按下按鈕才會進到下一步——**說明頁 → 設定頁 → 主遊戲畫面**。中間那一段「設定頁」只有真的有自己設定的兩頁才存在（`say-it-without-saying-it.html` 的難度／禁字／秒數精靈、`dare-conquest.html` 的 Start Game 卡片），這兩頁的按鈕文字是「▶️ 前往遊戲設定」；其餘六款的房間設定早就在 `index.html` 的 Step 1 統一做完了，頁面上本來就沒有任何設定可調（Kangaroo Court 的 Judge 開關是唯一例外，但它是一顆掛在 `room-mount` 上的開關，不是一個獨立畫面），所以刻意**不替它們生一個只有「繼續」按鈕的空設定頁**——那只是多一次點擊，不是一個步驟，這六頁的按鈕文字直接是「▶️ 開始遊戲」。頁面的接法只有兩行、不用改各頁的 inline script：`<body data-guide="wordwolf" data-guide-next="game">`（`data-guide` 對應 `guide-data.js` 的 key，`data-guide-next` 是 `setup` 或 `game`，只決定按鈕文字）加上 `<script src="guide-data.js">`／`<script src="guide.js">` 兩支。**遮蔽的做法**：`init()` 把 `.header` 之後的所有 body 子元素加上 `.guide-veiled`（`display: none !important`），這是一個**跟各遊戲自己在用的 `.hidden` 完全獨立的開關**——兩者不互相覆蓋，所以拿掉遮罩之後，頁面會回到它自己的腳本原本就設定好的狀態（`dare-conquest.html` 的 `#game-area` 仍然躲在 `.hidden` 後面、`fb-warning` 仍然只在真的沒有 Firebase 時才出現），不會因為說明頁而被強制顯示。遮蔽期間各頁的腳本照常執行（`ROOM.init()`、Firebase listener 都正常掛上），只是畫面先不給看。**唯一被跳過不遮的是 `#i18n-toggle`**：`i18n.js` 是在 `<head>` 載入、在 DOMContentLoaded 時把語言切換鈕 append 到 body 尾端，剛好落在「`.header` 之後」的範圍裡，不特別排除的話，說明頁開著的時候就切不了語言（實際踩過）。說明頁關掉之後，標題下方會留一顆低調的「📖 玩法說明」按鈕（`.guide-reopen`），主持人可以在遊戲進行到一半時隨時叫回說明，不會影響任何遊戲狀態（`open()`／`close()` 只是重新加上／拿掉同一批 `.guide-veiled`）。切換語言時會用 `I18N.onChange` 就地重畫，**不會把主持人踢回說明頁**（正在看遊戲畫面就只更新那顆按鈕的文字）。樣式在 `shared.css` 的「Game guide」段，顏色一律走各頁自己的 `var(--accent)` |
| `guide-data.js` | 📖 八款遊戲的說明內容（`GAME_GUIDES`），**中英雙語**——這是主持人畫面的介面文字而不是遊戲題目，所以跟著 `i18n.js` 的語言設定切換（`game-data.js` 的題目與每頁的英文遊戲名稱則永遠是英文，見 `i18n.js` 那一列）。每款遊戲一個 key（`wordwolf`／`taboo`／`hottake`／`sophies`／`persuade`／`scene`／`conquest`／`kangaroo`，對應各頁 `<body data-guide="…">`），內容是 `players`（建議人數）＋`tagline`（一句話講完這款遊戲）＋`sections` 陣列。每個 section 有 `icon`、`title`，以及下列四種內容**擇一**（`guide.js` 依哪個欄位存在決定畫法）：`paragraphs`（段落，用於「這是什麼遊戲」）、`steps`（自動編號，用於「怎麼玩」）、`roles`（`{emoji, name, desc}` 卡片，用於「角色與功能」——`dare-conquest.html` 用了兩個 roles 區塊，分別講三種道具與四種特殊格，因為那款遊戲每個人的角色其實一樣，真正要解釋的是道具與格子）、`tips`（項目符號，用於「主持人小提示」）、**`phones`（手機示意圖，用於「玩家手機上會看到什麼」）**。`phones` 是為了**會發 secret card 的遊戲**做的：那張卡片是全場唯一不能出現在分享畫面上的東西，所以主持人自己也沒看過，光用一句「每個人會拿到一個字」講不出它長什麼樣子——這個區塊直接把它畫出來。每支手機是 `{caption, card}`，`card` 的欄位全部選填：`name` ＋ `color`（1–8，就是 `play.html` 那組玩家配色）畫出填滿色的名字晶片、`emoji`／`word` 是卡片正中央的大字、`hint` 是分隔線下方那行小字、`buttons` `[{label, cls}]` 是該卡片真的有的按鈕（`is-green`／`is-red`／`is-choice`）；區塊另可帶一個 `note`，印在手機下方講「光看圖看不出來的那件事」（例如 Word Wolf 的兩張卡片刻意長得一模一樣，這正是沒有人知道誰是臥底的原因）。樣式在 `shared.css` 的 `.guide-phone`／`.gp-*`，**數值是照著 `play.html` 的實際卡片等比縮小抄過來的**（邊框 2px、圓角、名字晶片、6rem emoji…），原因跟 `is-green`／`is-red` 那組按鈕一樣：`play.html` 跟主持人頁沒有共用任何樣式表。卡片顏色走各頁自己的 `var(--accent)`，本來就與 `play.html` 的該款主題色相同。`card` 另外還有這些選填欄位，各自對應 `play.html` 的一個真實元件：`chip`（`{label, hex}`，`.pick-chip` 角色標籤）、`text`（`.pick-text` 情境本文）、`target`（`{emoji, word}`，`.taboo-target` 那個框起來的目標字）、`pills`（`{label, items}`，`.taboo-banned` 的紅色禁字藥丸）、`role`（`{key, name, hint}`，`.role-box` 角色框）、`sub`（`.pick-sub` 灰色小字，排在按鈕之前）。渲染順序固定為 name → chip → text → emoji → word → target → pills → role → sub → buttons → hint。另有 `turnbar`（`{name, color, suffix}`，`.conq-turnbar` 那條「現在輪到誰」的色條，只有 Dare Conquest 用得到）。**八款全部都有這個區塊**，每一款畫的是「這一頁最值得用圖講的那件事」，不是把卡片抄一遍：`wordwolf` 兩張一模一樣的字卡（沒有任何一張寫著「你是臥底」）、`taboo` 四張並排：開題目之前的 clue giver（只有一顆「👀 看題目」）、開了之後的 clue giver、裁判、猜題者（最後一張明顯矮一截，因為猜題者什麼都看不到）、`kangaroo` 陪審團的秘密投票鈕＋被抽到的證人卡、`scene` 兩位玩家同場景不同角色（別人的提示看不到）、`persuade` 裁判卡有判決鈕、團隊卡沒有、`sophies` 只有 A/B/C 三顆鈕（用來講「卡片刻意不印情境」）、`hottake` 相反地**有**印題目（投票當下要能重讀）、`conquest` 輪到自己 vs 別人在挑戰時的兩種按鈕組合。`sophies`／`hottake` 各只有一張，因為它們沒有「不同人看到不同東西」的對照可講。**卡片上的文字一律照抄 `play.html` 的 I18N 條目**（`clueGiverChip`／`forbiddenWordsLabel`／`tabooFoulBtn`／`buzzInBtn`／`guiltyBtn`／`prevoteSecret`／`youPlay`…），不要自己另外寫一套說法，理由跟按鈕那條一樣。常用的四個區塊標題抽成 `GUIDE_TITLES` 共用，不用在八款遊戲裡各寫一次。**撰寫原則是「重點摘要」而不是文件**：一個概念一句話、一個步驟一句話、一個角色一行，`paragraphs` 通常只有 1 段、`steps` 3–5 條、`tips` 2–3 條——寫長了主持人根本不會讀，這是實際看過完整版之後砍掉一半的結果。可以講「為什麼」（例如為什麼猜題者看不到禁字、為什麼平手算有罪），但要壓縮成一句。**說明裡提到按鈕時一律把按鈕畫出來，不要用文字描述**：檔案開頭的 `B(cls, label)` 會吐出一個真的 `<button>`，`cls` 直接掛**該頁自己的按鈕 class**（`btn-primary`／`btn-draw`／`btn-deal`／`spin-btn`／`td-choice-btn td-truth`／`status-line`／`kc-back-btn`…），所以畫出來的東西跟主持人待會兒真的要按的那顆長得一模一樣，不必另外維護一套仿樣式；`BROW(...)` 把多顆併成一排（`display:flex`），`VOTE()` 則是用 `pick-a-side.html` 自己的 `.vote-btn` 標記重建 Pick a Side 的三顆投票鈕（含長條與計數）。`label` 必須跟該按鈕在**那個語言**下的實際文字一致，來源是各頁 I18N dict 裡的 `xxxBtn` 條目，不要自己另外寫一個說法。玩家卡片（`play.html`）上的按鈕在主持人頁沒有對應 class，改用 `shared.css` 定義的 `is-green`／`is-red`（實心填滿，對應 Buzz in!／Taboo!／判決鈕）與 `is-choice`／`is-choice is-tint-red`／`is-tint-green`（外框式，對應 A/B/C 與陪審團 Guilty／Not Guilty），色值直接抄自 `play.html`。`players` 欄位（顯示在最上面那顆膠囊裡）**八款一律寫成範圍**（`2–6 人`／`4–9 人`），不要出現「N 人以上」這種另一種格式；沒有上限的遊戲就寫到 9，因為 `index.html` Step 1 的人數鈕本來就只到 9，寫成開放式只會讓八頁的膠囊長得不一樣 |
| `shared.css` | 所有遊戲頁共用樣式 + 主題變數（另有 `.guide-btn`／`.guide-btn-row`：說明頁裡引用的按鈕只把「塞進句子裡會壞掉」的部分中性化——`width: auto`、清掉 margin 與入場動畫、`pointer-events: none`——顏色/邊框/圓角一律留給該頁自己的 class，所以需要 `!important`，因為各頁的 `<style>` 一定晚於這個檔案載入）：頁首、`.hidden`、`.fb-warning`、`.section-hdr`、`.btn-primary`／`.btn-secondary`。`.header` 底下**只有 `<h1>`**，原本每頁 h1 下方那行說明玩法的 `<p>`（例如 say-it-without-saying-it.html 的「One player describes a secret word...」）已經全部拿掉——那是主持人分享桌面時，畫面上一段跟當下操作無關的小字，八款遊戲的主持人頁全部清乾淨，`.header p` 這條 CSS 規則也一併刪除（`dare-conquest.html` 自己覆寫的那份也刪了）。顏色一律走 `var(--accent)`（次要按鈕 hover 用 `--accent-alt`，Firebase 警告條用 `--warn`），每個頁面只要在自己的 `<style>` 開頭寫一行 `:root { --accent: … }` 就換色完成；形狀真的不一樣的頁面（例如 `dare-conquest.html` 的 `.section-hdr` 是 flex 排版）在自己的 `<style>` 覆寫即可，因為頁面樣式一定晚於這個檔案載入 |
| `scenario.css` | 三款「抽劇本」遊戲（Sophie's Choice／You're In The Scene／Persuade Together）共用的版面：劇本大卡、`.filter-bar` 類別篩選、`.btn-draw` 抽卡鈕、下方可瀏覽的完整劇本清單。同樣用 `--accent` 換色 |
| `game-data.js` | 所有遊戲的題目資料（hottake / persuade / scene 各 10 題；sophies 38 則兩難劇本；wordwolf **80** 組臥底詞組（只有 Word Wolf 使用。刻意只留同質性最高的一批 — 標準是「你能說出關於其中一個的句子，幾乎都同樣適用於另一個」：①同類別、同用途、同場合；②實體物要**形狀、顏色、大小都接近**（Donut/Bagel 圓形烘焙麵包、Palm Tree/Pine Tree 高大喬木、Scarf/Shawl 保暖披肩布料、Bowl/Plate 圓形餐具）；**唯一例外是 😊 Feelings 這個抽象類別**（15 組，取代原本的 🐾 Animals）——情緒本來就沒有形狀/顏色/大小可比，這裡的判斷標準改成「兩種情緒接近到真的容易搞混」（例如 Happy/Excited、Angry/Annoyed、Worried/Hopeful），而不是外觀相似；③**一律用簡單好懂的字**，不用 protractor、accordion、locomotive 這類冷門字；④不採用一句話就穿幫的組合：室內↔室外、白天↔晚上、互為相反（鑰匙／鎖）、一方包含另一方（巧克力／糖果）、只差在大小（山／丘、村莊／城市）、以及形狀顏色差太多的（足球／籃球、背包／行李箱）都不用）；forbidden 200 個日常單字，供 `room.js`／`dare-conquest.html` 的 Send Card Check 核對連結用；conquest 50 則互動挑戰指令（Dare），供 Dare Conquest 使用，**分成五大類型、每類 10 題**（🎤 Singing 唱歌／🗣️ Voices & Sounds 變聲與音效／🎭 Improv & Roleplay 即興與角色扮演／💬 To Their Face 當面稱讚或吐槽／🧠 Word Games & Memory 文字遊戲與記憶），每題有 `type`（五大類型）、`cat`（該題自己的標題，50 題各不相同，顯示在挑戰卡的小標籤上）、`text`、`seconds`；`{player}` 會被隨機替換成另一位玩家的名字（50 則中有 30 則帶 {player}，題目盡量設計成需要跟其他玩家互動），全部 50 則的 `seconds` 欄位一律為 45，內容以簡單好懂的用字為主，**句子刻意寫短、盡量不用俚語或需要文化背景才懂的說法**（因為使用者是在練習英文口說的初學到中階學習者，太長太繞或太美式俚語的句子唸出來反而聽不懂在玩什麼——這是實際發生過的使用者回饋，原本的版本句型偏長、也用了像 spirit animal／yearbook quote／opposite day 這類需要額外文化背景才懂的說法）。因為參與者沒有鏡頭，題目一律寫成「唸出來／說出來／做聲音」，不會有 show us 這類要看畫面的指令；conquestTruth 50 則真心話題目，供玩家選擇「TRUTH」時使用，同樣**分成五大類型、每類 10 題**（📱 Phone Reveal 當場打開手機唸出來／😳 Cringe & Confessions 糗事與自白／🧒 Back Then 童年與後悔的事／💘 Crushes & Dating 暗戀與約會／💰 Money & Work 金錢與工作），欄位與 Dare 相同；其中 8 則帶 {player}，用來把其他玩家拉進來（多半是問完自己的答案後、順手也問一句「Ask {player} too」），全部 50 則的 `seconds` 欄位一律為 45，用字同樣刻意簡單、避免俚語（原因與寫法同上一段的 Dare 題庫，例如原本的 left on read／dealbreaker／side hustle／impulse buy 都已改寫成更直白的說法）；因為參與者沒有鏡頭，Phone Reveal 這類題目一律寫成「唸出來／描述」而非「秀給大家看」；taboo **300** 張 Say It Without Saying It 卡片（`{ level, emoji, word, forbidden: [6 個字] }`，`level` 為 `easy`／`medium`／`hard`，各 **100** 張。分級標準是**具體→抽象光譜**：easy 是看得到摸得到的日常實物（Pineapple、Donut、Frog…），medium 是具體但要多想一下的場所／職業／活動（Courthouse、Surgeon、Festival…），hard 是純抽象的情緒或概念、完全指不出實體（Empathy、Resilience、Harmony…）。三個難度共 300 個字彼此不重複，字彙仍刻意簡單，六個禁字就是「最先想到用來解釋它的字」——主持人畫面可以設定實際生效 3～6 個（見 `say-it-without-saying-it.html`），數字愈小愈簡單，例如 🍍 Pineapple 禁 Spiky / Yellow / Tropical / Sweet / Crown / Juice）；kangaroo **30** 條 Kangaroo Court 的荒謬罪名（`{ emoji, charge }`，`charge` 是接在主持人畫面固定文案「⚖️ The defendant is charged with…」後面的一句話，通常兩個小節：一個大家都做過的小習慣接一個好笑的轉折，用字刻意簡單、不用法律術語，例如「wearing headphones with no music playing, just to avoid talking to people」）；kangarooWitness **24** 位 Kangaroo Court 可傳喚的證人（`{ emoji, name, evidence }`，`name` 是證人角色名、`evidence` 是接在該角色名後面的一句話帶出他們帶來的證物或立場，刻意寫得夠短、一行放得下（例如「The Old Teacher」＋「remembers this exact behavior from years ago」），因為主持人畫面上兩位證人的卡片會同時疊放展示，太長會佔用太多版面）— 遊戲題目的**內建預設值**。主持人可以在 `index.html` Step 2 按下按鈕改用自己的 Google Sheet 題庫（見 `sheet-data.js`），但這份檔案永遠是後備：任何一個資料集沒抓到或格式不對，那款遊戲就用這裡的題目，所以這裡不能空、也不能刪 |
| `sheet-data.js` | 📄 **題庫外部來源層**（`SHEET_DATA`），**十個頁面共用**（`index.html`、`play.html` 與八款遊戲頁），一定要排在 `game-data.js` **之後**載入，因為它做的事就是把抓下來的題庫覆蓋到 `GAME_DATA` 上（`GAME_DATA` 是 `const`，所以改的是它的**屬性**而不是重新指派）。兩個角色：①**載入即套用**——每一頁開啟時檔案結尾的 `apply()` 會把 localStorage（key: `icebreak-sheet-data.v1`）裡上次成功抓下來的題庫合併進 `GAME_DATA`，各頁自己的 inline script 之後才讀 `GAME_DATA`，所以完全不用改任何一款遊戲的程式碼；②**抓取**——`load(url, onProgress)` 只有 `index.html` **Step 1 精靈最後一步（子步驟 5「載入題庫並開始」）**那顆「📄 讀取線上題庫」按鈕會呼叫，也就是說**只有主持人主動按下去的那一刻才會連線 Google**，其餘任何時候（每一頁載入、每一次發牌、遊戲進行中）都只讀 localStorage，不發任何網路請求——這是刻意的，主持人正在帶活動時不該有任何一條線路可能卡住畫面。讀法是 Google 的 **gviz CSV 端點**（`https://docs.google.com/spreadsheets/d/{ID}/gviz/tq?tqx=out:csv&sheet={分頁名}`），用分頁**名稱**而不是 gid，主持人才不必為了 11 個分頁去抄 11 組 gid；試算表必須設成「知道連結的人皆可檢視」，否則讀不到。**網址是寫死在 `sheet-data.js` 開頭的 `SHEET_URL` 常數**，畫面上沒有輸入框、主持人不能也不需要指定別份試算表——本站就這一份共用題庫，它同時開放給協作者編輯（編輯者各自被加入編輯權限，「知道連結的任何人 → 檢視者」則是這支程式讀得到的原因）。要換成另一份試算表就是改那一行。畫面上**刻意不放任何連到那份試算表的連結或按鈕**（原本有一顆「📝 開啟題庫試算表」，已移除）——編輯者本來就有那份試算表的網址，不需要從主持人畫面點過去；主持人畫面在活動中是分享出去給所有參與者看的，不該把可編輯的題庫入口擺在上面。要注意這只是拿掉畫面上的入口，**不等於把網址藏起來**：`SHEET_URL` 就寫在這支檔案裡，而這支檔案會被部署成公開的 `/sheet-data.js`、repo 也是 public，任何人都讀得到。真的要讓網址不可見，靜態網站做不到（瀏覽器得自己去抓），得改成走一層自己的後端代理。按鈕在已經讀過之後文字會變成「🔄 重新讀取」——**這顆按鈕刻意不掛 `data-i18n`**，文字由 `initSheetLoader()` 依「有沒有載入過」自己切換；掛了的話 `i18n.js` 的 `applyStatic()` 會在 inline script 之後把它蓋回「📄 讀取線上題庫」。**失敗一律退回預設，而且是逐個資料集退**：Google 連不上、某個分頁不存在（404）、標題列缺必填欄位、分頁是空的——這些都只影響那**一個**資料集，其餘抓成功的照樣套用，抓不到的那款遊戲就用 `game-data.js` 的內建題庫，主持人不會開天窗（實測：11 個分頁裡 2 成功、1 個 404、1 個標題列錯、7 個空白 → 2 個覆蓋、9 個維持預設，零錯誤）。`SCHEMA` 定義 11 個資料集各自的欄位、必填欄位與「一列 → 物件」的組法（`scene` 的 `a_label`/`a_hint`…`f_hint` 會組回 `{a:{label,hint}}`、`taboo` 的 `forbidden1`…`forbidden6` 會組回 `forbidden` 陣列、`hottake` 是純字串陣列所以只有一欄），**這份 SCHEMA 同時也是產生範本 .xlsx 的依據，兩邊永遠一致**。CSV 是自己寫的 RFC4180 解析器而不是 `split(',')`——題目本身幾乎每條都有逗號、也有引號與換行（例如 kangaroo 的罪名），`split` 會把它們切爛。欄位比對看**標題文字**不看位置，所以主持人在試算表裡調換欄位順序不會壞；必填欄位留空的那一列會被跳過並回報跳過幾列。**每一格文字在解析當下就會被 `clean()` 拿掉 `<` 與 `>`**：題庫是從一份「開放給別人編輯」的試算表讀進來、又被各遊戲頁直接塞進 `innerHTML`，不處理的話任何一個編輯者都能在主持人畫面與玩家卡片上執行任意程式碼（實測 `<img src=x onerror=…>` 真的會跑）。**刻意是「拿掉」而不是轉義成 `&lt;`**，因為消費端有三種寫法：`play.html` 自己會 `esc()`（再轉一次會變成 `&amp;lt;`）、`kangaroo-court.html` 用 `textContent`（會把 `&lt;` 原封不動印出來）、其餘頁面直接 `innerHTML`——只有「純文字、不含 `<` `>`」在三種寫法下都正確。也不是只擋惡意：題目裡打一個 `cats < dogs` 的 `<` 一樣會讓版面解析錯亂。`scene` 的六個角色**一定會被補滿**（填不滿就用已填的循環補上），因為 `youre-in-the-scene.html` 的 `renderRoleGrid()` 是照房間人數跑迴圈的，少一個角色就會在 `role.label` 上丟 TypeError 讓整頁當掉（實測過；該處另外也加了 `if (!role) continue` 防呆）。`load()` 會回傳 `stored`，localStorage 寫不進去時（配額爆掉／無痕視窗）畫面顯示警告而不是打勾——否則會謊報成功、但每一頁其實都在用內建題庫。**讀到的內容是存在該台裝置的 localStorage**，所以每台電腦要各自讀取一次，別人在試算表上改了題目，其他人也要再按一次讀取才會更新——這是刻意的取捨：換來的是活動進行中絕不會有任何一條可能卡住的網路請求 |
| `prompt-sheet-template.xlsx` | 📄 給主持人上傳到 Google Drive 的**題庫範本**，11 個分頁對應 `GAME_DATA` 的 11 個資料集（分頁名稱＝`GAME_DATA` 的 key，不可以改名，`sheet-data.js` 是靠名稱找資料的），每頁第一列是標題列（必填欄位用黃底標示）、下面**預先填好目前全部 802 題內建題目**——不是空白範本，主持人是「改」而不是「從零填」。另有一個「說明 README」分頁寫上傳步驟、共用權限設定、各分頁對應哪款遊戲，以及題目撰寫原則（句子短、用字常見、不用美式俚語、因為沒鏡頭所以寫成「唸出來」）。**改題庫時這個檔案要跟著重新產生**：它是從 `game-data.js` ＋ `sheet-data.js` 的 SCHEMA 直接產出的，有一個往返測試確認「xlsx → CSV → 網站解析」的結果與內建題庫逐字元一致（802 題全過），改了資料或欄位定義就要重跑一次確認 |
| `game-render.js` | 共用渲染函式（GAME_RENDER） |
| `qrcode.js` | QR code 產生函式庫（vendored，qrcode-generator 1.4.4，MIT）— 八款主持人頁都用它選用性顯示玩家連結的 QR code |
| `SECURITY.md` | 安全檢查清單（不是程式碼）：這個專案沒有後端也沒有登入，所有防護都押在 Firebase Realtime Database 的 Rules 上。列出①驗證資料庫沒對外開放的兩條測試網址與正確的 Rules 內容、②確認 Firebase 是 Spark 免費方案、③`main` 的 branch protection（選配），以及一份「看起來可疑但其實不用處理」的清單（repo public、`apiKey` 被 commit、`roster` 開放讀寫、token 出現在網址列） |

**廢棄資料夾：** `_archive/`（comic, describe, emoji-quiz, finish, fortunately, icebreaker, never, persuade, plot-twist, story-chain, surprise, truths）— 請勿讀取。

---

## 技術方向

### 展示方式
- 主持人透過「桌面分享」讓參與者看到遊戲畫面
- 參與者透過語音回應，不需要自己操作畫面
- UI 需清楚易讀（大字體、高對比，方便螢幕分享時辨識）

### 建議技術棧
- 前端展示頁面（HTML / CSS / JavaScript 或 React）
- 不需要後端登入系統，盡量保持輕量
- 可考慮即時互動功能（如投票、計時器）

---

## 遊戲類型參考

以下為適合此場景的破冰遊戲方向：

| 遊戲類型 | 說明 |
|----------|------|
| **Two Truths and a Lie** | 每人說兩個真話、一個謊言，其他人猜哪個是謊言 |
| **Would You Rather** | 主持人出題，參與者語音選擇並說明理由 |
| **Random Question Generator** | 隨機產生輕鬆有趣的英文問題帶動話題 |
| **Word Association** | 接龍式文字聯想遊戲 |
| **Trivia / Quiz** | 英文知識問答，搭配計分板 |
| **Poll / Vote** | 即時投票，主持人畫面顯示結果 |
| **Bingo** | 賓果卡，依語音描述劃記 |

---

## UI / UX 要求

- 字體夠大，螢幕分享時清晰可見
- 高對比配色（深色背景 + 亮色文字，或反之）
- 操作簡單，主持人一個人能流暢控制
- 響應式設計（主持人桌機使用為主）

---

## 開發優先順序

1. 先建立**遊戲選單 / 主頁**，可切換不同遊戲
2. 完成至少 2–3 個核心遊戲的 MVP
3. 優化視覺設計，確保螢幕分享效果良好

---

## 注意事項

- 遊戲主內容（`game-data.js` 的題目、劇情、單字等）與每頁的遊戲名稱標題固定用**英文**呈現，是遊戲本身刻意保留的英文教學素材，不跟著語言設定切換；除此之外，主持人畫面上的介面文字（按鈕、狀態列、說明文字等）**預設繁體中文**，可透過畫面右上角的語言切換鈕改成英文——細節見 `i18n.js` 那一列
- **`game-data.js` 裡所有遊戲題目（不限 Dare Conquest，所有現役遊戲的題目/劇情/句子皆同此原則）一律要用簡單好懂的英文撰寫，這是長期規則、以後新增或修改題目都要遵守**：句子盡量短、用字盡量常見（避免生僻字），**尤其要避免美式俚語或需要額外文化背景才看得懂的說法**（例如 spirit animal／yearbook quote／opposite day／left on read／dealbreaker／side hustle／impulse buy 這類——不只是單字難，是要先懂美式生活習慣才知道在問什麼）。原因：使用者是在語音聊天室裡練習英文口說的初學到中階學習者，題目是要被**唸出來、當場理解、當場回答或表演**，不是給人安靜閱讀的文字，看不懂的句子會讓那一輪直接卡住玩不下去
- **每一頁的檔名必須等於該頁 `<h1>` 標題的 slug**：去掉 emoji 與標點、空格轉連字號（🙊 Say It Without Saying It → `say-it-without-saying-it.html`、🏰 Dare Conquest → `dare-conquest.html`）。早期的檔名是隨手取的代號（`dont-say-it.html`／`hottake.html`／`what-will-you-do.html`／`persuade-team.html`／`scene.html`／`conquest.html`），跟畫面上顯示的標題對不起來，講「改一下 Pick a Side」還要先想它是哪個檔案——已經全部更名。新增遊戲時比照辦理
- 程式碼與技術討論可使用中文
- 保持模組化，方便日後新增更多遊戲

---

## Git 規則

- 所有變更直接 commit 並 push 到 `main` branch
- 不使用 feature branch

## Kangaroo Court (Beta版)

`kangaroo-court-beta/` 是 Kangaroo Court 的獨立實驗副本，入口為 `kangaroo-court-beta/index.html`。Beta 的主持人、玩家卡片、题庫、說明、房間模組、樣式與設定均使用資料夾內自己的檔案；後續 Beta 修改請限定在此資料夾，不修改根目錄原版遊戲及共用模組。初始建立時根目錄 `index.html` 只新增 Beta 入口，原本八款遊戲入口保持不變。Beta 使用獨立的 localStorage 前綴 `kangaroo-beta-` 和 Firebase 房間路徑 `rooms/kangaroo-beta-{CODE}`；原版仍用 `rooms/{CODE}`。需另外設定 Beta 人數與傳送 Beta 玩家連結，不能沿用原版卡片。詳細維護說明見 `kangaroo-court-beta/README.md`。
