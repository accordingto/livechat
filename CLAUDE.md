## 專案背景

**聊天狼人 v1（2026-09-20）**：入口 `chat-wolf.html`。這款遊戲不使用既有
`play.html` 私人牌卡節點，而是由 `api/chat-wolf.js` 驗證每位玩家的 256-bit 重連
token，再從 Firebase `chatWolfRooms/{CODE}` 取出伺服器權威狀態並分成公開／個人／
狼隊投影。房主也是普通玩家，若為村民不會看到狼人或任務。六輪、兩個會議槽、
一次搖鈴、整組指認、兩個共享任務、人工核對與再玩一局均在
`api/_lib/chat-wolf-engine.cjs` 的原子狀態轉移內；前端只送意圖，不可自行推進。
介面文案集中在 `chat-wolf-copy.js`，規則、環境設定與測試矩陣見
`chat-wolf-mode.md`。此模式需要 Vercel 的 `FIREBASE_SERVICE_ACCOUNT_JSON`，不能把
服務帳號或當局秘密移到公開前端，也不能用 localStorage 冒充多人同步。

我是一個**英文線上聊天室的主持人**，負責帶領參與者進行互動活動。

### 溝通限制
- ✅ 語音溝通（所有互動皆透過語音進行）
- ✅ 主持人可分享桌面（螢幕共享）
- ❌ 無視訊鏡頭（不能開視訊）

### 核心需求
開發適合線上語音聊天室使用的**破冰遊戲（Icebreaker Games）**，遊戲內容必須：
1. 不依賴視訊，純語音即可進行
2. 主持人可透過**分享桌面**展示遊戲畫面給參與者看
3. 以**英文**為主要語言
4. 適合陌生人或初次見面的參與者快速熟識

---

## 專案結構（現役檔案）

**Let's Talk v0.1 新增說明**：原本七款遊戲之外新增一個談話模式，入口 `lets-talk.html`，沿用首頁房間設定與 `play.html` 玩家連結。下表原有「八款／七款」敘述是 Word Wolf 還在時的原版遊戲背景（現在是七款／六款）；共用頁現在另外服務 Let's Talk。詳細規則與同步設計見 `talk-mode.md`，修改本模式前請先讀取。新檔案為 `talk-engine.js`（純規則）、`talk-sync.js`（主持頁狀態與玩家同步）、`talk-ui.js`（雙語與玩家卡片）、`talk-host.js`（共同畫面與單機示範）、`talk-topics.js`（12 題原創簡單英文話題）、`talk.css`（限定本模式樣式）、`tests/talk-*.test.cjs`（無依賴測試）。`room.js` 僅新增主持端 `ROOM.playerRef(i)`；不可把其他人的 token 傳入玩家 payload。`?demo=1` 是不寫入 Firebase 的單機操作示範。暫不擴充原本 Google Sheet 題庫格式；本模式使用頁內收合說明，沒有另加強制教學步驟。

**Let's Talk v0.2**：題庫已由 12 題擴成 8 類、48 組議題與 192 個延伸問題（共 240 個問題），可依分類與中英文關鍵字搜尋、預覽討論路徑，或用「以這題修改」帶入手動出題。主問題可直接輸入中英文，選填名稱與每行一題的延伸問句；草稿用獨立的 `lets-talk-topic-draft.v1` 保存。進行中可選取任一延伸方向或臨場自訂問句，使用主持 `extend` 命令同步，保留輪次與口頭追問；玩家投影沿用 `talk.topic.followUp` 以相容已開啟的 v0.1 卡片。詳見 `talk-mode.md`；不變更既有 Google Sheet 格式。

**Let's Talk v0.3（試玩回饋）**：主持交棒／結束提問按鈕移到目前發言者下方，臨時有人離開可由主持接手；仍保留待追問提醒與過期操作防護。頁面上方先隨機預選完整問題，可重新抽題、手動改選或自訂；頁尾直接列出完整題庫與可展開的延伸，並提供分類／中英搜尋與「選這題」。隨機抽題依目前篩選，有其他題目時避開目前活動與預覽；抽題只改預覽，按「開啟話題」才發布。玩家輪到自己時有金色外框、大字「輪到你了！」與「我說完了」／「I'm done」按鈕。21 項規則／同步測試及新三人 Firebase 房間的主持代操作、重連、換題與切回其他遊戲已驗證，詳見 `talk-mode.md`。

Let's Talk 後續介面精簡：主持畫面移除 Round 數字與 Host controls 提示，保留直接操作按鈕；共同畫面與玩家卡的說明改為可以隨性自然地聊、卡片只是輔助。題庫／預覽的延伸問句不顯示方向標籤，延伸選單也顯示完整問題。輪次規則與題目資料不變。

**Let's Talk v0.4（引題試用）**：48 組話題增加簡短英文 `starter`，在問題下方提供情境或思考入口。主持「顯示引題（試用）」開關同步共同畫面與玩家卡片；活動內保存 `showStarters`，主持命令 `starters` 接受布林 `show`，不改倒數、追問或分享順序。新活動預設開啟，舊活動缺欄位時先關閉；本機偏好另存在 `lets-talk-starters.v1`。自訂引題最多 400 字，隨原草稿保存，沒填可用第一題延伸作為引題。空白的倒數／追問／反應區塊不再佔位，桌面狀態與主要按鈕並排，手機緊接排列。詳見 `talk-mode.md`。

**Let's Talk v0.5（題目說明）**：依試玩回饋將 48 段引題改寫為解釋原問題的兩段說明：釐清題意／關鍵詞，再提示如何開始回答；不用另一個情境或追問取代題意。開關改稱「顯示題目說明（試用）」，自訂說明上限 600 字，保留原 `starter`／`showStarters` 與草稿、偏好欄位。空白說明不再回退到 follow-up。舊房間若題目 ID 與主問句完全符合內建題庫，主持頁會用 `explain` 命令更新說明而不重設活動；自訂題不覆蓋。延伸問題及分享規則保持原樣。

> ⚠️ **只讀取以下檔案。`_archive/` 資料夾內的所有檔案為舊版廢棄遊戲，除非使用者明確要求，否則永遠不要讀取或修改。**

| 檔案 | 說明 |
|------|------|
| `index.html` | 主選單 / 遊戲入口頁，**兩步驟流程**：以前一進站就是選遊戲，主持人要先點進某一款才會看到「選人數／發連結」，七款各自重複一次；現在房間設定被拉到選遊戲**之前**、全站只做一次。**Step 1「Set up your room」**直接在首頁掛一份 `room.js` 的 `ROOM.init({ mount: 'room-mount', accent: '#e94560', counts: [2..9], defaultCount: 5 })`（跟各遊戲頁一模一樣的元件：人數鈕／姓名欄／Copy All Links／QR／Send Card Check），**但這裡永遠不呼叫 `ROOM.publish()`**——首頁不屬於任何一款遊戲，不發任何牌，玩家連結打開後就是 `play.html` 原本就有的「⏳ Waiting for the host to start a game...」預設畫面，等主持人稍後在某一款遊戲頁按下真正的發牌鈕才會變成該遊戲的卡片；因為房號／人數／姓名都是靠 `room-session-{CODE}`／`room-last-session` 這包 `localStorage` 在七款遊戲間同步（`dare-conquest.html` 雖然沒用 `room.js`，也讀寫同一包），所以這裡設定好、按下**Step 2** 前的「▶️ Continue to Games」按鈕後，等一下點進任何一款遊戲，該遊戲頁自己的 `ROOM.init()` 會自動讀到同一個房號與人數，不用重新設定一次（唯一的前提是 Firebase 要能連上——`ROOM.save()` 內部靠「已經配過 token」才會真的寫進 localStorage，沒有 Firebase 時人數/姓名只留在當前分頁的記憶體裡，不會跨頁同步，這點跟各遊戲頁原本的行為一致，不是本頁新引入的限制）。**Step 2「Choose a game」**是遊戲選單格線（`#game-list`）＋摘要列下方一顆 `.guide-opt` 勾選框「進入遊戲前先顯示玩法說明」，**預設打勾**：取消勾選才把 `site-guide` 設成 `'0'`，各遊戲頁的 `guide.js` 讀同一個值決定要不要擋一頁說明在前面（見 `guide.js` 那一列；兩邊都是比「不等於 `'0'`」，所以沒設定過就是顯示）。這顆勾選框刻意放在 Step 2 而不是 Step 1 的精靈裡——它不是房間設定，是「這一場要怎麼帶」，而且就在遊戲清單正上方，勾完馬上點下去就生效。遊戲卡片的順序是 Let's Talk → **Say It Without Saying It** → Pick a Side → Sophie's Choice → Persuade Together → You're In The Scene → Dare Conquest → Kangaroo Court；Say It 排第二是因為它是最常被點的一款（原本第二是 🐺 Word Wolf，**那款遊戲已整個移除**——連同 `word-wolf.html`、`GAME_DATA.wordwolf` 的 80 組詞、`SCHEMA.wordwolf`、範本 xlsx 的 `wordwolf` 分頁、`GAME_GUIDES.wordwolf`，以及 `play.html` 的臥底字卡與投票 UI。`rooms/{code}/roster` 這條路徑本來就只服務那組投票按鈕，現在沒有任何程式讀或寫它，`room.js`／`dare-conquest.html` 的寫入也一併拿掉；Firebase 的 Rules 留著，免得還開著的舊分頁中途開始寫入失敗，見 `SECURITY.md`），預設用 `.hidden` 藏起來，按下 Continue 才連同一條「🔗 Room ABC123 · N players ／ ✏️ Edit room setup」摘要列一起淡入；摘要列右側的「✏️ Edit room setup」可以隨時跳回 Step 1（`showStep(1)`／`showStep(2)` 兩個函式互斥切換 `#step-setup`／`#step-games`／`#game-list` 的 `.hidden`，並捲回頁面最上方），沒有任何資料會因為來回切換而遺失，因為狀態本來就活在 `ROOM` 自己的模組變數與 localStorage 裡，這兩個步驟只是同一份 DOM 的顯示/隱藏。**單獨打開首頁（網址列直接輸入或書籤進來，沒有帶 `?step=2`）都會先落在 Step 1**（不會因為之前設定過就自動跳到 Step 2），但欄位會用 `room.js` 既有的還原機制帶出上次的人數與姓名，所以多半只是確認一下就能按 Continue，不必每次重打。**但從遊戲頁按「← Back to Hub」回來的這條路徑例外**：七款遊戲頁的返回連結一律是 `index.html?step=2`（不是單純的 `index.html`），首頁的 inline script 在 `renderSetupSummary()` 之後會檢查 `new URLSearchParams(location.search).get('step') === '2'`，是的話直接呼叫 `showStep(2)`，讓主持人回到首頁時停在「選擇遊戲」畫面而不是要重新跑一次房間設定——房間本來就已經設定過（不然也進不了任何一款遊戲），沒有理由讓主持人每次換遊戲都要重新看一次 Step 1；要調整人數或連結，摘要列右側的「✏️ Edit room setup」隨時可以手動跳回 Step 1。人數／發連結這一步現在**只在首頁做一次**：七款遊戲各自頁面裡原本重複的房間設定卡片（人數鈕／姓名欄／Copy All Links／QR／Send Card Check）已經拆除，改用 `room.js` 的 `ROOM.init({ hideSetup: true, ... })`（`dare-conquest.html` 因為沒用 `room.js`，是直接刪掉自己那份對應實作），各遊戲頁進場時只剩下「這一頁自己才有」的功能——按鈕（例如 Say It Without Saying It 的難度按鈕、Kangaroo Court 的 Judge 開關）與發牌後的唯讀名單／答案顯示；要臨時調整人數或補發連結，要回首頁 Step 1（按摘要列的「✏️ Edit room setup」）處理，不再能在遊戲頁裡直接改，細節見 `room.js` 與各遊戲頁自己的說明）。**Step 1 本身現在是一個六步驟的精靈（wizard）**，一次只顯示一個子步驟，填完按「下一步」才會看到下一個。**子步驟的位置由 `SUBSTEPS = ['lang','count','names','links','check','sheet']` 這個陣列決定，程式裡一律用 `no('links')` 這種方式取號碼、不寫死數字**——原本是寫死的，在最前面插入語言那一步時，連結插槽、方式面板的上一步／下一步、標題 key 對照表、最後一頁的「上一步」全都得同步 +1，漏掉任何一個都會安靜地接到錯的面板；i18n 的 key 也跟著從 `substep1Title` 這種位置命名改成 `substepCountTitle`／`substepLinksTitle` 這種內容命名，插入新步驟時才不會整批錯位。**子步驟 1「選擇介面語言」**排在最前面，因為後面五步全都是用選到的語言寫的——先讓人讀五個畫面的中文、才問要不要英文，順序是反的：兩顆 `.method-btn`（「中文／繁體中文」與「EN／English」，**這兩顆的文字刻意不翻譯**，會自己改名的語言選擇鈕對讀不懂當前語言的人沒有用），按下去就是 `I18N.setLang()`，跟右上角那顆切換鈕共用同一包 `site-lang`、同步亮起；說明文字講清楚這只影響介面文字、而且是整站生效，下面另有一塊琥珀色 `.lang-note` 專門講這個選擇**不會**做的事——遊戲題目永遠是英文。放成獨立一塊而不是混進上面那段，是因為它是個但書。**進度圓點列因此要能被壓縮**：六顆全尺寸圓點加五條 18px 的連接線在 320px 手機上會把整頁推寬（實測 scrollWidth 335），所以連接線改成 `flex: 0 1 18px; min-width: 4px`（它沒有帶任何圓點沒帶的資訊，是最該讓步的部分），420px 以下圓點再縮到 26px，不會像過去那樣把人數鈕、姓名欄、Copy All Links、Send Card Check、Continue to Games 全部攤開在同一畫面：子步驟 1「設定房間人數」標題下方多一段說明文字（`data-i18n="index.substep1Desc"`，只有子步驟 1 才有，`initSetupWizard()` 組 `panel.innerHTML` 時特別判斷 `n === 1` 才插入這段 `<p class="substep-desc">`）：先講「先設定玩家人數」，接著解釋房號的用途（讓所有分頁/裝置同步同一批連結）並明講「通常不需要自己設定」，只有要換回之前用過的房間才需要用「載入」——這是因為房號欄位對第一次使用的主持人來說意義不明、容易誤以為是必填欄位，實際上 `ROOM.init()` 已經自動產生好一組房號，多數情況全程不用碰它。**ROOM 列排在 PLAYERS 人數鈕上面**（`room.js` 的 `buildDOM()`；`room-code-row`／`room-code-divider` 這兩個既有 id 沒有變，Firebase 未啟用時一樣靠 `id` 隱藏）——曾經試過反過來（人數在上、房號在下，理由是「人數才是每次都要看的必填設定」），但試玩下來房號其實不是背景細節：主持人回到這一步多半正是為了判斷「這一場算不算全新的活動」，房號跟緊接著的提醒（見下）就是在回答這個問題，理應先看到。**ROOM 列下方多一塊琥珀色的 `#room-new-note`**：「🆕 這是全新的一場活動嗎？按『新房間』重新產生房號，才不會跟上次活動的連結搞混。」——原本這一段只解釋了「載入」是做什麼的（換回之前用過的房間），完全沒提「新房間」該在什麼時候按，主持人若是延用上一場活動殘留的房號（`ROOM.init()` 預設會記住 `room-last-session`）開新場，玩家連結其實都還指向舊房間，卻沒有任何畫面提醒過這件事。這塊提示跟 ROOM 列一起用 `live`（`ROOM.enabled`）決定顯示與否，樣式仿 `index.html` 自己的 `.sheet-nudge`（同一種「主持人正在判斷要不要重新讀取／重新開房」的琥珀色調）。這個順序與提示只影響 `!cfg.hideSetup` 的完整版 UI（目前只有 `index.html` 用得到），六款 `hideSetup: true` 的遊戲頁不會畫出這兩列，不受影響。子步驟 2「設定玩家名稱」**只有**姓名輸入欄，沒有 Link／QR；子步驟 3「傳送玩家連結」（標題刻意不叫「複製所有連結」，因為 Copy All 只是三種發連結方式之一，取名成單一方法的名字會誤導）不會把 QR／Link／Copy All 三種發連結的方式同時攤開——這是實際發生過的卡關熱點，主持人常常搞不清楚三種方法要選哪一種、畫面資訊太多。改成**先選一種方法，才出現對應的介面**：畫面最上面是一句「選一種方式，把連結分享給玩家」（room.js 原本的「🔗 Player Links (optional)」標題與說明文字，跟這句話重複，已經拿掉——但那兩個節點還是要留在文件裡的某個地方，藏進一個 `display:none` 的容器，因為 `room.js` 的 `render()` 每次都會用 `getElementById` 抓這兩個節點寫入內容，整個刪除節點會讓 `render()` 噴錯），接著三顆等寬的方式選擇鈕（📱 手機掃描 QR Code／🔗 私訊連結給每個人／📋 複製全部貼到聊天室），選了其中一個才會往下展開對應的**一段說明文字**（解釋主持人具體要做什麼，例如 QR 方式會提醒「一次只顯示一位玩家的 QR Code」）＋對應的介面：QR 方式是每位玩家一欄的**唯讀**姓名＋一顆「QR」切換鈕，點下去才展開該玩家的 QR code，**跟舊版 `toggleQR()` 一樣一次只能開一個**——不能同時展開多位玩家的 QR code，因為主持人是要把螢幕分享出去讓大家各自掃描，好幾組 QR code 同時出現在畫面上時，手機鏡頭很容易掃到隔壁那組，這是實際試過同時展開全部又改回來的教訓；私訊連結方式是唯讀姓名＋一顆「連結」按鈕（不顯示 QR）；複製全部方式則完全不顯示逐一玩家的欄位，只有一顆 Copy All Links 按鈕。三種方式的姓名都跟子步驟 2 共用同一份資料、同樣是**唯讀**（要改名字得點回子步驟 2）。技術上 `room.js` 的 `ROOM.setLinksView()` 多兩種模式：`'link-only'`（唯讀姓名＋只有 Link 按鈕）、`'qr-only'`（唯讀姓名＋一顆 QR 切換鈕，內部一樣呼叫共用的 `toggleQR()`，跟 `'full'` 模式的 QR 按鈕行為完全一致）；`index.html` 用 `chosenMethod` 這個變數記住主持人選了哪個方法，切去子步驟 4 再切回子步驟 3 時會用 `selectMethod(chosenMethod)` 把 `#room-links` 重新搬回對應的插槽並還原畫面，離開整個 Step 1（按「✏️ Edit room setup」重新進來）才會把 `chosenMethod` 清空、重新要求選一次；子步驟 4「傳送核對卡片」＝ Send Card Check 按鈕＋每位玩家一欄的**唯讀**姓名（同樣不能改名字、也不提供 Link／QR）。**核對字結果不會一按 Send Card Check 就顯示**——這是刻意分成兩個動作：按下 Send Card Check 只把字發到玩家卡片上、主持人畫面上還看不到答案，`check-note` 顯示「請每位玩家先唸出卡片上看到的內容，核對完後按『顯示所有答案』查看正確答案」＋一顆獨立的「👁️ 顯示所有答案」按鈕；主持人先聽每位玩家唸出自己卡片上的字，聽完才按下這顆按鈕，這時候每位玩家欄位下方才會冒出核對字結果，`check-note` 也換成「✅ 答案已顯示」。如果先讓答案跟著 Send Card Check 一起顯示，主持人等於是自己先看到答案再核對，這場核對就失去意義了。技術上 `room.js` 新增 `checkRevealed` 這個獨立於 `checkWords` 的旗標：`sendCardCheck()` 只設定 `checkWords` 並把 `checkRevealed` 重設成 `false`；新函式 `revealCardCheck()` 才把 `checkRevealed` 設成 `true`；`render()` 畫每位玩家的 `.check-word-badge` 時同時檢查 `checkWords[i]` 與 `checkRevealed`，兩者都成立才畫出來；`clearCardCheck()`（`check-note` 裡的「✖️ 清除」連結）把兩個旗標一起歸零；子步驟 6「載入題庫並開始」＝ 題庫來源列（見 `sheet-data.js`：讀取／恢復預設兩顆按鈕與逐分頁結果）＋原本的 Continue to Games 按鈕。**「上次讀取是什麼時候」用天數講，不是印一個時間戳**（`今天`／`昨天`／`N 天前`，用 `daysAgo()` 比日曆日而不是 24 小時區間，因為 14 小時前發生的事講「昨天」才對）：主持人每場活動走一次這個精靈，唯一要判斷的就是「這份題目是不是今天讀的」，而一個 `2026/9/17 下午7:38:39` 要人自己心算。**只要不是今天讀的，就多一塊琥珀色的 `.sheet-nudge` 直說**「上次讀取：N 天前。如果有人改過題目，要按『重新讀取』才會更新——下面那排綠色打勾只代表上次讀成功，不代表現在是最新的」，同時給那顆按鈕加 `.is-stale`（一個 2.4 秒的緩慢光暈呼吸，不是閃爍：主持人正在分享這個畫面，它要被看見但不能變成全場最吵的東西；`prefers-reduced-motion` 下改成靜態光暈）。會需要這一塊，是因為那排綠色打勾正是問題本身——它們說的是「上次那一次讀取成功了」，主持人讀成「我是最新的」，於是跳過重新讀取，整場玩的是上禮拜的題目（實際回報過「這顆按鈕很容易被忽略」）。按鈕文字也從「🔄 重新讀取」改成「🔄 重新讀取（更新題目）」，講清楚它是做什麼的。從來沒讀過時**不**顯示這一塊，因為那時候畫面上本來就只有「讀取線上題庫」一個動作，不需要再催。題庫讀取刻意放在**房間設定的最後一步**而不是 Step 2：主持人每次帶活動前本來就會走一次這個精靈，順手按一下讀取最自然；放在 Step 2 的遊戲清單上方反而是一塊每次都要跳過的東西。三種畫面共用同一個 `#room-links` 節點與同一份玩家陣列，靠 `room.js` 新增的 `ROOM.setLinksView('names' | 'links' | 'check' | 'full')` 切換 `render()` 要畫出姓名欄／Link-QR／核對字這三塊的哪幾塊（`'full'` 是舊版三塊全部一起畫，預設值，只有 `index.html` 會呼叫這個 API 切換成其他三種）：姓名輸入框在非 `'names'`／`'full'` 模式下改成跟 `renderCompact()` 一樣的唯讀 `<div class="name-input is-static">`。`index.html` 的 `initSetupWizard()` 在 `ROOM.init()` 跑完後，把 `.room-card`／`#room-hdr`／`#room-note`／`#copy-all-btn`／`#check-btn`／`#check-note` 這些既有 DOM 節點分別「搬進」（`appendChild` 對已存在的節點是移動不是複製）五個 `.substep` 容器裡；`#room-links` 則不固定屬於哪一步，`showSubStep(n)` 每次都呼叫 `placeRoomLinks(n)`，依 `n` 是 2／3／4 把它搬進對應子步驟自己的一個空 `<div id="substep-N-links-slot">` 插槽、同時呼叫對應的 `ROOM.setLinksView(...)`，其餘步驟則保持原樣不動（反正父層是 `.hidden` 的子步驟，不會顯示）。節點本身、事件監聽器、`room.js` 內部靠 `id` 抓取節點的邏輯都不受影響，只是父層換了。子步驟上方有一排可點擊的數字圓點（`#substep-dots`）顯示目前進度，**只有已經走訪過的步驟才能點回去**（`subStepReached` 記錄目前最遠走到第幾步），不能跳著點還沒填過的步驟，符合「依序出現」的設計；每次從遊戲頁按「✏️ Edit room setup」或直接重新整理回到 Step 1，都會重置回子步驟 1（不記憶上次停在哪一步），因為房間本身多半已經設定過，主持人此時通常是要整個重新走一次，而不是接著上次的某個中間步驟。切換語言（`I18N.setLang`）不會把 wizard 重置回第一步，只會重新翻譯當前子步驟的標題與按鈕文字（`I18N.applyStatic()` 涵蓋新建立的節點）。 |
| `say-it-without-saying-it.html` | 🙊 Say It Without Saying It（Taboo 玩法，**主持人畫面現在分成「設定階段」與「遊戲階段」兩部分**，同樣套用 `index.html` Step 1 那套「一次只顯示一個子步驟、依序填完才能往下」的精靈模式，只是這裡走的是這款遊戲自己的三個參數而不是房間設定：`#phase-setup`（4 個 `.substep`：1／4 難度、2／4 禁字與生效個數、3／4 回合秒數、4／4 純粹一顆「🎮 開始遊戲」按鈕，`showTabooSubStep(n)` 切換 `.hidden`、`renderTabooDots()` 畫上方的圓點進度列，只有已經走訪過的步驟才能點回去，寫法與 `subStepReached` 邏輯跟 `index.html` 的 `showSubStep()`／`renderSubStepDots()` 完全同構）與 `#phase-game`（原本就有的計分板／控制列／發牌鈕／玩家名單／題庫，一字不改）；**三個設定元件裡，只有 `#rules-group-difficulty`（難度）會出現在遊戲階段**：實際帶活動時房間熱起來、Easy 的題目開始不夠玩，主持人就得換難度，為了這件事要按「⚙️ 編輯設定」跳回精靈、再按三次「下一步」回來太繞，所以它直接放在控制列右半。`#rules-group-forbidden`（禁字開關與生效個數）與 `#secs-field`（回合秒數）**仍然只存在於精靈裡**，遊戲階段看不到、也改不到——那兩項不是「兩輪之間會想動」的東西。做法是**同一個節點在兩個階段之間搬家**，不是複製一份：`enterGame()`（`startGame()` 與卡片發題兩條路都會走到它，見下）呼叫 `placeDifficulty('game')` 把 `#rules-group-difficulty` `appendChild` 進控制列的 `#game-difficulty-slot`，`editSettings()` 呼叫 `placeDifficulty('setup')` 搬回子步驟 1 的 `#substep-difficulty-slot`（`appendChild` 對已存在的節點是移動不是複製，跟 `index.html` 搬 `room-card`／`room-links` 同一招）。全文件永遠只有一個 `#level-btns`，所以 `renderLevelBtns()`／`setLevel()` 完全不用改，精靈與控制列也不可能顯示成不同的難度（實測：在遊戲畫面按「🔴 困難」→ `level`／`localStorage`／摘要列／下一次 `dealRound()` 抽到的卡片全部跟著變，跳回精靈看到的也是困難）。控制列（`#control-bar`）因此變回三塊：左邊回合計時（`⏱️ Round timer` 標籤＋Start／Reset，這兩顆是操作中的計時器本身，不是設定）、正中央倒數圓圈、右邊難度按鈕（`.cb-sect.rules`，側邊條是紅色、刻意 `left: auto; right: 0` 貼在控制列最右緣，跟左邊計時那條靛色側邊條左右對稱）。`grid-template-columns: 1fr auto 1fr` 的兩側都是 `1fr`，所以不論左右兩塊多寬，倒數圓圈都落在控制列正中央（實測中英 × 390／820／1280px，偏移全為 0px）。切換難度一樣**只影響下一次 `dealRound()`**，不會動到正在進行的那一輪。遊戲階段頂端的 `.phase-summary` 摘要列（🎚️ 難度 · 🚫 禁字 ON/OFF（生效個數）· ⏱️ 秒數，`renderPhaseSummary()` 在每次 `renderAll()`／`renderTimer()`／語言切換時重算，讀的是既有的 `level`／`forbiddenOn`／`bannedCount`／`timer.total` 這幾個變數，不是另外存一份）是遊戲進行中一次看完三項設定的地方（難度另外在下面的控制列有自己的按鈕，摘要列仍然照列——這行字的用途是「一次講完三項設定」，為了避免把「中等」講兩次而砍掉其中一項，讀起來反而像漏了）；要調整禁字或秒數，只能按右側「⚙️ 編輯設定」按鈕（`editSettings()`）跳回精靈、`tabooSubStepReached` 重設為 1（回到子步驟 1，邏輯與 `index.html` 的「✏️ Edit room setup」一致），**但刻意不呼叫 `resetRound()`／不動 `state`／`round`／`scores`／`queue`**——回合進行到一半也可以跳回去調整難度或秒數，調完按「開始遊戲」跳回來，遊戲會停在原本的畫面繼續進行，因為這三個設定本來就只影響下一次 `dealRound()`，不影響正在進行的這一輪（例如 `roundForbidden` 是發牌當下的快照，中途改 `bannedCount` 不會動到手上這輪的裁判與描述者已經看到的禁字清單）。**首次載入主持人畫面停在設定階段（`phase = 'setup'` 初始值），這時候卡片還不能發題**：頁面載入的最後一步一樣會 `dealId = newId(); publishRound()` 一次（`ROOM.init()` 還原房間時不會觸發 `onCountChange`，不發這一次的話卡片上會停著上一款遊戲的內容），但 `canDeal()` 在設定階段是 false，所以那份 payload 不帶 `canDeal`／`dealId`，卡片畫的是「⏳ 主持人正在設定遊戲…」而不是發題鈕。主持人按「🎮 開始遊戲」→ `enterGame()`（把精靈換成遊戲畫面、搬難度鈕、捲到最上面）→ 發佈，卡片這時才長出「🎬 發第一個題目」。**曾經試過反過來做**（`canDeal()` 不看 `phase`，卡片隨時能開局、主持畫面自己跟上），理由是「整局不需要主持人操作畫面」；實際帶活動時不行：主持人還在精靈裡選難度，玩家就先開了局，那一題用的是**上一場存著的**難度／禁字／秒數（`dontSayItOptions.v1`），而且主持人會被當場拽出精靈，等於他正在做的選擇被打斷又不算數。所以開局這一下保留給主持人，其餘（開題、搶答、判定、喊 Taboo!、裁決、換下一題）照舊全部在卡片上。**整局的資訊與判定全在玩家卡片上，主持人畫面只有計分板**（因為主持人在分享桌面，目標字與禁字都不能出現在上面）。選 **2–6 人**（每輪要一位描述、一位裁判、至少一位猜，但 **2 人局沒有裁判**——見下方 Forbidden words 開關），按「🎬 Deal a Word」抽卡並發出兩個角色。**角色是隨機抽的，但保證公平**：clue giver 用一個洗過牌的袋子（`giverBag`）輪流發，每位玩家都當過一次才會重新洗牌，且重新洗牌時不會讓剛當完的人接著再當；referee 則從「目前當過裁判次數最少」的人裡面隨機挑（`refCount`），所以順序每輪都不一樣、但次數完全平均（實測 4 人局 24 輪：每人各當 6 次 clue giver、6 次 referee，0 次連莊）：**clue giver 的卡片**＝倒數圈＋（有人搶答時才出現的）「🙋 xxx buzzed in」與「✅ Correct! +1 each／❌ Not it — keep going」兩顆判定鈕（實心填滿綠／紅底色，看起來就是可以按的按鈕，不是純文字配深底）＋目標字＋禁字（搶答訊息與那兩顆判定鈕刻意排在**倒數圈與目標字之間**、也就是卡片最上方，因為那是有人搶答的瞬間 clue giver 唯一需要立刻反應的東西，不該讓他往下捲；沒人搶答時這一區完全不佔位置，改在卡片最下方顯示一行「Keep describing…」提示）；**👀 referee 的卡片**＝倒數圈＋（有人搶答時才出現的）「🙋 xxx buzzed in」綠色訊息＋禁字＋緊接在清單下方的「🚨 Taboo!」按鈕（裁判是在聽這一輪、不是在玩，所以要知道現在桌上有一個答案等著；訊息的樣式與位置都跟 clue giver 卡片上那塊一樣——倒數圈下方——只是**沒有判定鈕**，判斷答案對不對是描述者的事。有人喊 Taboo! 時這塊會收起來，因為那已經把整輪凍結了，該讀的是舉發訊息）（實心紅底，跟判定鈕、搶答鈕走同一套「填滿底色」風格，不再是透明外框；禁字清單與那顆按鈕刻意相鄰，說明文字移到按鈕**下方**，不讓它把清單與按鈕拆開），該輪不搶答，但舉發成立可以得分。**裁判盯的只有 clue giver 一個人**：禁字是「描述這個字最先會想到的詞」，猜的人本來就會一直喊出接近答案的字，那不算犯規，只有描述者說出來才算。按鈕與說明文字都要把這件事講死（實際回報過的誤解）——按鈕寫「🚨 Taboo！描述者說了禁字」而不是含糊的「他說了禁字」，下面那行寫「只有 {描述者的名字}（描述者）說出禁字才算 — 猜的人說到不算。這輪你不用搶答。」，直接把這一輪描述者的名字帶進去；`guide-data.js` 的玩法說明與手機示意圖上的按鈕字樣也跟著同一套說法；**其餘 guesser 的卡片**＝**看不到禁字**，只有倒數圈與一顆大的「🙋 Buzz in!」搶答鈕。（原本禁字是發給所有 guesser 的，但禁字就是「解釋這個字最先會想到的詞」，等於直接把答案送出去——改成只有描述者與裁判持有這份清單，猜的人純靠聽。）**開局之後整局不需要主持人按任何按鈕**（開局那一下要主持人按「🎮 開始遊戲」，見上面「首次載入」那段）：發題、開題、搶答、判定、喊 Taboo!、裁決、換題全部在玩家卡片上，一輪結束也會自己接下一輪。**兩輪之間每一張卡片下方都有一顆「🎬 發下一個題目」**（第一次是「🎬 發第一個題目」），**誰先按都可以**——這款遊戲沒有固定主持人角色，而且 idle／done 這兩個狀態下沒有任何東西會被誤觸破壞（不像回合進行中按下去會把整輪作廢），所以刻意不限定角色、也不只給下一輪的 clue giver（那會卡在「那個人沒看手機」）。技術上主持頁多一個獨立於 `roundId` 的 `dealId`：`canDeal()`（`ROOM.enabled && phase === 'game' && !nextRoundIv && (state === 'idle' || state === 'done')`）為真時才把 `canDeal`／`dealId` 放進 payload，卡片送回 `{ id: dealId, choice: 'deal' }`，`handleAnswer` 的 `deal` 分支**排在 `state !== 'playing' && state !== 'ready'` 那道守衛之前**（它是唯一在回合之外執行的分支），驗證 `canDeal() && v.id === dealId` 才呼叫 `dealRound()`；`dealRound()` 當場把 `dealId` 清空，所以四個人同時按也只會發出一題，慢一步的那幾張卡片送上來的是過期 id，直接被丟掉（實測：四張卡片同時送出 → 只發一題；再用過期 id 送一次 → 被丟掉，進行中的回合不受影響）。`phase === 'game'` 這道關卡就是「設定階段不給發題」（見上一段為什麼）；它一併讓 deal 分支裡原本那句 `if (phase === 'setup') enterGame();` 變成走不到的死路，已經刪掉——能進到那裡就代表主持人已經在遊戲畫面上了（`cardDrove()` 仍然留著，說明頁有可能還蓋在上面）。`startGame()`（主持人自己按「🎮 開始遊戲」）仍會發一次（`state === 'idle'` 用 `publishRound()` 蓋掉上一款遊戲留在卡片上的資料並換一組 `dealId`，中途從「⚙️ 編輯設定」回來則用 `pushState()` 以免洗掉正在等判定的搶答）；`editSettings()` 會 `pushState()`＋`renderAll()`：`phase` 一翻成 `'setup'`，`canDeal()` 當場變 false，不推出去的話卡片會留著一顆按了沒反應的發題鈕，而 `play.html` 等 4.5 秒之後會冒出「主持人的遊戲頁面要開著」那則琥珀色警告——頁面明明開著，只是主持人在精靈裡，那則訊息等於說謊。用 `pushState()`（`.update()`）而不是 `publishRound()`（`.set()`），才不會洗掉正在等判定的搶答。所以主持人中途按「⚙️ 編輯設定」時，全場的發題鈕會一起收起來、卡片改顯示「主持人正在設定遊戲…」，按「開始遊戲」回來才恢復（實測四段轉換：載入 → 開始遊戲 → 編輯設定 → 開始遊戲，`canDeal` 依序 false／true／false／true，卡片文案同步跟著換）。`nextRoles`／`nextRoundAt`／`nextRoundIv`／`nextSeq` 這幾個變數**放在最上面的狀態宣告區、不放在 `armNextRound()` 旁邊**——`canDeal()` 讀 `nextRoundIv`，而 `publishRound()` 最早會在 `onCountChange` → `resetRound()` 時被叫到，早於那段程式碼執行到，留在原處會是 TDZ 錯誤；同理，`cardDrove` 與 `enterGame` 都是在 deal 分支被呼叫之前就宣告好的 `const`／函式宣告。**clue giver 的卡片上沒有「換一題」這種東西**（曾經做過一顆帶確認的「🛑 我想換一題」，已整個移除）：描述者手上握著目標字與禁字，給他一顆能把自己這一輪作廢的按鈕，等於讓抽到難字的人自己跳過——遇到難的字就是這款遊戲本身。要丟掉一輪只能由主持人按畫面下方那顆「🛑 結束 {name} 的回合」（帶確認面板），那是主持人在場邊做的判斷，不是描述者的權利。`handleAnswer` 因此沒有 `skip` 分支，卡片也不會送這個命令。主持人畫面上原本那幾顆按鈕（Deal／結束回合／裁決面板／Start）全部保留，除了「結束回合」之外都是手機壞掉、連不上時的備援。流程：**發題（不論是卡片按「🎬 發下一個題目」，或主持人按備援的「🎬 Deal a Word」）只是抽角色、抽卡，回合還沒開始**——題目是「鎖著」的（`state = 'ready'`），目標字與禁字**連 clue giver 與裁判都還看不到**（`payloadFor()` 的 `sealed` 判斷把 `target`／`emoji`／`forbidden` 一律壓成 `null`），倒數也設好秒數但**不跑**；輪到的那位 clue giver 要在**自己的卡片**上按「👀 看題目」（`play.html` 的 `tabooOpen()`，寫回 `{ id, choice: 'ready' }`），主持人端收到才 `startRound()`：這時才把題目與禁字推出去、才開始倒數（`state = 'playing'`）。這是為了換人的那幾秒——上一位 clue giver 答完題、手機要傳給下一位，或下一位還在切回自己的分頁，題目卻已經攤在螢幕上、秒數也在跑，等於白白吃掉一輪的時間，而且中途任何人瞄到那支手機就直接看到答案。主持人畫面在等待期間狀態列寫「🎤 換 {name} 描述 — 請他在自己的卡片上按『看題目 / Show my word』」——**兩種語言的按鈕名稱一定要一起寫出來**：語言是各自存在每個人瀏覽器的 `site-lang`，所以同一個房間裡有人的卡片是中文、有人是英文完全正常，主持人照著唸「請按看題目」，但那位玩家的卡片上寫的是 Show my word，他就會找不到而整輪卡在這裡等（實際回報過的「遊戲卡住」就是這個）。這是全站唯一一句「主持人畫面指名另一個人卡片上的按鈕」的文案，其餘幾句（`statusIdle`／`subStep4Desc` 等）指的都是主持人自己畫面上的按鈕、同一種語言，不受影響。另外**協定本身跟語言無關**：卡片寫回去的永遠是 `{ id, choice: 'ready'|'buzz'|'right'|'wrong'|'taboo'|'ruling', … }` 這些固定字串，不是畫面上的文字，所以中文主持人配全英文卡片可以完整跑完一輪（實測：開題目→搶答→判錯→Taboo!→否認→再搶答→答對計分→自動換人，全部正常、零錯誤），發牌鈕變成「👀 幫 {name} 開啟題目」、按下去等同替他開啟（手機壞掉、連不上時的備援；`toggleTimer()` 在這個狀態下按 ▶️ Start 也是同樣效果，因為「還沒開題目就開始計時」沒有意義）。接著 clue giver 用英文描述 → guesser 想到就按搶答（用講的把答案喊出來）→ 他的名字會出現在 clue giver 的卡片上 → clue giver 按 ✅ 則**他與該 guesser 各 +1**、這輪結束，**而且不用主持人再按任何按鈕：5 秒後自動換人、自動抽下一題**（`awardPoint()` → `endRound(res, true)` → `armNextRound()`，這 5 秒是留給主持人把答案唸出來的；期間按任何一顆按鈕都會取消倒數改成立刻執行）。**這 5 秒的倒數與「下一回合輪到誰」會同步顯示在每一位玩家的卡片上**：`armNextRound()` 刻意**在倒數開始前就把下一輪的 clue giver 與 referee 抽好**存進 `nextRoles`，而不是等 `dealRound()` 當下才抽——因為卡片要在這 5 秒內就報出名字，下一位描述者才有時間把手機拿起來、切回分頁；等回合開始才知道就來不及了（這也是上面那個「題目鎖著等他按」的同一個問題的另一半）。`endRound()` 因此把 `armNextRound()` 排在 `publishRound()` **之前**，結果與「下一位是誰」用同一次寫入送到所有卡片。`dealRound()` 之後會**消費**這份 `nextRoles`（用掉就清空）而不是重抽，所以卡片上報過的名字一定就是實際發到的人（若中途把禁字關掉，`refIdx` 一律回到 −1，卡片也不會報裁判）。倒數本身跟回合計時走同一套「只推一次、卡片自己跑秒」的做法：payload 是 `next { seq, in, giver, ref, youGive, youRef }`，`seq` 變了卡片才重新對時，**整段 5 秒倒數 0 次 Firebase 寫入**（實測 4 人局）。主持人畫面的狀態列同一行也會接上「🎤 下一位描述者：X · 👀 裁判：Y」，因為那是分享出去給所有人看的畫面。`cancelNextRound()` 只停倒數、**不清掉 `nextRoles`**（主持人提早按「下一回合」時，發到的還是剛剛報過的那兩個人）；要真的丟掉這份預抽只有 `dealRound()` 用掉它、或 `resetRound()`／換人數時整個清空。**答對、Taboo! 成立、時間到**這三種「這一輪自己走完了」的結尾都會自動續下一輪（`endRound(res, true)`）；只有**主持人手動按「🛑 結束 X 的回合」**會停在結果畫面等主持人自己按「▶️ 下一回合」，因為那通常是主持人要插話處理事情才按的。時間到原本也停在結果畫面等人按，但沒人答出來的那一輪其實最不需要講評，卡在那裡只是讓下一位描述者乾等，所以改成跟答對走同一條路——狀態列與每一位玩家的卡片一樣會顯示 5 秒倒數與「下一位描述者／裁判是誰」。按 ❌ 則回合繼續、重新開放搶答。**一次只有一個人能搶答**（試玩回饋）：有人按下搶答的那一刻，其他 guesser 卡片上的「🙋 Buzz in!」整顆消失（不是變灰，灰掉的按鈕還是會被戳），換成「⏸️ {name} 搶答了／等 {描述者} 判定完才能再搶答」，主持端 `buzz` 分支在 `queue.length` 時直接丟掉任何還是送上來的搶答（還沒重畫完的卡片、同一瞬間的兩根拇指），描述者按 ❌ 才重新開放。以前是排隊（第 2 位以後顯示「You're #N in the queue」），但排在後面的人等於免費聽到了前一個人的答案、描述者也容易一次判兩個，所以改成單人；`queue` 這個陣列因此永遠只有 0 或 1 個元素，狀態列不再列「排隊等著猜」的名單，改成一句「其他人要等判定完才能再搶答」。**被判錯的人不能馬上再搶答**（`guesserNums()` — clue giver／referee 以外的所有人）：判錯後那個人會被鎖住（存進 `rejected` 陣列），直到**這一輪剩下的每一位 guesser 都各自搶答並被判錯過一次**，鎖定才會整批解除、所有人重新開放搶答——例如 5 人局（C／D／E 三位 guesser）：C 搶答判錯 → 只剩 D、E 能搶；D 搶答判錯 → 只剩 E 能搶；E 搶答判錯 → C／D／E 三人同時解鎖，重新開放。被鎖住的人卡片上原本的「🙋 Buzz in!」按鈕會整個換成一則提示「❌ Not it — hang tight」，沒有可以按的按鈕；主持人送出的判錯（`handleAnswer` 的 `wrong` 分支）在 `rejected.push(...)` 之後會檢查 `rejected.length >= guesserNums().length`，滿了就整批清空 `rejected`，下一次搶答自然又全員開放。答對、換一位新的 clue giver、或裁判喊 Taboo! 都會直接結束整輪（`dealRound()`／`resetRound()` 會清空 `rejected`），所以這個鎖定規則只在同一輪、同一個人還沒答對之前生效。**落後加分（catch-up bonus）**：分數比全場平均（含自己）低 `CATCH_UP_GAP`＝3 分以上的人，這一輪的得分 ×2——答對 +2 而不是 +1、Taboo! 成立 +4 而不是 +2；扣分（描述者的 −1）**不**加倍，這是拉一把、不是推一把。倍率在**這一輪的分數落地之前**用當時的計分板判定（`isBehind(i)`／`multiplierFor(i)`），而且描述者與猜對的人**各自**判定：`awardPoint()` 先把兩個倍率都讀出來才加分，免得描述者的 +1 先把平均值墊高、猜的人就掉出門檻。分數不再是固定的 +1／+2，所以 `result` 多帶 `whoPts`／`giverPts`，狀態列（`gotItResult`／`foulResult`）、卡片（`gotItYou`／`gotItGiver`／`goodCatch`）與主持人的卡片預覽（`plusForYou`）都印實際數字，舊主持頁沒帶時退回 1／2。誰在加分中要讓全場看得到：主持計分板那一格掛一顆琥珀色實心的「🔥 ×2 落後加分」藥丸（`.sc-boost`），有人掛著時計分板底下才多一行說明規則的 `.sc-boost-note`；卡片的全場分數列（`board` 多一個 `x2` 欄位）在那個人的那一列印「🔥×2」，自己在加分中時另加一行「🔥 落後加分中：你這輪的得分 ×2」。2 人局也適用（平均含自己，領先 6 分才會觸發）；1 人時永遠不觸發。**Taboo! 是「舉發」而不是直接判定，而且裁決的人是被舉發的 clue giver 自己、不是主持人**：這不是誰比較公正的問題——**禁字從頭到尾不會出現在主持人畫面上**（整款遊戲的前提），主持人根本沒有資訊可以判斷那個字算不算禁字；真正同時握有「禁字清單」與「我剛剛到底說了什麼」的只有 clue giver 自己（裁判有清單，但被舉發的是對方）。所以裁判按下 Taboo! 之後，**兩顆裁決鈕長在 clue giver 的玩家卡片上**（`play.html` 的 `tabooRule(upheld)`，寫回 `{ id, choice: 'ruling', upheld }`）：卡片最上方先是一條紅色「🚨 {裁判} 說你講了禁字 — 你說呢？」，接著「🚨 我說了（他 +2，我 −1）」與「👌 我沒說 — 繼續」兩顆大按鈕，**這段期間原本的搶答判定鈕會整個讓位**（一次只回答一個問題，跟主持人畫面「裁決面板取代 Deal 那一排」是同一個原則）。主持人端 `handleAnswer` 的 `ruling` 分支**必須排在 clue giver 的搶答判定分支之前**——那個分支會把不是 `right`／`wrong` 的訊息直接 return 掉（實際踩過：ruling 送出去完全沒反應）；分支本身也會驗證送出的人真的是這一輪的 clue giver、且真的有人喊了 Taboo!，其他人冒名送 `ruling` 一律忽略。主持人畫面上方的狀態列仍會亮紅色顯示「🚨 Cara called Taboo! on Amy」（誰按的、被舉發的是誰，計分卡也會紅框標示），**畫面下方的裁決面板（`#call-row`）改成備援**：標題寫「等 {giver} 在自己的卡片上回答有沒有說 — 手機不方便時也可以由你代答：」，下面兩顆**實心填滿**的大按鈕（紅／綠）主持人一樣按得下去（手機壞掉、連不上時用，跟「👀 幫 X 開啟題目」同一種備援）。這個面板刻意長在**原本 Deal／結束回合那一排的位置**，而且面板出現時整條 `#deal-row` 會被隱藏（`renderAll()` 的 `ruling` 判斷同時控制兩者，一次只會有一組按鈕在畫面上）——舊版是把裁決鈕放在計分板**上方**、Deal 鈕留在下方，主持人的手本來就伸向畫面最下面那顆按鈕，兩個決定分在兩個地方，結果常常誤按成「🛑 結束這一回合」；同理，狀態列只講「發生了什麼」（分享畫面上所有人都看得到），問句只出現在下方的面板裡（主持人自己要回答的那一句），不再兩邊各講一次。「🚨 成立 — 裁判 +2，描述者 −1」＝成立，**抓到的裁判 +2 分**（這是裁判唯一的得分方式），**讓對方抓到的 clue giver −1 分**（可以扣成負數，沒有下限），猜題者 0 分，換下一位描述；分數卡在裁決當下會各自跑一次動畫：裁判是綠色的 `pointPop`（跟猜對時同一種「加分」動畫），clue giver 則是紅色的 `pointLoss`（scale 縮小＋紅色 box-shadow 閃一下，`popIdx`／`lostIdx` 各自追蹤要跑哪種動畫，避免扣分也套用跟加分一樣的綠色特效造成誤解）；「👌 我沒說／不成立」＝不成立，裁決面板消失、Deal 那一排回來、計時從停住的秒數接著跑、回合原封不動繼續（舉發者可以再按一次）。**成立時跟「答對」走同一條路：`endRound(res, true)` → `armNextRound()`**，所以一樣是 5 秒後自動換人、自動抽下一題，倒數與「下一回合誰描述、誰當裁判」也一樣顯示在每一位玩家的卡片上（見上一段）。主持人手動結束（🛑 結束 X 的回合）是唯一停在結果畫面等主持人自己按的收尾。裁決期間所有卡片都會顯示這條舉發訊息，裁判自己的卡片變成「🚨 You called Taboo! — waiting for the host」，**其他 guesser 的「🙋 搶答」按鈕會整顆消失**，換成一行「⏸️ 有人喊了 Taboo！— 等 {描述者} 回答之後才能再搶答。」，等 clue giver 裁決完才會自己回來（不成立 → 下一次 payload 就恢復；成立 → 整輪結束）。有人喊 Taboo! 的當下這一輪就停住了（計時也凍結），留著一顆按得下去的搶答鈕等於在一個已經停掉的回合裡收答案；按鈕是直接拿掉而不是變灰，因為灰掉的按鈕還是會被一直戳。主持人端 `handleAnswer` 的 `buzz` 分支另外擋一層（`if (tabooCalls.length) return`），還沒重畫完的卡片送上來的搶答一律丟掉、不會排進 `queue` 等裁決完才爆出來。主持人畫面只有：一行狀態列（誰在描述／誰搶答了／誰喊了 Taboo!／這輪結果與答案）、**一條控制列**，左右分成兩個一眼可辨的區塊（各有自己的彩色側邊條與小標題、中間一道分隔線；窄螢幕會上下堆疊）：**倒數圓圈固定在畫面正中央**（控制列用 `1fr auto 1fr` 三欄，不論左右控制項多寬，數字都剛好落在螢幕水平中線，實測 390–1280px 各寬度誤差皆為 0px），左邊「⏱️ Round timer」＝一組「− ／ 秒數 ／ +」的秒數欄位（**預設 90 秒**；−／+ 每按一次 ±30 秒並自動對齊到 30／60／90／120… 的整數倍，範圍 30–900；中間欄位仍可直接打任意秒數 5–900，打字途中不會被夾住、離開欄位才會校正。**刻意不用 `<datalist>`**：Safari 會依欄位現有內容過濾建議，iPad 上欄位是 60 時就只看得到 60 一個選項）＋Start／Reset；右邊是禁字開關（**無外框**的純文字切換鈕，不像其他按鈕有邊框，靠顏色跟滑動開關本身區分 on／off）＋**難度選擇**（🟢 Easy 300 字／🟡 Medium 200 字／🔴 Hard 200 字，**可以複選而不是三選一**：勾起來的難度會混在一起抽，勾一個就是只抽那一種、三個都勾就是整副 700 張。原本旁邊還有一顆「🔀 全部」，**已經移除**——它從來就不是一個難度（沒有任何一張卡片的 `level` 是 `'all'`），只是「整副牌」的另一個名字，改成勾選之後那個狀態自己會講，多一顆按鈕反而要主持人先搞懂它跟前三顆是不是同一類東西。跟著砍掉的還有 `LEVEL_CHOICES` 這個「三種難度＋all」的清單與 `allLabel` 字串，`LEVELS` 現在是唯一一份難度清單，按鈕、`localStorage` 驗證、摘要列與頁尾題庫瀏覽分組通通讀它。狀態從 `let level = 'medium'`（字串）換成 `let levels = ['medium']`（陣列），`isLevelOn(id)` 是共用的判斷；`levelDeck()` 改成「`level` 有被勾起來的卡片」的聯集，`used`（已出過清單）照樣生效，所以勾兩種就是那兩種加起來抽完才重來、三種都勾就是 700 張（實測連抽 60 輪：沒勾的那一種 0 次、勾起來的兩種都有、抽完整副之前 0 重複）。**最後一個勾不能取消**：`toggleLevel()` 在 `levels.length === 1` 時直接 return，那顆按鈕維持勾起來的樣子、加一個 `.is-locked`（只是把 cursor 與按下去的縮放效果拿掉，**不變灰**）與一行 title「至少要留一個難度」——沒有這道關卡，全部取消之後畫面會顯示「一個難度都沒有」，但 `levelDeck()` 為了不讓牌堆變空又會退回整副牌，下一次發牌就直接跟畫面上寫的矛盾。**三顆難度鈕跟旁邊的 Banned words 數量鈕共用同一個 `.level-btn` class，但只有難度鈕帶 `.is-check`**：多一個勾選方塊（`.lb-box`，勾起來是紅底白勾、沒勾是空心方框；**沒勾的時候方塊裡不放任何字元**，不是把 ✓ 設成 `color: transparent` 藏起來——那樣會讀螢幕軟體會唸出一個不存在的勾，對比稽核也會抓到一個透明的文字節點），因為那兩排長得一模一樣卻一個可複選、一個是三選一，主持人不該靠點點看才知道差別。**改難度不會清空「已出過」清單**（`toggleLevel()` 早期版本會 `used = []`，已經拿掉）：`used` 存的是 `DECK`（700 張卡片、載入時就綁死、永遠不會被重建或重新排序）自己的絕對索引，`levelDeck()` 永遠只是「過濾出目前勾起來的難度」，不會產生一份重新編號的新陣列，所以不管勾選怎麼變，`used` 裡每一個索引指到的都還是同一張卡片，沒有理由清空。實際回報過的 bug 正是清空這一步造成的：主持人把難度從「簡單＋中等」改成「簡單＋中等＋困難」，`used` 被歸零的瞬間，剛剛才發過的那幾張簡單／中等卡片全部變回「沒發過」，下一次 `dealRound()` 立刻就可能重複發出剛剛才問過的字。**這跟 `dare-conquest.html` 的 `usedPrompts`／`usedTruths` 是不同的情況、不能照抄同一招**：那邊的 `used` 存的是「合併後那副牌」（`dareDeck()`／`truthDeck()`，依勾選即時重新拼接）裡的位置，勾選一變，牌堆的形狀與內部順序真的會跟著改變（尤其拿掉中間那個難度時，後面難度的題目會整批往前挪位），舊索引可能因此指到別的題目，所以那邊清空是必要的安全機制；這裡的 `used` 從頭到尾指的都是同一份不變的 `DECK`，清空反而是憑空丟掉還有效的記憶。改難度只要讓 `dealRound()` 既有的「這個難度範圍已經抽完」判斷（`avail` 為空時才 `used = []` 重新開始）自然處理就好，不需要在 `toggleLevel()` 額外插手。實測：勾選「簡單＋中等」連續發 15 題不重複、`toggleLevel('hard')` 擴大成「簡單＋中等＋困難」後再發 15 題，跟前 15 題**零重疊**；`used.length` 在勾選前後（含縮小回「簡單＋中等」）維持不變，不會被歸零；刻意把整個「簡單」難度都標成已發過，再收窄到只勾「簡單」，`dealRound()` 既有的用盡即重置機制一樣正常接手，不受這次移除影響。選擇存進同一包 `localStorage`，舊的 `level` 字串會被讀進來轉換（`'all'` → 三個都勾，其他 → 只勾那一個）＋**🔢 Banned words 數量選擇**（2／3／4／5／6 五顆按鈕，控制每張卡的 6 個禁字裡實際生效幾個，數字愈小愈簡單；跟難度一樣，改變設定只影響下一次 Deal，不會動到正在進行的那一輪——`dealRound()` 會把 `card.forbidden.slice(0, bannedCount)` 存進獨立的 `roundForbidden`，卡片看到的清單一律讀 `roundForbidden` 而不是即時重算，所以就算回合進行中主持人手滑改了數量，這一輪裁判與描述者手上的清單也不會被動過；選擇同樣存進 `dontSayItOptions.v1`）（右半區塊本身不再有「🚫 Round rules」這行標題，窄螢幕時圓圈移到最上方、下面依序是計時控制與禁字設定；難度按鈕與 Banned words 按鈕**都會自動換行**（`flex-wrap: wrap`），手機直向窄螢幕下不會被切斷在螢幕邊緣外——這兩排按鈕原本沒設 `flex-wrap`，在窄螢幕上會撐開整個控制列、把畫面往右推出可視範圍）。這些設定分兩個 `.rules-group`（每組上方一行小字標籤、組跟組之間用一條淡紅色虛線分隔，避免在手機分享畫面上擠成一整團）：**Forbidden words 開關與 Banned words 數量選擇合併在同一組**（`🚫 Forbidden words` 標籤下先是 ON／OFF 開關，下面用自己的虛線再分隔出一行更小的 `🔢 how many` 子標籤與 2–6 按鈕——因為「開幾個字」本來就是「開不開禁字」的子設定，兩者永遠一起看、一起決定），關掉 Forbidden words 或人數鎖到 2 人時，這排數量按鈕會整組變半透明且無法點擊（`.banned-count-row.is-off`，`renderForbiddenToggle()` 統一控制），因為停用禁字時「幾個生效」已經沒有意義；`🎚️ Difficulty`（難度）是完全獨立的第二組，因為它跟開不開禁字無關，一直都能選。開關本身文字也簡化成單純的「ON／OFF」，因為標籤已經講了是什麼；窄螢幕時每組置中對齊，寬螢幕時整組靠右對齊。按「▶️ Start」開始倒數、可「⏸️ Pause」暫停續跑、「🔄 Reset」歸零重來。**有人搶答或喊 Taboo! 時倒數會自動凍結**（搶答的人不該在等 clue giver 判定時被扣秒數，喊 Taboo! 更是直接中斷了這一回合），主持人畫面與每位玩家的卡片同步停住，狀態列會補上「⏸️ Clock on hold」說明為什麼停了；等 clue giver 按 ❌（且排隊中沒有下一位）或主持人按「👌 No — keep playing」才從停住的秒數接著跑，按 ✅ 或裁決成立則直接結束該輪。程式用 `timer.autoHeld` 記住「這次是系統自己暫停的」，所以只會解除自己造成的暫停：主持人手動按過 ⏸️ Pause 的計時器不會被搶答結束時意外喚醒，而主持人手動按 ▶️／🔄 一律優先、會清掉這個旗標。凍結期間一樣不寫 Firebase（實測 4 人局凍結 5 秒＝0 次寫入，一次搶答含暫停共 5 次寫入）；大圓形數字會隨剩餘比例變色綠→橘→紅並在最後閃爍。**倒數靠的是牆上時鐘（`timer.endsAt`）而不是 `setInterval` 數格子**：瀏覽器會把「看不見的分頁」的 interval 節流到大約一分鐘一次、甚至整個凍結，所以主持人中途切到別的分頁（通話 App、題庫、任何東西）時，主持頁的 90 秒永遠不會到，而每一位玩家的卡片——跑在他們正在看的分頁裡——照樣走到 0 然後停在那裡，因為卡片只負責顯示、結束回合是主持頁的事。這就是回報過的「時間到了什麼都沒發生」。改讀牆上時鐘之後，被節流的那一次 tick 一跑就會發現回合早就該結束，另外還掛了一個 `visibilitychange`，主持人一切回這一頁就當場結算。順帶也解決主持人畫面的倒數跟卡片上的倒數愈跑愈不一樣（純數格子必然會漂移）：`play.html` 的 `tabooTimer` 同樣改成收到 payload 當下記一個 `endsAt`，tick 只負責讀時鐘。tick 間隔改成 250ms（數字還是每秒才變一次，只是暫停或重新對時之後不會慢半拍才更新）。`stopTicking()` 會在丟掉 `endsAt` 之前先把 `timer.left` 對回真正的剩餘秒數，暫停才會停在真的那一秒上。**倒數在 clue giver 於自己卡片上按下「👀 看題目」的那一刻才開始**（見上面的 `ready` 狀態；主持人不用再按一次 Start，那只是白白吃掉秒數），主持人畫面與所有卡片同步起跑；**倒數同步顯示在每一位玩家的卡片上**（同款圓圈，clue giver 在題目上方、guesser 在禁字上方），做法跟 Dare Conquest 一樣：主持人只在狀態改變時推一次 `timer { seq, state, remaining, total }`，卡片收到後自己每秒遞減，**倒數過程中完全不寫 Firebase**（實測 4 人局跑 8 秒＝0 次寫入）。歸零時該輪自動結束、0 分並公布答案；每抽一張新卡計時器會重設成設定秒數、但要等 clue giver 開啟題目才起跑）、Taboo! 裁決鈕（只在有人舉發時出現）、每人一張大計分卡（clue giver 靛色highlight 並標「🎤 Clue giver」、裁判琥珀色標「👀 Referee」、搶答者綠色浮起；全站一律用 **clue giver** 這個稱呼，不再出現 describing 字樣）、**一顆**「Deal a Word／Next Round」按鈕（獨佔整條寬度；一輪結束後會變成綠色並持續脈動、文字改為「▶️ Next Round — new clue giver」，避免主持人分享畫面時看不出來換人了）。原本旁邊還有一顆「🔁 Skip this word」，**已經移除**：不喜歡的題目本來就可以用「結束這一回合」處理掉，多一顆只是在分享出去的畫面上多一個會被誤按的東西。Skip 是 `dealRound(keepGiver)` 唯一的呼叫端，所以那個參數與它的分支（不重抽描述者、不消費 `nextRoles`）也一併刪掉，`dealRound()` 現在永遠照 `nextRoles` 報過的名字發牌；`.btn-skip` 的 CSS 與它的 i18n 條目也都清乾淨了。發牌鈕下方另有一顆「↺ 重設分數」（`.score-reset`，一顆小膠囊，刻意做成畫面上最低調的東西、離發牌鈕有一段距離，因為歸零分數是兩場之間才會做的事）：`resetScores()` 把 `scores` 全部歸零後呼叫 `publishRound()`，**不只是重畫主持人的計分板**——每張玩家卡片底下現在也印著全場分數，不推出去的話卡片會停在舊的數字上。**回合進行中那顆「🛑 結束 {name} 的回合」按下去不會直接結束，會先問一次**（`askEndTurn()`）：它就長在主持人的手本來就會伸過去的畫面最下方，實際被誤觸過，而誤觸的代價是整輪作廢。按下之後 `#confirm-row` 這塊琥珀色面板會**取代** `#deal-row`（跟 Taboo! 裁決面板同一個「一次只有一組按鈕在畫面上」的做法），上面一句「🛑 確定要結束 {name} 的回合嗎？這一輪不會計分。」，下面兩顆大按鈕：綠色的「👌 取消 — 繼續這一輪」排左邊（安全的答案排前面），琥珀色的「🛑 結束這一回合」排右邊。面板的 `padding-top` 特地放大到 36px，**是量過的**：390／820／1280px 三種寬度下，兩顆答案的矩形都完全落在「剛剛那顆結束按鈕」的矩形之外（原本只差 3px 會重疊），所以誤觸的第二下落在面板空白處、什麼都不會發生。這個問句還會**自己過期**：`askEndTurn()` 記下一份 `confirmSig`（`roundId`／`state`／`queue.length`／`tabooCalls.length`），`renderAll()` 每次重畫都比對一次，只要有人搶答、判錯讓佇列變動、有人喊 Taboo!、或這一輪已經結束，面板就自動收起來——主持人不會對著一個已經不成立的問題按「確定」。反過來說，有人正在等判定時主持人**仍然可以**結束這一輪（是記快照而不是禁止開啟，差別就在這裡）、連結區，以及一份**預設收合**的題庫（按「👁️ Browse all 700 words」才展開，避免分享畫面時暴雷；展開後依難度分成三組顯示）。控制列右半的「🚫 Forbidden words ON／OFF」開關：關掉時**所有人的卡片都不會出現禁字、也不再發裁判**（clue giver 只剩目標字與一行「No forbidden words this round」，原本的裁判變回一般 guesser 可以搶答），搶答與計分照常運作；開關可在回合進行中即時切換、會存進 `localStorage`（key: `dontSayItOptions.v1`）重整後保留。**2 人局強制關閉禁字**：1 位 clue giver＋1 位 guesser 已經沒有第三人能當裁判，所以人數選到 2 時開關會整顆鎖住變灰、文字改成「Forbidden words OFF (needs 3+ players)」，`toggleForbidden()` 這時點了也不會有反應；若原本 3 人以上且已開啟禁字，中途把人數調到 2 會自動關閉（`onCountChange`），調回 3 人以上開關才會解鎖（但不會自動重新打開，要主持人自己再按一次）；`ROOM.init()` 讀到其他遊戲存的房間人數剛好是 2 時也一樣會在載入當下強制關閉，不會停在「2 人卻要抽裁判」的壞狀態。沒有 Firebase 時這款無法進行（目標字沒有地方私下給 clue giver 看），會顯示警告並鎖住 Deal 按鈕。技術上：開新回合用 `ROOM.publish()`（`.set()`，順便清掉上一輪的作答），回合進行中改用 `ROOM.update()` 避免蓋掉玩家正在寫入的搶答；主持人收到搶答後會 `ROOM.clearAnswer()` 把該欄位清空，所以同一個人可以重複搶答。`ROOM.init()` 帶 `hideSetup: true`（人數／連結已在 `index.html` 設定好），`room-mount` 只留發牌後的唯讀名單：clue giver 的欄位框線用**綠色**（`is-giver-link`，跟頁面本身的靛色主題色不同，才能在一片靛色 UI 裡一眼認出來），referee 維持琥珀色（`is-ref-link`，跟計分板上的裁判配色一致）；「👁️ Show what's on player cards (spoilers — don't open while sharing)」這顆按鈕**不是透過 `extraRowHTML` 掛在暫離設定卡片裡，而是自己手動 `createElement` 出來、`insertBefore` 插在 `#room-links`（唯讀名單）正前方**，跟暫離卡片是分開的兩個區塊——它管的是底下這份唯讀名單要不要多印一份卡片內容，不是暫離／替補姓名那組設定，混在同一張卡片裡曾經讓主持人誤以為它是暫離功能的一個選項（實際回報過：截圖圈起來的是暫離卡片下緣，說這顆按鈕該跟下面的名單同一區）。因為 `#room-links` 這個節點本身是固定的（`ROOM.render()` 只會重畫它自己的子節點，不會把它整個換掉，見 `room.js` 那一列），插在它前面的這顆按鈕不需要在每次重畫後重新插入，一次到位。CSS 也跟著補上這段間距：原本 `.room-card + .room-links { margin-top: 18px }`（`room.js` 全站共用的樣式）靠的是「名單緊接在設定卡片後面」這個假設，這顆按鈕插進去之後兩者不再直接相鄰，改在這頁自己的 `<style>` 補一條 `#card-preview-toggle { margin: 18px 0 14px }`，把原本卡片與名單之間、以及按鈕與名單之間的間距重新接起來。**預設關閉**，按下後每位玩家的連結卡片下方會即時多印一份**縮小版的卡片重現**（`cardPreviewText()`），不是純文字轉述而是照著卡片本身的視覺元素畫：目標字用跟卡片上 `.taboo-target` 同款的深底框線小方塊（emoji＋粗體字）、禁字是一顆顆獨立的紅色藥丸（`.cp-pill`，跟卡片上 `.tb-word` 同一套配色），凡是卡片上**真的是按鈕**的東西這裡也用按鈕外觀呈現（`.cp-btn`）：guesser 沒搶答時的「🙋 Buzz in!」是綠色實心按鈕、referee 的「🚨 Taboo!」是紅色實心按鈕、有人搶答時 clue giver 那兩顆「✅ Correct／❌ Not it」判定鈕也各自用綠／紅按鈕呈現、兩輪之間每個人的「🎬 發下一個題目」用靛色的 `.cp-btn-deal`，其餘像排隊中、已搶答、被鎖住、回合結果等純資訊性狀態則維持一般文字（`.cp-note`），讓主持人不用拿起手機就能一眼看出每個人卡片上「長什麼樣子」，不只是「寫了什麼」；再按一次變回「🙈 Hide player cards」收合。主題色靛 `#6366f1`）。**`used`（本局已出過的卡片索引）現在會持久化，不再只活在分頁的記憶體裡**：主持人實際玩到重複的題目，回頭查是因為 `dealRound()` 靠一個頁面層級的 `let used = []` 記錄「這副牌已經抽過誰」，但這個陣列從來沒有寫進 `localStorage`——只要這個分頁重新整理過一次（Wi-Fi 不穩自動重連、手機把背景分頁喚醒時重新載入、單純手滑重整），`used` 就會歸零，接下來抽到的題目完全不記得先前那幾輪已經出過誰，才會抽出剛剛才問過的字。現在 `used` 跟 `forbiddenOn`／`levels`／`bannedCount` 存進同一包 `dontSayItOptions.v1`：`dealRound()` 每抽一張新卡就呼叫一次既有的 `saveOptions()`（原本只在改設定時呼叫，現在多一個呼叫點），頁面載入時的還原區塊多讀 `saved.used`；**用 `deckLen`（`DECK.length`）當一個簡單的指紋比對**，只有跟目前 `DECK` 的長度對得起來才真的還原，對不起來（例如主持人在別的分頁重新讀取過 Google Sheet 題庫、牌堆形狀變了）就讓 `used`留空從頭記——舊索引可能已經指到不同的字，比對不上時寧可少擋幾輪重複，也不要把新牌堆的字誤判成「這張出過了」而永遠抽不到。**`toggleLevel()` 原本也有一行 `used = []`，之後另一輪回報才發現是錯的、已經移除**（換難度並不會讓 `DECK` 的形狀改變——那是 `dare-conquest.html` 的合併牌堆才有的問題，這裡的索引永遠指向同一份不變的 `DECK`，詳見上面難度那段的說明），只是在那次移除之前，清空後也會經由同一個 `saveOptions()` 一併寫回 localStorage，這件事本身沒有錯，錯的是清空這個動作。實測：連續發 30 題不重複、重新整理分頁後 `used` 正確還原、重整後再發 30 題與重整前的 30 題**零重疊**；刻意把 `deckLen` 改成不符的假資料，確認會整批放棄還原、回到空清單而不是硬套錯誤的索引。

**暫時離開（away）**：房間裡設定房 room-card 的動作列多一排「💤 暫時離開」勾選鈕，每位玩家一顆（`renderAwayBtns()`，樣式跟難度勾選鈕同一套 checkbox 語言、但用灰色而不是這頁的紅／靛，因為它不是遊戲設定），主持人臨時遇到有人要離開語音一下（上廁所、接電話）可以直接勾起來——**不是把人踢出房間、也不清空他的分數**，只是讓 `drawGiver()`／`drawReferee()`／`guesserNums()` 這三個決定「這一輪誰上場」的地方都跳過這個人，直到主持人再勾一次取消。跟難度／禁字生效個數同一條規則：**只影響下一次抽角色，不會動到正在進行的這一輪**——已經抽到的描述者或裁判就算被勾成暫離，這一輪還是照原樣跑完（真的需要立刻中斷，用畫面下方原本就有的「🛑 結束 {name} 的回合」，那才是主持人真的要作廢這一輪時該按的鈕），描述者的卡片也一律不受 away 影響（描述者手上握著這一輪唯一的關鍵狀態——目標字，賽局進行中不能把他的操作介面收掉，見下）。**狀態存進 `away` 陣列**（`[i]` 對應座位 `i`），跟 Kangaroo Court 的 `judgeEnabled` 一樣經由 `ROOM.setExtra('away', away)`／`ROOM.getExtra('away')` 存進同一包 room session，重新整理這個分頁會還原。**至少要留 2 位在場玩家**：`toggleAway()` 在勾起來會讓在場人數（`activeCount()`，即 `ROOM.count` 扣掉目前暫離的人數）掉到 1 以下時直接拒絕（按鈕變 `.is-locked`、灰階且不可點、title 顯示「至少要留 2 位在場玩家」）——一個描述者沒有人可以猜就不成局；已經勾起來的隨時可以取消（增加在場人數永遠安全，不會鎖）。**Forbidden words 的「3 人以上」門檻改看在場人數，不是房間人數**：原本 `toggleForbidden()`／`renderForbiddenToggle()` 判斷的是 `ROOM.count < 3`（2 人局沒有第三人能當裁判），但一個 5 人房間若暫離 3 個人，剩下的 2 個人一樣沒有第三人能當裁判——繼續開著禁字，`drawReferee()` 會把僅剩的那位猜題者也抽去當裁判，猜的人直接歸零，整輪卡死。兩處都已經改成 `activeCount() < 3`；`toggleAway()` 勾起來造成在場人數跌破 3 時，會比照原本「人數掉到 2」的處理——立刻關掉 forbidden words、清掉還沒裁決的 Taboo! 舉發、釋放暫停的計時器（跟 `toggleForbidden()` 關閉分支同一段程式），不會停在一個裁判已經不存在、卻還開著禁字的壞狀態；跟人數變動同一個慣例，跌破門檻會自動關，回到 3 人以上並不會自動重新打開，要主持人自己再勾一次。**`drawGiver()`／`drawReferee()`／`guesserNums()` 三處都加上 `!away[i]` 的過濾**，公平演算法本身不變——`giverBag` 洗牌時只洗在場的人，重新加入的人要等下一次洗牌（bag 用完）才會被排進去，不特別插隊；`refCount` 在某人暫離期間不會累加，所以他一回來、演算法會因為他的次數比別人少而優先派給他，等於自動幫他補回錯過的輪替，不需要額外處理。玩家卡片端（`play.html` 的 `tabooCardHTML()`）多讀一個 `away` 欄位（`payloadFor()` 新增），暫離的猜題者／裁判打開卡片看到的是一張中性的「💤 你被標記為暫時離開」卡（有秒數時鐘就跟著顯示，沒有搶答鈕、沒有裁判鈕，不會誤觸），不是一顆「別人都看得到就我看不到的搶答鈕」；`handleAnswer()` 的 `buzz` 分支另外擋一層 `away[num-1]`，就算卡片沒重畫完、手滑送出搶答也一律丟掉。**這個判斷刻意排在 `isGiver` 分支之後、`isRef` 分支之前**：描述者的卡片永遠照描述者畫（`isGiver` 的 `return` 先執行，見上），裁判如果被標記暫離，卡片會直接變成暫離卡、收回「🚨 Taboo!」鍵——裁判這個角色本來就是「有在盯場」才有意義，沒理由讓一張已經被主持人認定「這個人現在不在」的卡片還留著一顆需要即時盯場才有用的按鈕。主持人畫面的計分板也加一個 `is-away` 樣式（純粹降低透明度＋文字轉灰階，不搶「描述者／裁判／搶答」這幾種正在發生的高亮），角色標籤顯示「💤 暫時離開」；但如果暫離的人剛好就是這一輪抽到的描述者或裁判（例如勾起來之前就已經在跑這一輪），計分板照樣顯示他真正的角色而不是暫離——那才是這一輪實際在發生的事。實測：4 人房暫離 1 人連續發 30 輪，描述者／裁判從未抽到那個人；取消暫離後 20 輪內重新抽到；3 人房暫離 2 人後第 3 次嘗試被拒絕（正確只留 1 人暫離）；重新整理分頁後 `away` 正確還原；5 人房暫離 3 人時 forbiddenOn 自動關閉且鎖住；2 人房兩顆按鈕皆鎖住無法勾選；320／390px 中英文下皆無溢位，`.away-btn.on` 文字對比 9.81:1、計分板暫離角色文字 4.88:1、名字 13.38:1，全部合格。

**替補姓名（substitute name）**：暫離常常不是「這個人等一下回來」，而是「這個人要離開了，換一個新的人接手」——所以每張暫離卡片下方多一排（`.temp-row`）：一個文字輸入框（`.temp-name-input`，placeholder 顯示這個座位原本的名字）＋一顆「🔗」複製鈕（`.copy-sub-btn`）。輸入框打的是**替補姓名**（存進獨立的 `tempNames` 陣列，跟 `away` 一樣經由 `ROOM.setExtra('tempNames', tempNames)` 存進同一包房間 session，重新整理會還原），**完全不動 `room.js` 裡 `names[i]` 那組七款遊戲共用的房間姓名**——換句話說，這是「這個座位在這一場遊戲裡臨時換了誰在玩」，不是「這個人真的改名了」，離開 Say It Without Saying It 回到其他遊戲或 index.html，原本的姓名完全不受影響。**`pname(i)` 是這支檔案裡唯一的取名入口**：有替補姓名就回傳替補姓名，沒有就回傳 `ROOM.name(i)`；檔案裡原本所有讀名字的地方（計分板、狀態列、`payloadFor()` 送給每張卡片的 `giverName`／`refName`／`board`（所有人在別人卡片上看到的即時分數榜）、`ROOM.setDecorator()` 給唯讀名單看的角色標籤、暫離卡片自己的勾選鈕標籤）全部從 `ROOM.name(i)` 換成 `pname(i)`——只有兩處刻意維持讀 `ROOM.name(i)`：`pname()` 自己的實作（它就是 fallback 的來源），以及輸入框的 placeholder（要顯示的正是**沒有**替補姓名時的原名，讓主持人一眼看出「這格原本是誰」）。`room.js` 的 `renderCompact()`（唯讀名單）新增一個小改動：`label.textContent` 改成優先讀 `decorate(i)` 回傳物件裡的 `info.name`，沒有才退回 `api.name(i)`——這是一個**只有這支檔案會用到**的可選欄位，其餘五款遊戲的 decorator 從來不回傳 `name`，所以完全不影響它們原本的行為。**複製連結按鈕複製的是「同一個座位」的連結，不是新開一個玩家**：`copySubLink(i)` 用 `ROOM.playerRef(i)`（`room.js` 原本就有的主持端專用 API，`talk-mode.md` 那次新增時就講明「不可把其他人的 token 傳入玩家 payload」——這裡完全沒有送進任何 payload，只是主持人自己複製起來要傳給真人）拿到這個座位的 Firebase reference，`.key` 就是它的 token；組出跟 `room.js` 的 `cardURL()` 完全同構的網址（`play.html?s=房號&p=token&n=替補姓名`），複製文字是「替補姓名\n連結」，跟 `room.js` 的 `shareText()` 同一種格式。主持人把這個連結傳給新的人，他打開後看到的名字晶片就是替補姓名（`play.html` 的 `nameBanner()` 讀的正是連結裡的 `?n=`），而且因為 token 沒變，這個人接手的就是原本那個座位在遊戲裡的身分——主持人這時通常會把這個座位的「暫時離開」取消勾選，讓它重新進入抽角色的輪替，之後不管是計分板、狀態列還是其他玩家卡片上的即時分數榜，看到的都會是替補姓名而不是原本的名字。**輸入框的變更刻意不觸發 `renderAwayBtns()` 重畫**（`setTempName()` 只呼叫 `renderAll()`，`renderAll()` 從不碰 `#away-btns`）——`renderAwayBtns()` 是用 `innerHTML = ''` 整排重建，如果打字的當下呼叫它，正在輸入的那個 `<input>` 會被拆掉重建，游標與焦點就丟了，這正是 `room.js` 自己的姓名輸入框當年解決過的同一個問題（見 `room.js` 那一列的說明），這裡採取同一個原則：**只有離散事件（暫離勾選、人數變動、切換語言）才整排重建，打字這種連續事件絕不重建**。實測：替補姓名會即時反映在計分板、`payloadFor()` 送給其他玩家卡片的 `board` 陣列、以及唯讀名單（一旦這個座位真的被抽進這一輪的角色）；清空替補姓名會立刻退回原本的房間姓名；複製出來的連結網址跟該座位的真實 token 完全一致（不是新座位）且正確帶上 `n=替補姓名`；連續打十幾個字（含空白）全程輸入框不失焦、一個字元都不會掉；重新整理分頁後替補姓名正確還原；320／390px 中英文下 `.temp-name-input` 文字對比 16.61:1、`.copy-sub-btn` 對比 4.63:1，皆無溢位。

**複製鈕旁邊多一顆「QR」按鈕，跟 `index.html` Step 1 傳送玩家連結那一步的 QR 功能是同一個東西，不是仿做的相似品**：`.link-btn-row`／`.btn-qr-toggle`／`.qr-inline` 三個 class 全部直接沿用 `room.js` 自己的樣式（`injectCSS()` 不管 `hideSetup` 是不是 true 都會把這包 CSS 注入 `<head>`，所以這頁本來就用得到，不用另外寫一份長得像的），連按鈕上的文字都是呼叫 `I18N.t('room', 'qrBtn')`（＝「QR」）——**沒有新增這個 i18n key**，因為它就是同一顆按鈕該有的字。但 `room.js` 的 `toggleQR(i, col, btn)`／`qrSVG()` 是私有函式、不在 `ROOM` 的公開 API 上，而且 `toggleQR()` 內部寫死讀 `cardURL(i)`（房間共用姓名），不會知道這裡的替補姓名——所以新增 `subCardURL(i)`（`copySubLink()` 也改呼叫它，兩邊網址組法不再各寫一份）、`subQrSVG()`（跟 `room.js` 的 `qrSVG()` 一樣呼叫 `qrcode.js` 這個全站共用的 vendored 函式庫，這頁本來就有載入）、`toggleSubQR(i, card, btn)` 三個函式，邏輯逐項照抄 `toggleQR()`：**QR 展開的連結是 `subCardURL(i)`**（同一個座位的真實 token，只是 `?n=` 換成替補姓名，不是憑空生出一個新玩家）、**同一時間只能展開一個**（開新的一個會把 `#away-btns` 底下其他已展開的 QR 全部收掉，跟 `toggleQR()` 只查 `#room-links` 同一個道理，這裡改查 `#away-btns`）、**`href` 用屬性指派而不是串進 HTML 字串**（避免替補姓名裡有什麼字元逃出屬性）。QR 按鈕跟複製鈕一樣在 `!ROOM.enabled` 時鎖住（沒有 Firebase 就沒有真的 token 可以編）。**QR 展開／收合不會被打字影響**：`toggleSubQR()` 把 QR 盒子插進 `.away-card` 節點本身、不是整個重建 `#away-btns`，跟 `setTempName()` 刻意不呼叫 `renderAwayBtns()` 是同一個原則——在某一位玩家的替補姓名欄位打字，不會把另一位（或同一位）已經展開的 QR 收起來。實測：每位玩家一顆 QR 按鈕；點開會出現真的 QR code（`<svg>`）而不是空殼；QR 連結網址跟 `copySubLink()` 複製出來的網址用的是同一個 token、且正確帶著替補姓名；點開另一位玩家的 QR 會自動收掉前一個；再點一次同一顆會收合；展開 QR 期間在別的欄位打替補姓名，QR 不會被收掉；320／390px 中英文下 `.copy-sub-btn` 對比 4.63:1、`.btn-qr-toggle` 對比 5.47:1，皆無溢位。

**暫離勾選鈕重新設計，加大字級與勾選框，並讓打勾／沒打勾看起來明顯不同**：實際回報過「勾選框太小、根本看不見」「打勾沒打勾樣式差不多」。原本 `.away-btn` 是**沒有底色沒有外框的純文字列**（`background: transparent; border: none`），勾選框（`.ab-box`）只有 15×15px、字級 .68rem，勾起來也只是把框線與底色一起換成灰色——跟旁邊 `.temp-name-input`／`.copy-sub-btn` 這些看起來「像按鈕」的控制項相比，`.away-btn` 反而是卡片裡最不像可以按的東西，勾了沒勾了兩種狀態的差異也只在那個 15px 小方塊裡。**現在 `.away-btn` 是一顆跟 `.temp-name-input` 同樣寬、同樣有底色外框的滿版按鈕**（`width:100%`，未勾時背景 `var(--bg)` 深色、勾起來整顆換成實心淺灰 `#94a3b8`），玩家名稱字級從 `.8rem` 放大到 `1.05rem`、勾選框從 15px 放大到 24px、勾號字級也放大到 `1.05rem` 粗體。**未勾／已勾兩種狀態刻意做成對比強烈的兩種按鈕，不是同一顆按鈕微調顏色**：未勾是深底＋灰字＋空心方框；已勾是**整顆按鈕變成實心淺灰**、方框跟著換成深底白勾（深色方塊浮在淺灰按鈕上，跟按鈕本身的底色再分出一層對比，不是勾選框自己換個邊框顏色就算了）、**姓名文字加上刪除線**——就算截圖只裁到卡片一角、勾選框沒入鏡，划線的名字本身也看得出「這個人現在不在陣容裡」。**整張 `.away-card` 也跟著變色**（新增 `.away-card.is-away`：卡片底色轉淡灰、外框轉灰），讓暫離狀態不必盯著小勾選框才看得出來，卡片本身的顏色遠遠一眼就分得出跟其他在場玩家不一樣（分享畫面時尤其重要，這是主持人分享桌面用的畫面，見全站規則）。技術上：姓名文字改包進獨立的 `<span class="ab-name">`（原本是裸的 text node，直接接在勾選框後面），才能只對名字套刪除線、不影響勾選框本身；`renderAwayBtns()` 建立每張卡片時多判斷 `on ? ' is-away' : ''` 決定卡片本身的 class。實測：未勾／已勾兩種按鈕在 320／390px 中英文下皆無溢位，已勾按鈕文字（深字配淺灰底）對比 7.91:1、未勾按鈕文字對比 7.00:1、已勾勾選框裡的白色勾號對比 20.28:1，全部遠超過 4.5:1 的門檻

**「👁️ 顯示玩家卡片內容」開關已從「暫時離開」設定卡搬出，改成緊貼在下方唯讀名單正上方**：使用者指出這顆按鈕跟「暫時離開」放在同一張卡片裡，會被誤會成同一組設定，但它其實是名單那一區塊的功能——用途是讓主持人不用拿起手機就能看到每張卡片實際顯示什麼，是名單旁的核對工具，不是遊戲規則或暫離設定的一部分。技術上它原本是經由 `room.js` 的 `extraRowHTML` 掛進 `ROOM.init()` 的 `actionsHTML` 那張卡片；現在 `actionsHTML` 只留「暫時離開」那一排，這顆按鈕改成在 `ROOM.init()` 執行完之後手動 `createElement` 一顆 `<button id="card-preview-toggle">`，`insertBefore` 插進 `#room-mount`、緊接在 `#room-links`（唯讀名單那個節點）前面成為它的上一個兄弟節點——`ROOM.render()`／`renderCompact()` 每次重繪只會重建 `#room-links` 自己的子節點，不會動到 `#room-mount` 底下其他既有的兄弟節點，所以這顆手動插入的按鈕會在每一次重繪之後原封不動留在原地，不必重新插入。`renderPreviewToggle()`（切換文字）與它的 `onclick`（切換 `cardPreviewOn`）完全不變，只是掛的節點換了位置、換了誰是它的鄰居。實測：320／390px 中英文下按鈕都正確顯示在唯讀名單正上方、不在暫離卡片裡；重新整理分頁、切換人數、`ROOM.render()` 之後按鈕仍在原位且點擊仍正常運作。

**遊戲畫面上的四項設定（難度／禁字開關／生效幾個禁字／回合秒數）收進同一顆可收合的小按鈕，做法完全比照 `dare-conquest.html` 自己的難度選單彈出視窗**：原本只有難度會出現在遊戲階段（`.cb-sect.rules` 右半永遠攤開三顆難度勾選鈕），禁字開關／生效個數／回合秒數則只存在於精靈裡，要調就得按「⚙️ 編輯設定」跳回精靈、離開正在分享的遊戲畫面。主持人反映這四項設定的切換都應該比照 Dare Conquest 那種「收合成一顆小按鈕、點開才展開」的省空間做法，不要有的攤開有的要跳頁。**現在 `.cb-sect.rules` 只剩一顆 `⚙️` 小膠囊（`#settings-toggle-btn`）**，膠囊上唯一的狀態提示是目前勾起的難度色點（`🟢`／`🟡`／`🔴`，跟難度勾選鈕自己用的圖示相同）——禁字開關、生效個數、秒數不需要在膠囊上重複顯示，因為 `.phase-summary` 摘要列本來就一直顯示這三項的目前狀態，膠囊只需要補上摘要列沒有另外用色點呈現的那一項。點下膠囊會展開 `#settings-popover`：一塊浮動面板，直接把 `#rules-group-difficulty`／`#rules-group-forbidden`（內含 `#banned-count-row`）／`#rules-group-seconds`（新增：把原本裸露在精靈子步驟 3 的 `#secs-field` 包進一個新的 `rules-group`，補上跟難度／禁字同一套的 `⏱️ Round timer` 標籤，這個標籤字串直接重用既有的 `taboo.roundTimer` i18n key，跟控制列左半計時器的標籤一字不差）**移進去，不是複製一份**——三個節點各自只有一份，`placeSettingsGroups('game')` 依序把三者 `appendChild` 進 `#settings-popover`，順序照精靈原本的 1→2→3（難度→禁字→秒數），讓既有的 `.rules-group + .rules-group` 虛線分隔線在三者之間自然出現，效果等同於它們原本註解裡講的「三個設定共用一欄」的樣子，只是現在那一欄裝進了彈出視窗，而不是固定畫在控制列上。`placeSettingsGroups('setup')` 則是反向操作，把三個節點分別搬回精靈子步驟 1／2／3 各自新增的插槽（`#substep-difficulty-slot`／`#substep-forbidden-slot`／`#substep-seconds-slot`）——跟 Dare Conquest 的 `placeDiffGroupInGame()` 不同的是這裡**雙向可逆**：Dare Conquest 沒有「遊戲中途跳回設定畫面」這件事，所以只需要單向搬一次；這款遊戲的「⚙️ 編輯設定」本來就會把畫面整個換回精靈、之後還要能換回遊戲畫面，所以 `enterGame()`／`editSettings()` 都呼叫同一個 `placeSettingsGroups(where)`，各自把三個節點分別導向彈出視窗或精靈插槽。

彈出視窗本身完全比照 Dare Conquest 的技術解法：`#settings-popover` 刻意寫在 `#phase-game` **外面**、當它的平行兄弟節點，而不是巢狀在觸發它的按鈕底下——`.control-bar` 帶著跑完就結束的 `fadeUp` 進場動畫（`animation: ... both`），`fill-mode: both` 會讓動畫最後一格的 `transform`（就算是單位矩陣）永久留在元素上，而帶 `transform` 的元素會建立自己的疊層環境，導致巢狀在它裡面的彈出視窗不管 `z-index` 多高都贏不了 DOM 順序排在後面的其他區塊（Dare Conquest 自己的難度彈出視窗就踩過這個坑，詳見該檔案那一列）。搬到 `#phase-game` 外面、`position: fixed`、`z-index: 9999`，並在每次開啟時用 `#settings-toggle-btn` 當下的 `getBoundingClientRect()` 現算位置（`positionSettingsPopover()`），就完全避開這個問題。**跟 Dare Conquest 不同的兩處，是因為這裡的按鈕位置與內容高度都不像 Dare Conquest 那麼單純**：①Dare Conquest 的控制列在窄螢幕下仍然維持一行、彈出按鈕永遠貼著容器右緣；這頁的控制列在 1024px 以下會整個疊成單欄（`grid-template-columns: 1fr`），`.cb-sect.rules` 也跟著置中，膠囊按鈕因此可能出現在手機螢幕水平中央而不是靠右——若照抄 Dare Conquest「永遠貼右下角展開」的算法，面板往左延伸的寬度可能直接超出螢幕左緣。②Dare Conquest 的面板只有一組難度勾選鈕，內容矮，不需要考慮撐到螢幕外；這裡一次收了三組設定，內容較高，矮螢幕手機（例如通話中把可視高度壓得很扁）可能連往下展開的空間都不夠。`positionSettingsPopover()` 因此在兩個維度都加了防呆：垂直方向若「按鈕下緣 + 面板高度」會超出視窗下緣，改成往上展開（`pop.style.bottom` 而不是 `top`）；水平方向算出的 `right` 值會被夾在 `[12px, 視窗寬度 - 12px - 面板寬度]` 之間，不管按鈕落在螢幕哪個水平位置，面板永遠完整落在可視範圍內。這兩個算法都需要面板「已經不是 `display:none`」才能量到真實尺寸，所以 `toggleSettingsPopover()` 開啟時的順序是先拿掉 `.hidden`、才呼叫 `positionSettingsPopover()`——兩者在同一段同步的程式碼裡完成，瀏覽器要等這段程式跑完才會真的重繪畫面，所以不會有面板先閃一下在錯的位置、才跳到正確位置的畫面。開關（`toggleSettingsPopover()`）、點擊面板以外關閉（用 `e.composedPath()` 而不是 `.contains(e.target)`——理由跟 Dare Conquest 完全一樣：勾選一個難度或生效字數會觸發 `#level-btns`／`#count-btns` 的 `innerHTML` 整排重建，按下去的那顆按鈕在自己的 click handler 裡被同一次點擊事件親手拆掉重建，等外層的 `document` 監聽器接手處理同一個事件時 `.contains()` 對這個「幽靈節點」一律回傳 false；`composedPath()` 記的是事件剛發生那一刻的路徑，不受之後的 DOM 變動影響）、視窗縮放時即時重新定位（`resize`）都直接照抄 Dare Conquest 的寫法。**唯一額外補的一處是「面板自己的捲動」**：Dare Conquest 的面板內容矮，從來不需要內部捲軸；這裡的面板加了 `max-height: min(70vh, 520px); overflow-y: auto`，矮螢幕手機上三組設定疊起來還是可能比可視高度高，所以面板本身要能捲動。但 Dare Conquest 原本「頁面任何捲動都關閉面板」的邏輯（`window.addEventListener('scroll', closeDiffPopover, true)`）若原封不動照搬，主持人在面板裡想往下捲看到「回合秒數」那一組，第一下捲動就會把整個面板關掉——`onSettingsPageScroll()` 因此多判斷一次捲動事件的 `e.target` 是不是落在 `#settings-popover` 自己裡面，是的話就不關閉，只有頁面本身（或面板以外的其他可捲動區域）在捲動時才關閉，讓主持人能在面板內部正常捲動瀏覽三組設定。實測：桌面／手機寬度（320／390／1280px，中英文）開啟面板皆完整落在可視範圍內、三組設定的勾選／切換／輸入全部正常運作且立即反映在 `.phase-summary` 摘要列與難度膠囊的色點上；在面板裡勾選一個生效字數後面板不會被自己的重繪誤判成「點在外面」而關閉；點面板以外任一處會關閉；短視窗（500px 高）下面板會自動改往上展開、面板自己能上下捲動且捲動時不會被誤判關閉，捲動頁面本身則會正確關閉；按「⚙️ 編輯設定」時三個節點正確搬回精靈子步驟各自的插槽、面板清空並收合，再次「🎮 開始遊戲」時三個節點正確搬回面板、不會產生重複節點；全站 45 項既有測試與 10 頁 smoke test 全數通過

**遊戲畫面新增第四項設定「📊 分數顯示」，決定玩家卡片上要怎麼呈現大家的分數**：精靈因此從 4 步變成 5 步（`TABOO_SUBSTEPS = 5`），新的子步驟 4 放在「回合秒數」之後、「開始遊戲」之前，`placeSettingsGroups()` 也跟著多搬一個 `#rules-group-scoredisplay`（跟難度／禁字／秒數同一套「節點搬家、不複製」的做法，見上——四個節點依精靈順序 `appendChild` 進同一個 `#settings-popover`，讓既有的 `.rules-group + .rules-group` 虛線分隔線自然出現在全部四組之間）。**三選一、單選**（跟難度那種可複選的勾選鈕不同，用的是跟 Banned words 數量選擇同一套 `.level-btn` 單選語言，沒有勾選方塊）：「全部顯示」＝原本的做法，每人一列名字＋分數＋（落後加分中才有的）⚡ 徽章；「全部隱藏」＝玩家卡片上完全不出現任何分數相關的東西；「最低～我～最高」＝**新的第三種格式**，只印房間目前的最低分、自己的名字與分數、最高分，例如「8 ～ Jessie (10) ～ 13」——想讓玩家有點概念但不想讓所有人的名字分數攤在螢幕上時用。狀態存進同一包 `dontSayItOptions.v1`（`let boardMode = 'full'`），`.phase-summary` 摘要列也跟著多印一段（`renderPhaseSummary()` 的第四個 `parts`），維持「一次講完全部設定」的慣例。技術上 `payloadFor()` 新增 `boardMode` 欄位，`board`（既有的整份陣列）只在 `'full'` 時填、`compactBoard`（新函式 `compactBoardFor(i)`，回傳 `{ min, max, n, p, x2 }`）只在 `'compact'` 時填，兩者互斥；`play.html` 依 `data.boardMode` 三選一決定要不要畫、畫哪一種（`'none'` 直接印空字串，`'compact'` 畫新的 `.tbd-compact` 一行，其餘走原本的 `.tbd-rows` 陣列或最舊的單行 `🏆 N 分` 後備，見 `play.html` 那一列）。**這個設定要能在 `state === 'idle'`（都還沒發過牌）時就即時生效**，跟難度／禁字／秒數三個「只影響下一次發牌」的設定不一樣——`setBoardMode()` 因此**不**比照它們檢查 `if (state !== 'idle') pushState()` 就跳過，而是**永遠**呼叫 `pushState()`：每張卡片一開始（連第一次發牌都還沒發生）就已經在顯示分數（`payloadFor()` 的 `base` 物件本來就帶 `board`／`boardMode`），開局前的第一份 payload 也早在 `ROOM.init()` 收尾時就發布過，所以任何時候呼叫 `pushState()` 都有東西可以更新；如果沿用舊的 `if (state !== 'idle')` 判斷，主持人在還沒發第一個字之前調整這項設定會完全無效——這是實測抓到的（先只判斷 idle 時忘記推播，卡片上的分數格式停在舊的，直到第一次 `dealRound()` 才會跟上）。實測：切三種模式、開局前與開局中皆即時反映到卡片；`compactBoard` 的 min/max 正確取自全房間（含暫離者的 0 分）；320／390px 中英文下皆無溢位，`.tbdc-mine` 用玩家自己顏色的邊框與淡底（跟 `.tbd-row.is-me` 同一顆顏色）對比 7.09:1，`.tbdc-edge`／`.tbdc-sep` 對比 5.29:1／7.16:1，全部合格。

**「🔥 ×2 落後加分」徽章改成純圖示，不再印文字**：原本計分板每一張落後的卡片上都掛一顆寫著「🔥 ×2 catch-up」的實心藥丸，主持人反映這樣太搶眼、想換成單純一個小圖示就好。`.sc-boost` 從「文字＋底色的藥丸」改成一顆 22×22px 的圓形圖示（`⚡`），原本的完整說明文字（`boostBadge` i18n key）**沒有刪掉，改放進 `title` 屬性當滑鼠／長按提示**，卡片下方那一行完整解釋「🔥 ×2 落後加分：分數比全場平均低 N 分以上的人，這輪得分加倍」的 `.sc-boost-note` 圖例維持原樣不變（那是「這個規則是什麼」的說明，跟每一張卡片自己的小圖示是兩件事，只有後者需要收斂）。`play.html` 的 `.tbd-boost`（卡片上「全場即時分數」列表裡同一顆徽章）與新的 `compactBoard.x2` 都比照改成同一顆縮小版（17px）圓形 `⚡` 圖示＋`title`（新增 `boostBadgeTitle` i18n key），玩家卡片下方原本那句「🔥 落後加分中：你這輪的得分 ×2」（`boostMine`）維持不變，理由同上。實測 320／390px 中英文下兩邊的圖示皆無溢位，主持計分板 9.44:1、玩家卡片 7-8:1 區間，皆合格。

**暫離（away）現在會立刻把分數歸零，而且永久失去這一局的落後加分資格——連回來以後也一樣**：這是因為分數歸零本身就會讓這個座位大幅落後全場平均，若不擋住，暫離、回來就變成一種「免費刷 ×2」的手段。新增 `catchUpBlocked` 陣列（跟 `scores`同樣是頁面層級的 `let`、不寫進 Firebase 也不經由 `ROOM.setExtra()` 持久化——分數本來就只活在這一頁的記憶體裡，重新整理就會歸零，這個旗標比照辦理），`syncScores()` 跟著把它一起補到跟 `ROOM.count`一樣長。`toggleAway(i)` 在勾起來（`turningOn`）那一刻：`scores[i] = 0`、`catchUpBlocked[i] = true`（同時把這個座位從 `popIdx`／`lostIdx` 動畫佇列裡摘掉，免得歸零的瞬間又疊一個加分特效）；`isBehind(i)` 多一道最前面的檢查：`!catchUpBlocked[i] && ...`，這個旗標一旦成立就永遠擋下 `isBehind()`，不看目前的分數或有沒有還在暫離。要清掉只有「↺ 重設分數」（`resetScores()` 現在同時把 `catchUpBlocked` 全部歸 `false`）——分數重新開始，落後加分的資格也一起重新開始，這才是玩家理解「新的一場」該有的樣子。因為這件事現在會影響每一張卡片當下看到的分數與 ⚡ 徽章，`toggleAway()` 原本「只有 `state !== 'idle'` 才 `pushState()`」的判斷也**一併拿掉**，改成無條件推播（原因跟上面 `setBoardMode()` 那段一模一樣：`away` 欄位跟分數一樣，從第一份 payload 就存在，任何時候都有東西可以更新；沒改之前的舊行為是「開局前把人標成暫離，那個人自己的卡片、其他人畫面上的『至少要留 2 位在場玩家』鎖住提示，都要等到第一次真的發牌才會更新」，是一個實測抓到的既有 bug，藉這次一起修掉）。實測：刻意讓一個高分玩家先合格 `isBehind`，暫離後分數歸零、`catchUpBlocked` 為真、`isBehind` 仍為 false；取消暫離後 `isBehind` 依然是 false（旗標沒有被清掉）；按「↺ 重設分數」後 `catchUpBlocked` 全數歸零。

**Forbidden words 在「2 人→暫離回到 3 人」之後打不開，其實是設定彈出視窗自己把自己關掉的問題，不是門檻算錯**：使用者回報「人數為 2 時關掉禁字沒問題，但暫離的人回來變成 3 人之後沒辦法恢復」——實測直接呼叫 `toggleForbidden()`／改 `away` 陣列的底層邏輯完全正確（`activeCount() < 3` 這個門檻本來就只在 2 或 1 人時鎖住，3 人一律解鎖），問題出在**畫面**：上一輪新增的設定彈出視窗（`#settings-popover`，見上面「四個功能收進同一顆按鈕」那段）有一條「點擊面板以外的地方就關閉」的規則，而「暫時離開」的勾選鈕群組（`#away-btns`）剛好長在彈出視窗**外面**（它是房間名單那一區塊的功能，不是遊戲設定）——主持人開著彈出視窗想順手把剛才暫離的人勾回來，那一下點擊本身就落在彈出視窗外，於是視窗自己先關掉，`#forbidden-toggle`（人在彈出視窗裡面）瞬間變成看不見也點不到，主持人以為「怎麼按了沒反應」，其實是找不到那顆按鈕了。修法是讓判斷「這次點擊算不算點在外面」的 `document` 監聽器**額外放行 `#away-btns`**：`e.composedPath()` 裡只要包含 `#settings-popover-wrap`、`#settings-popover` 或 `#away-btns` 任何一個，就不關閉——暫離設定跟彈出視窗裡的四項遊戲設定雖然是兩個各自獨立的 DOM 區塊，但對主持人來說是同一套「調整這一輪規則」的操作，不應該因為手伸到另一半就把自己正在看的那一半關掉。實測：開著彈出視窗、點暫離勾選鈕開/關，視窗全程不關；`#forbidden-toggle` 在整個過程中維持可見，3 人時點得下去也真的能重新打開。

**玩家卡片上可以自己申請「暫時離開」，按下前有二次確認並講清楚後果**：原本只有主持人畫面才能把人標成暫離，但玩家自己知道要離開的時間點通常比主持人早（正在講話中途臨時有事）。`payloadFor()` 新增 `awayLocked: activeCount() <= 2`（跟主持人自己那排暫離勾選鈕鎖住的門檻完全一樣），讓卡片能在申請一定會被拒絕的情況下先自己灰掉、不用真的送出去被打回票。`handleAnswer()` 新增 `v.choice === 'setAway'` 分支，刻意排在最前面（跟 `deal` 分支一樣，在「回合中才處理」的守衛之前）——暫離跟發牌一樣，不管現在是不是有一輪正在進行、甚至根本還沒開局都可以送；分支本身只在 `!away[num-1]` 時才呼叫 `toggleAway(num-1)`，讓它完全複用主持人那顆勾選鈕背後同一個函式（同一套「至少留 2 位在場玩家」門檻、同一套分數歸零與 `catchUpBlocked` 邏輯、同一套禁字連動）——卡片不會、也不需要自己再重寫一次這些規則，就算同一個請求因為某種原因送兩次，`away[num-1]` 已經是 `true` 的第二次會直接被前面那個 `if` 擋掉，不會被當成「切回來」誤觸。這顆申請鈕與確認流程的畫面實作在 `play.html`，見那一列。 |
| `crack-the-word.html` | 🔤 Crack the Word（沿用 Say It Without Saying It 的同一份 700 字 `taboo` 題庫，但反過來：沒有任何一位玩家私下知道答案。單字先變成一排空格，26 個字母平均分給全場（`crack-word-engine.js` 的 `distributeLetters()`：先把單字自己的幾個獨特字母洗牌、依一組洗過的座位順序輪流發完，再用**同一輪座位順序**（不重新開始）接著發完剩下的字母——這保證每個人拿到的「正確字母數」與「總字母數」最多只差 1，不會有人因為分到的座位晚而全是廢字母），玩家按下自己手上的字母，正確就在所有人卡片的空格裡揭曉、自己 +1 分，不在字裡則整場看到「已排除」清單。**每一輪要挑戰幾個字母由「開局的那個人」自己選、每輪重選一次**——這是使用者試玩回饋後改的：原本是設定精靈裡選一次難度，回饋是「短字太好猜」，需要的不是調整題庫難度分佈，而是讓開局的人自己決定這一輪要挑戰的長度，所以整個設定精靈直接拿掉，改成任何人在自己卡片按「🔤 抽第一題／換下一題」時，**先進入只有那個人（`chooserSeat`）看得到的三顆長度鈕**（🟢 短字 5 字母以下／🟡 中字 6–8／🔴 長字 9+，數字來自 700 字題庫依 `CRACK_ENGINE.tierOf(letterCount(word))` 重新分桶算出的實際題數：340／274／86，跟 Say It 的 easy/medium/hard 三個概念難度標籤完全無關——那是「這個字好不好懂」的分級，這裡要的是「要揭多少空格」，混在一起用會產生 `Underwear` 這種一望即知但字母數對不上概念難度的怪例子），其他人的卡片只看到「⏳ 等 {name} 選擇題目長度…」；選完那一刻才真正抽字、分字母、開局，不像其他四款抽題遊戲疊一層可攔截的五秒倒數——選長度本身已經是兩步驟的鄭重動作，不太需要再防一次手滑。**判定「猜對了沒」完全不需要主持人看一眼、也不需要任何人私下知道答案**：想搶答的人按「🙋 我知道了！」搶到唯一的猜題名額（同時間只有一人在猜，其他人的卡片顯示「⏸️ 某某正在猜」），輸入框直接打出完整單字送出，主持人分頁的 JS 本來就在記憶體裡握著這一輪的 `word`，收到猜測字串做去空白、大小寫無關的比對，自動判對錯——跟 Say It 的 `handleAnswer()` 驗證 `right`/`wrong` 是同一種「主持人分頁只是資料權威、不需要人手動判斷」的做法，而且比 Say It 更乾淨：主持人畫面上**從頭到尾不會出現原始單字**，連判定的當下都不用，因為判定本來就是字串比對，不是人眼看。猜錯的人被鎖住，直到這輪其他人都各猜錯一次才整批解鎖（沿用 Say It 裁判鎖定同一套「一圈猜錯全解鎖」邏輯）。**得分兩種**：猜對者的分數用 Wheel-of-Fortune 式公式（`CRACK_ENGINE.guesserScore(tier, revealedPositions, totalPositions)`）——這一輪猜對時已經揭曉的空格佔比愈高，這次得分愈低，最低仍保底 1 分，愈晚猜（愈多字母已經曝光）分數愈少是刻意的，鼓勵盡早動腦而不是等全部揭曉再打字；**按對字母的人也拿一點分數**（`LETTER_CREDIT`，固定 +1，不因長度分級而異）——這是使用者第二輪回饋明確要求的：原本只有猜對的人得分，但字母是不是被按出來完全是另一群人的貢獻，沒有分數等於白做工。主持人畫面**不是純計分板**，因為這款遊戲沒有秘密要對主持人畫面隱藏（空格與已揭曉字母本來就是公開資訊，跟哪個字母在哪個座位手上一樣，唯一真正的秘密——完整單字——從來不會被送到任何一張卡片以外的地方，也不會被送進 `payloadFor()` 回傳給任何座位，見下）：畫面上除了計分板，還照著卡片實際看到的畫出**同一份**空格列與逐座位字母按鈕（`renderRoundPanel()`），而且這些按鈕在主持人畫面上是**真的能點的備援**——手機臨時連不上時，主持人可以直接代替任何座位按字母、代搶答、代打字送出猜測，全部呼叫的是跟卡片請求完全相同的權威函式（`pressLetter()`／`requestBuzz()`／`submitGuess()`），不是另外寫一份平行邏輯。`ROOM.init()` 帶 `hideSetup: true`，人數 2–9、連結已在 `index.html` 設定好；沒有可調的遊戲設定，所以沒有精靈也沒有彈出視窗——這款遊戲唯一的「設定」（要挑戰幾個字母）本來就必須每輪重選、不可能一次設定完，做成精靈反而是多餘的一層。分數與 `used`（每個長度分桶已出過的題目索引，`localStorage` key `crackWordUsed.v1`，用跟 Say It 相同的 `deckLen` 指紋比對，牌堆形狀變了就放棄還原而不是硬套錯誤索引）持久化方式沿用 Say It 的既有慣例：分數只活在這一頁的記憶體裡、`used` 才寫進 `localStorage`。主題色藍 `#3b82f6`——既有遊戲用掉了紅／橘／青／綠／琥珀／紫／靛／粉，藍是唯一還沒被拿去當整頁主題色的既有站色（`play.html` 玩家配色的第 2 色）。**沒有 Firebase 時發題鈕會被真的鎖住**（跟 Kangaroo Court／Say It Without Saying It 同一類，見 `firebase-config.js` 那一列）——不是因為答案怕被主持人看到（字母與空格本來就不是秘密），而是沒有玩家連結就沒有分散在不同手機上的字母按鈕，主持人一個人用畫面上的備援按鈕代打全場，遊戲就沒有意義了。測試見 `tests/crack-word-engine.test.cjs`（字母分配公平性、tierOf 分桶邊界、guesserScore 單調遞減與保底 1 分、猜測比對大小寫/空白不敏感，共 14 項，純函式、不碰 DOM 與 Firebase，跑法與 `next-round.js` 的測試相同） |
**Crack the Word v2（試玩回饋：輪流按字母＋回合預算）**：兩點回饋都指向同一個問題——原本 26 個字母是「誰想按就按」，人多的時候會變成大家同時搶著戳自己的按鈕，而且沒有任何機制逼團隊真的去討論答案，反而是等字母機械式地被戳完。①**按字母現在一次只能一人**：新增 `currentTurnSeat`（0-based，`-1` 表示全場字母都按完了，沒有下一個輪次）。`pressLetter(seat, rid, letter)` 在既有的 `state !== 'playing'`／`roundId` 檢查之後多一道 `if (seat !== currentTurnSeat) return;`——這道檢查在**伺服器端**（主持人分頁）擋下，不是只靠卡片把按鈕變灰：卡片與主持人畫面的字母按鈕都用 `disabled` 呈現非本人回合，但就算有人繞過灰掉的按鈕硬送出請求，`pressLetter()` 一樣會直接丟棄（實測：讓非本回合的座位直接呼叫 `crackPressLetter()`，`turnsUsed` 完全不動）。**猜整個單字的搶答／輸入不受此限**——`requestBuzz()`／`submitGuess()` 沒有加這道檢查，任何人隨時都能按「🙋 我知道了！」，因為使用者原話講的是「猜字母」不是「猜單字」：按字母是逐格機械動作，猜單字才是團隊討論後的集體決定，兩者刻意分開。**輪到誰的座位由公平的洗牌袋決定，且要能撐過多輪**：`drawStarter()` 完全比照 Say It 的 `giverBag`——`turnBag` 是頁面層級的 `let`（不寫進 `localStorage`，跟 `scores` 同一個「重新整理就重來」的慣例），洗完的整袋座位一次發完，每位玩家都當過一次開局者才會重新洗牌，且重新洗牌時會確保新袋子最後一個要發的座位不會剛好等於 `lastStarter`（同一人不會連續兩次開局）。**這份公平只保證「誰先開局」，不是逐一 turn 都重新抽籤**：拿到開局權之後，同一輪內的順序就是固定的座位輪轉（`advanceTurn()` 從 `currentTurnSeat` 往後找下一個「還有字母沒按過」的座位，跳過已經把自己那份字母全部按完的人；如果全場都按完了就把 `currentTurnSeat` 設回 `-1`），因為輪轉本身（而非起點）不需要額外公平化——不管從哪個座位開始繞一圈，每個人在一輪裡輪到的次數本來就相近，真正需要公平演算法把關的只有「哪個人有優先開局的特權」這一件事。實測（`crackword-turns.mjs`）：4 人房連續呼叫 `drawStarter()` 100 個完整循環（400 次）＝每人恰好 100 次、零偏差；非本回合座位的按字母請求被伺服器端丟棄、`turnsUsed` 不變；真正輪到的座位按下後 `turnsUsed` 正確 +1、`currentTurnSeat` 正確前進到下一個還有字母的座位。②**每輪有一個算出來的回合預算，超過就整場扣分**：這是為了讓使用者說的「即使還沒按完所有字母，大家也要團結討論出答案」真的發生——如果字母遲早會被輪流按完、猜單字又沒有時間壓力，理性策略就是乾等按完再說，不需要討論。新增 `CRACK_ENGINE.turnLimitFor(word, difficulty)`：**依照使用者指定的三個因素**——`uniqueLetterCount(word)`（字母規則：獨特字母愈多，找齊的成本愈高）、`letterCount(word)`（字數）、`difficulty`（單字難易度，讀的正是 `game-data.js` 既有的 `taboo` 題庫的 `level` 欄位 `easy`／`medium`／`hard`——這個欄位在 v1 只用來分「短／中／長」的長度分桶，跟這裡要的「這個字好不好猜」完全是另一件事，這裡直接讀同一張卡片的 `card.level`）——算出 `unique * TURN_MULT[difficulty] + total * 0.3` 再夾進 `[TURN_MIN=6, TURN_MAX=30]`；`TURN_MULT = { easy: 1.2, medium: 1.6, hard: 2.2 }` 讓「看得到字母但要花力氣才想得出來」的抽象詞（`Resilience` 這種）拿到比同樣字母數的具體詞（`Pineapple`）更寬裕的預算——具體詞就算全字母暴露前很早就會被喊出來，抽象詞就算字母全揭露也可能一時想不到，這正是使用者要的「逼團隊討論」，用抽象詞給更多回合換取更多討論時間才合理。`turnLimit`／`turnsUsed` 在 `requestTier()`（`card.level || 'medium'` 當找不到欄位時的後備）算好、每次 `pressLetter()` 成功（不論按對按錯，都算用掉一個回合，因為預算限制的是「按的次數」本身，不是「按對的次數」）就 `turnsUsed++`，一旦 `turnsUsed >= turnLimit`（在**這次**按的加分／排除效果已經套用之後才檢查，所以觸發上限的那一下如果剛好按對，發動者一樣先拿到那 +1 分，再被扣的罰分抵銷一部分——這是實測抓到、刻意保留的行為：那一下確實是一次有效的按字母動作，理應照算）就呼叫 `finishRoundByLimit()`：不像超時的 Say It 直接 0 分收尾，**這裡是全場（不分角色）各扣 `CRACK_ENGINE.TURN_LIMIT_PENALTY`＝2 分，可以扣成負數、沒有下限**，跟 Kangaroo Court／Say It 的清單一致；`state` 一樣轉成 `'done'`，`lastResult.limitReached = true` 讓卡片與主持人畫面切到琥珀色「⏰ 回合數用完了」的收尾文案而不是綠色的猜對文案，答案照樣公布（此時繼續保密已經沒有意義）。**這筆預算是共同的，不是誰的責任配額**：扣分套用到每一位玩家，不只是最後按那一下的人，因為超時本來就是全場一起沒討論出答案，不是任何一個座位的錯。畫面上**跟主持人分享畫面同一份資訊**：`.cw-turnbar`／`.crack-turnbar` 同時顯示「輪到誰」與「回合 {已用}/{上限}」，最後 20% 的回合數轉成警示色（`--bad-text-soft`／`.is-low`）提醒大家快沒時間；輪到自己的那條在玩家卡片上會發光脈動（`.crack-turnbar.is-mine` 的 `turnGlow` 動畫），跟其他遊戲「輪到你」的既有慣例一致。字母按鈕本身也分兩層意義不同的「按不下去」：已經按過的（`is-correct`／`is-wrong`）維持全彩，因為那個結果已經定案；還沒按但不是自己回合的則統一降到 40% 透明度（`:disabled:not(.is-correct):not(.is-wrong)`），讀起來是「還沒輪到」而不是「這個字母已經沒了」，兩者不能用同一種灰階表示。實測（`crackword-turns.mjs`／`crackword-turn-shots.mjs`）：320／390px 中英文下 turnbar 與回合計數皆無溢位；超過上限的那一輪 4 人房全數扣 2 分（含觸發者當次按對的 +1 淨額外抵銷）；`node --test` 新增 6 項（`uniqueLetterCount`、`turnLimitFor` 依難度遞增、依獨特字母數遞增、夾在 `[TURN_MIN, TURN_MAX]`、同輸入必為同輸出、未知難度退回 medium 乘數），全站測試共 78 項全數通過。**v2.1（試玩回饋：回合數整體偏多）**：`turnLimitFor()` 算出來的預算是「這個字本身」的合理上限，但實際帶玩下來整體偏鬆，玩家希望主持人自己能再調鬆緊。新增一個**跟單字本身的長度分桶（`tier`）與概念難度（`card.level`）都無關的第三個旋鈕**——`turnDifficulty`（`'easy'|'medium'|'hard'`，預設 `'easy'`），主持人畫面在計分板正上方新增一列常駐的 `#turn-diff-row`（不是彈出視窗，也不跟著某一輪存在——這是主持人的長期偏好，不是單輪設定，所以固定顯示，`renderTurnDiffRow()` 由 `renderAll()` 每次一起重畫）：三顆單選鈕「🟢 簡單／🟡 中等／🔴 困難」，簡單＝`turnLimitFor()` 算出來的原始值（×1，不縮減，**預設**）、中等＝×2/3、困難＝×1/2，對應 `CRACK_ENGINE.applyTurnDifficulty(limit, turnDifficulty)`（`TURN_DIFF_MULT = { easy:1, medium:2/3, hard:1/2 }`，縮減後一樣夾在不低於 `TURN_MIN`，避免乘到極小值時預算掉到不合理的個位數以下）。**只影響下一輪**（跟禁字／秒數等其他遊戲的既有慣例一致）：`requestTier()` 算 `turnLimit` 時才讀取當下的 `turnDifficulty`，正在進行的那一輪不會被中途調整回溯縮水。**這個設定會持久化**（`localStorage` key `crackWordSettings.v1`），因為它是主持人的長期偏好、不是單輪才有意義的東西——跟只活在頁面記憶體裡的 `scores`／`turnLimit`／`turnsUsed` 刻意不同，理由也不同：分數重整就該歸零（新的一場），但「這一房的節奏要多緊」重整後沒有理由被重置成預設值再讓主持人重選一次。實測：`easy`／`medium`／`hard` 三種設定對同一個字分別算出遞減的 `turnLimit`、且與手算的 2/3、1/2 完全吻合；切換設定後重新整理分頁，設定正確還原；320px 中英文下三顆按鈕與標籤皆無溢位。`node --test` 新增 5 項（`applyTurnDifficulty` 的 easy 原值不變、medium/hard 縮減比例正確、絕不低於 `TURN_MIN`、未知設定退回 medium），全站測試共 82 項全數通過。**v2.2（使用者回饋：答對要全員一起加分）**：原本猜對只有猜中的那個人得分（`guesserScore(tier, revealedPositions, totalPositions)`，依 `tier` 訂出的基準分乘上「還沒揭曉的比例」），使用者的回饋很具體——「A 答對了，還剩下三個空格，每個人都加 3 分，但因為是 A 輸入的，所以 A 額外加一分，A 加四分」。這**整個換掉舊公式**，`TIER_BASE`／`guesserScore()` 直接刪掉（沒有其他地方在用），改成新的 `CRACK_ENGINE.remainingBlanks(word, revealedLetters)`＝`letterCount(word) - revealedPositions(word, revealedLetters)`，算的就是猜對當下卡片上實際還看得到幾個空格——**這個數字本身就是團隊分數**，不再是一個要另外解釋的公式，主持人畫面與卡片上印出的分數，跟玩家自己數空格數出來的數字完全一樣。`finishRound(solverSeat)` 現在先把 `teamPts`（＝`remainingBlanks()`）**發給全場每一個座位**（含猜中的人自己），再單獨給猜中的座位加 `CRACK_ENGINE.SOLVER_BONUS`（固定 +1，猜對的人是「當事人」，這是他額外的功勞，不隨字長或空格數縮放——跟 `LETTER_CREDIT` 按對字母的固定 +1 是同一種「小而穩定」的設計語言），兩者分開存進 `lastResult`（`teamPts`／`solverBonus`／`solverPts`＝兩者相加，`solverSeat` 這次也存進去，讓 `payloadFor()` 能算出每張卡片自己是不是猜中的那位）。**空格全滿（都還沒按任何字母）時團隊分數最高**（等於整個字長）、**全部字母都已經按出來才猜對則團隊分數是 0、只有猜中的人拿到那 +1**——這是刻意的：字母是全場一起按出來的公開資訊，早猜、少看幾個字母才猜出來的難度該由全場分享，不是只有動口那一下的人獨拿；但「願意先開口說出答案」本身仍然值得一個穩定的小獎勵，所以保留 `SOLVER_BONUS` 不隨團隊分數縮放。文案上**跟 Say It 的 `gotItYou`／`gotItGiver`／`gotItOther` 同一個慣例**——`payloadFor()` 送給每張卡片的 `result` 是 `Object.assign({}, lastResult, { mine: seat === lastResult.solverSeat })`，`play.html` 依 `data.result.mine` 分流成第一人稱（`crackResultLineMine`：「你猜對了！...你額外 +{bonus}（你共 +{pts}）」）與第三人稱（`crackResultLine`：「{name} 猜對了！...{name} 額外 +{bonus}（共 +{pts}）」）兩種文案，主持人畫面（分享給所有人看，沒有「自己」這個視角）維持單一版本的第三人稱文案。實測（`crackword-teamscore.mjs`）：4 字母單字全未揭曉時猜對，全場（含猜中者）各 +4、猜中者額外 +1 共 +5，`lastResult` 各欄位與逐座位分數完全吻合；猜中者卡片顯示「你猜對了」文案、其他座位顯示「{name} 猜對了」文案、主持人畫面與後者一致；320／390px 皆無溢位。`node --test` 汰換原本 3 項 `guesserScore` 測試為 4 項 `remainingBlanks`／`SOLVER_BONUS` 測試（含使用者原例「剩 3 個空格」的精確數字驗證、扣到底部不為負、排除字母不影響空格數），全站測試共 83 項全數通過。**v2.3（使用者回饋：預設難度改為簡單）**：試玩下來中等（原本的預設值）算出來的回合數還是偏多，`turnDifficulty` 的初始值從 `'medium'` 改成 `'easy'`——**只改 `crack-the-word.html` 裡 `let turnDifficulty = 'easy';` 這一行初始值**，`CRACK_ENGINE.applyTurnDifficulty()` 本身的三個乘數（`TURN_DIFF_MULT = { easy:1, medium:2/3, hard:1/2 }`）與「設定值不明時退回 medium」這個資料完整性安全網都不變——後者刻意保留 medium 而不是跟著改成 easy，因為那是防呆用的後備值（處理讀到壞掉或未知設定字串的情況），不是給新主持人看的預設體驗，兩者是不同的東西。已經存過 `crackWordSettings.v1` 的主持人不受影響（`loadSettings()` 讀到既有存檔一律照存檔值套用，這次只動了「從來沒設定過」時的起始值）。全站測試維持 83 項全數通過（沒有任何測試斷言這顆 UI 初始值，全部斷言的是 `applyTurnDifficulty()` 這個純函式在給定明確難度參數下的行為）。

**v2.4（使用者回饋：玩家卡片的字母改成小寫、換成能分辨 i／l 的字體）**：玩家卡片上的空格列（`.crack-tile`）與自己的字母按鈕（`.crack-letter-btn`）原本照著資料本身（`ALPHABET`／`tileLayout()` 都是大寫）直接印成大寫字母，使用者要求改成小寫，並指定字型要讓 `i` 跟 `l` 一眼區分。**只在 `play.html` 的 CSS 動手，不碰資料本身**：底層資料（`crack-word-engine.js` 的 `distributeLetters()`／`tileLayout()`、`crackPressLetter('${l.letter}')` 送回主持頁的字母、猜字比對）全部維持大寫不變，兩個 class 各自加一行 `text-transform: lowercase`，純粹是顯示層轉換——這樣主持頁與卡片之間的協定、`crack-word-engine.js` 的測試都完全不用動，只有玩家眼睛看到的字換了大小寫。**字體改成跨平台的等寬字堆疊**（`Consolas, 'SFMono-Regular', Menlo, 'Liberation Mono', monospace`，`.crack-letter-btn` 原本是 `font-family: inherit` 繼承全站的 `'Segoe UI', system-ui, sans-serif`）：等寬字體的小寫 `l` 通常帶一筆下方的鉤腳、`i` 有明顯的點，兩者在無襯線的介面字體裡縮到 40px 按鈕大小時容易被誤讀成同一豎劃，換成這組等寬字堆疊就不會；沒有另外載入 Google Fonts 或任何 CDN 字型檔，四種系統字型在 Mac／Windows／Linux 上都原生存在，符合全站「不新增額外相依」的慣例。**空格列上的標點符號（例如 `Ice Cream` 的空格）不受影響**：`tileLayout()` 對非字母字元一樣呼叫 `toUpperCase()` 但那是 no-op，`text-transform: lowercase` 對它們同樣沒有作用，只有真正的英文字母會變小寫。實測（`crackword-lowercase.mjs`／`crackword-lowercase-320.mjs`）：320／390px 下「limit」「procrastination」兩個含 `i`／`l` 的單字都正確顯示為小寫且不溢位，字母按鈕的 `i`（帶點）與 `l`（無點長豎劃）截圖上清楚可辨；`getComputedStyle` 確認兩個 class 都吃到新字型堆疊。全站測試維持 83 項全數通過（這是純 CSS 改動，沒有任何 JS 邏輯或資料格式變動）。

**v2.5（使用者回饋：字庫多一個「片語」選項，100 句）**：原本三顆長度鈕（短字／中字／長字）都是從 Say It Without Saying It 的同一份 700 字 `taboo` 題庫依字母數切出來的三個分桶；使用者要求多一個第四種選擇，題目是片語而不是單字，題庫要另外產生 100 句。**片語是全新的一份獨立題庫，不是從 `taboo` 分桶出來的第四種長度**：`game-data.js` 新增 `crackPhrase`（100 句，格式跟 `taboo` 一樣是 `{ level, emoji, word }`，只是沒有 `forbidden` 欄位——Crack the Word 從來不讀這個欄位，只有 Say It 需要），依內容親密度／抽象程度分三級各 40／40／20 句：**簡單**是具體、天天用得到的日常動作與招呼語（Good Morning、Thank You、Wake Up、Read A Book…）；**中等**是常見片語動詞與搭配詞（Look After、Give Up、Make A Decision、Keep In Touch…）；**困難**是諺語與慣用語，就算整句字母都揭曉了，字面意思也不會直接告訴你在講什麼（Long Time No See、Practice Makes Perfect、Actions Speak Louder Than Words…）。**跟全站題目規則一樣，一律簡單好懂的英文、不用需要美式文化背景才懂的俚語**（見 CLAUDE.md「注意事項」那條長期規則）——連「困難」等級的諺語也是全球英語教材普遍教過的成語（Practice Makes Perfect、Step By Step 這類），不是只有美國人才懂的哏。`level` 一樣餵給 `turnLimitFor()`：簡單片語就算揭露得少也好猜，困難的諺語就算全部揭露也可能想不到，跟 `taboo` 的分級邏輯完全同一套（見上面 v2 那段說明），這也是為什麼片語庫要照樣分三個 `level`，不能只當成一整包不分難易度的清單。**片語庫不進 `sheet-data.js` 的 SCHEMA**（跟 Dare Conquest 的簡單／中等內建牌堆是同一種處理——見 `game-data.js` 那一列），純內建、不能透過 Google 試算表覆蓋，因為這是另外產生的一份全新題庫，不是既有 `taboo` 題庫的延伸。技術上：片語不是 `CRACK_ENGINE.tierOf()` 算出來的長度分桶，而是一個獨立的第四個選項，所以**沒有**加進 `crack-word-engine.js` 的 `CRACK_ENGINE.TIERS` 常數（那裡是 `tierOf()` 的純函式分類結果，永遠只會是 short／medium／long 三種，加進去會讓一個從不會發生的回傳值混進純函式的分類清單，也會讓既有測試斷言的意義變得不精確）——`crack-the-word.html` 自己另外組一個頁面層級的 `ALL_TIERS = CRACK_ENGINE.TIERS.concat(['phrase'])`，所有原本讀 `CRACK_ENGINE.TIERS` 來畫按鈕／驗證選擇／組 `tierOptions` 的地方（`requestTier()` 的合法性檢查、`payloadFor()` 送給卡片的 `tierOptions`、主持人畫面備援的長度按鈕列）全部改讀 `ALL_TIERS`。新增 `deckFor(t)`（`t === 'phrase' ? PHRASE_DECK : DECK`）是唯一知道「這個選項的題目從哪一份題庫來」的地方，`pickWordIndex(t)` 與 `requestTier()` 都透過它取牌，不必為片語另外複製一份抽牌邏輯。`BUCKET.phrase` 直接是 `PHRASE_DECK` 的全部索引（`PHRASE_DECK.map((_, i) => i)`）——跟短／中／長不同，片語庫不需要再依長度細分，整份 100 句就是它自己的「牌堆」。**已抽過的清單持久化時多驗證一個欄位**：`SAVE_KEY`（`crackWordUsed.v1`）原本只拿 `DECK.length` 當指紋，現在多存一個 `phraseLen: PHRASE_DECK.length` 並在讀回時兩者都要對得上才還原，理由跟原本讀 `deckLen` 一樣——防的是其中一份題庫的形狀之後被改過（例如日後片語庫也開放編輯），舊索引卻對到不同的句子。玩家卡片端（`play.html`）的選長度按鈕本來就是泛型迭代 `data.tierOptions`（不管有幾個選項都自動畫出來），只需要補上 `tierEmoji`／`crackTier_phrase` 這兩個查表項目（💬，中英文字串），不用改任何邏輯。**片語本身的多字空白完全沿用既有機制**：`tileLayout()`／`CRACK_ENGINE.letterCount()` 早在 v1 就把空格當成「不算字母、不算秘密、原樣顯示的靜態格子」處理（`Ice Cream` 那類詞條就是這樣），片語只是比兩個字的詞條再多幾個空格，同一套程式碼原封不動可以用，`letterCount()`／`uniqueLetterCount()`／`turnLimitFor()`／`remainingBlanks()` 這些依「有幾個字母格」計分與算預算的函式全部自動正確處理更長的片語，不用改 `crack-word-engine.js` 一行。實測（`crackword-phrase-tier.mjs`）：`ALL_TIERS` 正確含 4 個選項、`BUCKET.phrase`／`PHRASE_DECK` 皆為 100；主持人畫面與玩家卡片的長度選單第四顆正確顯示「💬 片語（常用英文片語）· 100」；選片語後正確從 `crackPhrase` 抽出詞條（例如「Every Now And Then」「Do The Dishes」）並標記 `tier: 'phrase'`；帶空格的片語正常跑完猜對／團隊計分整個流程（`remainingBlanks()` 對多字片語一樣正確計算）；320／390px、中英文下選長度畫面與遊戲畫面皆無溢位。`node --test` 額外驗證 `crackPhrase` 陣列本身：100 句、零重複、`easy`／`medium`／`hard` 各 40／40／20、每一句都有 `word`／`level`／`emoji` 三個欄位、不含 `<`／`>` 字元（`sheet-data.js` 的 `clean()` 只在讀試算表那條路上擋這兩個字元，內建題庫必須自己先乾淨，見 `game-data.js` 那一列的既有規則）。全站測試維持 83 項全數通過（這批新增檢查目前是跑在 scratchpad 腳本裡，還沒收進 `tests/`，因為 `crackPhrase` 目前沒有專屬的引擎函式要測——它就是一份靜態資料，真正的行為測試已經涵蓋在既有的 `turnLimitFor`／`remainingBlanks`／`tileLayout` 測試裡）。

| `crack-word-engine.js` | 🔤 Crack the Word 的純邏輯層（字母公平分配、空格／字母的 tile 版面、計分），設計與 `next-round.js` 同構——不碰 DOM、不碰 Firebase，`node --test` 直接跑。主持人頁把它當唯一的權威計算來源：`distributeLetters()` 算出這一輪 26 個字母各自分給誰、`tileLayout()` 把單字拆成「哪些位置是字母、哪些是空格或連字號之類的標點」（`Ice Cream` 這種帶空格的詞條，空格本身不算需要揭曉的秘密，直接照原樣顯示）、`tierOf()`／`guesserScore()`／`revealedPositions()` 負責長度分桶與計分。26 個字母永遠全部分完（不管題目本身有幾個獨特字母），這是刻意的：使用者原始需求就是「將 26 個英文字母平均拆開來給所有玩家」，不是只分「有用的」那幾個——按到一個其實不在單字裡的字母、看到「不在裡面」的回饋，本身就是一種資訊（類似 Hangman 的排除法），全部分完也才能保證「每個人分到的字母數」這件事本身是均衡的，不會有人因為單字剛好只有 3 個獨特字母，就只分到 3 顆按鈕。**v2 新增** `uniqueLetterCount(word)`／`turnLimitFor(word, difficulty)`／`TURN_MULT`／`TURN_MIN`／`TURN_MAX`／`TURN_LIMIT_PENALTY`，為輪流按字母＋回合預算兩項機制提供算式，見 `crack-the-word.html` 那一段「v2」說明 |
| `pick-a-side.html` | 🔥 Pick a Side!（這頁的 `<style>` 開頭補了一行 `:root { --accent: #ff7043; }`——其餘七頁本來就有，只有這頁從頭到尾把 `#ff7043` 寫死，一直沒人發現，直到 `shared.css` 出現真的要靠 `var(--accent)` 取色的共用元件（說明頁）才炸出來：那些元件在這一頁會拿到空值，變成沒有底色的深字。主持人按 Spin 抽一句爭議意見，每位玩家在自己的卡片上按 👍 Agree／🤷 It Depends／👎 Disagree，主持人畫面的三根長條即時累加；主持人自己也可以直接點畫面上的按鈕幫沒有連結的人計票，最終票數 =「卡片票 + 手動點的票」。每次 Spin 會換一個 `voteId`，上一輪的票自動失效；`ROOM.init()` 帶 `hideSetup: true`，人數／連結已在 `index.html` 設定好；頁面最下方有**全部 20 句犀利意見**供主持人瀏覽（`hottake` 是純字串陣列、沒有分類，所以跟其他遊戲的題庫瀏覽比起來少了分類篩選列，直接編號 #1–#20 列成一個 grid），**預設收合**（按「👁️ 瀏覽全部 20 句犀利意見」才展開，避免分享畫面時暴雷）。**抽題由卡片決定**（見 `next-round.js`）：主持頁一載入就推一份 `round: 0` 的空 payload，每張卡片上是一顆橘色實心的「🎡 抽第一題」，誰先按就開始；之後投票鈕下方是「🎡 換下一題」，誰按都可以，按下去所有卡片一起跑五秒倒數（期間任何人都能按「等一下」停住），倒數完主持頁才 `spin()`。主持頁的 Spin 鈕與 ↺ Reset 都保留（Reset 一樣重發 `voteId`、也重開閘門）。`onPlayerData` 裡 `nextGate.handle(num, data)` 排在 `updateBars()` 之前，`pushToCards()` 把 `nextGate.payload()` 併進同一次 `ROOM.publish()`，主題色橘 `#ff7043`） |
| `sophies-choice.html` | 🤔 Sophie's Choice（抽一則兩難劇本，每位玩家在自己的卡片上按 A／B／C（C 是「自己的答案」），主持人畫面在劇本卡下方顯示三欄即時統計：票數 + 投給該選項的玩家名字，最高票那欄框線亮起；`ROOM.init()` 帶 `hideSetup: true`，人數／連結已在 `index.html` 設定好；頁面最下方有**全部劇本**供主持人瀏覽（含分類篩選列），**預設收合**（按「👁️ 瀏覽全部 N 個情境」才展開，避免分享畫面時暴雷）。**抽題由卡片決定**（見 `next-round.js`，做法與 `pick-a-side.html` 完全相同）：第一題任何人按「🎲 抽第一個情境」就開始，之後 A／B／C 下方是「🎲 換下一個情境」，誰按都可以，按了跑五秒倒數才換（期間任何人都能按「等一下」停住）；`draw()` 完成時刻意先 `round++`、`rolling = false` 再 `pushToCards(sc)`，順序反了閘門會在發牌當下被判成關閉，主題色琥珀 `#f59e0b`） |
| `persuade-together.html` | 🤝 Persuade Together!（抽劇本時會**隨機發牌決定誰當裁判**：一人拿到 👨‍⚖️ Judge、其餘拿到 👥 Persuader Team，各自在卡片上看到自己的身分與劇情；主持人畫面的「The Judge」下方會標出「🎴 Played by 名字」，並在下方顯示一條裁判狀態列（等待中／✅ 被說服／❌ 沒被說服）。裁判的卡片上有「✅ You convinced me!／❌ Not convinced」兩顆按鈕，按下後主持人畫面即時變色；`ROOM.init()` 帶 `hideSetup: true`，人數／連結已在 `index.html` 設定好；頁面最下方有**全部劇本**供主持人瀏覽（含分類篩選列），**預設收合**（按「👁️ 瀏覽全部 N 個場景」才展開，避免分享畫面時暴雷）。**抽題由卡片決定**（見 `next-round.js`）：這是四款裡最乾淨的一款，因為裁判的判決本身就是回合的結尾——`canDraw()` 是 `!rolling && (judgeIdx < 0 || verdict() !== null)`，判決還沒出來時 payload 的 `nextId` 是 `null`、任何卡片都看不到按鈕，判決一到主持頁 `nextGate.sync()` 把 `nextId` 推出去，**每一張卡片（含裁判）**最下方都出現青色實心的「🎲 下一個場景」，誰先按就開始五秒倒數（跟其他三款同一套，期間任何人都能按「等一下」停住）；抽完重新發裁判、閘門又關上等下一次判決。第一次抽題同樣任何人按「🎲 抽第一個場景」即可。`dealRoles()` 沒有場景時改呼叫 `publishIdle()`，主持頁 Draw 鈕下方的 `.next-status` 只在閘門開著時顯示，主題色青 `#06b6d4`） |
| `youre-in-the-scene.html` | 🎭 You're In The Scene（抽場景後**自動依房間人數把角色隨機發給每位玩家**，每個人的卡片上只有自己那一個角色（字母、角色名、演法提示）＋場景描述，不用再互相喬誰演誰；主持人畫面的角色卡下方會標出「🎴 名字」對照表；`ROOM.init()` 帶 `hideSetup: true`，房間人數／連結已在 `index.html` 設定過，這頁的 `room-mount` 只剩發牌後的唯讀名單顯示每人分到的角色；頁面最下方有**全部場景**供主持人瀏覽（含分類篩選列），**預設收合**（按「👁️ 瀏覽全部 N 個場景」才展開，避免分享畫面時暴雷）。**抽場景由卡片決定**（見 `next-round.js`，做法與 `pick-a-side.html` 相同）：第一次任何人按「🎭 抽第一個場景」，之後角色框下方是「🎭 換下一個場景」，誰按都可以，按了跑五秒倒數才換場景並重新發角色（期間任何人都能按「等一下」停住）——演完沒有任何訊號可以偵測，所以由演的人自己決定；`dealRoles()` 把 `nextGate.payload()` 併進每一張角色 payload，`draw()` 完成時先 `round++`、`rolling = false` 再 `renderScene(sc)`（發角色在裡面），主題色粉 `#ec4899`） |
| `dare-conquest.html` | 🏰 Dare Conquest（主持人單頁，Firebase 只用於選配的玩家投票、沒有 Firebase 時整個遊戲照常運作：選 2–6 人、8×4 寬螢幕比例的 board 外圍 20 格路徑，START 在左上角、順時針前進，輪到自己時按地圖正中央的「🎲 Roll」丟一顆骰子、移動 1–6 格（只有一顆骰子，沒有兩顆骰子的選項）；骰子按鈕旁還有兩顆技能鈕「🎯 High Roll (N)」與「🔻 Low Roll (N)」，各自整局限用 2 次，要在按「Roll」之前先按下啟動（按鈕文字不變，改成框線變紅、文字變黃色表示已啟動），啟動後當回合丟出的每一顆骰子都保證是 4/5/6（High Roll）或 1/2/3（Low Roll），直接換到下一位玩家後才會解除；已啟動的技能再按一次可以取消（框線與文字變回灰色）並退回這次名額，若啟動其中一個時另一個技能還沒用掉、按下另一顆技能鈕會直接切換（原本啟動的那個技能名額退回、改扣新按的），兩者互斥、同時只會有一個生效，次數用完按鈕不會消失，會顯示「(0)」並鎖定無法再按，次數會跟著玩家資料一起存進 localStorage，重新整理不會重置，停在一般格上，若該地已經是自己的土地：不會有任何事發生，直接換下一位玩家；停在無主或對手土地上才會先跳出綠色/紅色的「Truth or Dare」選擇畫面，由當前玩家自己自由喊出「😇 TRUTH」或「😈 DARE」，主持人依玩家喊的內容點擊對應按鈕（不是系統或對手指定）：選 TRUTH 會從**目前勾起來的難度合起來的那副**真心話題庫隨機抽一題讓玩家誠實回答（按鈕文字為「✅ Honest!／❌ Not Answered!」、面板呈金色主題、題目除了個人隱私類（尷尬回憶、小謊言、不敢說的怪癖等）也有不少評論／比較其他玩家的題目，例如「你覺得這房間裡誰最可能先被淘汰？」「{player} 給你的第一印象是什麼？」，帶點爭議性與互動性）、選 DARE 則從**目前勾起來的難度合起來的那副**互動挑戰題庫隨機抽一題（跟其他玩家告白、開嗆聲互酸、投票淘汰、幫其他玩家頒一個損人不利己的獎、唱指定歌曲、說笑話、講繞口令等，過半題目會直接點名 {player} 互動），不論 TRUTH 或 DARE 全部題目一律 45 秒，題目文字本身不再寫秒數（改由計時器顯示），畫面會顯示一顆「⏱️ Start 45s Timer」按鈕，主持人先把題目唸完、按下按鈕才開始倒數，倒數圈會隨秒數變色：綠→橘→紅並閃爍，開始倒數後會多出現「⏸️ Pause／▶️ Resume」與「🔄 Restart」兩顆按鈕，可以暫停/繼續、或直接重新從頭倒數；倒數秒數會同步到**每一位玩家**的卡片上（當事人在題目下方、其他人在投票鈕上方，都是一顆同款的倒數圈，一樣綠→橘→紅並閃爍），主持人端只在狀態改變時（開始／暫停／繼續／重新開始／歸零）推送一次 `players/{token}/conquestTimer`，卡片收到後自己每秒遞減，避免每秒寫一次 Firebase，過程中 Did It!/Failed!（或 Honest!/Not Answered!）仍可隨時按，計時不會自動判定，完成才算過關。**倒數歸零不是判定，畫面上也不能看起來像判定**：這個倒數只是讓表演的人有點緊張感，時間到不代表沒過關——過不過永遠是主持人按那兩顆判定鈕決定的。原本歸零時圓圈會停在紅色並**無限脈動**（`.critical`），那是一個警報；在其他玩家的卡片上它正好就壓在「❌ Failed!」上面，等於在指示大家投失敗（實際回報過）。現在主持畫面與玩家卡片都多一個 `.is-done` 狀態：歸零時拿掉 `low`／`critical`、停掉動畫、圈內的數字換成 `⏰`，顏色換成中性灰（主持頁走 `--text-dim`／`--border-2`，卡片是同值的 `#9999bb`／`#3a3a5e`，實測對比 6.60:1 與 4.88:1，都過 4.5:1 那條線），下面接一行灰色小字「⏰ 時間到 — 這個倒數沒有輸贏」（主持頁 `timeUpNote`／卡片 `conqTimeUp`）。`timerBlockHTML()` 重畫（語言切換、重繪）時也算一次 `done`，所以切語言不會把紅色警報叫回來；`restartPromptTimer()`／`resetPromptTimer()` 會把 `.is-done` 與那行字一起收掉。卡片端 `paintCardTimer()` 用同一個判斷，`syncCardTimer()` 收到主持頁推的 `state: 'done'` 時不啟動 interval，所以卡片就停在這個中性狀態；**設定畫面是「🎚️ 題目難度」三顆勾選鈕＋一顆「🎲 Start Game」按鈕**（`#setup-card` 裡的 `#diff-row` 與 `.control-row`，外加規則說明 `.rules-note`）。**這組勾選鈕在遊戲開始後仍然找得到、也仍然能改，但改成一顆不佔版面的小按鈕**：一開始（試玩回饋）是把整組勾選鈕原樣搬進控制列常駐顯示，結果三張直立的難度卡疊在回合資訊旁邊，把控制列撐高了一大截——在一個大部分畫面都該留給棋盤的分享畫面上，這樣太浪費。現在控制列右側只有一顆小膠囊「🎚️ 🟢」（`#diff-toggle-btn`，圖示是目前勾起的每個難度各自的顏色圓點——🟢／🟡／🔴，跟勾選框本身同一組顏色，兩個難度都勾就顯示兩顆點，一眼判斷勾了幾個不用打開），點下去才彈出一塊浮動面板（`#diff-popover`）顯示完整的三顆勾選鈕；`#diff-label`＋`#diff-row` 仍然包在同一個 `#diff-group` 節點裡，`startGame()`／`resumeGame()` 呼叫 `placeDiffGroupInGame()` 把它整個搬進這塊面板（`appendChild` 對既有節點是搬移不是複製，同一組按鈕、同一個 `toggleDifficulty()`，兩邊不會不同步），關閉時面板整個 `display:none`，不佔任何版面，只留下常駐的那顆小膠囊。**`#diff-popover` 這個節點刻意寫在 HTML 裡 `#game-area` 外面、當 `<body>` 的一個平行元素**，不是巢狀在觸發它的按鈕底下：`.control-bar` 與 `.board-stage` 都掛著跑完就結束的進場動畫（`animation: fadeUp ... both`），`fill-mode: both` 會讓動畫最後一個 keyframe 的 `transform` 永久留在元素上（就算是單位矩陣、看起來跟沒動過一樣），而任何帶 `transform` 的元素都會建立自己的疊層環境（stacking context）——這代表面板如果生在 `.control-bar` 底下，`z-index` 再高也只能贏過 `.control-bar` 自己那個環境裡的東西，贏不了 DOM 順序排在後面、同樣帶著疊層環境的 `.board-stage`（棋盤本身），面板會被畫在棋盤下面、點下去點到的是棋盤格子（實測抓到過：勾選鈕位置對、看起來也在最上層，但 `elementFromPoint()` 回報最上面其實是 `board-center`，點擊完全點不到勾選鈕）。搬到 `#game-area` 外面、`position: fixed`、`z-index: 9999` 之後就跳出了這整個問題——面板的座標由 `positionDiffPopover()` 在**每次開啟時**用 `#diff-toggle-btn` 當下的 `getBoundingClientRect()` 現算（沒有父層可以讓 CSS 直接錨定，所以錨點交給 JS），開啟時另外掛一個 `resize` 監聽即時重算、一個 capture 模式的 `scroll` 監聽（含棋盤自己 `overflow-x:auto` 的內部捲動）直接關閉面板，避免面板飄在跟按鈕不同的地方；點擊面板或按鈕以外的任何地方會關閉面板（`document` 上掛一個全域 `click` 監聽，用 `e.composedPath()` 而不是 `wrap.contains(e.target)` 判斷是否點在面板內——勾一個難度會觸發 `renderDiffButtons()` 整個重畫 `#diff-row` 的 `innerHTML`，那個按鈕在自己的 click handler 裡被同一次點擊事件親手拆掉重建，等外層的 `document` 監聽器接手處理這同一個事件時，`e.target` 已經是個不再掛在文件上的「幽靈節點」，`.contains()` 對它一律回傳 false，面板會被誤判成「點在外面」而自己關掉；`composedPath()` 記的是事件剛發生那一刻的路徑，不受之後的 DOM 變動影響，勾兩三個難度可以連續勾、面板不會勾一次自己關一次）。這樣房間玩熱了、Easy 題目不夠用時，主持人不用結束整局重開，點開這顆膠囊、勾／取消 Medium、Hard 就能擴充牌堆。Dare Conquest 沒有像 Say It 那樣「跳回精靈」的中途設定頁，所以搬進面板是單向的——只在進遊戲那一刻搬一次，不會、也不需要搬回設定畫面。**三個難度可以複選而不是三選一**（跟 `say-it-without-saying-it.html` 的難度勾選同一套做法）：勾起來的難度會**混在一起抽**，勾簡單＋中等就是兩套加起來 200 題 Dare／200 題 Truth，三個都勾就是 250／250。每張卡是一個勾選方塊（`.db-box`，勾起來是強調色實心＋白勾、沒勾是空心方框，**沒勾時方塊裡不放任何字元**——透明的 ✓ 會被讀螢幕軟體唸出來、也會被對比稽核抓到）＋難度名稱，**下面刻意不放任何說明文字**：原本每張卡下面有一句「適合剛見面的陌生人」這類熟悉度描述，實際帶活動時主持人是把這個畫面分享給全場看的，等於當著大家的面標註「我覺得我們還不熟」，很尷尬（實際回報過）。難度名稱本身（簡單／中等／困難）已經夠用。**預設只勾簡單**（這是個帶陌生人破冰的站，開局就問私事會冷場），選擇存進 `dareConquestDifficulty.v1`，存的是 JSON 陣列；v1 存的是單一字串，讀取時會被當成「只勾那一個」接住。**最後一個勾不能取消**（`toggleDifficulty()` 在 `difficulties.length === 1` 時直接 return，那顆維持勾起來、加一個 `.is-locked` 只拿掉 cursor、**不變灰**，配一行 title「至少要留一個難度」）——沒有這道關卡，全部取消之後 `deckFor()` 為了不讓牌堆變空會退回困難，畫面上卻顯示一個都沒勾。`difficulties` **永遠照 `DIFFICULTIES` 的順序排**（`sortTiers()`），因為 `usedPrompts`／`usedTruths` 存的是「合併後那副牌的索引」，順序一變就會指到別的題目。**遊戲進行中可以隨時切換**（設定畫面與遊戲畫面共用同一份 `#diff-group`，見上）：`toggleDifficulty()` 在改完 `difficulties` 之後會**清空 `usedPrompts`／`usedTruths`**——這兩份「已出過」清單存的是「合併後那副牌」的索引，勾選一變，牌堆的形狀就跟著變（尤其是取消勾選一個排在中間的難度時，後面難度的題目會整批往前挪位），舊索引可能會誤指到別的題目；清空只是讓「已出過」的記憶重新開始算，不影響當下正在螢幕上的題目，換來的是絕不會誤判「這題出過了」或「這題還沒出過」。切換難度同時會呼叫 `saveGame()`（遊戲還沒開始時 `saveGame()` 自己會判斷 `gameStarted` 為假直接跳過，不是無條件寫入），確保中途改的勾選會跟著存檔／續局一起保留，不會因為重新整理後選回「Resume Game」而被存檔裡的舊 `difficulties` 蓋回去。`saveGame()` 把 `difficulties` 一起存下來，`resumeGame()` 還原時先套回這個陣列再套 `usedPrompts`／`usedTruths`（也認得舊版單一 `difficulty` 字串的存檔，進行中的遊戲不會因為改版而讀不回來）。切換難度會順手重畫頁尾的題庫瀏覽（那份清單就是合併後的牌堆）。技術上 `PROMPTS`／`TRUTHS` 這兩個 `const` 已經換成 `dareDeck()`／`truthDeck()` 兩個函式，回傳所有勾起來的難度的聯集——牌堆要跟著主持人勾的走，而勾選發生在那兩行執行之後。房號／人數／姓名／連結這一步已經搬到 `index.html` 的 Step 1 統一設定過一次，`dare-conquest.html` 原本因為沒用 `room.js` 而自己仿寫的那一整份設定 UI 與邏輯——ROOM 列（房號輸入框／Load／New Room）、PLAYERS 人數鈕、「🔗 Player Links」每人一欄的姓名輸入框＋Link／QR、Copy All Links、Send Card Check，以及背後的 `cardURL()`／`cardShareText()`／`copyCardLink()`／`copyAllLinks()`／`sendCardCheck()`／`clearCardCheck()`／`qrSVG()`／`toggleCardQR()`／`renderPlayerLinks()`／`loadRoomCode()`／`newRoom()`／`setPlayerCount()` 等函式與對應 CSS——已整段移除；`initSession()`（沿用 `room-last-session` 讀出房號／人數／姓名／token，跟其他遊戲共用同一包 localStorage）與 `attachVoteListeners()`（用 `ensureTokens()` 依人數配 token 供玩家投票用）不受影響、Start Game 時一樣各自跑一次，所以房號、玩家人數、姓名依然是跟其他遊戲共用的同一組設定，只是不能再從這頁調整人數或補發連結——要改，回 `index.html` 的 Step 1（「✏️ Edit room setup」）。輪到誰的回合時，**那位玩家的卡片上會由上而下垂直排列「🔻 Low Roll (N)」「🎯 High Roll (N)」兩顆技能鈕與一顆「🎲 Roll the Dice」按鈕，按下去就會觸發主持人畫面上對應的動作**（技能的啟動／取消／互斥切換與名額扣退全部由主持人端的 toggleSkill 處理，卡片只是發出請求，啟動中的技能在卡片上一樣是紅框＋黃字，次數用完顯示「(0)」並鎖定；擲骰同理）（骰子與結果仍由主持人畫面決定；主持人畫面正在擲骰／移動／有挑戰進行中時三顆按鈕都會自動變灰，不是自己回合則完全不顯示，重複送出的請求會被忽略）；接著跳出 Truth or Dare 選擇畫面時，**同一位玩家的卡片上也會出現 😇 TRUTH／😈 DARE 兩顆按鈕**，可以直接從自己手機喊題型（不必用講的讓主持人代點；其他人的卡片顯示回合列、「picking Truth or Dare…」與 ⏳，主持人畫面上的兩顆按鈕仍然可以按，先到先算），主持人抽到題目時會把「誰的挑戰／題型／題目文字」推送到每位玩家的卡片（`players/{token}` 的 `conquest` 欄位，`{player}` 會換成純文字名字），**其他玩家**的卡片上出現跟主持人畫面一樣的兩顆按鈕（✅ Did It!／❌ Failed!，Truth 題則是 ✅ Honest!／❌ Not Answered!），投票寫進自己的 `players/{token}/conquestVote`（含該題的 id）；**輪到的那位玩家自己不投票**，他的卡片改成顯示這題的題目文字（他要照著做，是唯一需要在手機上看到題目的人），主持人畫面把票數直接做成兩顆判定按鈕內的小圓形徽章（「✅ Did It! (2)」「❌ Failed! (1)」），該側 0 票時徽章不出現、按鈕跟沒有投票功能時長得一樣，但**投票只是參考、最終仍由主持人按判定按鈕**；每抽一題會產生新的 vote id，舊票自動失效不用清除，換題或判定後玩家卡片會回到待機狀態（名字、回合列、⏳、戰績）；未設定 Firebase（或 CDN 連不到）時投票區塊自動隱藏，遊戲完全不受影響；抽到題目後、判定之前，挑戰卡右上角（跟題型標籤同一排）還有一顆「🔄 New Question (N)」，玩家看到題目覺得不想做可以直接換一題（換到的一定是同類型：抽 TRUTH 就換 TRUTH、抽 DARE 就換 DARE），按下去不會馬上換，會先在按鈕正下方跳出「Change this question?」確認小視窗（✅ Yes／✖️ Cancel，並顯示剩幾次），確認期間題目與判定按鈕會變暗且不可點，避免誤觸浪費次數；**換題次數沒有上限**，換題會重置計時器、被換掉的題目這輪不會再出現。原本是每位玩家整局 3 次（用完顯示「(0)」並鎖定），已經拿掉：那個上限只會卡到「抽到自己真的做不到的題目」的人，而那正是換題唯一該存在的情況；防誤觸本來就是靠上面那個確認小視窗，不是靠次數。`MAX_SWAPS`、`player.swaps`、按鈕上的 `(N)` 與「這局還剩 N 次」那行字都一併清掉了；若落地的是無主土地，過關即直接佔領，若落地的是對手土地（搶地盤），過關後不會馬上換手，而是彈出金色「🎲 Dice-Off!」面板讓進攻方與地主各自的骰子（可一次看到雙方骰子與點數）比大小：進攻方先按「🎲 Roll」骰（**進攻方的骰子只會出現 3、4、5、6**，算是完成挑戰後的優勢，地主骰子維持 1～6 正常機率。原本是 4／5／6，實測進攻方三次有兩次搶得下來（66.7%），守方等於守不住、根本沒有比骰子的意義；改成 3～6 之後是 58.3%，還是進攻方有利，但真的會輸）、骰出結果後地主的骰子按鈕才會解鎖，地主按下後骰；**這兩顆骰子也可以由當事人自己在卡片上按**（進攻方與地主各自的卡片會出現一顆「🎲 Roll」，主持人畫面上該側按鈕還沒解鎖時卡片上也是灰的，其他人的卡片只顯示「A vs B」與 ⏳；主持人端會驗證是不是本人、以及該側是否真的可以骰，主持人畫面上的兩顆按鈕一樣可以代按）、進攻方點數大於地主才算搶地成功（平手算地主守住），骰出結果的瞬間贏的一方骰子會立刻發光脈動、頭上跳出皇冠 👑、點數變成綠色高亮，輸的一方則變暗變灰階，清楚標示贏家，成功搶地瞬間該格會有搶奪特效：格子震動晃動、指揮官色系放射狀色彩衝擊波、白色粒子爆散、並彈出「🏴 CAPTURED!」浮動標籤，挑戰沒過關（Failed!/Not Answered!）則不會進入 Dice-Off、土地直接留在地主手上，地圖格子不顯示題型、可佔領的一般格不放任何圖示（純空白，佔領後才會整格變色），20 格中除了 START 還有 2 個 BONUS、1 個 LUCKY、1 個 GIFT，停在 START/BONUS/LUCKY/GIFT 上都要先過一次 Truth or Dare 挑戰（同樣由玩家自選 TRUTH/DARE、主持人點對應按鈕，面板會依格子類型變色並顯示「🏁/⭐/🎲/🎁 START/BONUS/LUCKY/GIFT Challenge」），過關才會真的觸發獎勵、失敗則什麼都不會發生直接換人：BONUS 必定從目前地最多的對手（領先者）手上奪取 2 塊地（多人並列領先時隨機挑一位下手），觸發搶奪特效；只有在沒有任何對手擁有土地時（例如遊戲剛開始）才會改成隨機取得 2 塊無主土地、LUCKY 與指定玩家交換全部領地（2 人局自動指定唯一對手，3 人以上由主持人點選對象）、GIFT 效果與 BONUS **完全相同**（同樣從領先者手上奪取 2 塊地，只有圖示、顏色與文案不同；兩者共用同一個 `seizeFromLeader()`，數量由 `SEIZE_COUNT` 常數控制），START 格現在也帶有 LUCKY 屬性：玩家停在 START 時不再只是「安全格、什麼都不會發生」，過關後會直接觸發跟 LUCKY 格一樣的「交換全部領地」效果，觸發特殊事件時該格會有發光閃爍特效、挑戰卡框線與圖示也會依事件類型變色，特殊事件訊息不會自動消失，主持人看完按下「👍 OK」才會換下一位玩家，玩家目前所在格子（無論一般格或特殊格）都會顯示玩家圖示，輪到誰的那位玩家會有明顯標示：所在格子外圍有對應顏色的發光脈動外框、該玩家的圖示會放大並持續彈跳、圖示上方有小箭頭指標與擴散光環，換人時立即跟著移動，方便主持人一眼看出目前輪到誰，一般土地佔領後整個格子會填滿玩家對應顏色（不再只是外框變色或右上角小圖示），格子正中央還會顯示該玩家的名字（白字、超過一行會自動換行最多兩行並截斷），骰子改為可 3D 翻滾的真實骰子（CSS 3D 立方體、6 面點數、擲出時有拋起彈跳動畫）並放在地圖正中央，回合資訊獨立顯示在 board 上方的控制列（「Player X's turn · Round N」，Round 是每個玩家都輪過一次才會 +1 的完整回合數，不是每人每次擲骰都算一輪），挑戰卡改為浮動疊加在地圖正中央的卡片（不佔版面、不會把地圖往下擠），只有真的有挑戰/特殊事件/LUCKY 選擇對象時才會出現，地圖上方所有區塊（標題、計分板、控制列）皆採緊湊排版以節省版面，遊戲不會因為土地全數佔領而自動結束、答對挑戰仍可從對手手中搶地讓局勢持續變化，遊戲畫面沒有「Restart Board」「End Game」「Change Players」等控制按鈕，相鄰（棋盤外圈路徑上前後相接）且同一位玩家擁有的土地之間，交界處會出現一小段磚紋橋樑/城牆結構、顏色跟該玩家的地一致，視覺上把兩塊地連成一體（純視覺標示，不影響分數），頁面最下方分別有 Dare 題庫與 Truth 題庫供主持人瀏覽，兩份題庫**各自獨立、預設收合**（各自一顆「📋 瀏覽全部 N 個大冒險／😇 瀏覽全部 N 個真心話」按鈕，互不影響——只展開 Dare 不會連帶展開 Truth，避免分享畫面時暴雷），遊戲進度會在每個回合結束（換人、Dice-Off 結算、特殊格確認）時自動存進 `localStorage`（key: `dareConquestSave.v1`），只存「回合與回合之間」的乾淨狀態、不存挑戰/Dice-Off 進行到一半的中間畫面，重新整理或關閉分頁後回到設定畫面會看到「🕹️ You have a game in progress」提示，可選擇 Resume Game 還原棋盤與計分，或 Start New Instead 捨棄重來） |
| `kangaroo-court.html` | ⚖️ Kangaroo Court（主持人單頁；人數 **4–9 人**（陪審團人數 = 總人數 − 4，若關掉 Judge 則是總人數 − 3）與玩家連結已在 `index.html` 統一設定過，這頁的 `ROOM.init()` 帶 `hideSetup: true`，`room-mount` 只留 Judge 開關（`extraRowHTML`）與發牌後標出每人角色的唯讀名單，每局隨機發出 🙇 Defendant／⚔️ Prosecutor／🛡️ Defense Attorney／🧑‍⚖️ Judge 四個不同角色（Judge 是否上場由主持人自己控制，見下方「Judge 開關」），其餘全是 👥 Jury；發牌方式仿 `say-it-without-saying-it.html`：Defendant 用一個洗過牌的袋子輪流發、每人都當過一次才重新洗牌且不連莊，Prosecutor／Defense／Judge 則各自從「目前擔任次數最少」的人裡隨機挑（Judge 只在開關為 ON 時才會抽），四個角色互斥，長期次數完全平均。按「⚖️ Draw a Charge」從 30 條短、生活化、用字簡單的「罪名」（`game-data.js` 的 `kangaroo`，每條是一個能一口氣唸完的小情境，通常兩個小節：一個大家都做過的小習慣，接一個好笑的轉折，讀起來像個迷你故事而不是乾巴巴的罪名標籤，刻意不用法律術語或生僻字，例如「wearing headphones with no music playing, just to avoid talking to people」「using someone else's shampoo in the shower and never mentioning it」）隨機抽一條，同時開放陪審團在自己卡片上**秘密**投票 Guilty／Not Guilty（寫進 `players/{token}/kcPrevote = {id, choice}`，這一票在結果公布前完全不會顯示在主持人畫面或任何人的卡片上）。流程共九步（這局有 Judge 的話是十步）。**整場審判現在由玩家卡片推進，主持人不必碰主畫面**（`next-round.js` 那套做法）：①**開案**走 `NEXT_ROUND` 閘門——開庭前每張卡片都有「⚖️ 抽第一條罪名」，誰按就開（第一案不倒數）；判決出來（有 Judge 的局要到 Judge's Ruling 那一步）之後每張卡片都有「⚖️ 下一個案件」，按了所有卡片一起跑五秒倒數、任何人可按「等一下」停住，倒數完才 `dealNewCase()`。閘門只在 `idle`／`judgment`／沒有 Judge 的 `reveal` 開放（`canDraw`），其餘階段 payload 的 `nextId` 是 `null`。②**每一個階段由「正在發言的那一方」在自己卡片上按一顆「✅ 我講完了」推進**：主持頁每次換階段都 `newStep()` 鑄一組一次性的 `stepId`（`startPhase`／`dealNewCase`／`doReveal`／`resetToIdle` 都會鑄），`payloadFor(i)` 只對 `stepOwners()` 回傳的座位送出 `stepId`（其他人是 `null`、卡片不畫按鈕），卡片寫 `players/{token}/kcStep = { id }`，主持頁 `handleStep()` 驗證 id 相符且該座位真的是這一階段的當事人才 `advance()`；上一階段慢一步送上來的 id 直接丟掉。當事人的對照：`prosecution`／`prosecution-witness` 是檢察官、`defense`／`defense-witness` 是辯護律師、`cross` 與 `finalvote` 是 Judge（沒有 Judge 的局檢辯任一方都可以，payload 帶 `stepAnyLawyer` 讓卡片說明「檢方或辯方任一位按都可以」）、`reveal` 是 Judge（按了進 `judgment`；沒有 Judge 的局 `reveal` 沒有當事人，直接開放下一案閘門）、`prevote` 是檢察官（見③的備援）。③**兩次秘密投票在全體陪審員投完的那一刻自動推進**（`onPlayerData` 裡 `allJuryVoted()` → `advance()`，`prevote` 進 Prosecution、`finalvote` 直接 `doReveal()`），每一份投票 id 只自動推進一次（`autoAdvancedBallot`），所以主持人用「⬅️ 回上一步」退回同一份投票時不會又被彈回去；有陪審員不在手機前時，檢察官（prevote）或 Judge／律師（finalvote）的卡片上有一顆「不等了，開始發言／揭曉判決」可以直接推進，卡片上同時印著「N / M 位陪審員已投票」（payload 的 `votes`）。主持人畫面上那顆狀態鈕、「⬅️ 回上一步」、「🔀 換一條罪名」全部保留：狀態鈕是備援（`advance()` 同一條路），後兩者仍是主持人專用；狀態鈕下方多一行 `.next-status`（`#cards-drive-note`）說明流程由卡片推進，下一案倒數期間改顯示同一個秒數。`onPlayerData` 的順序是 `nextGate.handle()` → `handleStep()` → `renderJuryTally()` → `allJuryVoted()`，頁面載入時若 `phase === 'idle'` 就 `ROOM.publish()` 一次讓卡片拿到開案鈕。**`kangaroo-court-beta/` 完全沒動**，Beta 仍是主持人按狀態鈕推進的舊流程。原本的主持人操作說明如下，現在是備援路徑：主持人依序按左側流程圖下方那顆狀態鈕（同時是 CTA，見下）推進：開放陪審團投票 → Prosecution 發言（只有一段論述，不分開庭／結辯）→ **Prosecution's Witness** → Defense 發言 → **Defense's Witness** → Cross-Examination（Prosecutor／Defense 輪流問 Defendant 問題、Defendant 必須回答）→ 陪審團**最終**投票（寫進另一個獨立欄位 `kcFinalvote`，跟 `kcPrevote` 分開存，因為兩票都要留到 Reveal 才一起讀出來比較，若共用同一個欄位會被最終投票蓋掉）→「⚖️ Reveal Verdict!」→（這局有 Judge 才會出現）**🧑‍⚖️ Judge's Ruling**（純儀式性的最後一步，沒有任何按鈕或功能——主持人按狀態鈕推進，把發言權交給 Judge，讓他當場口頭宣讀已經公布的判決；沒有 Judge 的局直接從 Reveal Verdict 跳去開下一案，不會出現這步，`FLOW_STEPS` 側欄清單也不會顯示這一格，靠 `judgeIdx >= 0` 動態過濾）。Reveal 揭曉的判決結果與完整陪審團明細，會一路留到 Judge's Ruling 這步結束才清空（`payloadFor()`／`renderJuryTally()` 都在 `phase === 'reveal' || phase === 'judgment'` 時顯示同一份結果），讓法官宣讀判決時，主持人畫面與所有玩家卡片上都還看得到剛公布的判決。每一方都是先發言、再傳喚證人補強論點，兩方之間依然交替出場、絕不連續兩步輪到同一方。

**🧑‍⚖️ Judge 開關**：ROOM 設定卡片（`room.js` 的 `extraRowHTML`）多一列「🧑‍⚖️ Judge」的無外框 ON/OFF 切換鈕（`toggle-btn`，跟 `say-it-without-saying-it.html` 的 Forbidden words 開關同一套視覺，配色換成 Judge 角色色紫 `#8b5cf6`），預設 ON；狀態存進同一包 room session（`ROOM.setExtra('judgeEnabled', …)`），換遊戲、重新整理都會保留。切換只影響**下一次** Draw a Charge：`dealRoles()` 只在開新案件時讀一次這個開關，正在進行中的案件不會被動到。關掉時該局不發 Judge，陪審團因此多一位（回到總人數 − 3），角色卡片、流程圖側欄、玩家連結欄的 Judge 相關 UI 都會自動不顯示；重新打開開關不會補發，要等下一次 Draw a Charge。**4 人局強制關閉 Judge**：Defendant／Prosecutor／Defense 三個角色已經佔滿 3 位，4 人局若再發一位 Judge 陪審團就會歸零、投票沒有意義，所以人數選到 4 時開關會整顆鎖住變灰、文字改成「OFF (needs 5+ players)」，`toggleJudge()` 這時點了也不會有反應；若原本 5 人以上且已開啟 Judge，中途把人數調到 4 會自動關閉（`onCountChange`），調回 5 人以上開關才會解鎖（但不會自動重新打開，要主持人自己再按一次）；`dealRoles()` 另外也擋了一層（`judgeEnabled && n >= 5` 才會抽 Judge），就算開關狀態不小心跟人數對不上也不會真的發出一個沒有陪審團的 Judge。**這款遊戲本身需要至少 4 人**（被告／檢察官／辯護律師三個固定角色，加上沒有法官時至少要留 1 人當陪審團），但房間人數是在 `index.html` Step 1 統一設定、六款遊戲共用的同一個數字，`ROOM.init()` 傳的 `counts: [4, 5, 6, 7, 8, 9]` 只是「這頁自己看得懂的範圍」，不代表可以把共用人數直接搬過來用——若房主在 Step 1 設定的是 3 人以下（Kangaroo Court 用不到的人數），這頁**不會**把 `ROOM.count` 偷偷墊高成 4 並存回共用的 `room-session-{CODE}`（那樣一來，房主晚點切回 `index.html` Step 1 會發現人數莫名其妙從 3 變成 4——這是實際發生過的 bug，根因是 `room.js` 的 `ROOM.init()` 以前不分青紅皂白地對每個遊戲頁都呼叫 `clampCount()` 把讀到的人數硬夾進該頁自己的 `counts` 範圍、夾完又經由 `ensureTokens()`／`save()` 寫回同一包 localStorage，等於任何一款 `counts` 範圍比首頁窄的遊戲，光是被訪問過一次就會悄悄覆寫全站共用的人數；`room.js` 現在改成只有非 `hideSetup`（目前只有 `index.html` 自己）才會夾範圍，六款 `hideSetup: true` 的遊戲一律照實讀共用人數，絕不回寫夾過的值）。取而代之，`fb-warning` 下方多一條同樣風格的 `#count-warning` 提示，`ROOM.count < 4` 時顯示「⚠️ Kangaroo Court 至少需要 4 位玩家…請回首頁把房間人數調高」，狀態鈕（`advance()`／`goBack()`／`dealNewCase()`）在人數不足時全部直接 return（`countOK()` 統一判斷），`renderStatus()` 也會把狀態鈕文字換成「🔗 房間人數不足（需要 4 人以上）」並加上 `not-clickable`，跟 Firebase 未設定時的鎖法一致；要玩，只能回 `index.html` Step 1 把人數調到 4 以上，這頁自己絕不會替房主做這個決定。全程沒有倒數計時，每個階段都由主持人自己掌握節奏、按狀態鈕才推進到下一步；狀態鈕在進入 Reveal 階段時會脈動提醒主持人該按下一步。狀態鈕下方另有一顆較低調的「⬅️ Previous Step」，可以退回上一階段修正誤按：由於每一方的證人只在各自的 Witness 階段抽一次、直到對方進入自己的 Witness 階段前都不會被覆寫，所以退回任何一個 Witness 階段都能拿回原本那位證人，不需要重抽。Pre-Vote（第一步）與尚未抽罪名時沒有東西可以退，按鈕會鎖住。

**⚔️🛡️ 傳喚證人（Witness）**：Prosecution's Witness／Defense's Witness 這兩步，各自從**目前的陪審團**裡隨機挑一位扮演證人（跟 Prosecutor／Defense 一樣的「目前次數最少」公平演算法，`witnessCount` 記錄每人被抽中次數；同一案子優先讓兩位不同的陪審員分別演雙方證人，`witnessThisCase` 記錄本案已用過的人，人數不夠（例如只有 1 位陪審員）時允許同一人連續扮演兩次），同時從新題庫 `game-data.js` 的 `kangarooWitness` 隨機抽一張證人卡（`{ emoji, name, evidence }`，`evidence` 刻意寫得簡短，一行放得下，例如「🧑‍🏫 The Old Teacher — remembers this exact behavior from years ago」）。主持人畫面此時：①罪名卡本身排版跟兩張證人卡看齊、統一走**小標題列（icon＋文字）在上、內容在下**的格式（emoji 縮小跟「⚖️ THE DEFENDANT IS CHARGED WITH…」小標籤同一行，罪名句子在下面一行；不像早期版本那樣把大 emoji 跟句子擠在一起硬撐出很高的卡片），高度因此跟下面的證人卡幾乎一樣高，三張卡疊在一起不會一大一小；②角色卡（這局有 Judge 的話依序是 Judge／Defendant／Prosecutor／Defense，Judge 固定排最前面；沒有 Judge 就只有後三格）旁多一格金黃色「🕵️ Witness — 某某」，正在傳喚的那一方（Prosecutor 或 Defense）跟 Witness 兩格同時發光（只在該方的 Witness 階段當下顯示，過了就消失；Judge 則是輪到 Judge's Ruling 那一步時自己發光）；③罪名卡下方是一份**證人記錄**（`#witness-log`），依實際發生的先後順序（永遠是 Prosecution 先、Defense 後）往下疊放——**傳喚過的證人會一路留在畫面上到這局結束**，不會在換下一步之後就消失：兩張證人卡**用不同顏色區分**、且跟角色卡的配色對應——Prosecution 的證人是橘色（跟 Prosecutor 角色卡同一色），Defense 的證人是藍色（跟 Defense 角色卡同一色），一眼就能看出這張是哪一方傳喚的；正在傳喚中的那一張標「⚔️/🛡️ Prosecution/Defense calls their witness…」並整張發光；已經問完的那一張標題改成過去式「called their witness」、發光效果收掉、透明度降到 .7，但內容（證人 emoji 跟角色名同一行顯示、下面接證物一句話＋「🎭 Played by 某某」，emoji 跟名字擠在同一行是為了省版面，兩張證人卡疊在一起時才不會太佔高度）原封不動留著，讓主持人分享畫面時，晚一步才專心聽的人也能回頭看到前面證人說過什麼；**Prosecution 的證人卡在 Defense／Defense's Witness 這兩步會暫時整張隱藏**（`renderWitness()` 的 `hideProsecution` 判斷），把畫面留給正在進行的 Defense 論述與證人，直到 Cross-Examination 開始才重新出現（Defense 的證人卡不受影響，一直都在），用「⬅️ Previous Step」從 Cross-Examination 退回 Defense's Witness 也會正確重新把 Prosecution 證人卡藏起來；重新 Draw a Charge 才會清空這份記錄。玩家卡片（`play.html`）依 `isWitness` 欄位切換：被抽中的陪審員看到金黃主題卡＋證人角色與證物提示（「Stay in character — improvise your answers!」）；正在傳喚的那一方看到「❓ Question your witness!」；其餘所有人（被告、另一方、其他陪審員）只看到一行「👀 某某 is testifying…」。**這款遊戲沒有計分**：Reveal 時只比較每位陪審員的預投票與最終投票，標示誰翻了票（往哪個方向），純粹當作話題／戰績參考，不換算成任何分數；**被告**的命運只看最終票數：Guilty **大於等於** Not Guilty（含平手、含沒人投票）就是有罪，只有 Not Guilty 真的較多才是無罪——這是一個 Kangaroo Court，平手不會給被告罪疑唯輕的好處。主持人畫面有一份「⚖️ Verdict」卡：投票期間顯示「N / M 位陪審員已投票」，Reveal 後改成完整結果——逐一列出每位陪審員的預投票→最終投票（翻票的會標一枚「flipped to Guilty」或「flipped to Not Guilty」的小標籤，純資訊、不帶分數）。玩家連結區用 `room.js`，Defendant／Prosecutor／Defense／Judge 的連結欄位分別用紅／橘／藍／紫框線標示身分（`is-role-defendant`／`is-role-prosecutor`／`is-role-defense`／`is-role-judge`）；Jury 的連結欄位不特別標色。玩家卡片（`play.html` 的 `game: 'kangaroo'`）依 `role` 與 `phase` 切換：非 Jury 在投票階段看到「🤫 陪審團正在投票」等待訊息；Jury 在 `prevote`／`finalvote` 階段看到 🚨 Guilty／✅ Not Guilty 兩顆大按鈕；輪到自己發言的 Prosecutor／Defense 看到「🎤 The floor is yours」，其他人看到「⚔️/🛡️ 某某正在發言…」；Cross-Examination 階段 Defendant 看到「🎯 Answer every question」、Prosecutor／Defense 看到「❓ Ask the defendant a question」、Jury 看到「👀 Watching…」；**Judge's Ruling** 階段（只在這局有 Judge 時才會出現）Judge 自己的卡片顯示「🧑‍⚖️ Deliver your ruling!」（純文字、沒有按鈕——這一步不需要任何互動，只是提醒該他上場口頭宣讀陪審團的判決），其他人看到「👀 某某 is delivering the verdict…」；Reveal 階段每張卡片都會顯示這局的判決結果，以及（若是陪審員）自己是否翻票。沒有 Firebase 時整局無法進行（陪審團的票必須保密，不能讓主持人畫面直接看到），會顯示警告並鎖住「Draw a Charge」按鈕。頁面最下方有**全部 30 條罪名**供主持人瀏覽，**預設收合**（按「👁️ 瀏覽全部 30 條罪名」才展開，避免分享畫面時暴雷）；主題色紅 `#e94560`（`index.html` 裡唯一沒被其他遊戲用過的預留色）） |
| `play.html` | 🔗 **全部八款遊戲**共用的玩家頁面：玩家開啟自己的私人連結後看到當下這局的內容，依 Firebase 資料中的 `game` 欄位自動切換樣式；同一個 Room Code + 同一批玩家連結可以在八款遊戲間直接切換使用，不用重新產生連結。所有卡片最上方都用**同一顆名字晶片**（同字級、同形狀）。**玩家開啟連結但主持人還沒發牌時**（`data.game` 是空的／未知），卡片顯示 `showError('waitingDeal')` 畫出的 `.secret-card.theme-waiting`：跟其餘八款遊戲共用同一個 `.secret-card` 卡片殼，只是用琥珀色（不借任何一款遊戲的主題色，免得等一下真的發牌時像是突然變了顏色）、一顆置中的 🎴、「你的祕密卡片」標籤（`waitingCardEyebrow`，跟頁面本身「你的祕密卡片／Your Secret Card」的品牌用詞一致，不是另外發明的「個人卡片」之類說法）、名字晶片（`.name-banner.is-neutral` 只描外框，不填色，理由同上）、一段說明這張卡片是做什麼用的文字（`waitingCardLead`）、分隔線、「⏳ 等待主持人發牌…」狀態字，以及一行「把這個分頁留著，不用重新整理」提醒（`waitingCardKeep`）。**這幾行字的字級調過三次**：第一次是從近乎正文大小的 `.72rem`／`.78rem`／`.92rem` 分別調到 `.85rem`（eyebrow）／`1.08rem`（lead）／`1.05rem`（狀態字）／`.92rem`（keep-note）；主持人拿實機 iPhone 截圖回報「文字大一點」，代表 devtools 裡看起來夠大的字級，搬到真的手機螢幕上（往往還帶一點縮放）還是偏小，所以第二次又整批調大到 `1rem`／`1.22rem`／`1.2rem`／`1.05rem`（狀態字的 `line-height` 跟著從 1.55 提到 1.6）。第三次主持人單獨針對「你的祕密卡片」這個標題再要求放大，其餘三行（說明段落／狀態字／keep-note）維持第二輪的字級不變：eyebrow 從 `1rem` 加到 `1.3rem`，同時把 `letter-spacing` 從 `.1em` 收到 `.08em`（大寫英文版「YOUR SECRET CARD」在更大字級加寬字距容易在窄螢幕頂到邊，先收窄一點抵銷）。三輪都只動字級（與這一輪的字距）、配色與版面結構不變，320–390px 手機寬度下都沒有溢位（實測 320px 最窄時，中文標題單行、英文版標題自然換成兩行「YOUR SECRET／CARD」，說明段落換行成五行，全部仍完整落在卡片邊框內）。**卡片上的大字永遠不會被從中間切斷**：核對卡的單字（`.card-word`）、Say It Without Saying It 的目標字（`.tt-word`）、You're In The Scene／Persuade Together 的角色名（`.rb-name`）、Kangaroo Court 的證人名（`.wb-name`）這四種「一個字就是整張卡」的文字，字級原本是寫死的、或由 `wordFontSize()` 依字串長度分級猜的——但那個猜法不知道這支手機有多寬，實測 320–390px 下 Neighborhood／Videographer／Procrastination 都會被拆成「Neighborho／od」這種前後看起來都不是字的兩段。現在 `showCard()` 改成先 `renderCard()` 畫完、再跑一次 `fitCardWords()`：用 Range 量出每個英文單字實際佔了幾行，只要有單字跨行就把字級調小 1px 再量一次，直到整個單字落在同一行或碰到各自的下限（18／18／16／14px）。刻意用「量」而不是再加一層字數分級，因為題庫可以從 Google Sheet 換成任何人寫的字（見 `sheet-data.js`），字數分級永遠追不上。中文不參與這個調整（`wordSplitAcrossLines()` 會跳過含 CJK 的字串）——中文本來就沒有空格、整句就是一個「字」，到行尾換行是正常排版，跟著縮小只會讓每張卡的說明文字莫名其妙變小。實測全部 taboo 目標字、200 個核對字、全部場景角色與證人名在 320／360／390px 三種寬度下都沒有任何單字被切斷，縮到最小的是 320px 的場景角色名 17px，其餘都在 21px 以上。**八款遊戲共用同一組玩家配色**，前六色順序就是 Dare Conquest 棋子的順序：1 紅 `#ef4444`／2 藍 `#4f9eff`／3 綠 `#22c55e`／4 黃 `#f59e0b`／5 紫 `#a78bfa`／6 青 `#06b6d4`，所以同一個人不管主持人切到哪一款遊戲，卡片顏色都不會變，也永遠對得上他在棋盤上的棋子。Dare Conquest 棋盤本身只有 6 顆棋子（人數上限 6），第 7、8 色（萊姆綠 `#84cc16`、粉紅 `#ec4899`）只給允許超過 6 人的遊戲用，目前只有 Kangaroo Court（4–9 人）會用到。晶片是**整塊填滿該玩家的顏色**、配深色文字（`#0b0b18`）：八個顏色配深字的對比度都在 5:1 以上（紅色最低，實測 5.19:1），彩色文字配深底則做不到八色都合格。四款「用手機作答」的遊戲各有一組樣式：`game: 'hottake'` 顯示題目＋👍/🤷/👎 三顆按鈕、`game: 'sophies'` 顯示情境（`🎯 情境` 標籤＋情境本文）＋ A/B/C 三顆選項按鈕——**情境原本刻意不印在卡片上**，理由是「留在主持人分享的畫面上，卡片再印一次只會讓大家低頭看手機」，但那個理由預設了每位玩家都看得清楚分享畫面；實際上玩家多半是拿手機在語音通話裡，分享畫面不是被通話 App 蓋住就是小到讀不了，結果變成非得請主持人再唸一次才答得出來（實際回報過），所以改成照印（兩者都寫進 `players/{token}/vote = { id, choice }`，可隨時改投）、`game: 'scene'` 顯示自己被分到的角色與提示、`game: 'persuade'` 顯示自己是 Judge 還是 Team（只有 Judge 有 ✅／❌ 判決鈕）——**這四款（hottake／sophies／scene／persuade）的卡片最下方另有 `nextBlock()` 畫出的「換下一題」區塊**（見 `next-round.js`）：payload 沒有 `nextId` 就整塊不畫，其餘時候是一顆該遊戲主題色實心的 `.next-btn`（第一題寫「抽第一題／抽第一個情境／抽第一個場景」，之後寫「換下一題／換下一個情境／換下一個場景／下一個場景」），`nextTap()` 寫進獨立的 `players/{token}/next = { id, go: true }`，不碰 `vote`，所以投完票再換題不會把票洗掉。**按下去不會馬上換題，而是每張卡片一起跑一段五秒倒數**（`.next-count`：一顆該遊戲主題色的倒數圈 `.nc-ring`＋「馬上換下一題／還在講的話，按下面停下來」），下面接一顆中性外框的「✋ 等一下，先別換」（`.nc-stop`，`nextStop()` 寫 `{ seq, stop: true }`），**任何人都能按**——手滑的是一個人，還在講話的可能是另一個人。倒數期間原本那顆換題鈕會整個消失（一次只有一個動作在畫面上）。秒數是卡片自己跑的：`syncNextCount()` 靠 payload 的 `nextSeq` 判斷要不要重新對時，然後用 `setInterval` 從 `nextIn` 每秒遞減，**整段倒數不寫 Firebase**，跟 Say It 的回合計時同一套做法。**第一題不倒數**（`round === 0`，卡片上什麼都還沒有）。送出後按鈕先變成停用的「換題中…」，**4.5 秒還沒得到回應就冒出琥珀色的 `.next-warn`**（倒數圈走到 0 之後又乾等 4.5 秒也一樣）：「沒有換成功 — 主持人的遊戲頁面要開著而且停在最前面（切到其他分頁或 App 時，手機/平板會把它凍結）」，按鈕同時回來讓他再按一次——能抽題的只有主持頁，它被凍結時卡片按什麼都不會有反應，這則提示是為了不讓它變成靜默失敗（實際回報過）。另外 payload 的 `nextCount` 小於自己的 `playerNum` 時（連結來自更大的舊房間、主持頁根本沒在聽這個座位）改印「你的連結是第 N 位，但房間只有 M 人」並不畫按鈕。這四款主持頁一載入就推 `round: 0` 的空 payload，卡片在還沒抽題時顯示名字晶片＋一句「還沒有題目…任何人都可以抽第一題」＋那顆按鈕，不再是「⏳ 等待主持人」（`scene` 沒有 `role` 但有 `nextId` 時走這條，沒有 `nextId` 才退回原本的 `waitingRoles`）、`game: 'taboo'` 依 `state` 與 `isGiver` 切換（**整局可以完全不碰主持人畫面**，見 `say-it-without-saying-it.html` 那一列）：`state: 'idle'` 或 `'done'` 且 payload 帶 `canDeal` 時，**每一個人**的卡片上都有一顆靛色實心的「🎬 發下一個題目」（`.taboo-deal`，`tabooDeal()` 送 `{ id: dealId, choice: 'deal' }`，quote 的是 `dealId` 而不是 `voteId`——回合之外沒有 roundId 可以引用）；自動續下一輪的那 5 秒不會出現（`canDeal` 為假），因為下一題本來就在路上。`state: 'ready'`（題目已抽、還沒開始）時 clue giver 看到一顆「👀 看題目」按鈕（按下去才會拿到目標字並讓全場開始倒數，見 `say-it-without-saying-it.html` 那一列），**其他每個人則先看到自己這一輪的角色**：裁判是琥珀色的「👀 你是裁判」＋「這輪你不用猜 — 專心聽 {描述者} 有沒有說出禁字」，其餘人是綠色的「🙋 你這輪負責猜」＋「{描述者} 開始形容之後，想到答案就按搶答鈕」，下面才接「{name} 是描述者」與「等 {name} 開啟題目…」＋⏳。原本這一段對「除了描述者以外的所有人」都只寫同一句「等 X 開啟題目」——那句話指名的是全場唯一已經知道自己要幹嘛的人，剩下兩三位根本不知道自己這輪是裁判還是猜的，要等題目開了、按鈕冒出來才知道，而那時候倒數已經在跑了（禁字關掉時沒有裁判，所以判斷是 `data.isRef && data.forbiddenOn !== false`，全部人都會看到「負責猜」）；`'playing'` 時 clue giver 看到目標字＋禁字、guesser 只看到禁字＋🙋／🚨 兩顆鈕、回合結束後所有人看到結果與答案；答對而自動續下一輪時（見 `say-it-without-saying-it.html` 那一列），答案下方還會多一塊 `.next-up`：左邊一顆倒數圈（卡片自己每秒遞減，`nextTimer` 靠 `next.seq` 對時，跟回合計時同一套做法），右邊寫出下一回合的描述者與裁判是誰；輪到自己的那張卡整塊變綠並持續脈動、文字換成「🎤 換你描述！」／「👀 你當裁判」，讓下一位在這 5 秒內就把手機拿起來。**主持人把某人標記「💤 暫時離開」時**（見 `say-it-without-saying-it.html` 那一列），那個人的卡片在 `ready`／`playing` 狀態下改成一張中性的「💤 你被標記為暫時離開」卡（分數與計時圈仍照常顯示，只是拿掉搶答鈕／裁判鍵），不會出現一顆按了也沒用、其他人卻在等的按鈕；這個判斷排在描述者判斷之後——描述者的卡永遠照描述者畫，因為 away 只影響下一次抽角色，不會把正在描述的人卡片拔掉。**卡片最下方是一份所有人的即時分數**（`.taboo-board`，不是只有自己那一分）：主持人畫面上本來就有大計分板，但玩家是低頭看自己的手機，只看到自己幾分根本不知道自己排第幾。資料走 payload 新增的 `board` 欄位（`[{ n: 名字, p: 分數, me }]`，主持頁的 `boardFor(i)` 依座位順序組出來，名字要一起送是因為卡片自己沒有名單；六列大約幾十個 byte，而且是搭在本來就會發生的那幾次寫入上，**不增加任何 Firebase 流量**）。每一列是一顆「名字＋分數」的小方塊，**自動換行排列而不是固定兩欄**：兩欄在 360–430px 下每欄只有約 130px，Christopher／Wolfgang-Amadeus 這種真實名字會被 ellipsis 切掉，等於白列一份看不出是誰的分數；換行排列讓短名字一行擠好幾個、長名字自己獨佔一行（實測 320／390／430px × 2／6／9 人 × 中英文名字，0 個被切斷、0 溢位；2 人局高度 75px，6 個中文名字 116px，最壞的 6 個長英文名字在 320px 是 280px）。自己那一列用**自己的玩家色**畫外框與淡底（跟名字晶片同一顆顏色，不用讀就知道哪一列是自己），分數用 `chipInk()` 調亮的同色（九個座位色各自實測，對比全部合格）。舊主持頁分頁送來的 payload 沒有 `board` 欄位時，會退回原本那行「🏆 N 分」而不是整塊消失。（禁字清單上方的小標籤全站統一寫「🚫 Forbidden words」，不再用「Never say these」等其他說法，跟主持人畫面右側的 Forbidden words 開關、`play.html` 其餘提示文字用詞一致）。`game: 'conquest'` 時卡片刻意做得很輕：自己的名字、**一條永遠都在的回合列**（大字顯示現在輪到誰：色球圖示＋名字，整條的框線、底色、光暈都用該玩家的棋子顏色；輪到自己時顯示「YOUR TURN」並持續明暗脈動），當下能按的按鈕、題型標籤，以及底部一行自己的戰績（🚩 N tiles · Round N）；Truth or Dare 選擇階段回合列下方會多一行小字（自己是「Truth or Dare?」、別人是「picking Truth or Dare…」）。題目文字**只有正在接受挑戰的那位玩家**看得到（他要照著做；其他人看主持人畫面就好，卡片再印一次只會讓大家低頭看手機）。按鈕依狀態切換：輪到自己且可擲骰時是「🔻 Low Roll (N)」「🎯 High Roll (N)」兩顆技能鈕加上「🎲 Roll the Dice」（棋盤忙碌中則全部變灰）、Truth or Dare 選擇階段是 😇 TRUTH／😈 DARE 兩顆、有題目在進行時，別人是 ✅／❌ 兩顆投票鈕（選過的那顆會亮起、可以改投）、當事人則是題目文字（不能投自己），其餘情況只顯示一個 ⏳。`game: 'kangaroo'` 依 `role`（defendant／prosecutor／defense／judge／jury）、`phase` 與 `isWitness` 切換——**這款的流程也由卡片推進**（見 `kangaroo-court.html` 那一列）：還沒開庭時（`!data.case`）卡片是名字晶片＋「還沒開庭…任何人都可以抽第一條罪名」＋ `nextBlock()` 的「⚖️ 抽第一條罪名」（沒有 `nextId` 才退回原本的「⏳ 等待下一個案件」）；每張卡片的 `shell()` 最下方固定接 `kcStepBlock()` 與 `nextBlock()`——前者只在 payload 給了 `stepId` 時畫出該階段的「✅ 我講完了／問完了／詰問結束／揭曉判決／宣讀完了」按鈕（`KC_STEP_LABEL` 依 `phase` 選字，顏色用該角色的色，`kcStep()` 寫 `players/{token}/kcStep = { id }`，送出後 4.5 秒沒換階段一樣冒出「主持頁沒在聽」的琥珀色提示），後者在判決之後畫「⚖️ 下一個案件」與五秒倒數（跟其他四款同一塊程式）。投票階段非陪審員的卡片多印一行「N / M 位陪審員已投票 — 全部投完會自動進入下一步」。Jury 在 `prevote`／`finalvote` 階段看到 Guilty／Not Guilty 兩顆按鈕（分別寫進 `kcPrevote`／`kcFinalvote`，兩個獨立欄位不互相覆蓋），其他角色看到「陪審團投票中」等待訊息；`prosecution-witness`／`defense-witness` 階段被抽中的陪審員（`isWitness: true`）卡片變金黃主題，顯示證人角色與證物提示，正在傳喚的那一方看到「❓ Question your witness!」，其餘人看到「👀 某某 is testifying…」；輪到發言的 Prosecutor／Defense 看到「🎤 The floor is yours」；Cross-Examination 階段 Defendant 看到「🎯 Answer every question」、Prosecutor／Defense 看到「❓ Ask a question」；Reveal 階段顯示判決結果（平手一律算 Guilty），陪審員另外會看到自己是否翻票；**`judgment` 階段**（緊接在 Reveal 之後、只在這局有 Judge 時才會出現）判決結果留在卡片上不會消失，Judge 自己另外看到「🧑‍⚖️ Deliver your ruling!」（純文字、沒有按鈕，這一步不需要互動，只是提醒該他上場口頭宣讀剛公布的判決），其他人看到「👀 某某 is delivering the verdict…」——這款遊戲不計分，全部只是資訊性顯示。`game: 'cardcheck'` 不屬於任何一款遊戲，是 `room.js`「🔍 Send Card Check」核對連結用的：卡片只有名字晶片＋大大的字＋emoji＋「Tell the host what you see!」，沒有任何按鈕或互動，純粹唸出來給主持人核對。`game: 'buttoncheck'` 同樣不屬於任何一款遊戲，是 `room.js`「🔢 Send Button Check」核對用的（見 `room.js` 那一列）：卡片顯示名字晶片＋一句「主持人會口頭喊出一個數字」的說明＋三顆上中下堆疊、各自滿版寬的**綠色實心**數字按鈕（`.checknum-btn`，跟地圖遊戲「🎲 Roll」骰子鈕同一套「這是主要動作」配色，往下 4px 的實色陰影做出立體按鍵感、按下深綠＋白邊並往下位移，數字是兩位數 10–99，見下方 `room.js` 那一列的說明），按了寫進獨立的 `players/{token}/checkPress = { id, value }`（跟 `vote` 分開存，因為這張卡片不代表任何一輪真正的遊戲），下方文字隨之從「⏳ 等待主持人喊出數字…」換成「✅ 你按下了：N」。**`cardcheck`／`buttoncheck` 這兩張卡套用了等待卡片（`theme-waiting`）的設計概念**：主持人覺得等待卡片（見上面「⏳ 等待主持人發牌…」那段）比這兩張核對卡好看，兩者本來就是同一類東西——都是「跟這一輪真正的遊戲無關、用來告訴玩家一件跟網站本身有關的事」的卡片，只是原本套的是最陽春的 `.secret-card` 殼，沒有標籤、沒有光暈、提示字級也是預設大小，擺在做過一輪視覺加強的等待卡片旁邊像沒做完的除錯畫面。做法是**共用同一組樣式、不重新發明**：`.secret-card.theme-cardcheck`（`cardcheck`／`buttoncheck` 共用同一個 theme class）疊加跟 `waitingGlow` 同款的 3 秒吐息光暈（`checkGlow`，色相沿用這個主題本來就有的石板灰 `rgba(148,163,184,…)`，只是換個名字而不是複製貼上一份一樣的 keyframes）；名字晶片上方多一顆跟 `.waiting-eyebrow` 同字級同粗細的 `.check-eyebrow` 小標籤，寫死這張卡是做什麼用的（「🔍 核對卡片」／「🔢 按鈕測試」，兩個新 i18n key `cardCheckEyebrow`／`buttonCheckEyebrow`）——核對字卡片本身已經有大 emoji＋大字當作視覺主體，不需要再解釋；按鈕核對卡沒有這個主視覺，所以原本掛在 `.pick-sub`（跟其他遊戲共用、字級偏小）的說明文字改成新的 `.check-lead`（其實就是 `.waiting-lead` 換個名字，字級與行高一致），讀起來才跟等待卡片的說明段落一樣好讀；兩張卡片的 `.secret-hint`（底部狀態字）也比照等待卡片放大到 1.15rem／行高 1.6，不是全站預設的 .85rem。**核對字卡片的大 emoji／單字本身不動**——那已經是這張卡最大最主要的元素，等待卡片放大字級是因為它原本除了圖示什麼都很小，兩者起點不同，不需要套一樣的放大幅度。對比度全部量過（eyebrow 7.09:1、lead／hint 6.60:1、按鈕本身 7.34:1、按下狀態 7.13:1），320–390px 手機寬度下兩種語言都沒有溢位。**後續主持人要求把這套風格套用到全站所有玩家卡片，不只這兩張與等待卡片**：於是拆成三個可以獨立套用、風險各不相同的部分。①**呼吸光暈**：`theme-conquest`／`theme-hottake`／`theme-sophies`／`theme-scene`／`theme-taboo` 這五款卡片一律是純 class、從不用 `style=` 覆寫顏色，所以直接照抄 `waitingGlow`／`checkGlow` 的公式，各自新增一個同名邏輯的 `pulseX` keyframes（`pulseConquest`／`pulseHottake`／`pulseSophies`／`pulseScene`／`pulseTaboo`），兩個關鍵影格分別是該主題原本的 `box-shadow` 與加深加大的版本，套進各自的 `.secret-card.theme-X` 規則。**`theme-persuade` 與 `theme-kangaroo` 分成「還沒分身分」與「已經分身分」兩種狀態，只有前者能套用**：後者會用 `style="border-color:${hex};box-shadow:0 0 60px ${hex}2e"` 依裁判／隊伍或依角色（被告／檢察官／辯護律師／法官／陪審團）動態換色，而 CSS animation 只要正在跑，就會**整段時間覆蓋掉它所動畫的那個屬性**，就算是 inline style 也贏不了——如果讓 class 層級直接套用呼吸動畫，已經分身分那張卡的角色專屬顏色會被動畫每一影格蓋回主題預設色，等於角色配色整個失效。做法是新增一個範圍更窄的 `.is-idle-glow` 修飾類別，只掛在「還沒分身分」那個 render 分支的 HTML 上（`persuade` 的 `!data.situation && !data.team`、`kangaroo` 的 `!data.case`），呼吸動畫只寫在 `.secret-card.theme-persuade.is-idle-glow`／`.secret-card.theme-kangaroo.is-idle-glow` 這種更精確的複合選擇器上；已經分身分的版本完全不動，維持原本的靜態角色配色。**Kangaroo Court 還沒抽案件時**沒有專屬的主題色（`.secret-card.theme-kangaroo` 本身沒設 `border-color`／`box-shadow`，退回 `.secret-card` 基底的石板灰），色值跟 `theme-cardcheck` 完全一樣，所以它的 `.is-idle-glow` 直接**重用** `checkGlow`，不用再刻意寫一份數值相同的 keyframes。②**卡片底部狀態字放大**：`.secret-hint` 的基礎字級從 `.85rem` 直接調到 `1.15rem`（行高 1.6），等於全站每一款遊戲卡片的底部提示字自動跟上；`theme-waiting` 自己那個更大的 `1.2rem` 覆寫（見上面「你的祕密卡片」那三輪調整）維持不動、不因為統一而縮回去。③**卡片內文字放大**：`.pick-sub`（每一款遊戲卡片上「現在該做什麼／為什麼看不到按鈕」那類說明句，功能上等同 `.waiting-lead`／`.check-lead`）從 `.85rem` 調到 `1.05rem`（行高 1.55）——沒有直接套用 `.waiting-lead`／`.check-lead` 的 1.15～1.22rem，是因為 `.pick-sub` 在 Taboo、Kangaroo Court 這類版面本來就比較擠的卡片上，還兼著印很短的單行標籤（不是完整句子），實測過這兩款在 320–390px 下最擠的幾種狀態（描述者卡片：目標字＋六個禁字藥丸＋提示；陪審團待機卡：計分板＋Deal 按鈕＋說明）都沒有溢位，才定案在這個字級，沒有再往上加到跟等待卡片一樣大。三項改動都只動字級／行高與新增的 `box-shadow` 動畫，不動任何版面結構與顏色本身，對比度不受影響（跟字級無關，只跟顏色有關，量測結果與调整前相同）；實測涵蓋 hottake／sophies／scene／persuade（待機與已判角色兩種）／taboo（描述者、待機＋計分板）／conquest／kangaroo（待機、陪審團）在 320／390px、中英文下均無溢位。**這次的統一範圍只到 `play.html` 的共用 `.secret-card` 卡片殼**：`lets-talk.html` 的玩家卡片走完全獨立的 `talk-ui.js`／`talk.css`，`dare-conquest.html` 主持人畫面本身也不是玩家卡片，兩者都不在「秘密卡片」這套視覺語言的家族裡，這次沒有連動修改。**上面那次統一只放大了卡片上的「說明文字」（`.pick-sub`／`.secret-hint`），卻漏了卡片上真正最重要的元素——按鈕本身的文字**：主持人隔一輪回報「所有遊戲的玩家卡片的按鈕內的文字都太小了」，回頭查才發現多顆按鈕的字級本來就比周圍說明文字還小、这次放大說明文字反而把這個落差襯得更明顯：`.pick-btn`（A/B/C、Agree/Disagree、Taboo 的 Correct!/Not it!、Kangaroo Court 的 Guilty/Not Guilty、Persuade Together 的 Convinced/Not convinced，共用同一顆 class）原本 `.95rem`、`.conq-vote`（Did It!/Failed!）原本 `.98rem`、`.conq-td`（😇 TRUTH／😈 DARE）原本 `.95rem`、`.conq-skill`（🔻 Low Roll／🎯 High Roll）原本 `.85rem`——全站最小的按鈕文字，比任何一行說明句都小、`.taboo-foul`（🚨 Taboo!，裁判舉發鍵）原本 `.85rem`、`.nc-stop`（✋ 等一下）原本 `.95rem`。這幾顆全部調到 `1.1rem`（`.conq-skill` 因為標籤還帶著「(N)」次數、且卡片本身版面較窄，保守到 `1.05rem`），統一成跟 `.taboo-buzz`／`.taboo-deal`／`.next-btn`／`.conq-roll`（原本就有 1.05～1.15rem，這幾顆沒有這個問題）差不多的字級——玩家會拿手機盯著的是「要按哪一顆」，這是全卡片最需要一眼就看清楚的文字，理應不小於（甚至該大於）旁邊的輔助說明，而不是像原本那樣反而是卡片上最小的字。逐一實測 Dare Conquest（擲骰／技能鈕、TRUTH／DARE 選擇、Did It!／Failed! 投票）、Say It Without Saying It（裁判 Taboo! 鍵、描述者 Correct!／Not it! 裁決鍵）、Pick a Side（Agree/It Depends/Disagree）、Kangaroo Court（Guilty/Not Guilty）在 320／390px、中英文下皆無溢位或換行問題，對比度不受影響（只動字級，顏色沒變）

**Say It Without Saying It 的分數顯示現在有三種模式，卡片依 `data.boardMode` 分流**（設定入口在 `say-it-without-saying-it.html`，見那一列）：`'full'`（沿用原本 `.taboo-board`／`.tbd-rows` 那份完整名單，行為不變）、`'none'`（`score` 這個區塊整個印成空字串，卡片上完全不出現任何分數資訊）、`'compact'`（**新的一種畫法**，只印房間目前最低分～自己的名字與分數～最高分，例如「0 ～ Player 2 (20) ～ 20」）。三者互斥，一個 host tab 舊到還沒有 `boardMode` 欄位時（只送 `board` 陣列）會自然落到 `Array.isArray(data.board)` 那條既有分支，不會顯示空白。`'compact'` 的畫面是新的 `.tbd-compact`：兩顆低調的 `.tbdc-edge`（純數字，室內最低／最高分）夾著一顆 `.tbdc-mine`（自己的名字＋分數，跟 `.tbd-row.is-me` 同一套「用自己的玩家色畫外框與淡底」處理——`border: 1.5px solid var(--me)` 配 `background: color-mix(in srgb, var(--me) 16%, #0d0d1a)`，讓「這是我」在只印三個數字的精簡版裡依然一眼可辨），中間用純文字的 `~` 當分隔（`.tbdc-sep`）。落後加分中還是會在 `.tbdc-mine` 後面掛上同一顆 ⚡ 圖示（見下一段），下方也會出現同一句 `boostMine` 完整說明。

**「🔥 ×2」徽章改成純圖示 `⚡`，不再印文字**（設計決策與理由見 `say-it-without-saying-it.html` 那一列，這裡只記卡片端的實作）：`.tbd-boost`（全場分數列表版）與新的 `compactBoard.x2`（精簡版）用的是同一顆縮小版（17×17px）圓形圖示，`title` 屬性放完整說明（新增 `boostBadgeTitle` i18n key，跟主持人畫面的 `boostBadge` 講同一句話），玩家自己落後加分中時卡片下方那句完整的 `boostMine`（「🔥 落後加分中：你這輪的得分 ×2」）維持原樣不變——徽章本身收斂成圖示，但「這件事現在正在發生、對我有什麼影響」這句完整的話還是要講清楚，不能只靠一個 ⚡ 讓玩家自己猜。**`awaySub`（暫離卡片下方的說明文字）也跟著改了用詞**：原本寫「安心，你的分數還在」，這句話已經不成立（見下一段，暫離現在會立刻歸零分數），改成明講「你的分數已經歸零，且這局遊戲剩下的時間都不會再獲得 ×2 落後加分」，避免暫離的玩家看著舊文案以為自己分數沒事。

**玩家可以在自己的卡片上申請暫時離開，按下去之前有一次確認、講清楚後果**：`shell()`（`tabooCardHTML()` 組裝每一種狀態畫面共用的外殼函式）在 `${score}` 後面固定接一段 `tabooAwaySelfBlock(data)`，`data.away` 為真時整段印空字串（已經在暫離卡片上，沒有東西好再申請）。沒暫離時預設是一顆低調的外框按鈕「💤 我要暫時離開」（`.taboo-away-ask`，故意做成沒有底色的次要按鈕——這是一顆很少會按到、按下去也不該搶過遊戲本身按鈕視覺份量的功能鍵，`data.awayLocked` 為真時直接 `disabled` 並在下面補一行「至少要留 2 位在場玩家，現在無法離開」，跟主持人畫面自己那排暫離勾選鈕鎖住時的邏輯與門檻完全一致，見 `say-it-without-saying-it.html` 那一列的 `awayLocked` 欄位）；點下去不會馬上送出，而是**這張卡片本身**换成一塊琥珀色確認面板（`.taboo-away-confirm`）：一句「確定要暫時離開嗎？你的分數會立刻歸零，之後也不會再獲得 ×2 落後加分優勢」，下面兩顆按鈕，安全的答案（👌 取消，我還在）排前面用綠色、會動用到的答案（😥 確定，我要離開）排後面用琥珀色——跟主持人畫面「結束回合」「Taboo! 裁決」那幾個確認面板同一套「安全答案先講、顏色分開」的慣例。**這是純粹的本機 UI 狀態，不透過 Firebase 來回**：`let tabooAwaySelfConfirm = false` 是一個模組層級的旗標，`tabooAskAway()`／`tabooCancelAway()` 只是切換它再呼叫 `showCard(latestData)` 重新畫一次目前這份資料——之所以不能讓這個「正在確認中」的狀態活在 Firebase 或某個由 `data` 推導出來的欄位裡，是因為 `renderCard()` 是**整份 HTML 重建**（見上面「玩家開啟連結但主持人還沒發牌時」那段對 `showCard()`／`renderCard()` 的說明），任何跟這次確認完全無關的欄位變動（哪怕只是別人分數改了、`board` 陣列跟著重算）都會觸發同一份卡片重繪，若確認狀態不是活在獨立於 `data` 之外的模組變數裡，畫到一半的確認框會被下一次無關的資料推播直接沖掉。確定送出時 `tabooRequestAway(btn)` 直接把 `{ choice: 'setAway' }` 寫進 `players/{token}/vote`，**不像其他遊戲動作那樣經過 `tabooSend()`**（那個共用函式要求先有 `latestData.voteId` 才送得出去）——暫離跟主持頁的 `deal` 一樣，不管現在有沒有一輪在進行、甚至根本還沒開局都要送得出去，沒有 `voteId` 可以引用，見 `say-it-without-saying-it.html` 的 `handleAnswer()` 那段。實測：申請按鈕在 `awayLocked` 時正確鎖住、點了沒有反應；點「我要暫時離開」→ 確認面板正確出現，文字提到分數歸零與失去 ×2 優勢；點「取消」→ 面板收起、主持端 `away` 完全沒被動到；重新點開、點「確定，我要離開」→ 主持端 `away[num-1]` 變 `true`、分數歸零、`catchUpBlocked[num-1]` 為真，卡片自己也在下一次資料推播時正確變成「你被標記為暫時離開」的中性畫面；320／390px 中英文下確認面板與兩顆按鈕皆無溢位，文字對比 9.37:1、取消鈕 7.34:1、確定鈕 8.02:1，皆合格 |
| `firebase-config.js` | 八款遊戲共用的 Firebase Realtime Database 設定（host 與 play.html 共用，需自行申請免費專案並填入；全部共用同一個 `rooms/{roomCode}/players/{token}` 路徑。沒有 Firebase 時八款遊戲分成三種狀況：①**主要按鈕會被真的鎖住**的有 **Kangaroo Court**（陪審團的票必須保密）、**Say It Without Saying It**（目標字沒有地方私下給 clue giver 看）與 **Crack the Word**（`deal.disabled = !ROOM.enabled`，按鈕文字同樣改成「🔗 Player links needed」——原因跟前兩款不一樣：字母與答案本來就不怕主持人畫面看到，鎖住是因為沒有 Firebase 就沒有玩家連結，遊戲的核心就是分散在不同手機上的字母按鈕，主持人自己用畫面上的備援按鈕整局代打，等於一個人分飾多角，遊戲直接失去意義）這三款，**沒有任何「👁️ Reveal」之類的主持人畫面備援能繞過這個鎖**；②其餘各款只是關掉卡片互動、遊戲本身照常可玩） |
| `room.js` | 🔗 **共用的玩家卡片層**（`ROOM`），**七款遊戲共用**（Pick a Side / Sophie's Choice / Persuade Together / You're In The Scene / Say It Without Saying It / Kangaroo Court / Crack the Word；只有 `dare-conquest.html` 因為設定畫面長得不一樣，保留自己的實作）。**這七款遊戲現在全部帶 `hideSetup: true`**——房號／人數／姓名／連結這一步已經搬到 `index.html` 的 Step 1 統一做過一次，遊戲頁自己不再需要重複那份設定 UI，`ROOM.init()` 收到 `hideSetup: true` 時會整個跳過下面這段「設定畫面 UI」的完整渲染邏輯（`buildDOM()`／`render()` 開頭各自多一條 `if (cfg.hideSetup) { … return; }` 分支），改走新函式 `renderCompact()`：不畫 ROOM 列／PLAYERS 人數鈕／room-hdr／room-note／Copy All Links／Send Card Check／姓名輸入框／Link／QR，只保留 `cfg.actionsHTML`（例如 Say It Without Saying It 的難度按鈕）與 `cfg.extraRowHTML`（例如 Kangaroo Court 的 Judge 開關、Say It Without Saying It 的 card-preview 開關）這兩個「真的是遊戲功能」的掛點，下面另外重新畫一份**唯讀**的名單：只列出「有 `setDecorator` 回傳 `.answer`」的玩家（沒發牌或還沒作答的人不出現），每一列是靜態的名字文字（不能編輯）＋該玩家的 `answer` 內容，`cls` 沿用同一套 CSS class（`.link-col.is-role-defendant` 這類），所以六款遊戲原本就有的角色/狀態配色完全不用改就能在精簡版底下正常顯示。要臨時調整人數或補發連結，回 `index.html` 的 Step 1（`✏️ Edit room setup`）處理，不能再從遊戲頁裡改。**精簡模式下那張動作卡片與下面的玩家名單之間要有間隙**（`.room-card + .room-links { margin-top: 18px }`）——它們是直接相鄰的兩個 sibling，中間沒有任何東西，原本是貼著 0px，兩塊面板黏在一起會被看成一個壞掉的框；完整版的設定畫面因為中間還隔著 room-hdr／room-note／兩顆按鈕，這條相鄰選擇器不會命中，所以只修到需要修的那一邊。以下這段「設定畫面 UI」完整版的行為說明，現在只適用於 `hideSetup` 不為 true 的情況（目前只有 `index.html` 自己的 Step 1 是這樣用）。負責：房號與 token 的產生與保存、`room-session-{CODE}`／`room-last-session` 的讀寫（**跟其他五款遊戲完全共用**，人數與姓名雙向同步；`clampCount()` 只在 `!cfg.hideSetup` 時套用——也就是目前只有 `index.html` 自己會把讀到的人數夾進 `cfg.counts` 範圍。六款 `hideSetup: true` 的遊戲讀到共用人數時一律照實使用，就算比自己宣告的 `counts` 範圍還小或還大也不會夾，因為夾完的值會經由 `ensureTokens()`／`save()` 寫回同一包共用 session，等於把首頁設定的人數悄悄改掉；哪一款遊戲需要更多人才能玩（例如 Kangaroo Court 的 4 人下限），要由那款遊戲自己判斷並鎖住按鈕、提示回首頁調整，不能靠夾人數硬湊出一個不存在的玩家）、設定畫面 UI（ROOM 列 ／ PLAYERS 人數鈕 ／ 一位玩家一欄的姓名＋Link／QR，排版與 `dare-conquest.html` 完全一致，主題色由 `accent` 參數決定；每位玩家一欄的卡片用 CSS Grid 排版（`.room-links { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); }`，不是舊版的 `flex-wrap`）：220px 這個下限在這個網站實際會出現的版面寬度下自然算出「每排最多 3 欄」，人數不是 3 的倍數時，最後一排沒填滿的卡片會維持跟其他卡片一樣的寬度、不會被拉伸撐滿整排（例如 5 人局時第 4、5 位不會有一張佔滿整排的巨大卡片）；2–3 人這種本來就不到 3 欄的情況，`auto-fit` 會讓卡片照樣平分整排寬度，不會出現空白欄位。Link 按鈕複製成功後文字會短暫變成 Copied、按鈕本身也會綠色填色並跑一次放大彈回的 `copyPop` 動畫再變回 Link，`btn._copyRevert` 確保連續快點時只有最後一次點擊的還原計時器生效，`.btn-copy` 帶 `min-width: 0` 避免窄螢幕下文字收縮不了、把 QR 按鈕擠出卡片外）、`publish()` 推播到每位玩家的 token 節點並寫 roster、每個 token 各自掛一個 listener 回收玩家的回覆（`attach()` 的 handler 把 `cfg.onPlayerData` 包在 try/catch 裡並 `console.error`：驅動一整輪的卡片訊息——搶答、投票、換下一題——全都走這條路，某一款遊戲的重畫噴一次錯就會讓那一頁從此收不到任何玩家動作）。姓名輸入框每打一個字就會觸發 `onNameChange` → `render()` 重繪整份連結列表（讓 decorator 能即時反映），但重繪不會動到正在輸入中的那個欄位本身——`render()` 會先把目前有焦點的 `.name-input`（如果有）從 DOM 摘下來，重繪其餘欄位後原封不動插回同一個節點並呼叫一次 `focus()`，值、游標位置、選取範圍全部沿用同一個 DOM element，不重新 `createElement`；若每次都重新建立並事後用 `setSelectionRange` 還原，在連續多次重繪下會有時機差、丟字（實測：打字打到一半、空白鍵後面的字元完全消失）。同一個 handler 也不再對每個字元呼叫 `.trim()`（那樣會在打出空白的當下就把它吃掉，導致打不出兩個字的英文名字），改成只在讀取端（`ROOM.name()`、`cardURL()`、`shareText()`）trim。`room-note` 下方固定有一顆「📋 Copy All Links」按鈕（`.room-btn.primary`，跟 Load 按鈕同一套外框樣式；只在 Firebase 啟用時顯示），一次把目前所有玩家的連結複製到剪貼簿，每位玩家一段「姓名＋換行＋連結」、段落之間空一行分隔，方便主持人整包貼到聊天室；複製成功後文字變成綠色的「Copied」並跑 `copyPop` 動畫（跟每欄自己的 Link 按鈕共用同一顆動畫、同一種「連續快點只有最後一次計時器生效」的 `_copyRevert` 寫法），1.5 秒後變回原本文字。這顆按鈕在 DOM 裡緊接在 `room-note` 之後、`room-links` 之前，若遊戲自己想在同一行加一顆按鈕，兩顆按鈕會自然並排在同一行（`say-it-without-saying-it.html` 的「👁️ Show what's on player cards」目前是透過下面的 `extraRowHTML` 掛上去，不再用 `insertAdjacentHTML` 手動插入）。緊接著 Copy All Links 右邊是「🔍 Send Card Check」（`.room-btn.check-btn`，自己一套天藍色 `#38bdf8` 外框＋淡藍底色，跟 New Room／QR 那種中性灰的次要按鈕區分開——原本這顆是刻意跟 New Room 走同一套中性灰、不跟 Copy All Links 的強調色搶視覺，但實測下來太不明顯、容易被忽略，所以改成自己專屬的藍色調：既不是會被誤認成「跟 Copy All Links 同一個動作」的粉紅強調色，也不是容易被忽略的灰色），發完連結、正式開局前的一次性檢查：從 `game-data.js` 的 `forbidden`（200 個日常單字，每個都帶 emoji，人數上限 8 遠遠夠用）洗牌抽出跟目前人數一樣多、互不重複的字，`ROOM.publish()` 蓋成 `{ game: 'cardcheck', word, emoji }` 送給每個人，玩家卡片（`play.html` 的 `game: 'cardcheck'`）只顯示名字晶片＋大大的字＋「Tell the host what you see!」，主持人就能一個一個問「你卡片上是什麼」，答案要跟自己畫面上看到的對得起來，對不起來就代表兩人的連結拿反了。送出後不會馬上變成答案卷——`checkWords` 有值只代表「已經發出去」，`room-links` 底下要等主持人聽完每位玩家唸出卡片內容、自己按下另一顆「👁️ Reveal Answers」按鈕（`revealCardCheck()`，設定獨立的 `checkRevealed = true`）之後，每一欄名字連結卡片才會冒出一排「emoji 字」小字（`.check-word-badge`，虛線分隔，跟 say-it-without-saying-it.html 的 card-preview 一樣是主持人限定的私人資訊；只印該字自己的 emoji，不再多加一個放大鏡圖示）；沒按 Reveal 之前，旁邊只有一行提示「🔍 Check words sent — have each player say what they see out loud first, then tap "Reveal Answers" to check them against the real thing.」＋「👁️ Reveal Answers」與「✖️ Clear」兩顆按鈕，按下 Reveal 之後提示改成「✅ Answers revealed — re-deal the game whenever you're done checking.」且 Reveal 按鈕消失、只留「✖️ Clear」（純文字按鈕，點了把 `checkWords` 與 `checkRevealed` 一起設回 `null`／`false` 重繪，不會動 Firebase 上的資料）。這個「發送」與「顯示答案」刻意分兩步，是因為核對的意義在於主持人**還沒看到答案之前**先聽玩家自己唸出來，若送出當下就顯示答案，主持人等於是自己先偷看了答案，核對就失去意義。這是一次性的覆蓋：`ROOM.publish()` 用的是 `.set()`，會直接蓋掉玩家卡片上原本那款遊戲真正發的資料，所以要在還沒開局、或兩輪之間用；核對完之後主持人只要照原本流程重新發牌（`Deal a Word`／`Draw a Charge`…）就會把 `game` 欄位蓋回真正的遊戲，卡片自然變回正常內容。換人數、換房號（`setCount`／`loadRoomCode`／`newRoom`）都會把 `checkWords` 清空，避免舊的答案卷對到新的人。

**🔢 Send Button Check（第二種核對）**：同一個子步驟裡的第二顆同款按鈕（同一個 `.check-btn` class、同一套天藍色），但**刻意不會跟「🔍 Send Card Check」一起出現**——早期版本兩顆按鈕一開始就並排顯示，試玩回饋是「畫面太亂，看了不知道要先按哪一顆」。現在這個子步驟被設計成**嚴格照順序、一次只出現一個下一步該按的按鈕**：一進來只有「🔍 Send Card Check」；按下去才冒出「👁️ Reveal Answers」（跟 `check-note` 一起，見上）；按下 Reveal 之後（`checkRevealed` 變 `true`），「🔢 Send Button Check」才出現。`render()` 裡 `btncheck-btn` 的隱藏條件是 `!live || !checkRevealed`（不是像核對字自己的按鈕只看 `!live`），所以在核對字送出但**還沒**按 Reveal 的那個中間狀態，畫面上除了核對字自己的 Reveal／Clear 之外，什麼都不會多長出來；核對字整個還沒送出（`checkWords` 是 `null`）時 `checkRevealed` 自然也是 `false`，一樣不會提早出現。核對字只能證明「玩家看得到卡片、連結沒拿反」，但看得到不代表按得下去——手機瀏覽器版本太舊、觸控事件被某個 App 蓋住、網路根本沒連上 Firebase，都會讓卡片顯示正常卻按什麼都沒反應，而核對字完全抓不到這種情況。這顆按鈕測的是「按鈕按下去，主持人這邊看得到」：按下後隨機抽 3 個不重複的**兩位數**數字（10–99，`checkNumbers`；原本是 1–9 的個位數，太好猜／太容易不看卡片就跟著鄰座喊的答案按，改成兩位數逼玩家真的要看自己的卡片；90 個候選值遠比抽 3 個還多，直接用一個 `Set` 邊擲邊收集到 3 個不重複值即可，不用像個位數時那樣洗牌整個固定池再切片），`ROOM.publish()` 把這**同一組**數字分別發給每位玩家，**但每張卡片各自洗牌成不同的按鈕排列順序**（`checkNumbers.slice().sort(() => Math.random() - 0.5)`，每個 `payload` 函式呼叫各洗一次），這樣主持人才能只喊一次「大家按 7」就讓全場一起動作，而不是每個人的「7」都長在螢幕同一個位置——如果順序一樣，隔壁的人瞄一眼螢幕位置就能跟著按，測不出「這個人有沒有真的看懂自己卡片上寫什麼」。玩家卡片（`play.html` 的 `game: 'buttoncheck'`）用跟 A/B/C 選項同一套 `.pick-btn` 語言另外做了三顆數字鍵（`.checknum-row`／`.checknum-btn`）。**經過三輪試玩調整**：第一版三顆並排、背景 `#0d0d1a`（跟 `.pick-btn` 一樣，但那個顏色**就是頁面本身的底色**，`.pick-btn` 靠圖示＋文字硬撐出「這是按鈕」的感覺，光禿禿一個數字沒有這層線索，回饋是「看不出來是按鈕」）；接著改成同一排、亮一階的底色配自創的立體陰影；後來數字改成兩位數（見下）需要更寬的版面，加上主持人要求「不要三顆擠在同一排」，`.checknum-row` 從 `flex-direction: row` 改成 `column`，三顆變成**上中下堆疊、各自滿版寬**的長條按鈕，同時把顏色換成**實心綠**——但一開始是自己配了一套「深色底＋往下 4px 硬陰影＋按下時 `translateY(3px)`」的立體按鍵效果，主持人指名要「參考擲骰子那顆按鈕」，才發現地圖遊戲的「🎲 Roll」（`.conq-roll`）本來就是同一個綠、卻走完全不同的路數：柔和發散的光暈陰影（`box-shadow: 0 6px 18px rgba(34,197,94,.25)`）、按下去只是單純 `scale(.98)`，沒有位移也沒有色調變化的 hover。**現在 `.checknum-btn` 直接照抄 `.conq-roll` 的配方**（背景／外框／深綠字／陰影／按下效果全部逐項對應，只有 `border-radius` 從 14px 沿用一致），確保這是「全站同一種綠色按鈕」而不是恰好同色但手感不同的另一種控制項。按下之後的那顆（`.pressed`）**不換色系、只加深＋鑲一圈白邊光暈**（`background: #166534; border-color: #fff; box-shadow: 0 0 20px rgba(255,255,255,.35)`），維持「三顆都是綠色按鈕」的一致性，同時清楚標出「這是你按的那顆」。按了會把 `{ id, value }` 寫進獨立的 `players/{token}/checkPress`（跟 `vote` 分開存，因為這張卡片不屬於任何一輪真正的遊戲），`id` 對應的是 `sendButtonCheck()` 當下發出去的 `checkButtonId`，重新送出或清除都會換一個新 id，讓上一輪按過的殘留值不會被誤判成這一輪按的。**這顆核對沒有「顯示答案」那道關卡**——跟核對字不同，核對字的重點是主持人**還沒看到答案前**先聽玩家唸出來，這裡完全沒有這個顧慮：三個數字是主持人自己按下按鈕當下就決定好、本來就攤在自己畫面上的（`btncheck-note` 直接印出「已送出這三個數字：**5, 1, 4**」，數字用 `<strong>` 標成天藍色，因為這是主持人接下來要口頭唸出來的東西，必須一眼找到），沒有「主持人偷看答案」這回事，所以按下去之後就是即時看結果，沒有中間的保密步驟。**按下的結果會即時顯示在主持人畫面上**：`attach()` 內部原本只在 `cfg.onPlayerData` 有提供時才會處理玩家訊息（多數遊戲頁自己接手重畫），但 `index.html` 這個精靈本身不是遊戲、沒有提供這個 callback，所以額外加了一條「`checkNumbers` 有值時，收到任何玩家資料就呼叫一次 `render()`」——只在按鈕測試真的在進行中才會這樣做，其餘時間（`checkNumbers` 是 `null`）完全不受影響；這個子步驟的姓名本來就是唯讀的（`linksView === 'check'`），不會有正在輸入中的姓名欄位被這個重畫打斷。每位玩家的連結卡片下方用跟核對字一樣的 `.check-word-badge` 樣式（虛線分隔）顯示「🔢 等待按下…」或「🔢 按下了：N」（`is-pressed` 時字轉綠色），讀的是 `attach()` 本來就會自動更新的 `data[i+1]`，不需要另外接一條資料管線。API：`ROOM.init({ mount, accent, counts, defaultCount, linksNote, actionsHTML, extraRowHTML, hideSetup, onPlayerData, onCountChange, onNameChange })`（`hideSetup: true` 即上述精簡模式）、`ROOM.publish(payload 或 i => payload)`（`.set()`）、`ROOM.update(...)`（`.update()`，不會蓋掉玩家的作答）、`ROOM.clearAnswer(i)`、`ROOM.setDecorator(i => ({ marked, cls, answer, name }))`（`cls` 讓各遊戲把自己的狀態色畫在欄位上，例如 Say It Without Saying It 的 `is-giver-link`、Kangaroo Court 的 `is-role-defendant`／`is-role-prosecutor`；`name` 是選填欄位，`renderCompact()` 唯讀名單畫姓名時優先讀它、沒有才退回 `api.name(i)`，給的是「這個座位這場遊戲裡實際上是誰在玩」而不一定是房間共用的姓名——目前只有 Say It Without Saying It 的替補姓名功能會回傳這個欄位，其餘遊戲的 decorator 不設就完全不受影響，見那一列的說明）、`ROOM.setExtra(key, value)`／`ROOM.getExtra(key)`（把該遊戲自己的設定存進同一包 room session，例如 Kangaroo Court 的 `judgeEnabled`；經由這組 API 存取才不會被別的遊戲覆蓋掉）、``ROOM.setLinksView('names' | 'link-only' | 'qr-only' | 'check' | 'full')`（切換 `render()` 要畫姓名欄／Link／QR／核對字這幾塊的哪幾塊，非 `'names'`／`'full'` 時姓名欄改成唯讀；`'qr-only'` 直接展開 QR code、不透過原本一次只能開一個的 `toggleQR()`；**展開的 QR code 本身是一條連到該玩家卡片的連結**（`toggleQR()` 把 SVG 包進一個 `<a target="_blank" rel="noopener">`，`href` 是用 `a.href = cardURL(i)` 指派上去而不是串進 HTML 字串，這樣玩家名字裡有什麼都跑不出屬性外），掃描本來就是它的用途，但主持人常常人就在放著它的那台機器前面——要檢查某位玩家的卡片、要在第二個螢幕打開、要把筆電遞過去——原本只能複製連結再貼上；只有 `index.html` 的 Step 1 精靈會用到，預設 `'full'` 等同舊版行為，其餘六款 `hideSetup: true` 的遊戲完全不受影響）、ROOM.enabled / count / names / code / answers / name(i)`。沒有 Firebase（或 CDN 連不到）時自動隱藏 ROOM 列與連結按鈕、標題改成「👥 Players」，遊戲照常運作 |
| `i18n.js` | 🌐 **全站雙語切換層**（`I18N`），**十個頁面共用**（`index.html`、`play.html`、`room.js` 服務的七款遊戲，加上獨立實作的 `dare-conquest.html`）。**預設英文，畫面右上角一顆「中文／EN」切換鈕**（樣式與 DOM 由 `i18n.js` 自己注入，各頁不用另外寫）可切到繁體中文。**只有存著明確的 `'zh'` 才是中文**（`readLang()` 比的是 `=== 'zh'`，`setLang()` 用同一條，不然兩者會對不上）：沒設定過、清過瀏覽器、值壞掉，通通是英文。這是一個練英文的聊天室，而且主持人看到的第一個畫面就是語言選擇那一步——落在英文、旁邊擺著「中文」，順序才對；反過來是先要人讀一整頁中文，才問他要不要英文。**選到的那一邊是填滿該頁 `var(--accent)` 配 `--btn-ink` 深字**（跟全站主要動作同一套處理），未選的那一邊是膠囊上的灰字。原本選到的是 `#2a2a52`、底下的膠囊是 `#22224a`——差一個色階，實測對比 **1.16:1**，在被縮放又被視訊壓縮過的分享畫面上根本看不出來現在是哪一種語言（實際回報過）；膠囊本身也跟著壓暗成 `#1a1a38`，讓那顆選中的晶片浮出來。**尺寸維持原本那顆小膠囊（37×104px，padding 3px／按鈕 6px 13px／12px 字）**，改的只有「哪一邊亮起來」——曾經放大過並在左邊加一顆 🌐，但主持人畫面上這顆鈕不是主角，佔的版面比它值得的多，已經改回來（`.is-floating` 那個放大版本也一併移除，沒有停靠點時就是原尺寸浮在右上角）。這顆鈕**不是浮在畫面上的，而是「停靠」進該頁本來就有的頂列**（遊戲頁與 Let's Talk 是 `.topbar`，首頁是 `.hero`）：`buildToggle()` 找到那一列就 `appendChild` 進去並加上 `.is-docked`（`position: static`），找不到才退回原本 `position: fixed` 釘右上角的寫法。原因是釘住的版本在 1400px 以下會**蓋住每一款遊戲的「回合 N／案件 N」**（實測 320／390／768／1280px 全中，只有 1920px 因為內容置中才閃得開）；同一段注入的 CSS 另外給 `.topbar > a.back` 加 `margin-right: auto`（auto margin 會在 `space-between` 之前吃掉剩餘空間，所以返回連結仍然靠最左、回合計數器跟切換鈕併排在右邊），並讓 `.topbar`／`.hero` 可以換行，手機寬度下切換鈕會掉到第二行而不是把頁面撐寬。停靠的選擇器是 `[data-i18n-dock], .topbar, .hero`——第一個是通用掛點，讓任何一頁不必改 `i18n.js` 就能提供位置，目前沒有任何一頁用到它（留著是因為它讓新頁面不必改這支檔案就能指定位置）。**`play.html` 沒有頂列，所以它走的就是 `position: fixed` 浮在右上角這條退路**：曾經替它做過一列 `.card-tools`（跟卡片同寬、靠右對齊）讓鈕進到版面裡，但那一列只為了一顆小膠囊就吃掉一整條高度，已經拆掉——它浮在卡片右上角本來就不擋任何東西。這一頁另外在自己的 `<style>` 裡寫一行 `#i18n-toggle { --accent: #6366f1; }`，因為 `play.html` 自己沒有 `--accent`，不給的話選中的那一邊會退回全站紅。**停靠點刻意是 `.header` 之前的那一列**，因為 `guide.js` 遮蔽的是「`.header` 之後的所有 body 子元素」——放在頂列，說明頁開著的時候照樣切得動語言（`guide.js` 那條「跳過 `#i18n-toggle` 不遮」的判斷因此在七款遊戲頁上都不會命中了，但留著，它保護的是沒有頂列、切換鈕退回 `append` 到 body 尾端的情況）。選擇存進 `localStorage`（key: `site-lang`），跨頁、跨遊戲共用同一個語言設定，且套用範圍是**整個瀏覽器**而不是單一房間——這跟 `room-session-{CODE}` 那組房間資料是完全獨立的兩包 `localStorage`。**翻譯範圍的界線**：主持人畫面上「跟遊戲主內容無關」的所有介面文字——按鈕（含推進遊戲流程的動作鈕，例如 Kangaroo Court 的「⚖️ Draw a Charge」、Say It Without Saying It 的「🎬 發一個題目」、Dare Conquest 的「🎲 Roll the Dice」都會翻譯）、狀態列、標籤、說明文字、警告訊息、題庫瀏覽區的分類標題——預設全部顯示英文，可切成繁體中文；**兩種東西永遠不翻譯、兩種語言下都固定顯示英文**：①每頁最上方的遊戲名稱標題（`<h1>`，例如「⚖️ Kangaroo Court」「🙊 Say It Without Saying It」），視為品牌名稱；②`game-data.js` 裡的所有題目內容本身（罪名、真心話/大冒險題目、兩難劇本、Taboo 目標字與禁字、證人證詞等）——這些就是 CLAUDE.md 開頭说的「英文單字／劇情／問題」，是遊戲的英文教學素材，故意不跟著切換。API：`I18N.registerDict(namespace, { key: { zh, en } })` 讓每個頁面（或 `room.js`）登記自己的一份翻譯表，`I18N.t(namespace, key)` 依目前語言取字串（找不到則退回 `en`、再退回 key 本身）、`I18N.onChange(fn)` 讓頁面在切換語言的當下重新呼叫自己的 render 函式（否則已經畫出來的動態內容不會自動換字）、`data-i18n="namespace.key"` 屬性讓純靜態 HTML 文字自動被套用與同步（`I18N.applyStatic()` 在每次切換語言時全文件重新掃一次，不管這個節點是何時被誰插進 DOM 的，所以就算是 `room.js` 的 `buildDOM()` 或某款遊戲自己動態组出來的 `actionsHTML`／`extraRowHTML` 字串，只要帶了這個屬性一樣會被抓到並同步更新）。每個頁面的做法都是同一套：`<script src="i18n.js">` 放在 `<head>`、盡量早（不依賴任何其他腳本，其他腳本可以放心晚載入），頁面自己的 inline script 裡用一個簡短的小 helper（`room.js` 是 `rt(key)`、`play.html` 是 `pt(key, vars)`，帶 `{placeholder}` 字串替換）包一層 `I18N.t()`，取代原本寫死的英文字串。沒有這個腳本時（理論上不會發生，因為每頁都會載入）`I18N` 是 undefined，各頁呼叫端有做 `typeof I18N !== 'undefined'` 的防呆，不會直接噴錯 |
| `guide.js` | 📖 **共用的玩法說明層**（`GUIDE`），八款遊戲頁全部掛著，**預設顯示、但可以關掉**。`init()` 第一件事是讀 `localStorage` 的 `site-guide`（跟 `site-lang` 一樣是整個瀏覽器共用的一包），**只有剛好是 `'0'` 才直接 return**——不遮蔽、不插入任何節點，頁面維持它自己的腳本原本設定好的樣子。要關掉是在首頁 Step 2 取消勾「進入遊戲前先顯示玩法說明」（見 `index.html` 那一列）。**比較的是「不等於 `'0'`」而不是「等於 `'1'`」**，兩邊都是：沒設定過、瀏覽器被清掉、或這台裝置根本沒開過首頁，通通會看到說明——還沒做過選擇的人，比較可能就是需要它的那個人。`index.html` 那顆勾選框必須用同一個比較，不然沒碰過它的裝置上會出現「勾選框沒勾、遊戲頁卻顯示說明」。以下說明是顯示時的行為：讀完按下按鈕才會進到下一步——**說明頁 → 設定頁 → 主遊戲畫面**。中間那一段「設定頁」只有真的有自己設定的兩頁才存在（`say-it-without-saying-it.html` 的難度／禁字／秒數精靈、`dare-conquest.html` 的 Start Game 卡片），這兩頁的按鈕文字是「▶️ 前往遊戲設定」；其餘六款的房間設定早就在 `index.html` 的 Step 1 統一做完了，頁面上本來就沒有任何設定可調（Kangaroo Court 的 Judge 開關是唯一例外，但它是一顆掛在 `room-mount` 上的開關，不是一個獨立畫面），所以刻意**不替它們生一個只有「繼續」按鈕的空設定頁**——那只是多一次點擊，不是一個步驟，這六頁的按鈕文字直接是「▶️ 開始遊戲」。頁面的接法只有兩行、不用改各頁的 inline script：`<body data-guide="taboo" data-guide-next="setup">`（`data-guide` 對應 `guide-data.js` 的 key，`data-guide-next` 是 `setup` 或 `game`，只決定按鈕文字）加上 `<script src="guide-data.js">`／`<script src="guide.js">` 兩支。**遮蔽的做法**：`init()` 把 `.header` 之後的所有 body 子元素加上 `.guide-veiled`（`display: none !important`），這是一個**跟各遊戲自己在用的 `.hidden` 完全獨立的開關**——兩者不互相覆蓋，所以拿掉遮罩之後，頁面會回到它自己的腳本原本就設定好的狀態（`dare-conquest.html` 的 `#game-area` 仍然躲在 `.hidden` 後面、`fb-warning` 仍然只在真的沒有 Firebase 時才出現），不會因為說明頁而被強制顯示。遮蔽期間各頁的腳本照常執行（`ROOM.init()`、Firebase listener 都正常掛上），只是畫面先不給看。**唯一被跳過不遮的是 `#i18n-toggle`**：不特別排除的話，說明頁開著的時候就切不了語言（實際踩過）。八款遊戲頁現在都有 `.topbar`，語言鈕會停靠進去、落在 `.header` **之前**，所以這條判斷實際上不會命中；留著是為了沒有頂列的頁面——那時 `i18n.js` 會退回把鈕 append 到 body 尾端，剛好落在「`.header` 之後」的範圍裡。**說明頁關掉之後不會留下任何按鈕**：原本標題下方有一顆低調的「📖 玩法說明」可以隨時叫回說明，已經整顆拿掉（`.guide-reopen` 這個 class 與它的 CSS 也一併刪除）——主持人畫面是分享出去給所有參與者看的，一顆自己幾乎不會按的東西沒有理由常駐在上面。說明頁每次進頁面還是先看到它，按「▶️ 開始遊戲」才進遊戲；要重看就重新整理該頁。**但玩家不用等主持人讀完**：六款由卡片推進的遊戲（Say It／Pick a Side／Sophie's／Scene／Persuade／Kangaroo）在主持頁收到第一個被接受的卡片動作時——`nextGate.handle()` 回傳 true、Say It 的 deal 分支、Kangaroo 的 `handleStep()`（現在回傳布林）與陪審團全員投完的自動推進——都會呼叫各頁的 `cardDrove()`（`if (window.GUIDE) GUIDE.close()`），把說明頁收掉、露出遊戲畫面；不然玩家已經在手機上開打，分享出去的畫面卻還停在玩法說明，主持人連遊戲開始了都不知道（實測六款：說明開著 → 卡片按第一題 → `#game-guide` 收起、回合 1 發出）。`close()` 因此改成已關閉就直接 return（多了 `isOpen()`），否則之後每一次卡片動作都會把主持人捲回頁面最上方。`window.GUIDE = { open, close }` 仍然保留，需要時可以從 console 叫回來。切換語言時會用 `I18N.onChange` 就地重畫，**不會把主持人踢回說明頁**（正在看遊戲畫面就完全不動）。樣式在 `shared.css` 的「Game guide」段，顏色一律走各頁自己的 `var(--accent)` |
| `guide-data.js` | 📖 七款遊戲的說明內容（`GAME_GUIDES`），**中英雙語**——這是主持人畫面的介面文字而不是遊戲題目，所以跟著 `i18n.js` 的語言設定切換（`game-data.js` 的題目與每頁的英文遊戲名稱則永遠是英文，見 `i18n.js` 那一列）。每款遊戲一個 key（`taboo`／`hottake`／`sophies`／`persuade`／`scene`／`conquest`／`kangaroo`，對應各頁 `<body data-guide="…">`），內容是 `players`（建議人數）＋`tagline`（一句話講完這款遊戲）＋`sections` 陣列。每個 section 有 `icon`、`title`，以及下列四種內容**擇一**（`guide.js` 依哪個欄位存在決定畫法）：`paragraphs`（段落，用於「這是什麼遊戲」）、`steps`（自動編號，用於「怎麼玩」）、`roles`（`{emoji, name, desc}` 卡片，用於「角色與功能」——`dare-conquest.html` 用了兩個 roles 區塊，分別講三種道具與四種特殊格，因為那款遊戲每個人的角色其實一樣，真正要解釋的是道具與格子）、`tips`（項目符號，用於「主持人小提示」）、**`phones`（手機示意圖，用於「玩家手機上會看到什麼」）**。`phones` 是為了**會發 secret card 的遊戲**做的：那張卡片是全場唯一不能出現在分享畫面上的東西，所以主持人自己也沒看過，光用一句「每個人會拿到一個字」講不出它長什麼樣子——這個區塊直接把它畫出來。每支手機是 `{caption, card}`，`card` 的欄位全部選填：`name` ＋ `color`（1–8，就是 `play.html` 那組玩家配色）畫出填滿色的名字晶片、`emoji`／`word` 是卡片正中央的大字、`hint` 是分隔線下方那行小字、`buttons` `[{label, cls}]` 是該卡片真的有的按鈕（`is-green`／`is-red`／`is-choice`／`is-accent`；`is-accent` 是填滿該頁 `var(--accent)` 配 `--btn-ink` 深字的主要動作，目前只有 Say It 的「🎬 發下一個題目」用）；區塊另可帶一個 `note`，印在手機下方講「光看圖看不出來的那件事」（例如 Say It Without Saying It 的猜題者卡片上什麼都沒有，這正是禁字不能發給他們的原因）。樣式在 `shared.css` 的 `.guide-phone`／`.gp-*`，**數值是照著 `play.html` 的實際卡片等比縮小抄過來的**（邊框 2px、圓角、名字晶片、6rem emoji…），原因跟 `is-green`／`is-red` 那組按鈕一樣：`play.html` 跟主持人頁沒有共用任何樣式表。卡片顏色走各頁自己的 `var(--accent)`，本來就與 `play.html` 的該款主題色相同。`card` 另外還有這些選填欄位，各自對應 `play.html` 的一個真實元件：`chip`（`{label, hex}`，`.pick-chip` 角色標籤）、`text`（`.pick-text` 情境本文）、`target`（`{emoji, word}`，`.taboo-target` 那個框起來的目標字）、`pills`（`{label, items}`，`.taboo-banned` 的紅色禁字藥丸）、`role`（`{key, name, hint}`，`.role-box` 角色框）、`sub`（`.pick-sub` 灰色小字，排在按鈕之前）。渲染順序固定為 name → chip → text → emoji → word → target → pills → role → sub → buttons → hint。另有 `turnbar`（`{name, color, suffix}`，`.conq-turnbar` 那條「現在輪到誰」的色條，只有 Dare Conquest 用得到）。**七款全部都有這個區塊**，每一款畫的是「這一頁最值得用圖講的那件事」，不是把卡片抄一遍：`taboo` 五張並排：兩輪之間任何人都有的「🎬 發下一個題目」、開題目之前的 clue giver（只有一顆「👀 看題目」）、開了之後的 clue giver、裁判、猜題者（最後一張明顯矮一截，因為猜題者什麼都看不到）、`kangaroo` 陪審團的秘密投票鈕＋被抽到的證人卡、`scene` 兩位玩家同場景不同角色（別人的提示看不到）、`persuade` 裁判卡有判決鈕、團隊卡沒有、`sophies` 情境＋A/B/C 三顆鈕（跟 `hottake` 一樣，題目要印在卡片上，投票當下要能自己讀）、`conquest` 輪到自己 vs 別人在挑戰時的兩種按鈕組合。`sophies`／`hottake` 各只有一張，因為它們沒有「不同人看到不同東西」的對照可講。**卡片上的文字一律照抄 `play.html` 的 I18N 條目**（`clueGiverChip`／`forbiddenWordsLabel`／`tabooFoulBtn`／`buzzInBtn`／`guiltyBtn`／`prevoteSecret`／`youPlay`…），不要自己另外寫一套說法，理由跟按鈕那條一樣。常用的四個區塊標題抽成 `GUIDE_TITLES` 共用，不用在七款遊戲裡各寫一次。**撰寫原則是「重點摘要」而不是文件**：一個概念一句話、一個步驟一句話、一個角色一行，`paragraphs` 通常只有 1 段、`steps` 3–5 條、`tips` 2–3 條——寫長了主持人根本不會讀，這是實際看過完整版之後砍掉一半的結果。可以講「為什麼」（例如為什麼猜題者看不到禁字、為什麼平手算有罪），但要壓縮成一句。**說明裡提到按鈕時一律把按鈕畫出來，不要用文字描述**：檔案開頭的 `B(cls, label)` 會吐出一個真的 `<button>`，`cls` 直接掛**該頁自己的按鈕 class**（`btn-primary`／`btn-draw`／`btn-deal`／`spin-btn`／`td-choice-btn td-truth`／`status-line`／`kc-back-btn`…），所以畫出來的東西跟主持人待會兒真的要按的那顆長得一模一樣，不必另外維護一套仿樣式；`BROW(...)` 把多顆併成一排（`display:flex`），`VOTE()` 則是用 `pick-a-side.html` 自己的 `.vote-btn` 標記重建 Pick a Side 的三顆投票鈕（含長條與計數）。`label` 必須跟該按鈕在**那個語言**下的實際文字一致，來源是各頁 I18N dict 裡的 `xxxBtn` 條目，不要自己另外寫一個說法。玩家卡片（`play.html`）上的按鈕在主持人頁沒有對應 class，改用 `shared.css` 定義的 `is-green`／`is-red`（實心填滿，對應 Buzz in!／Taboo!／判決鈕）與 `is-choice`／`is-choice is-tint-red`／`is-tint-green`（外框式，對應 A/B/C 與陪審團 Guilty／Not Guilty），色值直接抄自 `play.html`。`players` 欄位（顯示在最上面那顆膠囊裡）**七款一律寫成範圍**（`2–6 人`／`4–9 人`），不要出現「N 人以上」這種另一種格式；沒有上限的遊戲就寫到 9，因為 `index.html` Step 1 的人數鈕本來就只到 9，寫成開放式只會讓八頁的膠囊長得不一樣 |
| `shared.css` | 所有遊戲頁共用樣式 + 主題變數。**主持人畫面上的每一顆按鈕都有看得見的底色**——這是一條全站規則，不是某一頁的裝飾：`:root` 定義 `--btn-face`（`#22224a`，明顯比頁面底色 `#0d0d1a` 亮的石板色）、`--btn-face-hi`（hover）與 `--btn-ink`（`#050510`，填滿強調色時的深色字——原本是 `#0b0b18`，但那讓最亮的靛色 Deal 鈕只有 4.37:1，再壓深一點全部過線）。**主要動作**（`.btn-primary`／`.btn-deal`／`.btn-draw`／`.spin-btn`／`guide-start`／Kangaroo 的狀態鈕／首頁的「下一步」與 Continue）一律是**實心強調色＋深色字＋淡淡的同色陰影**；**其餘所有按鈕**（`.btn-secondary`／`.reset-btn`／`.deck-toggle`／`.timer-btn`／`.toggle-btn`／`.level-btn`／`.filter-btn`／`.ps-btn`／`.scenario-item`／`.substep-dot`／`.substep-nav-btn`／`.method-btn`／`.edit-setup-btn`／`room.js` 的 `.room-btn`／`.room-count-btn`／`.btn-copy`／`.btn-qr-toggle`／Dare Conquest 的 `.td-choice-btn`／`.timer-ctrl-btn`／`.btn-judge`／`.btn-swap`／Kangaroo 的 `.kc-back-btn`）一律坐在 `--btn-face` 上，Pick a Side 的三顆投票鈕則各自用自己顏色的淡色填滿。原因：原本大多數按鈕是「深底＋彩色外框」，那個深底（`#13132b`／`#1a1a38`）跟頁面底色幾乎分不出來，投影出去只看得到一個框，**看起來像標籤而不是可以按的東西**——Taboo! 裁決鈕就是這樣被誤認成下面那顆 Deal 鈕的（見 `say-it-without-saying-it.html` 那一列）。`index.html` 不載入這個檔案，所以它自己的 `<style>` 開頭複製了同樣三個 token（`room.js` 兩邊共用，寫成 `var(--btn-face, #22224a)` 帶 fallback）。加新按鈕時比照辦理：**不要再出現底色等於頁面底色的按鈕**。

**第三條全站規則：顏色一律走 token，不要再寫死 hex。**改配色時只動 `:root` 那份清單，不用在十一個檔案裡逐處搜尋替換，而且每一處顏色都會保持一致（曾經有過同一種「面板灰」在不同頁面漂成三種相近但不同的值）。token 分成三組，**依「這個顏色在做什麼」而不是「它長什麼樣」來挑**：①**面與線**：`--bg`（頁面）／`--surface`（卡片面板）／`--surface-2`（面板裡的面板）／`--surface-b`（漸層的暗側）／`--border`（一般細線）／`--border-2`（要被看見的線）／`--border-3`（按鈕自己的外框）／`--btn-face`／`--btn-face-hi`／`--btn-off`（disabled）；②**文字**：`--text`／`--text-dim`／`--text-muted`（見上一條規則）；③**狀態色**：這裡有一個關鍵區分——**同一個顏色當「形狀」和當「文字」是兩件事**。亮到足以當實心底色的綠，疊在自己身上當字就讀不到了。所以凡是畫**形狀**（底色、框線、光暈）的地方，那些鮮豔的 hex **照樣寫死**（沒有人要讀它們）；凡是畫**文字**的地方一律走 `--ok-text`／`--ok-text-lit`／`--bad-text`／`--bad-text-soft`／`--warn-text`／`--warn-text-soft`／`--gold-text`／`--info-text`／`--violet-text`／`--accent-text`／`--accent-alt-text`。要整體調整全站文字的亮度，改這一排就夠了。**per-card 的 accent 要自己派生**：首頁八張遊戲卡各自用 `--accent` 蓋掉自己的主題色，`--accent-text` 若只定義在 `:root` 就會拿到全站紅，所以 `.game-card` 自己算一份 `color-mix(in srgb, var(--accent) 55%, #fff)`。**實心強調色填滿的按鈕一律配 `--btn-ink` 深字，不要配白字**。全站目前有 155 處「面與線」與 207 處「文字」走 token；改配色之後要跑一次對比稽核。

**第二條全站規則：任何人要讀的字，對比一律 ≥ 4.5:1（大字 ≥ 3:1）。**這頁不是給人安靜閱讀的網頁，是主持人**分享桌面**投影出去的畫面，先被縮放、再被視訊壓縮一次才到觀眾眼前——在開發機上只是「比較低調」的灰，到那一端就整個不見了。實際掃描全站（11 頁 × 中英 × 每一種遊戲狀態 ＋ 110 份真實玩家卡片 payload）曾經一次抓到 56 種不合格的樣式，最糟的是三款抽劇本遊戲抽卡前那句「按下抽取揭曉情境」（`#333`，**1.44:1**）與 Kangaroo 的「按下 ⚖️ 抽一條罪名」（`#444`，1.87:1）——這兩句正是「現在該按哪一顆」的唯一說明。所以 `:root` 的灰階不再是隨手挑的 `#333`～`#888`，而是三個量過的 token：`--text`（`#eee`，16.6:1）／`--text-dim`（`#9999bb`，7.0:1，內文，以及**任何長在按鈕面上的字**）／`--text-muted`（`#82829f`，5.2:1，頁面或卡片上的小標籤）；`--text-faint`（`#2a2a50`）只畫線與浮水印，**永遠不拿來寫字**。為什麼按鈕面上的字要用比較亮的那一個：`--text-muted` 在頁面底色上是 5.2:1，但在 `--btn-face`（`#22224a`）上只剩 4.0:1。

強調色同理：**能當實心底色，不代表能當字**。`:root` 因此多一個 `--accent-text`（shared.css 預設 `var(--accent)`），凡是**用強調色寫字**的地方都走它，邊框、填色、光暈則維持原本的品牌色不動（那些是形狀，沒有人要讀）。目前四頁需要覆寫：say-it 靛 `#6366f1 → #8183f4`（最亮的強調色，當字只有 3.4:1）、index／kangaroo 紅 `#e94560 → #ec5f76`、scene 粉 `#ec4899 → #ee57a1`；橘／琥珀／青／綠本來就夠亮，不用覆寫。`room.js` 跟著多一個 `--room-accent-text: var(--accent-text, ${accent})`，所以 Copy All Links／Link／QR 這些鈕的字自動跟著各頁走。說明頁裡引用玩家卡片按鈕的那組 class 另外多了 `.guide-btn.is-accent`／`.gp-btn.is-accent`（填滿 `var(--accent)` 配 `--btn-ink`），給 Say It 卡片上那顆發題鈕用；一樣是照著 `play.html` 的實際樣式抄過來的，不是另外發明一套。`play.html` 的角色晶片（`.pick-chip`）顏色是 JS 內嵌的，改用 `chipInk(hex)`＝`color-mix(in srgb, ${hex} 78%, #fff)`：晶片的框線與 10% 淡底維持角色原色，只有字被調亮。填滿強調色的按鈕一律配 `--btn-ink` 深字，不要配白字（首頁與 say-it 的精靈圓點、「下一步 →」原本是白字，只有 3.8／4.5:1，改成深字之後同時也符合上面那條「實心強調色＋深色字」的規則）。

驗收方式不是用眼睛看：`scratchpad` 裡那支稽核腳本會把每個文字節點的**實際合成底色**（一路往上疊父層的半透明背景）量出來算對比，跑遍所有遊戲狀態與所有卡片 payload，目前是 **0 種不合格**。改動配色之後要再跑一次。純 emoji 的節點要跳過——emoji 是彩色字形，`color` 根本不會畫到它，不跳過會一直誤報。（另有 `.guide-btn`／`.guide-btn-row`：說明頁裡引用的按鈕只把「塞進句子裡會壞掉」的部分中性化——`width: auto`、清掉 margin 與入場動畫、`pointer-events: none`——顏色/邊框/圓角一律留給該頁自己的 class，所以需要 `!important`，因為各頁的 `<style>` 一定晚於這個檔案載入）：頁首、`.hidden`、`.fb-warning`、`.section-hdr`、`.btn-primary`／`.btn-secondary`。`.header` 底下**只有 `<h1>`**，原本每頁 h1 下方那行說明玩法的 `<p>`（例如 say-it-without-saying-it.html 的「One player describes a secret word...」）已經全部拿掉——那是主持人分享桌面時，畫面上一段跟當下操作無關的小字，七款遊戲的主持人頁全部清乾淨，`.header p` 這條 CSS 規則也一併刪除（`dare-conquest.html` 自己覆寫的那份也刪了）。顏色一律走 `var(--accent)`（次要按鈕 hover 用 `--accent-alt`，Firebase 警告條用 `--warn`），每個頁面只要在自己的 `<style>` 開頭寫一行 `:root { --accent: … }` 就換色完成；形狀真的不一樣的頁面（例如 `dare-conquest.html` 的 `.section-hdr` 是 flex 排版）在自己的 `<style>` 覆寫即可，因為頁面樣式一定晚於這個檔案載入 |
| `scenario.css` | 三款「抽劇本」遊戲（Sophie's Choice／You're In The Scene／Persuade Together）共用的版面：劇本大卡、`.filter-bar` 類別篩選、`.btn-draw` 抽卡鈕、下方可瀏覽的完整劇本清單。同樣用 `--accent` 換色 |
| `game-data.js` | 所有遊戲的題目資料（hottake 20 句；persuade / scene 各 10 題；sophies 10 則兩難劇本；forbidden 200 個日常單字，供 `room.js`／`dare-conquest.html` 的 Send Card Check 核對連結用；conquest 50 則互動挑戰指令（Dare），供 Dare Conquest 使用，**分成五大類型、每類 10 題**（🎤 Singing 唱歌／🗣️ Voices & Sounds 變聲與音效／🎭 Improv & Roleplay 即興與角色扮演／💬 To Their Face 當面稱讚或吐槽／🧠 Word Games & Memory 文字遊戲與記憶），每題有 `type`（五大類型）、`cat`（該題自己的標題，50 題各不相同，顯示在挑戰卡的小標籤上）、`text`、`seconds`；`{player}` 會被隨機替換成另一位玩家的名字（50 則中有 30 則帶 {player}，題目盡量設計成需要跟其他玩家互動），全部 50 則的 `seconds` 欄位一律為 45，內容以簡單好懂的用字為主，**句子刻意寫短、盡量不用俚語或需要文化背景才懂的說法**（因為使用者是在練習英文口說的初學到中階學習者，太長太繞或太美式俚語的句子唸出來反而聽不懂在玩什麼——這是實際發生過的使用者回饋，原本的版本句型偏長、也用了像 spirit animal／yearbook quote／opposite day 這類需要額外文化背景才懂的說法）。因為參與者沒有鏡頭，題目一律寫成「唸出來／說出來／做聲音」，不會有 show us 這類要看畫面的指令；conquestTruth 50 則真心話題目，供玩家選擇「TRUTH」時使用，同樣**分成五大類型、每類 10 題**（📱 Phone Reveal 當場打開手機唸出來／😳 Cringe & Confessions 糗事與自白／🧒 Back Then 童年與後悔的事／💘 Crushes & Dating 暗戀與約會／💰 Money & Work 金錢與工作），欄位與 Dare 相同；其中 8 則帶 {player}，用來把其他玩家拉進來（多半是問完自己的答案後、順手也問一句「Ask {player} too」），全部 50 則的 `seconds` 欄位一律為 45，用字同樣刻意簡單、避免俚語（原因與寫法同上一段的 Dare 題庫，例如原本的 left on read／dealbreaker／side hustle／impulse buy 都已改寫成更直白的說法）；因為參與者沒有鏡頭，Phone Reveal 這類題目一律寫成「唸出來／描述」而非「秀給大家看」。**Dare Conquest 的題庫有三套難度**，主持人在該頁設定畫面選（見那一列）：`conquest`／`conquestTruth` 是**困難**（原本那兩套，維持原 key **是因為 Google 試算表就是寫進這兩個 key**——讀試算表覆蓋的永遠是困難那一套，見 `sheet-data.js`）、`conquestEasy`／`conquestTruthEasy` 是**簡單**（給剛見面的陌生人：完全不碰隱私、不會尷尬，Dare 全是表演或猜測、Truth 全是喜好與日常）、`conquestMedium`／`conquestTruthMedium` 是**中等**（給聊過兩次天的人：稍微大膽一點、有點自嘲，但不碰約會、金錢困難與真正的秘密）。`seconds` 一律 45、欄位完全相同。**題數：簡單與中等各 100 題 Dare ＋ 100 題 Truth，困難維持 50 ＋ 50**（困難那套是試算表在寫的，見下）。簡單／中等的 Dare 是四個類型各 25 題、Truth 是五個類型各 20 題；困難兩套都是五個類型各 10 題。**簡單與中等刻意沒有「🎤 Singing」這個類型**——唱歌是這五種裡門檻最高的一種，剛見面或只聊過兩次的人被點到要開口唱，多半是直接換題或冷場，而那正是這兩個難度要避免的；困難那套維持五個類型、唱歌照舊。所以 Dare 的類型是：困難＝唱歌／變聲／即興／當面說／文字遊戲，簡單與中等＝後面那四種，差別在內容的親密程度；Truth 的類型則是逐套不同，因為困難那套的「糗事與自白」「曖昧與約會」本質上就不適合剛見面的人，簡單是 ⭐ 最喜歡的／🍜 吃的／🌍 地方與旅行／🎬 影劇與音樂／🗓️ 日常生活，中等是 😅 小小自白／🧒 那些年／🎯 你的看法／😴 習慣／🎒 求學與工作（類型標題的中文在 `dare-conquest.html` 自己的 i18n dict 裡，題目本身照全站規則維持英文）。**簡單／中等這兩套不在 `sheet-data.js` 的 SCHEMA 裡**，所以試算表既不會覆蓋它們、範本 .xlsx 也不會多出分頁；taboo **700** 張 Say It Without Saying It 卡片（`{ level, emoji, word, forbidden: [6 個字] }`，`level` 為 `easy`／`medium`／`hard`，分別是 **300／200／200** 張。分級標準是**具體→抽象光譜**：easy 是看得到摸得到的日常實物（Pineapple、Donut、Frog…），medium 是具體但要多想一下的場所／職業／活動（Courthouse、Surgeon、Festival…），hard 是純抽象的情緒或概念、完全指不出實體（Empathy、Resilience、Harmony…）。三個難度共 700 個字，除了 `Fish` 出現兩次（🐟 動物與 🎣 釣魚，禁字不同）之外彼此不重複，字彙仍刻意簡單，六個禁字就是「最先想到用來解釋它的字」——主持人畫面可以設定實際生效 3～6 個（見 `say-it-without-saying-it.html`），數字愈小愈簡單，例如 🍍 Pineapple 禁 Spiky / Yellow / Tropical / Sweet / Crown / Juice）；kangaroo **30** 條 Kangaroo Court 的荒謬罪名（`{ emoji, charge }`，`charge` 是接在主持人畫面固定文案「⚖️ The defendant is charged with…」後面的一句話，通常兩個小節：一個大家都做過的小習慣接一個好笑的轉折，用字刻意簡單、不用法律術語，例如「wearing headphones with no music playing, just to avoid talking to people」）；kangarooWitness **24** 位 Kangaroo Court 可傳喚的證人（`{ emoji, name, evidence }`，`name` 是證人角色名、`evidence` 是接在該角色名後面的一句話帶出他們帶來的證物或立場，刻意寫得夠短、一行放得下（例如「The Old Teacher」＋「remembers this exact behavior from years ago」），因為主持人畫面上兩位證人的卡片會同時疊放展示，太長會佔用太多版面）— 遊戲題目的**內建預設值**。主持人可以在 `index.html` 房間設定最後一步按下按鈕改用線上的 Google Sheet 題庫（見 `sheet-data.js`），但這份檔案永遠是後備：任何一個資料集沒抓到或格式不對，那款遊戲就用這裡的題目，所以這裡不能空、也不能刪。**內容是那份試算表的快照，不是各自演化的兩套題目**：題目平常是在試算表上改的，要把改動收回這裡就跑 `tools/sheet-to-game-data.py`（見 `prompt-sheet-template.xlsx` 那一列），不要手動兩邊各改一次。手寫這個檔案時要留意 `sheet-data.js` 的 `clean()` 只在「讀試算表」那條路上擋 `<`／`>`，內建題庫沒有經過它，所以這裡的題目要自己不含這兩個字元，否則會把版面解析壞掉 |
| `sheet-data.js` | 📄 **題庫外部來源層**（`SHEET_DATA`），**十一個頁面共用**（`index.html`、`play.html` 與八款遊戲頁），一定要排在 `game-data.js` **之後**載入，因為它做的事就是把抓下來的題庫覆蓋到 `GAME_DATA` 上（`GAME_DATA` 是 `const`，所以改的是它的**屬性**而不是重新指派）。兩個角色：①**載入即套用**——每一頁開啟時檔案結尾的 `apply()` 會把 localStorage（key: `icebreak-sheet-data.v1`）裡上次成功抓下來的題庫合併進 `GAME_DATA`，各頁自己的 inline script 之後才讀 `GAME_DATA`，所以完全不用改任何一款遊戲的程式碼；②**抓取**——`load(url, onProgress)` 只有 `index.html` **Step 1 精靈最後一步（子步驟 6「載入題庫並開始」）**那顆「📄 讀取線上題庫」按鈕會呼叫，也就是說**只有主持人主動按下去的那一刻才會連線 Google**，其餘任何時候（每一頁載入、每一次發牌、遊戲進行中）都只讀 localStorage，不發任何網路請求——這是刻意的，主持人正在帶活動時不該有任何一條線路可能卡住畫面。讀法是 Google 的 **gviz CSV 端點**（`https://docs.google.com/spreadsheets/d/{ID}/gviz/tq?tqx=out:csv&headers=1&sheet={分頁名}`），用分頁**名稱**而不是 gid，主持人才不必為了 10 個分頁去抄 10 組 gid。**分頁名稱就是遊戲名稱**（`Say It Without Saying It`／`Pick a Side`／`Sophie's Choice`…，見下面那段），不是 `GAME_DATA` 的 key；試算表必須設成「知道連結的人皆可檢視」，否則讀不到。**`headers=1` 是必要的，不是保險**：不給這個參數時 gviz 會自己猜每個分頁有幾列標題，而且是逐個分頁各猜各的——猜錯的那一個分頁，第一列資料會被當成標題列，於是程式看不到 `word`／`text` 這些欄位名，回報「標題列缺少必填欄位」，但試算表本身完全正常，主持人怎麼檢查都找不到問題（實際回報過：當時 11 個分頁裡只有 `taboo` 這一個失敗，它的標題列明明就是 level／emoji／word）。每個分頁的格式都是固定的一列標題，所以直接寫死不讓它猜。同一組問題的另一半是**錯誤訊息在說謊**：gviz 在查詢被拒、權限不對或被導去登入頁時回的是一段 JS（`google.visualization.Query.setResponse({… status:'error' …})`）或一整頁 HTML，餵給 CSV 解析器不會噴錯，只會解析出一列莫名其妙的「標題」，然後同樣被回報成「標題列缺少必填欄位」。`notCSV()` 現在會先認出這兩種回應並照實說（Google 給的理由會原文帶出來，依序找 `detailed_message` → `message` → `reason`，**不能寫成一條 alternation**：JSON 裡 `reason` 排在 `detailed_message` 前面，一條 regex 會先撞上 `access_denied` 這種機器代碼，把真正寫給人看的那句蓋掉）；真的缺欄位時，訊息也會附上「實際讀到的標題列是：…」，因為缺欄位的原因幾乎都是「這一列根本不是標題列」，光說缺哪一欄看不出來。**網址是寫死在 `sheet-data.js` 開頭的 `SHEET_URL` 常數**，畫面上沒有輸入框、主持人不能也不需要指定別份試算表——本站就這一份共用題庫，它同時開放給協作者編輯（編輯者各自被加入編輯權限，「知道連結的任何人 → 檢視者」則是這支程式讀得到的原因）。要換成另一份試算表就是改那一行。畫面上**刻意不放任何連到那份試算表的連結或按鈕**（原本有一顆「📝 開啟題庫試算表」，已移除）——編輯者本來就有那份試算表的網址，不需要從主持人畫面點過去；主持人畫面在活動中是分享出去給所有參與者看的，不該把可編輯的題庫入口擺在上面。要注意這只是拿掉畫面上的入口，**不等於把網址藏起來**：`SHEET_URL` 就寫在這支檔案裡，而這支檔案會被部署成公開的 `/sheet-data.js`、repo 也是 public，任何人都讀得到。真的要讓網址不可見，靜態網站做不到（瀏覽器得自己去抓），得改成走一層自己的後端代理。按鈕在已經讀過之後文字會變成「🔄 重新讀取」——**這顆按鈕刻意不掛 `data-i18n`**，文字由 `initSheetLoader()` 依「有沒有載入過」自己切換；掛了的話 `i18n.js` 的 `applyStatic()` 會在 inline script 之後把它蓋回「📄 讀取線上題庫」。**失敗一律退回預設，而且是逐個資料集退**：Google 連不上、某個分頁不存在（404）、標題列缺必填欄位、分頁是空的——這些都只影響那**一個**資料集，其餘抓成功的照樣套用，抓不到的那款遊戲就用 `game-data.js` 的內建題庫，主持人不會開天窗（實測：11 個分頁裡 2 成功、1 個 404、1 個標題列錯、7 個空白 → 2 個覆蓋、9 個維持預設，零錯誤）。**`Dare Conquest - Dare`／`Dare Conquest - Truth` 這兩個分頁對應的是那款遊戲的「困難」難度**（`GAME_DATA.conquest`／`conquestTruth`）：Dare Conquest 現在有簡單／中等／困難三套題庫，讀試算表覆蓋的永遠只有困難那一套，簡單與中等是內建的、不進 SCHEMA（見 `game-data.js` 那一列）。`SCHEMA` 定義 10 個資料集各自的欄位、必填欄位與「一列 → 物件」的組法（`scene` 的 `a_label`/`a_hint`…`f_hint` 會組回 `{a:{label,hint}}`、`taboo` 的 `forbidden1`…`forbidden6` 會組回 `forbidden` 陣列、`hottake` 是純字串陣列所以只有一欄），**這份 SCHEMA 同時也是產生範本 .xlsx 的依據，兩邊永遠一致**（連分頁順序都是：`Object.keys(SCHEMA)` 的排列順序就是首頁遊戲清單的順序，範本的分頁也照這個順序長出來）。**分頁名稱與 `GAME_DATA` 的 key 是兩件事**：每一項多一個 `tab` 欄位存主持人在試算表上看到的名字（遊戲名），key 仍然是 `taboo`／`hottake` 這些程式碼各處在讀的東西——原本兩者是同一個字串，結果試算表上十個分頁叫 `hottake`／`conquestTruth`／`kangarooWitness`，跟主持人畫面上的遊戲名對不起來，要改哪一款的題目得先猜它是哪個分頁（實際回報過）。`tabName(key)` 是唯一的取名入口，`load()` 拿它組網址、`onProgress(分頁名, res)` 也是回報這個名字（Step 1 那排逐分頁結果因此印的是遊戲名而不是 key）。**舊分頁名（＝key）仍然讀得到**：某個分頁用新名字抓失敗時會再用 key 試一次，只在失敗時才多送一次請求，換的是「分頁改名改到一半、或有人手上還開著舊的複本」不會讓整款遊戲開天窗。`tests/sheet-data.test.cjs`（7 項，換掉 `fetch`）守的就是這一段：要對的分頁、撇號不被吃掉、資料落在 key 上而不是分頁名上、退回舊名字、以及失敗只影響那一個資料集——因為「有沒有去要對的那個分頁」只有真的連上 Google 才看得出來，那件事沒辦法在測試裡做。CSV 是自己寫的 RFC4180 解析器而不是 `split(',')`——題目本身幾乎每條都有逗號、也有引號與換行（例如 kangaroo 的罪名），`split` 會把它們切爛。欄位比對看**標題文字**不看位置，所以主持人在試算表裡調換欄位順序不會壞；必填欄位留空的那一列會被跳過並回報跳過幾列。**每一格文字在解析當下就會被 `clean()` 拿掉 `<` 與 `>`**：題庫是從一份「開放給別人編輯」的試算表讀進來、又被各遊戲頁直接塞進 `innerHTML`，不處理的話任何一個編輯者都能在主持人畫面與玩家卡片上執行任意程式碼（實測 `<img src=x onerror=…>` 真的會跑）。**刻意是「拿掉」而不是轉義成 `&lt;`**，因為消費端有三種寫法：`play.html` 自己會 `esc()`（再轉一次會變成 `&amp;lt;`）、`kangaroo-court.html` 用 `textContent`（會把 `&lt;` 原封不動印出來）、其餘頁面直接 `innerHTML`——只有「純文字、不含 `<` `>`」在三種寫法下都正確。也不是只擋惡意：題目裡打一個 `cats < dogs` 的 `<` 一樣會讓版面解析錯亂。`scene` 的六個角色**一定會被補滿**（填不滿就用已填的循環補上），因為 `youre-in-the-scene.html` 的 `renderRoleGrid()` 是照房間人數跑迴圈的，少一個角色就會在 `role.label` 上丟 TypeError 讓整頁當掉（實測過；該處另外也加了 `if (!role) continue` 防呆）。`load()` 會回傳 `stored`，localStorage 寫不進去時（配額爆掉／無痕視窗）畫面顯示警告而不是打勾——否則會謊報成功、但每一頁其實都在用內建題庫。**讀到的內容是存在該台裝置的 localStorage**，所以每台電腦要各自讀取一次，別人在試算表上改了題目，其他人也要再按一次讀取才會更新——這是刻意的取捨：換來的是活動進行中絕不會有任何一條可能卡住的網路請求 |
| `prompt-sheet-template.xlsx` | 📄 上傳到 Google Drive 當題庫的**範本**，也就是主持人實際在編輯的那份試算表的出廠內容。**一個「說明 README」分頁 ＋ 10 個資料分頁**，分頁名稱就是**遊戲名稱**（`Say It Without Saying It`／`Pick a Side`／`Sophie's Choice`／`Persuade Together`／`You're In The Scene`／`Dare Conquest - Dare`／`Dare Conquest - Truth`／`Kangaroo Court - Charges`／`Kangaroo Court - Witnesses`／`Card Check`），**排列順序＝首頁遊戲清單的順序**（Say It → Pick a Side → Sophie's → Persuade → Scene → Dare Conquest → Kangaroo Court，不是遊戲的 `Card Check` 排最後）。一款遊戲有兩份題庫時用 ` - ` 接上是哪一份；`Let's Talk` 不在這裡（它的話題走 `talk-topics.js`）。每頁第一列是標題列（必填欄位黃底、選填欄位灰底）、凍結在最上面，下面**預先填好目前全部 1104 題內建題目**——不是空白範本，主持人是「改」而不是「從零填」。**這個檔案不是手寫維護的**，`tools/` 下有一對方向相反的工具維持它跟 `game-data.js` 同步：`build-sheet-template.py` 從 `game-data.js` ＋ `sheet-data.js` 的 `SCHEMA` 產生這個 .xlsx（分頁名、欄位、順序全部讀 SCHEMA，不在產生器裡重寫一份，兩邊才不會漂）；`sheet-to-game-data.py <下載下來的.xlsx>` 是反過來，把主持人在 Google 試算表上改過的題目收回成 `game-data.js` 的內建預設。**日常編輯題目是在試算表上做的**（那份才是工作中的版本），`game-data.js` 是「網站讀不到試算表時用的」那一份、會慢慢落後；要讓兩邊回到同一個內容就是跑後者、再跑前者。後者只換掉 `key: [` 到 `],` 之間那一段，各資料集上方那幾段說明註解原封不動；分頁名認遊戲名也認舊的 key（跟 `load()` 同一套），找不到的分頁就不動那個資料集、不會把它清空。兩支都不自己寫解析，一律跑 `sheet-data.js` 真正的 `parseTab()`，所以「網站讀試算表會拿到什麼」跟「寫進 `game-data.js` 的是什麼」必然一致。產生完會自動做一次往返驗證：xlsx → CSV → `sheet-data.js` **真正的** `parseTab()` → 跟 `GAME_DATA` 逐欄比對（目前 1104 題全部逐字元相符）。刻意跑真正的解析器而不是在 Python 裡再寫一份，不然驗的是兩份會各自漂走的實作。只有這支維護工具需要 `openpyxl`（`pip install openpyxl`），網站本身照舊沒有任何相依、沒有 build step。**`wordwolf` 分頁已經移除**（那款遊戲整個下架了，見 `index.html` 那一列），舊的試算表上若還留著，網站不會去讀它，刪掉即可 |
| `game-render.js` | 共用渲染函式（GAME_RENDER） |
| `next-round.js` | 🔁 **「由卡片決定何時換下一題」的共用閘門**（`NEXT_ROUND`），四款抽題遊戲共用（Pick a Side／Sophie's Choice／You're In The Scene／Persuade Together）。做法**跟 Say It Without Saying It 一樣**：主持頁每次發完題就 `open()` 一組一次性的 `nextId` 隨 payload 送到卡片，卡片要換題就寫 `players/{token}/next = { id, go: true }`，主持頁 `handle(num, data)` 驗證 id 相符就 `arm()`——**不是馬上換題，而是開始一段五秒倒數**，倒數結束才 `draw()`。`arm()` 當場把 id 作廢，所以同時按的第二個人、重送、還沒重畫的卡片送上來的都是過期 id，只會開出一段倒數（實測兩張卡同時按只換一題）。**倒數是跟 Say It 的回合計時同一套「只推一次、卡片自己跑秒」**：payload 帶 `nextSeq`（換一段倒數就 +1，卡片靠它重新對時）與 `nextIn`（倒數的**總秒數**，不是剩餘秒數——剩餘秒數會讓 `sync()` 每秒都判定成「payload 變了」而每秒寫一次 Firebase），卡片收到後自己每秒遞減，**整段倒數 0 次 Firebase 寫入**。主持頁自己則是靠 `endsAt` 這個牆上時鐘觸發，不是數 interval 格子，因為背景分頁的計時器會被節流甚至凍結；`sync()` 另外會補開已經過期的倒數，所以主持分頁睡醒的當下就會把題目換掉。**倒數期間任何人都可以按「✋ 等一下，先別換」**（卡片寫 `{ seq, stop: true }`，主持頁驗證 seq 等於目前這一段才 `cancelArm()`，並重新 `open()` 一組新 id 讓大家可以再按）——這比原本「同一支手機按第二下確認」好：手滑的是一個人，但還在講話的可能是另一個人，現在任何人都攔得住；而且停止也是一次性命令、不是計數，最壞情況只是停得太晚、題目照換。**刻意沒有「幾個人同意才換」的計數**：第一版是「超過半數按了才抽」，而那個計數正是實際壞掉的地方——它是活在主持人那一個分頁裡的跨卡片狀態，只要有一次事件漏掉、順序錯開、重入（Firebase 對本機寫入是**同步**觸發事件的，`ROOM.publish()` 迴圈裡就會重入 `onPlayerData`），或分頁被手機凍結，主持頁就停在 0、每張卡片卻都顯示自己已準備好，而且後面再怎麼按都救不回來（實際回報：卡片顯示已準備好、主持頁一直 0 / 2，兩個人都按也不換題）。**每一場的第一題不倒數**（`delay: () => round > 0 ? 5000 : 0`，卡片上什麼都還沒有，沒有人會被打斷，直接換最快）。`canDraw()` 為假時（抽題動畫進行中、Persuade 還沒判決）payload 的 `nextId` 是 `null`，卡片整塊不畫、送上來的請求也不受理。主持頁呼叫 `nextGate.watch(500)`（倒數要每秒更新主持人自己那行字，所以比原本快）＋ `onTick` 回呼重畫狀態列；`sync()` 沒變化就不寫入，所以那個 tick 平常是零流量。純函式、不碰 DOM 與 Firebase，`tests/next-round.test.cjs` 用 node 直接跑（14 項）。**主持頁接法有兩個順序陷阱**（實際踩過）：`draw()` 完成時要先 `round++` 與 `rolling = false` 再發牌（Sophie's／Scene 原本是反過來的，結果閘門在發牌當下被判成關閉、卡片永遠拿不到按鈕）；`publishIdle()` 要放在所有 `let` 宣告之後（Persuade 的 `currentScenario` 宣告得很晚）。主持頁的 Spin／Draw 鈕下方那行 `.next-status`（樣式在 `shared.css`）在倒數期間會被倒數本身接管，顯示「⏱️ N 秒後換下一題 — 還在講的話，請玩家在卡片上按『等一下』」，因為這是分享出去的畫面，所有人都該看到跟卡片上同一個數字。**能抽題的只有主持頁**，所以主持人的遊戲頁面必須開著而且在最前面——手機／平板會凍結背景分頁，那時候卡片按什麼都不會有反應，`play.html` 因此有一則「沒有換成功」的提示（見該列） |
| `qrcode.js` | QR code 產生函式庫（vendored，qrcode-generator 1.4.4，MIT）— 八款主持人頁都用它選用性顯示玩家連結的 QR code |
| `SECURITY.md` | 安全檢查清單（不是程式碼）：這個專案沒有後端也沒有登入，所有防護都押在 Firebase Realtime Database 的 Rules 上。列出①驗證資料庫沒對外開放的兩條測試網址與正確的 Rules 內容、②確認 Firebase 是 Spark 免費方案、③`main` 的 branch protection（選配），以及一份「看起來可疑但其實不用處理」的清單（repo public、`apiKey` 被 commit、`roster` 開放讀寫、token 出現在網址列） |

**廢棄資料夾：** `_archive/`（comic, describe, emoji-quiz, finish, fortunately, icebreaker, never, persuade, plot-twist, story-chain, surprise, truths）— 請勿讀取。

---

## 技術方向

### 展示方式
- 主持人透過「桌面分享」讓參與者看到遊戲畫面
- 參與者透過語音回應，不需要自己操作畫面
- UI 需清楚易讀（大字體、高對比，方便螢幕分享時辨識）

### 建議技術棧
- 前端展示頁面（HTML / CSS / JavaScript 或 React）
- 不需要後端登入系統，盡量保持輕量
- 可考慮即時互動功能（如投票、計時器）

---

## 遊戲類型參考

以下為適合此場景的破冰遊戲方向：

| 遊戲類型 | 說明 |
|----------|------|
| **Two Truths and a Lie** | 每人說兩個真話、一個謊言，其他人猜哪個是謊言 |
| **Would You Rather** | 主持人出題，參與者語音選擇並說明理由 |
| **Random Question Generator** | 隨機產生輕鬆有趣的英文問題帶動話題 |
| **Word Association** | 接龍式文字聯想遊戲 |
| **Trivia / Quiz** | 英文知識問答，搭配計分板 |
| **Poll / Vote** | 即時投票，主持人畫面顯示結果 |
| **Bingo** | 賓果卡，依語音描述劃記 |

---

## UI / UX 要求

- 字體夠大，螢幕分享時清晰可見
- 高對比配色（深色背景 + 亮色文字，或反之）
- 操作簡單，主持人一個人能流暢控制
- 響應式設計（主持人桌機使用為主）

---

## 開發優先順序

1. 先建立**遊戲選單 / 主頁**，可切換不同遊戲
2. 完成至少 2–3 個核心遊戲的 MVP
3. 優化視覺設計，確保螢幕分享效果良好

---

## 注意事項

- 遊戲主內容（`game-data.js` 的題目、劇情、單字等）與每頁的遊戲名稱標題固定用**英文**呈現，是遊戲本身刻意保留的英文教學素材，不跟著語言設定切換；除此之外，主持人畫面上的介面文字（按鈕、狀態列、說明文字等）**預設英文**，可透過畫面右上角的語言切換鈕改成繁體中文——細節見 `i18n.js` 那一列
- **`game-data.js` 裡所有遊戲題目（不限 Dare Conquest，所有現役遊戲的題目/劇情/句子皆同此原則）一律要用簡單好懂的英文撰寫，這是長期規則、以後新增或修改題目都要遵守**：句子盡量短、用字盡量常見（避免生僻字），**尤其要避免美式俚語或需要額外文化背景才看得懂的說法**（例如 spirit animal／yearbook quote／opposite day／left on read／dealbreaker／side hustle／impulse buy 這類——不只是單字難，是要先懂美式生活習慣才知道在問什麼）。原因：使用者是在語音聊天室裡練習英文口說的初學到中階學習者，題目是要被**唸出來、當場理解、當場回答或表演**，不是給人安靜閱讀的文字，看不懂的句子會讓那一輪直接卡住玩不下去
- **每一頁的檔名必須等於該頁 `<h1>` 標題的 slug**：去掉 emoji 與標點、空格轉連字號（🙊 Say It Without Saying It → `say-it-without-saying-it.html`、🏰 Dare Conquest → `dare-conquest.html`）。早期的檔名是隨手取的代號（`dont-say-it.html`／`hottake.html`／`what-will-you-do.html`／`persuade-team.html`／`scene.html`／`conquest.html`），跟畫面上顯示的標題對不起來，講「改一下 Pick a Side」還要先想它是哪個檔案——已經全部更名。新增遊戲時比照辦理
- 程式碼與技術討論可使用中文
- 保持模組化，方便日後新增更多遊戲

---

## Git 規則

- 所有變更直接 commit 並 push 到 `main` branch
- 不使用 feature branch

## Kangaroo Court (Beta版)

`kangaroo-court-beta/` 是 Kangaroo Court 的獨立實驗副本，入口為 `kangaroo-court-beta/index.html`。Beta 的主持人、玩家卡片、题庫、說明、房間模組、樣式與設定均使用資料夾內自己的檔案；後續 Beta 修改請限定在此資料夾，不修改根目錄原版遊戲及共用模組。初始建立時根目錄 `index.html` 只新增 Beta 入口，原本七款遊戲入口保持不變。Beta 使用獨立的 localStorage 前綴 `kangaroo-beta-` 和 Firebase 房間路徑 `rooms/kangaroo-beta-{CODE}`；原版仍用 `rooms/{CODE}`。需另外設定 Beta 人數與傳送 Beta 玩家連結，不能沿用原版卡片。詳細維護說明見 `kangaroo-court-beta/README.md`。
